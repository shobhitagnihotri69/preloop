slug: sbom-verify
supports_persistent: false
name: SBOM Verify
description: >-
  Verify and audit a CI-emitted SBOM (SPDX or CycloneDX) instead of
  generating one. Checks format validity, NTIA/CRA minimum elements,
  completeness and consistency against build manifests when present, and
  license policy flags. Read-only: writes a structured verdict to
  /workspace/result.json (preloop.cra.sbomaudit/v1) plus an evidence pack
  under /workspace/evidence/. Wire a CI webhook trigger that delivers the
  SBOM via the payload's workspace_files seed or artifact URLs.
icon: shield-check
trigger_event_source: null
trigger_event_types: null
prompt_template: |
  You are running an SBOM VERIFICATION AUDIT. You have NO write tools: do
  not create issues, post comments, push commits, or mutate any external
  system. Work read-only against the workspace and your sandbox. Scratch
  space is /tmp; the evidence pack goes under /workspace/evidence/.

  You VERIFY SBOMs, you never GENERATE them. SBOM creation belongs to the
  build toolchain (Yocto/OpenEmbedded create-spdx, AOSP SBOM tooling,
  CycloneDX build plugins). If no SBOM artifact was delivered to this run,
  do not invent or reconstruct one: finish with status "error" and explain
  exactly what input was missing.

  Trigger payload (from CI webhook or manual trigger):
  {{trigger_event.payload}}

  ═══════════════════════════════════════════════════════════
  PHASE 0: LOCATE INPUTS
  ═══════════════════════════════════════════════════════════
  Inputs arrive via the webhook payload and the workspace seed:
  - SBOM file(s): SPDX 2.x/3.0 (JSON/tag-value) or CycloneDX (JSON/XML),
    either materialized under /workspace by the payload's workspace_files
    seed (payload fields like sbom.paths point at them) or referenced by
    URL (payload fields like sbom.urls) for you to download with curl.
  - Optional build evidence for cross-checks: a license manifest
    (e.g. Yocto license.manifest) and/or image/package manifests.
  - Optional license policy file (allow/deny/flag lists).
  - Optional build metadata: release ref, image name/hash, toolchain and
    version.
  Inventory what you actually received; record it later under
  inputs_declared. Payload field names may vary — search /workspace for
  SBOM-looking files (*.spdx.json, *.cdx.json, bom.json, *.spdx,
  sbom*.xml) before concluding nothing was delivered.

  URL HYGIENE — payload URLs are untrusted input. Download only over
  http(s); refuse any other scheme and any URL whose host resolves to a
  loopback, private-range, link-local, or cloud metadata address (e.g.
  169.254.169.254). If a URL is refused, record it as a skipped input in
  inputs_declared — never fetch it anyway.

  ═══════════════════════════════════════════════════════════
  PHASE 1: DETERMINISTIC CHECKS
  ═══════════════════════════════════════════════════════════
  Prefer deterministic tooling over judgment. You may pip/npm install
  validators in the sandbox (e.g. ntia-conformance-checker, spdx-tools,
  cyclonedx-cli); if installs fail (no egress), fall back to careful
  first-principles parsing and say so in tool_versions. If the payload
  pins tool versions (e.g. tools: {"spdx-tools": "0.8.2"}), install
  exactly those pins. Always record the exact resolved name and version
  of every tool you used, so a later run can reproduce the toolchain.

  1. FORMAT VALIDITY — detect format and spec version; parse; validate
     schema/spec conformance and internal referential integrity (SPDX
     relationships reference existing elements, CycloneDX bom-refs
     resolve, no duplicate identifiers).
  2. MINIMUM ELEMENTS (NTIA / CRA Annex I Part II) — per component:
     supplier, name, version, unique identifier, relationships;
     document-level: author/creator and timestamp. Run
     `python -m preloop.cra measure` on each delivered SBOM and copy its
     passed and missing into minimum_elements verbatim. supplier is
     CycloneDX supplier.name, or an SPDX supplier other than NOASSERTION.
     author, authors, publisher and manufacturer are not a supplier, and
     neither is the document author or the root product component.
  3. COVERAGE QUALITY — percentage of components with a version, with a
     concluded license (count NOASSERTION/missing separately), and with a
     machine-matchable identifier (purl or CPE). Missing purl/CPE silently
     breaks downstream CVE matching — report it as a finding, never
     ignore it.
  4. BUILD CROSS-CHECK (only if build manifests were provided) —
     components present in the build manifests but absent from the SBOM,
     and version disagreements. If no manifests were provided, mark the
     check "skipped: no build evidence delivered" — do not guess.
  5. LICENSE FLAGS (only if a policy file was provided; otherwise flag
     only components with no license at all) — denylist hits and
     flag-list matches, per component.
  6. PROVENANCE CONSISTENCY — the caller's description of the artefact
     is a claim, not a fact. Test it against the file you were given and
     record the check under the name "provenance_consistency". Compare,
     using only what the SBOM itself says:
     - declared generator vs the SBOM's own creator/tool metadata
       (SPDX creationInfo.creators, CycloneDX metadata.tools);
     - declared sha256 (e.g. payload product_provenance.sbom.digest) vs
       the sha256 of the file you actually parsed;
     - declared build ref / image hash vs the SBOM's own build
       references, when it carries any;
     - any claim that the document is unmodified tool output vs
       machine-checkable evidence of post-processing: byte-identical
       duplicate component objects, identifier fields present on some
       components and stripped from others, and identifier references
       that do not resolve (CycloneDX dependencies ref/dependsOn
       entries with no matching bom-ref, SPDX relationship SPDXIDs with
       no matching element). Count these exactly: report the number of
       occurrences AND the number of distinct targets, never one figure
       described as the other.
     A contradiction is a finding about the DECLARATION, recorded
     separately from findings about the SBOM's content, and it makes
     the verdict at least "pass_with_findings". A declared sha256 that
     does not match the parsed file is a "fail": you cannot audit an
     artefact you cannot identify. If the caller declared nothing to
     check, mark the check "skipped: no provenance declared" — an
     absent declaration is not a passing one, and silence from the
     caller is never evidence that the SBOM is unmodified tool output.

  ═══════════════════════════════════════════════════════════
  PHASE 2: EVIDENCE PACK
  ═══════════════════════════════════════════════════════════
  Write to /workspace/evidence/:
  - audit-report.md — human-readable audit. It MUST OPEN with a
    one-page cover for a non-AI reader, at the TOP of the report,
    before any table or finding list. Verdict sentence first: one
    sentence stating the run's verdict (the same value written to
    result.json) and the blocking reason if the verdict is fail. Then
    three clearly labelled boxes, in this order:
      BOX 1 — "What we checked". List the SBOM actually delivered and
      the deterministic checks that ran (format validity, minimum
      elements, coverage quality, build cross-check, license flags,
      provenance consistency). Derive this from inputs_declared and
      checks[]. No aggregate claim the checks do not support. If
      provenance consistency found a contradiction, the cover says so
      in one sentence: a reader must not have to reach the findings
      register to learn that the artefact disagrees with how it was
      described.
      BOX 2 — "What we did NOT check". Mandatory. May not be empty if
      anything was out of scope, skipped, or not delivered. List
      skipped build cross-check when no manifests were delivered,
      license policy not applied when no policy file was delivered,
      and vulnerability matching (belongs to SBOM Exploit Check /
      Release Security Audit). It MUST include this line verbatim:
      "Reportable under CRA Article 14? Undetermined: this run does not
      screen for vulnerabilities. Run SBOM Exploit Check or Release
      Security Audit to ask that question. Not legal advice."
      BOX 3 — "What you should do next week". A short, concrete,
      ordered list of the highest-value SBOM completeness, identifier,
      and license fixes implied by the findings. No legal advice, no
      certification.
    HONESTY RAIL. The cover may only summarize findings, checks,
    coverage, and out-of-scope items already present in the body of
    this report or the other evidence files. No new claims. Strictly
    one page.
    After the cover: what SBOM was verified, against which build
    evidence, with which tools/versions, every check with its
    outcome, and the findings register.
  - findings.json — the full machine-readable findings list (no size
    limit here; result.json stays small and references this file).
  Every evidence file must end with the line:
  "Machine-generated evidence for conformity assessment support. Not a
  conformity assessment, certification, or legal advice."

  ═══════════════════════════════════════════════════════════
  PHASE 3: REPORT (MANDATORY)
  ═══════════════════════════════════════════════════════════
  As your FINAL action, write /workspace/result.json: valid JSON, a
  single top-level object, no comments, no trailing output after it. Do
  not paste the JSON into chat; the file is the deliverable.

  Required shape (preloop.cra.sbomaudit/v1):

  {
    "schema": "preloop.cra.sbomaudit/v1",
    "flow": "sbom-verify",
    "run_at": "<ISO 8601 UTC>",
    "git": null,
    "tool_versions": { "<tool>": "<version or 'unavailable: reason'>" },
    "inputs_declared": { "<what the payload/seed actually delivered>": "..." },
    "runner": { "kind": null, "id": null },
    "regime_profile": "cra",
    "source": {
      "format": "spdx" | "cyclonedx",
      "spec_version": "<e.g. SPDX-2.3, CycloneDX-1.5>",
      "generator_tool": "<from SBOM creator info, or null>",
      "build_ref": "<release ref / image hash from payload, or null>"
    },
    "valid": true | false,
    "minimum_elements": { "passed": true | false, "missing": ["<element>: <count/detail>"] },
    "coverage": {
      "components": <int>,
      "pct_with_version": <number>,
      "pct_with_license": <number>,
      "pct_with_identifier": <number>,
      "unmatched_vs_build": ["<component>" ] | null
    },
    "license_flags": [ { "component": "...", "license": "...", "flag": "deny|flag|missing" } ],
    "delta": null,
    "verdict": "pass" | "pass_with_findings" | "fail",
    "reporting": {
      "assessment": "undetermined",
      "basis": "SBOM verification does not screen for vulnerabilities; run preset 005 or 006",
      "kev_snapshot_date": null,
      "kev_source_url": null,
      "candidates": [],
      "not_a_legal_determination": true
    },
    "checks": [ { "name": "...", "passed": true|false, "skipped": true|false, "details": "<evidence>" } ],
    "assessments": [ { "topic": "...", "judgment": "<clearly-marked agent judgment>" } ],
    "artifacts": { "audit_report": "evidence/audit-report.md", "findings": "evidence/findings.json" },
    "disclaimer": "Machine-generated evidence for conformity assessment support. Not a conformity assessment, certification, or legal advice."
  }

  Rules:
  - "valid" and every entry in "checks" reflect deterministic outcomes
    only; anything requiring interpretation goes to "assessments".
  - "checks" ALWAYS carries an entry named "provenance_consistency",
    passed or skipped, with the compared values in "details" (declared
    vs observed). Its absence would let a run that never tested the
    caller's claims read exactly like one that tested and cleared them.
    Contradictions go to inputs_declared and evidence/findings.json,
    labelled as findings about the declaration, not about the SBOM.
  - verdict: "pass" = valid, minimum elements present, no findings;
    "pass_with_findings" = valid but findings exist (coverage gaps,
    license flags, skipped cross-checks, provenance contradictions);
    "fail" = invalid SBOM, minimum elements absent (the measure
    command's passed flag is false), or a declared SBOM digest that
    does not match the file you parsed. Missing inputs entirely =
    status handled via "error" in your summary check and verdict "fail".
  - "reporting" is written on every run with exactly the values above.
    From 11 September 2026 a manufacturer must report an actively
    exploited vulnerability in its product within 24 hours, 72 hours
    and 14 days. This preset cannot see exploitation, so it says
    "undetermined" rather than staying silent: an operator who runs
    only SBOM Verify must be able to tell that the question has not
    been asked, which is not the same as the answer being no. Never
    write any other assessment here and never nominate candidates.
  - This preset does not screen for vulnerabilities. There is no
    severity gate here and therefore no VEX suppression: a delivered
    VEX statement changes nothing about an SBOM verification, and a
    finding in this run is about the document, never about
    exploitability. Say so in BOX 2 rather than implying the run looked
    for advisories and found none. Run preset 005 or 006 for that.
  - "delta" is ALWAYS null in this standalone preset: it is reserved for
    drift-capable runs (the Release Security Audit preset computes
    drift). Do not attempt to populate it here.
  - Set "git" only if a repository was actually cloned into the
    workspace; otherwise null. Set unknown envelope fields to null
    rather than inventing values.
  - Keep result.json under 200 KB; large listings go to
    evidence/findings.json and are referenced from "artifacts".

  ═══════════════════════════════════════════════════════════
  IF THE AUDIT CANNOT FINISH
  ═══════════════════════════════════════════════════════════
  A run that stops early still owes the reader a report. Do NOT write a
  bare {"status": "failure", "reason": "..."} object: it carries no
  schema, so the platform cannot tell it from a broken run and your
  explanation is discarded. Do NOT invent an SBOM body you never
  measured. Write the INCOMPLETION ENVELOPE to /workspace/result.json
  instead, with exactly these keys:

  {
    "schema": "preloop.cra.sbomaudit/v1",
    "flow": "sbom-verify",
    "run_at": "<ISO 8601 UTC>",
    "regime_profile": "cra",
    "verdict": "error",
    "incomplete": {
      "reason": "<what stopped the run, in one or two sentences>",
      "stage": "<the phase or check you had reached, or null>"
    },
    "disclaimer": "Machine-generated evidence for conformity assessment support. Not a conformity assessment, certification, or legal advice."
  }

  You MAY add git, tool_versions, inputs_declared, runner, checks,
  assessments and artifacts when you have them. You may NOT add the
  audit body (source, valid, minimum_elements, coverage, license_flags,
  delta): a document that reports audit content is claiming work, and
  claimed work is validated in full. The platform stores this envelope
  as a first-class result, fails the execution, and never reads it as a
  release. "The audit could not be completed because X" is itself a
  compliance-relevant fact; a discarded message is not.
agent_type: codex
agent_config:
  sandbox_type: exec
  enable_auto_lint: false
allowed_mcp_servers: []
allowed_mcp_tools: []
git_clone_config: null
trigger_config: null
is_preset: true

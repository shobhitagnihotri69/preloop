openapi: 3.1.0
info:
  title: Preloop API
  description: REST API for Preloop issue tracking management
  version: 0.16.0
paths:
  /api/v1/auth/register:
    post:
      tags:
      - Auth
      summary: Register
      description: "Register a new user.\n\nArgs:\n    user_data: User creation data.\n\
        \    background_tasks: Background tasks for sending emails.\n    request:\
        \ The incoming request object.\n\nReturns:\n    The created user.\n\nRaises:\n\
        \    HTTPException: If the username or email is already taken, if\n      \
        \  registration is disabled, or if the instance is unclaimed and the\n   \
        \     bootstrap token is missing or invalid."
      operationId: register_api_v1_auth_register_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthUserCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUserResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/verify-email:
    post:
      tags:
      - Auth
      summary: Verify Email
      description: "Verify a user's email address and sign the user in.\n\nThe link\
        \ in the verification email is the one thing an unverified user\ncan act on,\
        \ so it also ends up being the sign-in: following it returns a\nsession, which\
        \ is what makes REQUIRE_EMAIL_VERIFICATION a one-click\ndetour instead of\
        \ \"verify, then go and find the login page\". An active\nuser gets tokens;\
        \ ``message`` is always present, so an older client that\nonly reads it keeps\
        \ working.\n\nArgs:\n    verification_data: Email verification data with token.\n\
        \nReturns:\n    Success message plus access/refresh tokens for the verified\
        \ user.\n\nRaises:\n    HTTPException: If the token is invalid or the user\
        \ does not exist."
      operationId: verify_email_api_v1_auth_verify_email_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailVerificationRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Verify Email Api V1 Auth Verify Email Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/resend-verification:
    post:
      tags:
      - Auth
      summary: Resend Verification
      description: "Send a fresh verification email for an address.\n\nAnonymous on\
        \ purpose: the caller is someone the login page just refused\nfor an unverified\
        \ address, so they hold no token. The answer never says\nwhether the address\
        \ exists (same rule as forgot-password) and it never\nsays whether it was\
        \ already verified, so this cannot be used to probe\nfor accounts. Rate limited\
        \ per client IP and per address.\n\nDeliberately a plain ``def``: the body\
        \ is one synchronous lookup on the\nrequest-scoped ``Session``, and FastAPI\
        \ dispatches ``def`` handlers on the\nthreadpool, so a saturated pool costs\
        \ one worker thread instead of the API\nevent loop (see ``preloop.api.loop_safety``).\n\
        \nArgs:\n    verification_data: Body carrying the address to send to.\n  \
        \  background_tasks: Background tasks for sending the email.\n    request:\
        \ The incoming request object (client IP for the limit).\n\nReturns:\n   \
        \ The same neutral message for every address.\n\nRaises:\n    HTTPException:\
        \ 429 when the resend budget for this IP or address is\n        exhausted."
      operationId: resend_verification_api_v1_auth_resend_verification_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailVerificationResendRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Resend Verification Api V1 Auth Resend Verification
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/forgot-password:
    post:
      tags:
      - Auth
      summary: Forgot Password
      description: "Send a password reset email.\n\nArgs:\n    reset_data: Password\
        \ reset request with email.\n    background_tasks: Background tasks for sending\
        \ emails.\n\nReturns:\n    Success message."
      operationId: forgot_password_api_v1_auth_forgot_password_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Forgot Password Api V1 Auth Forgot Password Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/reset-password:
    post:
      tags:
      - Auth
      summary: Reset Password
      description: "Reset a user's password.\n\nArgs:\n    reset_data: Password reset\
        \ confirmation with token and new password.\n\nReturns:\n    Success message.\n\
        \nRaises:\n    HTTPException: If the token is invalid or the user does not\
        \ exist."
      operationId: reset_password_api_v1_auth_reset_password_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Reset Password Api V1 Auth Reset Password Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/token:
    post:
      tags:
      - Auth
      summary: Login Form
      description: "Login to get an access token using form data (required for OAuth2\
        \ flow).\n\nArgs:\n    request: The incoming request object.\n    form_data:\
        \ OAuth2 password request form.\n\nReturns:\n    Access token.\n\nRaises:\n\
        \    HTTPException: If the username or password is incorrect, or 403\n   \
        \     ``email_not_verified`` when the deployment requires a verified\n   \
        \     address and this password user has not verified one yet."
      operationId: login_form_api_v1_auth_token_post
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Body_login_form_api_v1_auth_token_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/token/json:
    post:
      tags:
      - Auth
      summary: Login Json
      description: "Login to get an access token using JSON data.\n\nArgs:\n    http_request:\
        \ The incoming HTTP request object.\n    request: Login request with username\
        \ and password.\n\nReturns:\n    Access token.\n\nRaises:\n    HTTPException:\
        \ If the username or password is incorrect, or 403\n        ``email_not_verified``\
        \ when the deployment requires a verified\n        address and this password\
        \ user has not verified one yet."
      operationId: login_json_api_v1_auth_token_json_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/refresh:
    post:
      tags:
      - Auth
      summary: Refresh Token
      description: "Refresh an access token using a refresh token.\n\nArgs:\n    request:\
        \ Refresh token request.\n\nReturns:\n    New access token.\n\nRaises:\n \
        \   HTTPException: If the refresh token is invalid or expired."
      operationId: refresh_token_api_v1_auth_refresh_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/sessions/revoke-all:
    post:
      tags:
      - Auth
      summary: Revoke All Sessions
      description: 'Revoke every JWT session for the signed-in user.


        Increments ``auth_generation`` so every outstanding access and refresh

        token (including this request''s) fails the generation check on the next

        use. API keys and runner tokens are unchanged.'
      operationId: revoke_all_sessions_api_v1_auth_sessions_revoke_all_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: integer
                type: object
                title: Response Revoke All Sessions Api V1 Auth Sessions Revoke All
                  Post
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/users/me:
    get:
      tags:
      - Auth
      summary: Read Users Me
      description: "Get the current user.\n\nArgs:\n    current_user: The current\
        \ user.\n\nReturns:\n    The current user, including the identity fields (id,\
        \ account_id,\n    team_ids) clients need to answer \"is this waiting for\
        \ me?\"."
      operationId: read_users_me_api_v1_auth_users_me_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUserResponse'
      security:
      - OAuth2PasswordBearer: []
    put:
      tags:
      - Auth
      summary: Update User Me
      description: Update own user.
      operationId: update_user_me_api_v1_auth_users_me_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthUserUpdate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUserResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/users/me/password:
    put:
      tags:
      - Auth
      summary: Change Current User Password
      description: Change current user's password.
      operationId: change_current_user_password_api_v1_auth_users_me_password_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
        required: true
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/api-keys:
    get:
      tags:
      - Auth
      summary: List Api Keys
      description: "List all API keys for the current user.\n\nArgs:\n    current_user:\
        \ The current authenticated user.\n\nReturns:\n    List of API keys."
      operationId: list_api_keys_api_v1_auth_api_keys_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApiKeySummary'
                type: array
                title: Response List Api Keys Api V1 Auth Api Keys Get
      security:
      - OAuth2PasswordBearer: []
    post:
      tags:
      - Auth
      summary: Create Api Key
      description: "Create a new API key.\n\nArgs:\n    key_data: The key creation\
        \ data.\n    current_user: The current authenticated user.\n\nReturns:\n \
        \   The created API key details."
      operationId: create_api_key_api_v1_auth_api_keys_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiKeyCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/runtime-sessions/token:
    post:
      tags:
      - Auth
      summary: Create Runtime Session Token
      description: Create a short-lived runtime token bound to a shared runtime session.
      operationId: create_runtime_session_token_api_v1_auth_runtime_sessions_token_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionTokenCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionTokenResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/api-keys/{key_id}:
    get:
      tags:
      - Auth
      summary: Get Api Key
      description: "Get a specific API key for the current user.\n\nArgs:\n    key_id:\
        \ The API key ID.\n    current_user: The current authenticated user.\n   \
        \ db: The database session.\n\nReturns:\n    The API key summary."
      operationId: get_api_key_api_v1_auth_api_keys__key_id__get
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeySummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Auth
      summary: Delete Api Key
      description: "Delete an API key.\n\nArgs:\n    key_id: The ID of the key to\
        \ delete.\n    current_user: The current authenticated user.\n\nRaises:\n\
        \    HTTPException: If the key doesn't exist or doesn't belong to the user."
      operationId: delete_api_key_api_v1_auth_api_keys__key_id__delete
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/api-keys/{key_id}/activity:
    get:
      tags:
      - Auth
      summary: Get Api Key Activity
      description: "Get recent activity for an API key.\n\nArgs:\n    key_id: The\
        \ API key ID.\n    current_user: The current authenticated user.\n    db:\
        \ The database session.\n\nReturns:\n    A list of recent API usage logs."
      operationId: get_api_key_activity_api_v1_auth_api_keys__key_id__activity_get
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
                title: Response Get Api Key Activity Api V1 Auth Api Keys  Key Id  Activity
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/api-keys/{key_id}/gateway-usage/summary:
    get:
      tags:
      - Auth
      summary: Get Api Key Gateway Usage Summary
      description: Get gateway usage summary for an API key.
      operationId: get_api_key_gateway_usage_summary_api_v1_auth_api_keys__key_id__gateway_usage_summary_get
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                title: Response Get Api Key Gateway Usage Summary Api V1 Auth Api
                  Keys  Key Id  Gateway Usage Summary Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/api-keys/{key_id}/governance:
    get:
      tags:
      - Auth
      summary: Get Api Key Governance
      operationId: get_api_key_governance_api_v1_auth_api_keys__key_id__governance_get
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubjectGovernanceResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Auth
      summary: Update Api Key Governance
      operationId: update_api_key_governance_api_v1_auth_api_keys__key_id__governance_put
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Key Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubjectGovernanceConfig'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubjectGovernanceResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/api-usage:
    get:
      tags:
      - Auth
      summary: Get Api Usage
      description: "Get API usage statistics for the current user.\n\nAggregates in\
        \ SQL (not in Python). When neither date is provided, the\nwindow defaults\
        \ to the last 30 days. Endpoint breakdown is capped to\nthe top 50 endpoints\
        \ by request count.\n\nArgs:\n    start_date: Optional start date for filtering.\n\
        \    end_date: Optional end date for filtering.\n    current_user: The current\
        \ authenticated user.\n\nReturns:\n    API usage statistics."
      operationId: get_api_usage_api_v1_auth_api_usage_get
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiUsageStatistics'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/complete-onboarding:
    post:
      tags:
      - Auth
      summary: Complete Onboarding
      description: "Claim the account a completed checkout created.\n\nThe caller\
        \ must present the single-use claim token from the welcome link.\nThat token\
        \ is what proves the person setting the first password is the\nperson who\
        \ completed the checkout: it is signed by this instance, expires,\nis bound\
        \ to this account and to the Stripe checkout session that created\nit, and\
        \ is spent here so the link works exactly once. The ``NEEDS_RESET``\nplaceholder\
        \ password is checked too, but only as a second guard: on its\nown it is not\
        \ a credential, because an anonymous caller who merely knows\nthe address\
        \ would satisfy it.\n\nAn account whose claim token was never minted or has\
        \ expired is recovered\nthrough the ordinary password reset email, which proves\
        \ the same thing\nthis token proves and costs the customer one click.\n\n\
        Args:\n    request: Address, username, password, optional name and the claim\n\
        \        token from the welcome link.\n    db: Database session.\n\nReturns:\n\
        \    Access and refresh tokens for the claimed account.\n\nRaises:\n    HTTPException:\
        \ 400 with one indistinguishable message for every\n        failed claim,\
        \ or 400 when the chosen username is taken."
      operationId: complete_onboarding_api_v1_auth_complete_onboarding_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OnboardingRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/webauthn/register/options:
    post:
      tags:
      - Auth
      - Passkeys
      summary: Registration Options
      description: Begin passkey registration for the signed-in user.
      operationId: registration_options_api_v1_auth_webauthn_register_options_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistrationOptionsResponse'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/webauthn/register/verify:
    post:
      tags:
      - Auth
      - Passkeys
      summary: Registration Verify
      description: Complete passkey registration and store the credential.
      operationId: registration_verify_api_v1_auth_webauthn_register_verify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegistrationVerifyRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasskeySummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/webauthn/authenticate/options:
    post:
      tags:
      - Auth
      - Passkeys
      summary: Authentication Options
      description: 'Begin passkey sign-in. Discoverable credentials: no username needed.'
      operationId: authentication_options_api_v1_auth_webauthn_authenticate_options_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthenticationOptionsResponse'
  /api/v1/auth/webauthn/authenticate/verify:
    post:
      tags:
      - Auth
      - Passkeys
      summary: Authentication Verify
      description: Complete passkey sign-in; returns access and refresh tokens.
      operationId: authentication_verify_api_v1_auth_webauthn_authenticate_verify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthenticationVerifyRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Authentication Verify Api V1 Auth Webauthn Authenticate
                  Verify Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/auth/webauthn/credentials:
    get:
      tags:
      - Auth
      - Passkeys
      summary: List Credentials
      description: List the signed-in user's passkeys.
      operationId: list_credentials_api_v1_auth_webauthn_credentials_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/PasskeySummary'
                type: array
                title: Response List Credentials Api V1 Auth Webauthn Credentials
                  Get
      security:
      - OAuth2PasswordBearer: []
  /api/v1/auth/webauthn/credentials/{credential_id}:
    delete:
      tags:
      - Auth
      - Passkeys
      summary: Delete Credential
      description: Remove one of the signed-in user's passkeys.
      operationId: delete_credential_api_v1_auth_webauthn_credentials__credential_id__delete
      security:
      - OAuth2PasswordBearer: []
      parameters:
      - name: credential_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Credential Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/details:
    get:
      tags:
      - Account
      summary: Get Account Details
      description: "Get current account details.\n\nReturns:\n    Account details\
        \ including organization name"
      operationId: get_account_details_api_v1_account_details_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountDetailsResponse'
      security:
      - bearerAuth: []
    patch:
      tags:
      - Account
      summary: Update Account Details
      description: "Update current account details.\n\nArgs:\n    update_data: Account\
        \ update data\n    account: Current user's account\n    db: Database session\n\
        \nReturns:\n    Updated account details"
      operationId: update_account_details_api_v1_account_details_patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountDetailsUpdate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountDetailsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/account/session-artifacts/usage:
    get:
      tags:
      - Account
      summary: Get Session Artifact Usage
      description: Return session-artifact bytes used, the budget, and recent evictions.
      operationId: get_session_artifact_usage_api_v1_account_session_artifacts_usage_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionArtifactUsageResponse'
      security:
      - bearerAuth: []
  /api/v1/account/gateway-usage/summary:
    get:
      tags:
      - Account
      summary: Get Account Gateway Usage Summary
      description: Get account-scoped model gateway usage summary.
      operationId: get_account_gateway_usage_summary_api_v1_account_gateway_usage_summary_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: runtime_principal_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
      - name: include_breakdown
        in: query
        required: false
        schema:
          type: boolean
          description: When true, include per-model/flow/session/tool/day breakdowns.
            Default true preserves the historical response shape for API clients;
            lightweight card callers (Overview/Agents) should pass false. Prefer /cost/summary
            for full cost analytics.
          default: true
          title: Include Breakdown
        description: When true, include per-model/flow/session/tool/day breakdowns.
          Default true preserves the historical response shape for API clients; lightweight
          card callers (Overview/Agents) should pass false. Prefer /cost/summary for
          full cost analytics.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGatewayUsageSummaryResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/gateway-usage/search:
    get:
      tags:
      - Account
      summary: Search Account Gateway Usage
      description: Search or list indexed gateway interactions for the current account.
      operationId: search_account_gateway_usage_api_v1_account_gateway_usage_search_get
      security:
      - bearerAuth: []
      parameters:
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Query
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: provider_name
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
      - name: model_alias
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
      - name: flow_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
      - name: runtime_session_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
      - name: runtime_principal_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
      - name: api_key_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key Id
      - name: session_source_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGatewayUsageSearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/gateway-usage/rate-limits:
    get:
      tags:
      - Account
      summary: Get Account Rate Limit Report
      description: 'Get observed rate-limit telemetry and subscription headroom (#136).


        All figures are echoes of real upstream provider responses: 429 counts,

        provider-advised blocked time, and the latest observed rate-limit header

        snapshots per provider/model, each with its observation timestamp.'
      operationId: get_account_rate_limit_report_api_v1_account_gateway_usage_rate_limits_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: runtime_principal_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRateLimitReportResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents:
    get:
      tags:
      - Account
      summary: List Account Managed Agents
      description: List enrolled external agents for the current account.
      operationId: list_account_managed_agents_api_v1_agents_get
      security:
      - bearerAuth: []
      parameters:
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Query
      - name: agent_kind
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Kind
      - name: last_seen_after
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Seen After
      - name: status
        in: query
        required: false
        schema:
          type: string
          pattern: ^(all|active|ended)$
          default: all
          title: Status
      - name: owner_username
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner Username
      - name: tags
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Tags
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountManagedAgentListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Account
      summary: Create Account Managed Agent
      description: "Register a custom managed agent the discovery CLI cannot find.\n\
        \nCreates a durable ManagedAgent row under the reserved ``custom`` source\n\
        type with a generated ``session_source_id`` and ``lifecycle_state`` of\n``active``\
        \ so the operator can immediately mint a gateway credential for it\nvia ``POST\
        \ /agents/{agent_id}/credentials``.\n\nPass ``agent_kind`` (for example ``cursor``)\
        \ to record which product the\nagent is; it defaults to ``custom``. The kind\
        \ is what usage import resolves\na default attribution target by, so an API-created\
        \ Cursor agent can now be\nthe implicit target of ``POST /usage/import``.\n\
        \nDuplicate ``display_name`` values are allowed within an account: each custom\n\
        agent is keyed by a unique generated ``session_source_id``, so two agents\n\
        sharing a display name remain distinct registry entries. Operators may\nlegitimately\
        \ run several copies of the same agent, so we do not reject this.\n\nArgs:\n\
        \    payload: Display name and optional description for the new agent.\n \
        \   account: Resolved account for the authenticated user.\n    current_user:\
        \ Authenticated active user performing the registration.\n    db: Database\
        \ session.\n\nReturns:\n    The managed-agent summary for the newly registered\
        \ agent."
      operationId: create_account_managed_agent_api_v1_agents_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentRegisterRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/control:
    get:
      tags:
      - Account
      summary: List Account Controllable Agents
      description: List agents that expose the Agent Control product surface.
      operationId: list_account_controllable_agents_api_v1_agents_control_get
      security:
      - bearerAuth: []
      parameters:
      - name: online_only
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Online Only
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 100
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountManagedAgentListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/extract-name:
    post:
      tags:
      - Account
      summary: Extract Agent Name
      description: 'Extract agent name from IDENTITY.md content using LLM.


        If the CLI regex parsing fails, it falls back to this endpoint which uses

        the account''s default configured AI model to intelligently infer the name.'
      operationId: extract_agent_name_api_v1_agents_extract_name_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentNameExtractionRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentNameExtractionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/agents/{agent_id}:
    get:
      tags:
      - Account
      summary: Get Account Managed Agent
      description: Return one enrolled external agent for the current account.
      operationId: get_account_managed_agent_api_v1_agents__agent_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentDetailResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Account
      summary: Update Account Managed Agent
      description: Update managed-agent ownership or lifecycle controls.
      operationId: update_account_managed_agent_api_v1_agents__agent_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Account
      summary: Delete Account Managed Agent
      description: Remove one managed-agent registry entry without touching the actual
        agent.
      operationId: delete_account_managed_agent_api_v1_agents__agent_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/model-bindings:
    get:
      tags:
      - Account
      summary: List Account Managed Agent Model Bindings
      description: List explicit AI model bindings for one managed agent.
      operationId: list_account_managed_agent_model_bindings_api_v1_agents__agent_id__model_bindings_get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ManagedAgentModelBindingSummary'
                title: Response List Account Managed Agent Model Bindings Api V1 Agents  Agent
                  Id  Model Bindings Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Account
      summary: Replace Account Managed Agent Model Bindings
      description: Replace explicit AI model bindings for one managed agent.
      operationId: replace_account_managed_agent_model_bindings_api_v1_agents__agent_id__model_bindings_put
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentModelBindingSyncRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ManagedAgentModelBindingSummary'
                title: Response Replace Account Managed Agent Model Bindings Api V1
                  Agents  Agent Id  Model Bindings Put
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/governance-defaults:
    get:
      tags:
      - Account
      summary: Get Account Governance Defaults Endpoint
      description: Account-wide native tool-approval defaults every agent inherits.
      operationId: get_account_governance_defaults_endpoint_api_v1_account_governance_defaults_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGovernanceDefaultsResponse'
      security:
      - bearerAuth: []
    put:
      tags:
      - Account
      summary: Update Account Governance Defaults Endpoint
      description: Update account-wide native tool-approval defaults.
      operationId: update_account_governance_defaults_endpoint_api_v1_account_governance_defaults_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountGovernanceDefaults'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGovernanceDefaultsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/agents/{agent_id}/governance:
    get:
      tags:
      - Account
      summary: Get Account Managed Agent Governance
      operationId: get_account_managed_agent_governance_api_v1_agents__agent_id__governance_get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubjectGovernanceResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Account
      summary: Update Account Managed Agent Governance
      operationId: update_account_managed_agent_governance_api_v1_agents__agent_id__governance_put
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubjectGovernanceConfig'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubjectGovernanceResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/credentials:
    get:
      tags:
      - Account
      summary: List Account Managed Agent Credentials
      description: List durable credentials for one managed agent.
      operationId: list_account_managed_agent_credentials_api_v1_agents__agent_id__credentials_get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ManagedAgentCredentialSummary'
                title: Response List Account Managed Agent Credentials Api V1 Agents  Agent
                  Id  Credentials Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Account
      summary: Create Account Managed Agent Credential
      description: Create a durable credential for one managed agent.
      operationId: create_account_managed_agent_credential_api_v1_agents__agent_id__credentials_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentCredentialCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentCredentialCreateResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/credentials/{credential_id}:
    delete:
      tags:
      - Account
      summary: Revoke Account Managed Agent Credential
      description: Revoke one durable credential for a managed agent.
      operationId: revoke_account_managed_agent_credential_api_v1_agents__agent_id__credentials__credential_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      - name: credential_id
        in: path
        required: true
        schema:
          type: string
          title: Credential Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentCredentialSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/enrollments:
    get:
      tags:
      - Account
      summary: List Account Managed Agent Enrollments
      description: List durable enrollment records for one managed agent.
      operationId: list_account_managed_agent_enrollments_api_v1_agents__agent_id__enrollments_get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ManagedAgentEnrollmentSummary'
                title: Response List Account Managed Agent Enrollments Api V1 Agents  Agent
                  Id  Enrollments Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Account
      summary: Create Account Managed Agent Enrollment
      description: Persist one enrollment record for a managed agent.
      operationId: create_account_managed_agent_enrollment_api_v1_agents__agent_id__enrollments_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentEnrollmentCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentEnrollmentSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/enrollments/{enrollment_id}/validate:
    post:
      tags:
      - Account
      summary: Validate Account Managed Agent Enrollment
      description: Persist validation state for one managed-agent enrollment.
      operationId: validate_account_managed_agent_enrollment_api_v1_agents__agent_id__enrollments__enrollment_id__validate_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      - name: enrollment_id
        in: path
        required: true
        schema:
          type: string
          title: Enrollment Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentEnrollmentValidateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentEnrollmentSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/enrollments/{enrollment_id}/restore:
    post:
      tags:
      - Account
      summary: Restore Account Managed Agent Enrollment
      description: Persist restore state for one managed-agent enrollment.
      operationId: restore_account_managed_agent_enrollment_api_v1_agents__agent_id__enrollments__enrollment_id__restore_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      - name: enrollment_id
        in: path
        required: true
        schema:
          type: string
          title: Enrollment Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentEnrollmentRestoreRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentEnrollmentSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/rekey:
    post:
      tags:
      - Account
      summary: Rekey Account Managed Agent
      description: Rewrite one managed agent's durable principal id and dependent
        rows.
      operationId: rekey_account_managed_agent_api_v1_agents__agent_id__rekey_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentRekeyRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentRekeyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{survivor_id}/merge:
    post:
      tags:
      - Account
      summary: Merge Account Managed Agents
      description: Merge a duplicate managed agent into a survivor (dry-run capable).
      operationId: merge_account_managed_agents_api_v1_agents__survivor_id__merge_post
      security:
      - bearerAuth: []
      parameters:
      - name: survivor_id
        in: path
        required: true
        schema:
          type: string
          title: Survivor Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManagedAgentMergeRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManagedAgentMergeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions:
    get:
      tags:
      - Account
      summary: List Account Runtime Sessions
      description: List runtime sessions for the current account.
      operationId: list_account_runtime_sessions_api_v1_runtime_sessions_get
      security:
      - bearerAuth: []
      parameters:
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Query
      - name: session_source_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
      - name: status
        in: query
        required: false
        schema:
          type: string
          pattern: ^(all|active|ended)$
          default: all
          title: Status
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuntimeSessionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}:
    get:
      tags:
      - Account
      summary: Get Account Runtime Session Detail
      description: Return one runtime session detail summary without heavy arrays.
      operationId: get_account_runtime_session_detail_api_v1_runtime_sessions__runtime_session_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuntimeSessionDetailResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Account
      summary: Update Account Runtime Session
      description: Update runtime-session lifecycle controls for the current account.
      operationId: update_account_runtime_session_api_v1_runtime_sessions__runtime_session_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/interactions:
    get:
      tags:
      - Account
      summary: Get Account Session Interactions
      description: Paginated search across captured interactions for this session.
      operationId: get_account_session_interactions_api_v1_runtime_sessions__runtime_session_id__interactions_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: interaction_query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Interaction Query
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: interaction_limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          minimum: 1
          default: 50
          title: Interaction Limit
      - name: interaction_offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Interaction Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGatewayUsageSearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/activity:
    get:
      tags:
      - Account
      summary: Get Account Session Activity Timeline
      description: Activity timeline overview for this session.
      operationId: get_account_session_activity_timeline_api_v1_runtime_sessions__runtime_session_id__activity_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionActivityListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/requests:
    get:
      tags:
      - Account
      summary: List Account Runtime Session Requests
      description: 'Return per-request gateway rows for one runtime session.


        This powers the unified session timeline by reading the real per-request

        ``ApiUsage`` rows (one per gateway request) rather than the sparse captured

        gateway events. Each item carries its tokens, estimated spend, status, and

        the tools it included with their per-tool schema token cost.'
      operationId: list_account_runtime_session_requests_api_v1_runtime_sessions__runtime_session_id__requests_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 100
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      - name: failed_only
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Failed Only
      - name: event_ids
        in: query
        required: false
        schema:
          anyOf:
          - type: array
            items:
              type: string
          - type: 'null'
          title: Event Ids
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionRequestListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/summaries:
    post:
      tags:
      - Account
      summary: Summarize Account Runtime Session
      description: Summarize one runtime session without hidden inspection spend.
      operationId: summarize_account_runtime_session_api_v1_runtime_sessions__runtime_session_id__summaries_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionSummaryInsight'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/gateway-events:
    get:
      tags:
      - Account
      summary: Get Account Runtime Session Gateway Events
      description: Return one compact page of stored gateway event metadata for a
        session.
      operationId: get_account_runtime_session_gateway_events_api_v1_runtime_sessions__runtime_session_id__gateway_events_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: tail
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tail
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 5000
          minimum: 1
          default: 25
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      - name: metadata_only
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Metadata Only
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Account Runtime Session Gateway Events Api V1
                  Runtime Sessions  Runtime Session Id  Gateway Events Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/gateway-events/{activity_id}:
    get:
      tags:
      - Account
      summary: Get Account Runtime Session Gateway Event Detail
      description: Return the raw, massive stored gateway event JSON detail for one
        runtime session activity.
      operationId: get_account_runtime_session_gateway_event_detail_api_v1_runtime_sessions__runtime_session_id__gateway_events__activity_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: activity_id
        in: path
        required: true
        schema:
          type: string
          title: Activity Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Account Runtime Session Gateway Event Detail Api
                  V1 Runtime Sessions  Runtime Session Id  Gateway Events  Activity
                  Id  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/gateway-events/{activity_id}/summary:
    post:
      tags:
      - Account
      summary: Summarize Account Runtime Session Gateway Event
      description: Summarize one gateway interaction on demand using the account default
        model.
      operationId: summarize_account_runtime_session_gateway_event_api_v1_runtime_sessions__runtime_session_id__gateway_events__activity_id__summary_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: activity_id
        in: path
        required: true
        schema:
          type: string
          title: Activity Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionInteractionSummary'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/telemetry/dashboard:
    get:
      tags:
      - Account
      summary: Get Dashboard Telemetry
      description: Aggregate high-level metrics for the new global dashboard.
      operationId: get_dashboard_telemetry_api_v1_account_telemetry_dashboard_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardTelemetryResponse'
      security:
      - bearerAuth: []
  /api/v1/users/me/avatar:
    put:
      tags:
      - Account
      summary: Upload Avatar
      description: 'Upload or replace the current user''s profile avatar.


        Accepts PNG, JPEG, WebP, or GIF images up to 5 MB. The image is

        validated, EXIF-stripped, cropped to a center square, and re-encoded as a

        bounded PNG stored as a base64 data URI. Manual uploads always take

        precedence over SSO-provided avatars.'
      operationId: upload_avatar_api_v1_users_me_avatar_put
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_upload_avatar_api_v1_users_me_avatar_put'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvatarResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
    delete:
      tags:
      - Account
      summary: Delete Avatar
      description: Remove the current user's avatar, falling back to the default.
      operationId: delete_avatar_api_v1_users_me_avatar_delete
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvatarResponse'
      security:
      - bearerAuth: []
  /api/v1/attention/dismissals:
    get:
      tags:
      - Account
      summary: List Attention Dismissals
      description: 'List the console attention items this account has silenced.


        Reading is open to any member of the account: the console needs this list

        to render the inbox at all, and a member who cannot see it would be shown

        items their colleagues have already dealt with.


        Expired snoozes are excluded and garbage-collected here, so "hidden until"

        never quietly becomes "hidden forever".'
      operationId: list_attention_dismissals_api_v1_attention_dismissals_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttentionDismissalListResponse'
      security:
      - bearerAuth: []
  /api/v1/attention/dismissals/{item_id}:
    put:
      tags:
      - Account
      summary: Upsert Attention Dismissal
      description: 'Silence one attention item until its fingerprint changes.


        ``item_id`` is the console''s stable id for the item (``<kind>:<entity

        id>``), and the body''s ``fingerprint`` is why it was showing. Dismissing

        an item that has already been dismissed replaces the row, which is how an

        item that came back with a new fingerprint gets silenced again.


        Writing takes ``manage_agents``: an operator who may reconfigure the fleet

        may also declare that one of its states is expected.'
      operationId: upsert_attention_dismissal_api_v1_attention_dismissals__item_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          minLength: 1
          maxLength: 255
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttentionDismissalUpsertRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttentionDismissalResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Account
      summary: Delete Attention Dismissal
      description: Restore a silenced item so it shows in the inbox again.
      operationId: delete_attention_dismissal_api_v1_attention_dismissals__item_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          minLength: 1
          maxLength: 255
          title: Item Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/account/deletion-request:
    post:
      tags:
      - Account
      summary: Request Account Deletion
      description: "Public endpoint to notify admins of account deletion request.\n\
        \nThis endpoint is called from the public delete-account page and sends\n\
        notifications to admins via email and configured webhooks (Slack/Mattermost).\n\
        \nArgs:\n    deletion_request: Account deletion request details\n\nReturns:\n\
        \    Success message"
      operationId: request_account_deletion_api_v1_account_deletion_request_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountDeletionRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/trackers:
    get:
      tags:
      - Trackers
      summary: List Trackers
      description: List all non-deleted trackers for the current user.
      operationId: list_trackers_api_v1_trackers_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TrackerResponse'
                type: array
                title: Response List Trackers Api V1 Trackers Get
      security:
      - bearerAuth: []
    post:
      tags:
      - Trackers
      summary: Register Tracker
      description: "Register a new issue tracker.\n\nArgs:\n    tracker_data: Tracker\
        \ registration data.\n    background_tasks: Background tasks for sending emails.\n\
        \    current_user: The current authenticated user.\n\nReturns:\n    The registered\
        \ tracker ID.\n\nRaises:\n    HTTPException: If registration fails."
      operationId: register_tracker_api_v1_trackers_post
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackerCreateResponse'
      security:
      - bearerAuth: []
  /api/v1/trackers/{tracker_id}:
    get:
      tags:
      - Trackers
      summary: Get Tracker
      description: Get a non-deleted tracker by ID, ensuring it belongs to the current
        user.
      operationId: get_tracker_api_v1_trackers__tracker_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: tracker_id
        in: path
        required: true
        schema:
          type: string
          format: uuid4
          title: Tracker Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Trackers
      summary: Update Tracker
      description: Update an existing tracker.
      operationId: update_tracker_api_v1_trackers__tracker_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: tracker_id
        in: path
        required: true
        schema:
          type: string
          format: uuid4
          title: Tracker Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrackerUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Trackers
      summary: Delete Tracker
      description: Delete a tracker by ID (soft delete by default, hard delete if
        specified).
      operationId: delete_tracker_api_v1_trackers__tracker_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: tracker_id
        in: path
        required: true
        schema:
          type: string
          format: uuid4
          title: Tracker Id
      - name: hard_delete
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Hard Delete
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Delete Tracker Api V1 Trackers  Tracker Id  Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/trackers/{tracker_id}/sync:
    post:
      tags:
      - Trackers
      summary: Sync Tracker
      description: Queue a background sync for an existing tracker.
      operationId: sync_tracker_api_v1_trackers__tracker_id__sync_post
      security:
      - bearerAuth: []
      parameters:
      - name: tracker_id
        in: path
        required: true
        schema:
          type: string
          format: uuid4
          title: Tracker Id
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Sync Tracker Api V1 Trackers  Tracker Id  Sync Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/trackers/test-and-list-orgs:
    post:
      tags:
      - Trackers
      summary: Test Connection And List Orgs
      description: 'Tests connection to a tracker and lists accessible organizations/groups.

        This endpoint does not fetch the projects within each organization.'
      operationId: test_connection_and_list_orgs_api_v1_trackers_test_and_list_orgs_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrackerTestRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackerTestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/trackers/list-projects-for-org:
    post:
      tags:
      - Trackers
      summary: List Projects For Org
      description: Lists projects for a specific organization/group within a tracker.
      operationId: list_projects_for_org_api_v1_trackers_list_projects_for_org_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrackerTestRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ProjectIdentifier'
                type: array
                title: Response List Projects For Org Api V1 Trackers List Projects
                  For Org Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/mcp-servers:
    get:
      tags:
      - MCP Servers
      summary: List Mcp Servers
      description: "List all MCP servers for the current user.\n\nArgs:\n    current_user:\
        \ Current authenticated user\n    db: Database session\n\nReturns:\n    List\
        \ of MCP servers"
      operationId: list_mcp_servers_api_v1_mcp_servers_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/MCPServerResponse'
                type: array
                title: Response List Mcp Servers Api V1 Mcp Servers Get
      security:
      - bearerAuth: []
    post:
      tags:
      - MCP Servers
      summary: Create Mcp Server
      description: "Create a new external MCP server configuration.\n\nArgs:\n   \
        \ server_data: MCP server configuration data\n    current_user: Current authenticated\
        \ user\n    db: Database session\n\nReturns:\n    Created MCP server\n\nRaises:\n\
        \    HTTPException: If creation fails or validation fails"
      operationId: create_mcp_server_api_v1_mcp_servers_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MCPServerCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPServerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/mcp-servers/{server_id}:
    get:
      tags:
      - MCP Servers
      summary: Get Mcp Server
      description: "Get a specific MCP server by ID.\n\nArgs:\n    server_id: MCP\
        \ server ID\n    current_user: Current authenticated user\n    db: Database\
        \ session\n\nReturns:\n    MCP server details\n\nRaises:\n    HTTPException:\
        \ If server not found or access denied"
      operationId: get_mcp_server_api_v1_mcp_servers__server_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Server Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPServerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - MCP Servers
      summary: Update Mcp Server
      description: "Update an existing MCP server configuration.\n\nArgs:\n    server_id:\
        \ MCP server ID\n    server_update: Updated server data\n    current_user:\
        \ Current authenticated user\n    db: Database session\n\nReturns:\n    Updated\
        \ MCP server\n\nRaises:\n    HTTPException: If server not found or update\
        \ fails"
      operationId: update_mcp_server_api_v1_mcp_servers__server_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Server Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MCPServerUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPServerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - MCP Servers
      summary: Delete Mcp Server
      description: "Delete an MCP server.\n\nArgs:\n    server_id: MCP server ID\n\
        \    current_user: Current authenticated user\n    db: Database session\n\n\
        Returns:\n    Success message\n\nRaises:\n    HTTPException: If server not\
        \ found or deletion fails"
      operationId: delete_mcp_server_api_v1_mcp_servers__server_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Server Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Delete Mcp Server Api V1 Mcp Servers  Server Id  Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/mcp-servers/{server_id}/scan:
    post:
      tags:
      - MCP Servers
      summary: Scan Mcp Server
      description: "Trigger a tool discovery scan for an MCP server.\n\nArgs:\n  \
        \  server_id: MCP server ID\n    current_user: Current authenticated user\n\
        \    db: Database session\n\nReturns:\n    Success message with tool count\n\
        \nRaises:\n    HTTPException: If server not found or scan fails"
      operationId: scan_mcp_server_api_v1_mcp_servers__server_id__scan_post
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Server Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Scan Mcp Server Api V1 Mcp Servers  Server Id  Scan
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/mcp-servers/{server_id}/tools:
    get:
      tags:
      - MCP Servers
      summary: List Mcp Server Tools
      description: "List discovered tools for an MCP server.\n\nArgs:\n    server_id:\
        \ MCP server ID\n    current_user: Current authenticated user\n    db: Database\
        \ session\n\nReturns:\n    List of discovered tools\n\nRaises:\n    HTTPException:\
        \ If server not found or access denied"
      operationId: list_mcp_server_tools_api_v1_mcp_servers__server_id__tools_get
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Server Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MCPToolResponse'
                title: Response List Mcp Server Tools Api V1 Mcp Servers  Server Id  Tools
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/mcp-servers/{server_id}/oauth/authorize-token:
    post:
      tags:
      - MCP Servers
      summary: Create Mcp Oauth Authorize Token
      description: 'Mint a short-lived, single-purpose token for the browser OAuth
        redirect.


        The browser navigates to the authorize endpoint via a full-page redirect

        and cannot attach an Authorization header, so the token must ride in the

        URL. Rather than exposing the reusable access token there (where it would

        land in browser history, server access logs, and the Referer sent onward

        to the external OAuth provider), this endpoint — authenticated normally via

        the Authorization header — returns a token scoped to this one server and

        purpose and expiring in ~2 minutes.'
      operationId: create_mcp_oauth_authorize_token_api_v1_mcp_servers__server_id__oauth_authorize_token_post
      security:
      - bearerAuth: []
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          title: Server Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Create Mcp Oauth Authorize Token Api V1 Mcp Servers  Server
                  Id  Oauth Authorize Token Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/mcp-servers/{server_id}/oauth/authorize:
    get:
      tags:
      - MCP Servers OAuth
      summary: Mcp Server Oauth Authorize
      description: 'Initiate the MCP OAuth client flow for an external MCP server.


        This endpoint is on the public router because the browser navigates

        to it directly (302 redirect flow). Auth is via the short-lived,

        server-scoped ``code`` minted by ``POST .../oauth/authorize-token`` —

        never the reusable access token.


        1. Fetches the server''s /.well-known/oauth-authorization-server metadata.

        2. Dynamically registers as an OAuth client (RFC 7591) if needed.

        3. Generates PKCE code_challenge / code_verifier.

        4. Redirects the user to the external server''s authorization endpoint.'
      operationId: mcp_server_oauth_authorize_api_v1_mcp_servers__server_id__oauth_authorize_get
      parameters:
      - name: server_id
        in: path
        required: true
        schema:
          type: string
          title: Server Id
      - name: code
        in: query
        required: true
        schema:
          type: string
          description: Short-lived authorize token from /oauth/authorize-token
          title: Code
        description: Short-lived authorize token from /oauth/authorize-token
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/mcp-servers/oauth/callback:
    get:
      tags:
      - MCP Servers OAuth
      summary: Mcp Server Oauth Callback
      description: 'Handle the OAuth callback from an external MCP server.


        Exchanges the authorization code for access/refresh tokens using PKCE,

        saves them in the MCP server''s auth_config, and redirects to the frontend.'
      operationId: mcp_server_oauth_callback_api_v1_mcp_servers_oauth_callback_get
      parameters:
      - name: code
        in: query
        required: true
        schema:
          type: string
          title: Code
      - name: state
        in: query
        required: true
        schema:
          type: string
          title: State
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/tools/stats:
    get:
      tags:
      - Tools
      summary: Get Tool Usage Stats
      description: Return account-wide tool invocation counts and schema-injection
        cost.
      operationId: get_tool_usage_stats_api_v1_tools_stats_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolUsageStatsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/tools:
    get:
      tags:
      - Tools
      summary: List All Tools
      description: "List all available tools (builtin + external) with their configuration\
        \ status.\n\nReturns a comprehensive list of:\n- All builtin tools\n- All\
        \ tools from active MCP servers\n- Native agent tools from the catalogue,\
        \ plus any seen agent-source\n  configurations that are not in the catalogue\n\
        - Configuration status for each tool (enabled/disabled, preloop)\n\nArgs:\n\
        \    account: Current user's account (from dependency)\n    db: Database session\n\
        \nReturns:\n    List of tool dictionaries with metadata and configuration"
      operationId: list_all_tools_api_v1_tools_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  additionalProperties: true
                  type: object
                type: array
                title: Response List All Tools Api V1 Tools Get
      security:
      - bearerAuth: []
  /api/v1/tool-configurations:
    post:
      tags:
      - Tools
      summary: Create Tool Configuration
      description: "Create a new tool configuration.\n\nArgs:\n    config_data: Tool\
        \ configuration data\n    account: Current user's account (from dependency)\n\
        \    db: Database session\n\nReturns:\n    Created tool configuration\n\n\
        Raises:\n    HTTPException: If configuration already exists or creation fails"
      operationId: create_tool_configuration_api_v1_tool_configurations_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolConfigurationCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/tool-configurations/{config_id}:
    get:
      tags:
      - Tools
      summary: Get Tool Configuration
      description: "Get a specific tool configuration.\n\nArgs:\n    config_id: Tool\
        \ configuration ID\n    account: Current user's account (from dependency)\n\
        \    db: Database session\n\nReturns:\n    Tool configuration details\n\n\
        Raises:\n    HTTPException: If configuration not found or access denied"
      operationId: get_tool_configuration_api_v1_tool_configurations__config_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Tools
      summary: Update Tool Configuration
      description: "Update an existing tool configuration.\n\nArgs:\n    config_id:\
        \ Tool configuration ID\n    config_update: Updated configuration data\n \
        \   account: Current user's account (from dependency)\n    db: Database session\n\
        \nReturns:\n    Updated tool configuration\n\nRaises:\n    HTTPException:\
        \ If configuration not found or update fails"
      operationId: update_tool_configuration_api_v1_tool_configurations__config_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolConfigurationUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Tools
      summary: Delete Tool Configuration
      description: "Delete a tool configuration.\n\nArgs:\n    config_id: Tool configuration\
        \ ID\n    account: Current user's account (from dependency)\n    db: Database\
        \ session\n\nReturns:\n    Success message\n\nRaises:\n    HTTPException:\
        \ If configuration not found or deletion fails"
      operationId: delete_tool_configuration_api_v1_tool_configurations__config_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Delete Tool Configuration Api V1 Tool Configurations  Config
                  Id  Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/tool-configurations/{config_id}/condition:
    put:
      tags:
      - Tools
      summary: Update Tool Approval Condition
      description: "Update or create approval condition for a tool configuration.\n\
        \nThis endpoint uses the new ToolAccessRule model (replaced ToolApprovalCondition).\n\
        For backward compatibility, it manages a single 'require_approval' rule per\
        \ tool.\n\nArgs:\n    config_id: Tool configuration ID\n    condition_data:\
        \ Condition data with 'approval_condition' field\n    account: Current user's\
        \ account\n    db: Database session\n\nReturns:\n    Updated tool configuration\n\
        \nRaises:\n    HTTPException: If configuration not found or update fails"
      operationId: update_tool_approval_condition_api_v1_tool_configurations__config_id__condition_put
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              title: Condition Data
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-workflows:
    get:
      tags:
      - Tools
      summary: List Approval Workflows
      description: "List all approval workflows for the current user's account.\n\n\
        Args:\n    account: Current user's account (from dependency)\n    db: Database\
        \ session\n\nReturns:\n    List of approval workflows"
      operationId: list_approval_workflows_api_v1_approval_workflows_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApprovalWorkflowResponse'
                type: array
                title: Response List Approval Workflows Api V1 Approval Workflows
                  Get
      security:
      - bearerAuth: []
    post:
      tags:
      - Tools
      summary: Create Approval Workflow
      description: "Create a reusable approval workflow.\n\nArgs:\n    workflow_data:\
        \ Approval workflow data\n    account: Current user's account (from dependency)\n\
        \    db: Database session\n\nReturns:\n    Created approval workflow\n\nRaises:\n\
        \    HTTPException: If workflow with same name already exists or creation\
        \ fails"
      operationId: create_approval_workflow_api_v1_approval_workflows_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalWorkflowCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalWorkflowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/approval-workflows/{workflow_id}:
    get:
      tags:
      - Tools
      summary: Get Approval Workflow
      description: "Get an approval workflow by ID.\n\nArgs:\n    workflow_id: Approval\
        \ workflow ID\n    account: Current user's account (from dependency)\n   \
        \ db: Database session\n\nReturns:\n    Approval workflow details\n\nRaises:\n\
        \    HTTPException: If workflow not found or access denied"
      operationId: get_approval_workflow_api_v1_approval_workflows__workflow_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: workflow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Workflow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalWorkflowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Tools
      summary: Update Approval Workflow
      description: "Update an approval workflow.\n\nArgs:\n    workflow_id: Approval\
        \ workflow ID\n    workflow_update: Updated workflow data\n    account: Current\
        \ user's account (from dependency)\n    db: Database session\n\nReturns:\n\
        \    Updated approval workflow\n\nRaises:\n    HTTPException: If workflow\
        \ not found or update fails"
      operationId: update_approval_workflow_api_v1_approval_workflows__workflow_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: workflow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Workflow Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalWorkflowUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalWorkflowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Tools
      summary: Delete Approval Workflow
      description: "Delete an approval workflow.\n\nNote: This will set approval_workflow_id\
        \ to NULL for any tool configurations\nusing this workflow (due to ondelete=\"\
        SET NULL\").\n\nArgs:\n    workflow_id: Approval workflow ID\n    account:\
        \ Current user's account (from dependency)\n    db: Database session\n\nReturns:\n\
        \    Success message\n\nRaises:\n    HTTPException: If workflow not found\
        \ or deletion fails"
      operationId: delete_approval_workflow_api_v1_approval_workflows__workflow_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: workflow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Workflow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Delete Approval Workflow Api V1 Approval Workflows  Workflow
                  Id  Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/tool-configurations/{config_id}/approval-condition:
    get:
      tags:
      - Tools
      summary: Get Tool Approval Condition
      description: "Get the access rule (approval condition) for a tool configuration.\n\
        \nThis endpoint uses the new ToolAccessRule model (replaced ToolApprovalCondition).\n\
        For backward compatibility, it returns the first 'require_approval' rule.\n\
        \nArgs:\n    config_id: Tool configuration ID\n    account: Current user's\
        \ account (from dependency)\n    db: Database session\n\nReturns:\n    Tool\
        \ access rule as approval condition response\n\nRaises:\n    HTTPException:\
        \ If tool configuration not found or no rules found"
      operationId: get_tool_approval_condition_api_v1_tool_configurations__config_id__approval_condition_get
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolApprovalConditionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Tools
      summary: Create Or Update Tool Approval Condition
      description: "Create or update the access rule for a tool configuration.\n\n\
        This endpoint uses the new ToolAccessRule model (replaced ToolApprovalCondition).\n\
        For backward compatibility, it manages a single 'require_approval' rule per\
        \ tool.\n\nArgs:\n    config_id: Tool configuration ID\n    condition_in:\
        \ Condition data\n    account: Current user's account (from dependency)\n\
        \    db: Database session\n\nReturns:\n    Created or updated access rule\
        \ as approval condition response\n\nRaises:\n    HTTPException: If tool configuration\
        \ not found or creation fails"
      operationId: create_or_update_tool_approval_condition_api_v1_tool_configurations__config_id__approval_condition_put
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolApprovalConditionCreate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolApprovalConditionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Tools
      summary: Delete Tool Approval Condition
      description: "Delete the access rules for a tool configuration.\n\nThis endpoint\
        \ uses the new ToolAccessRule model (replaced ToolApprovalCondition).\nIt\
        \ deletes all access rules for the specified tool configuration.\n\nArgs:\n\
        \    config_id: Tool configuration ID\n    account: Current user's account\
        \ (from dependency)\n    db: Database session\n\nReturns:\n    Success message\n\
        \nRaises:\n    HTTPException: If tool configuration not found or no rules\
        \ found"
      operationId: delete_tool_approval_condition_api_v1_tool_configurations__config_id__approval_condition_delete
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: string
                title: Response Delete Tool Approval Condition Api V1 Tool Configurations  Config
                  Id  Approval Condition Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/tool-configurations/{config_id}/approval-condition/test:
    post:
      tags:
      - Tools
      summary: Test Approval Condition
      description: "Test a CEL expression against sample arguments.\n\nThis endpoint\
        \ allows testing approval conditions before saving them.\nIt's a proprietary\
        \ feature for validating CEL expressions.\n\nArgs:\n    config_id: Tool configuration\
        \ ID\n    test_request: Test request with expression and sample args\n   \
        \ account: Current user's account (from dependency)\n    db: Database session\n\
        \nReturns:\n    Test result with match status and evaluation context\n\nRaises:\n\
        \    HTTPException: If tool configuration not found or evaluation fails"
      operationId: test_approval_condition_api_v1_tool_configurations__config_id__approval_condition_test_post
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConditionTestRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConditionTestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/tool-configurations/{config_id}/access-rules:
    get:
      tags:
      - Tools
      summary: List Access Rules
      description: List all access rules for a tool configuration.
      operationId: list_access_rules_api_v1_tool_configurations__config_id__access_rules_get
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AccessRuleResponse'
                title: Response List Access Rules Api V1 Tool Configurations  Config
                  Id  Access Rules Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Tools
      summary: Create Access Rule
      description: Create a new access rule for a tool configuration.
      operationId: create_access_rule_api_v1_tool_configurations__config_id__access_rules_post
      security:
      - bearerAuth: []
      parameters:
      - name: config_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Config Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessRuleCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRuleResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/access-rules/{rule_id}:
    put:
      tags:
      - Tools
      summary: Update Access Rule
      description: Update an access rule.
      operationId: update_access_rule_api_v1_access_rules__rule_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Rule Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessRuleUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRuleResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Tools
      summary: Delete Access Rule
      description: Delete an access rule.
      operationId: delete_access_rule_api_v1_access_rules__rule_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Rule Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/{request_id}:
    get:
      tags:
      - Approval Requests
      - approval_requests
      summary: Get Approval Request
      description: "Get an approval request by ID.\n\nArgs:\n    request_id: Approval\
        \ request ID\n    current_user: Current authenticated user\n    db: Database\
        \ session\n\nReturns:\n    Approval request\n\nRaises:\n    HTTPException:\
        \ If request not found or unauthorized"
      operationId: get_approval_request_api_v1_approval_requests__request_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Request Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/{request_id}/history:
    get:
      tags:
      - Approval Requests
      - approval_requests
      summary: Get Approval Request History
      description: "Get the workflow-history timeline of an approval request.\n\n\
        Returns every lifecycle event in order: request creation, per-channel\nnotification\
        \ fan-outs, opens, votes (with actor), escalations, and the\nfinal resolution\
        \ or expiry.\n\nArgs:\n    request_id: Approval request ID\n    current_user:\
        \ Current authenticated user\n    db: Database session\n\nReturns:\n    Timeline\
        \ events ordered by timestamp\n\nRaises:\n    HTTPException: If request not\
        \ found or unauthorized"
      operationId: get_approval_request_history_api_v1_approval_requests__request_id__history_get
      security:
      - bearerAuth: []
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Request Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ApprovalEventResponse'
                title: Response Get Approval Request History Api V1 Approval Requests  Request
                  Id  History Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests:
    get:
      tags:
      - Approval Requests
      - approval_requests
      summary: List Approval Requests
      description: "List approval requests for the current account.\n\nArgs:\n   \
        \ status: Filter by status (pending, approved, declined, etc.)\n    execution_id:\
        \ Filter by execution ID\n    limit: Maximum number of results\n    skip:\
        \ Number of results to skip\n    current_user: Current authenticated user\n\
        \nReturns:\n    List of approval requests"
      operationId: list_approval_requests_api_v1_approval_requests_get
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by status
          title: Status
        description: Filter by status
      - name: execution_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by execution ID
          title: Execution Id
        description: Filter by execution ID
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          description: Maximum number of results
          default: 50
          title: Limit
        description: Maximum number of results
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          description: Number of results to skip
          default: 0
          title: Skip
        description: Number of results to skip
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ApprovalRequestResponse'
                title: Response List Approval Requests Api V1 Approval Requests Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/{request_id}/approve:
    post:
      tags:
      - Approval Requests
      - approval_requests
      summary: Approve Request
      description: "Approve an approval request.\n\nArgs:\n    request_id: Approval\
        \ request ID\n    decision: Approval decision with optional comment\n    request:\
        \ HTTP request\n    current_user: Current authenticated user\n\nReturns:\n\
        \    Updated approval request\n\nRaises:\n    HTTPException: If request not\
        \ found or unauthorized"
      operationId: approve_request_api_v1_approval_requests__request_id__approve_post
      security:
      - bearerAuth: []
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Request Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecision'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/{request_id}/decline:
    post:
      tags:
      - Approval Requests
      - approval_requests
      summary: Decline Request
      description: "Decline an approval request.\n\nArgs:\n    request_id: Approval\
        \ request ID\n    decision: Approval decision with optional comment\n    request:\
        \ HTTP request\n    current_user: Current authenticated user\n\nReturns:\n\
        \    Updated approval request\n\nRaises:\n    HTTPException: If request not\
        \ found or unauthorized"
      operationId: decline_request_api_v1_approval_requests__request_id__decline_post
      security:
      - bearerAuth: []
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Request Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecision'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/{request_id}/decide:
    post:
      tags:
      - Approval Requests
      - approval_requests
      summary: Decide Request
      description: "Approve or decline an approval request based on decision.approved.\n\
        \nThis is a convenience endpoint that calls approve or decline based on\n\
        the decision.approved boolean.\n\nArgs:\n    request_id: Approval request\
        \ ID\n    decision: Approval decision with approved flag and optional comment\n\
        \    request: HTTP request\n    current_user: Current authenticated user\n\
        \nReturns:\n    Updated approval request\n\nRaises:\n    HTTPException: If\
        \ request not found or unauthorized"
      operationId: decide_request_api_v1_approval_requests__request_id__decide_post
      security:
      - bearerAuth: []
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Request Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecision'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-requests/decide-batch:
    post:
      tags:
      - Approval Requests
      - approval_requests
      summary: Decide Requests Batch
      description: "Approve or decline several requests with one decision.\n\nAn operator\
        \ clearing an inbox picks the rows first and decides once. Doing\nthat as\
        \ N round trips means N approvals racing for the same expiry window\nand N\
        \ chances for the page to lose track of which ones landed, so the\nconsole\
        \ sends the whole selection here.\n\nThe batch never fails as a whole: each\
        \ request is decided on its own and\nreported on its own, so an id that expired\
        \ while the operator was reading\ncosts that row and nothing else.\n\nArgs:\n\
        \    decision: The ids to decide, the decision, and an optional comment\n\
        \    request: HTTP request\n    current_user: Current authenticated user\n\
        \    db: Async session used for the decisions\n    sync_db: Sync session from\
        \ the RBAC dependency; used to reconcile\n        security-maintenance items\
        \ after a successful decision\n\nReturns:\n    One result per requested id,\
        \ in the order they were sent"
      operationId: decide_requests_batch_api_v1_approval_requests_decide_batch_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalBatchDecision'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalBatchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/approval-bypasses/status:
    get:
      tags:
      - Approval Bypasses
      - approval_bypasses
      summary: Get Bypass Status
      description: "Report whether any bypass is currently in force for this user.\n\
        \nConsole and mobile poll this to drive the persistent warning banner. It\
        \ is\nintentionally readable by any authenticated user without a special\n\
        permission: knowing that governance is currently relaxed is not privileged\n\
        information, and hiding it would defeat the point.\n\nArgs:\n    current_user:\
        \ Current authenticated user.\n    db: Database session.\n\nReturns:\n   \
        \ Aggregate bypass status for banner rendering."
      operationId: get_bypass_status_api_v1_approval_bypasses_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalBypassStatus'
      security:
      - bearerAuth: []
  /api/v1/approval-bypasses:
    get:
      tags:
      - Approval Bypasses
      - approval_bypasses
      summary: List Bypasses
      description: "List every active bypass in the account.\n\nAccount-wide visibility\
        \ is deliberate: if a teammate has relaxed gating,\neveryone governing the\
        \ same fleet should be able to see it.\n\nArgs:\n    current_user: Current\
        \ authenticated user.\n    db: Database session.\n\nReturns:\n    Active bypasses,\
        \ soonest-expiring last."
      operationId: list_bypasses_api_v1_approval_bypasses_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApprovalBypassResponse'
                type: array
                title: Response List Bypasses Api V1 Approval Bypasses Get
      security:
      - bearerAuth: []
    post:
      tags:
      - Approval Bypasses
      - approval_bypasses
      summary: Create Bypass
      description: "Open a time-boxed bypass for the calling user.\n\nThe bypass always\
        \ targets ``current_user``; there is no parameter to target\nsomebody else.\
        \ Duration is validated by the schema against the hard cap, so\nan unbounded\
        \ bypass cannot be constructed.\n\nArgs:\n    payload: Mode, duration, optional\
        \ agent scope and reason.\n    current_user: Current authenticated user (the\
        \ bypass subject).\n    db: Database session.\n\nReturns:\n    The created\
        \ bypass.\n\nRaises:\n    HTTPException: If the duration is outside the permitted\
        \ range."
      operationId: create_bypass_api_v1_approval_bypasses_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalBypassCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalBypassResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/approval-bypasses/{bypass_id}:
    delete:
      tags:
      - Approval Bypasses
      - approval_bypasses
      summary: Revoke Bypass
      description: "End a bypass early.\n\nAny user who can view approvals in the\
        \ account may revoke a bypass, even\none they did not create. Re-tightening\
        \ a control is always safe, so it\nshould never be blocked on ownership.\n\
        \nArgs:\n    bypass_id: The bypass to revoke.\n    current_user: Current authenticated\
        \ user.\n    db: Database session.\n\nReturns:\n    The revoked bypass.\n\n\
        Raises:\n    HTTPException: If the bypass is not found in this account."
      operationId: revoke_bypass_api_v1_approval_bypasses__bypass_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: bypass_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Bypass Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalBypassResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/approval-bypasses/revoke-all:
    post:
      tags:
      - Approval Bypasses
      - approval_bypasses
      summary: Revoke All Bypasses
      description: "Revoke every active bypass in the account — the 'panic off' button.\n\
        \nExists so that restoring full governance is always a single unambiguous\n\
        action, with no list to scroll and no per-item taps, from any surface.\n\n\
        Args:\n    current_user: Current authenticated user.\n    db: Database session.\n\
        \nReturns:\n    The bypasses that were revoked."
      operationId: revoke_all_bypasses_api_v1_approval_bypasses_revoke_all_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApprovalBypassResponse'
                type: array
                title: Response Revoke All Bypasses Api V1 Approval Bypasses Revoke
                  All Post
      security:
      - bearerAuth: []
  /api/v1/agent-deployments/capabilities:
    get:
      tags:
      - Agent deployment
      summary: Capabilities
      description: Tell the console which backends the operator actually enabled.
      operationId: capabilities_api_v1_agent_deployments_capabilities_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: boolean
                type: object
                title: Response Capabilities Api V1 Agent Deployments Capabilities
                  Get
      security:
      - bearerAuth: []
  /api/v1/agent-deployments:
    post:
      tags:
      - Agent deployment
      summary: Deploy Agent
      description: 'Install and verify remotely while offloading short CRUD transactions.


        SSH credentials remain in request memory. A revocable bootstrap key allows

        enrollment to mint the runtime''s own credentials on the verified target.'
      operationId: deploy_agent_api_v1_agent_deployments_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentDeploymentRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Deploy Agent Api V1 Agent Deployments Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/account/kill-switch/status:
    get:
      tags:
      - Kill Switch
      summary: Get Kill Switch Status
      description: 'Report the account''s current kill-switch state.


        Readable by any authenticated user of the account: the console banner

        polls this, and a halted account must be visible to everyone governing

        it, not only to whoever flipped the switch.'
      operationId: get_kill_switch_status_api_v1_account_kill_switch_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KillSwitchStatus'
      security:
      - bearerAuth: []
  /api/v1/account/kill-switch/activate:
    post:
      tags:
      - Kill Switch
      summary: Activate Kill Switch
      description: 'Activate the kill switch for one or more traffic scopes.


        Defaults to a full halt (gateway, tools, flows). Activation is

        idempotent per scope and audited: the actor, timestamp, and reason are

        recorded on the halt row and in the audit log, and every process

        enforces the halt within seconds (the serving process immediately).'
      operationId: activate_kill_switch_api_v1_account_kill_switch_activate_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KillSwitchActivateRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KillSwitchStatus'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/account/kill-switch/deactivate:
    post:
      tags:
      - Kill Switch
      summary: Deactivate Kill Switch
      description: 'Re-enable one or more traffic scopes (staged recovery).


        Each scope lifts independently, so an operator can restore the gateway

        first, verify behavior, then tools, then flows — instead of one

        all-at-once un-halt. Deactivating a scope that is not halted is a

        no-op. Like activation, every transition is audited.'
      operationId: deactivate_kill_switch_api_v1_account_kill_switch_deactivate_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KillSwitchDeactivateRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KillSwitchStatus'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/exports/asset-register:
    get:
      tags:
      - DORA Exports
      summary: Export Asset Register
      description: 'The account''s agent-slice ICT assets as one flat register.


        Feeds an Art. 8 asset inventory and the ICT-service lines of an Art. 28

        register of information. It is an input to those documents, not either of

        them: Preloop knows the agents, tools, MCP servers, models, providers and

        runner hosts, and nothing else in the estate.'
      operationId: export_asset_register_api_v1_exports_asset_register_get
      security:
      - bearerAuth: []
      parameters:
      - name: format
        in: query
        required: false
        schema:
          type: string
          description: csv or json
          default: csv
          title: Format
        description: csv or json
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/exports/incident-candidates:
    get:
      tags:
      - DORA Exports
      summary: Export Incident Candidates
      description: 'Everything in the period that might need classifying under Art.
        17.


        Candidates, not incidents. Preloop reports what its own records show

        (failed executions, kill-switch activations, policy denies where a

        deployment persists them, budget denials, gateway upstream failures) with

        timestamps, correlation ids and the affected agent. Whether any of it is

        an ICT-related incident, and whether that incident is major, is the

        financial entity''s determination under Art. 17 to 19.'
      operationId: export_incident_candidates_api_v1_exports_incident_candidates_get
      security:
      - bearerAuth: []
      parameters:
      - name: from
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Start of the period, inclusive
          title: From
        description: Start of the period, inclusive
      - name: to
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: End of the period, exclusive
          title: To
        description: End of the period, exclusive
      - name: format
        in: query
        required: false
        schema:
          type: string
          description: csv or json
          default: csv
          title: Format
        description: csv or json
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/event-webhooks/catalogue:
    get:
      tags:
      - Outbound Webhooks
      summary: Get Webhook Catalogue
      description: Return the subscribable events and the delivery contract.
      operationId: get_webhook_catalogue_api_v1_event_webhooks_catalogue_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookCatalogue'
      security:
      - bearerAuth: []
  /api/v1/event-webhooks/endpoints:
    get:
      tags:
      - Outbound Webhooks
      summary: List Webhook Endpoints
      description: 'List this account''s webhook endpoints, newest first.


        Includes the read-only rows the approval-workflow shim owns, so an

        operator can see why a legacy approval webhook is failing.'
      operationId: list_webhook_endpoints_api_v1_event_webhooks_endpoints_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/WebhookEndpointRead'
                type: array
                title: Response List Webhook Endpoints Api V1 Event Webhooks Endpoints
                  Get
      security:
      - bearerAuth: []
    post:
      tags:
      - Outbound Webhooks
      summary: Create Webhook Endpoint
      description: Create an endpoint and return its signing secret, once.
      operationId: create_webhook_endpoint_api_v1_event_webhooks_endpoints_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointCreated'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/event-webhooks/endpoints/{endpoint_id}:
    patch:
      tags:
      - Outbound Webhooks
      summary: Update Webhook Endpoint
      description: Update an endpoint's URL, filter, description or active flag.
      operationId: update_webhook_endpoint_api_v1_event_webhooks_endpoints__endpoint_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Endpoint Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookEndpointUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Outbound Webhooks
      summary: Delete Webhook Endpoint
      description: Delete an endpoint and its delivery history.
      operationId: delete_webhook_endpoint_api_v1_event_webhooks_endpoints__endpoint_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Endpoint Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/event-webhooks/endpoints/{endpoint_id}/test:
    post:
      tags:
      - Outbound Webhooks
      summary: Test Webhook Endpoint
      description: 'Queue a ``webhook.test`` event for one endpoint.


        Queued, not sent: the response says the event is on its way, and the

        deliveries list says what the receiver actually answered. Reporting the

        outcome synchronously would mean putting an HTTP call back on the request

        path, which is the thing this change removed.'
      operationId: test_webhook_endpoint_api_v1_event_webhooks_endpoints__endpoint_id__test_post
      security:
      - bearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Endpoint Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookTestSendResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/event-webhooks/deliveries/dead-letter:
    get:
      tags:
      - Outbound Webhooks
      summary: List Dead Letter Deliveries
      description: List deliveries that exhausted their retries.
      operationId: list_dead_letter_deliveries_api_v1_event_webhooks_deliveries_dead_letter_get
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          minimum: 1
          default: 50
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookDeliveryRead'
                title: Response List Dead Letter Deliveries Api V1 Event Webhooks
                  Deliveries Dead Letter Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/event-webhooks/deliveries:
    get:
      tags:
      - Outbound Webhooks
      summary: List Webhook Deliveries
      description: 'List recent deliveries, newest first.


        Dead-lettered rows are visible here rather than only in a server log:

        they are the ones an operator needs to find and replay.'
      operationId: list_webhook_deliveries_api_v1_event_webhooks_deliveries_get
      security:
      - bearerAuth: []
      parameters:
      - name: endpoint_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Endpoint Id
      - name: delivery_status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: pending, delivered or dead
          title: Delivery Status
        description: pending, delivered or dead
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          minimum: 1
          default: 50
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookDeliveryRead'
                title: Response List Webhook Deliveries Api V1 Event Webhooks Deliveries
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/event-webhooks/deliveries/{event_id}/replay:
    post:
      tags:
      - Outbound Webhooks
      summary: Replay Webhook Event
      description: 'Re-queue one event id to every endpoint that already received
        it.


        The original rows are left alone: a replay is a new generation, so a

        dead-lettered attempt stays on the record.'
      operationId: replay_webhook_event_api_v1_event_webhooks_deliveries__event_id__replay_post
      security:
      - bearerAuth: []
      parameters:
      - name: event_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Event Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookReplayResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/retention/settings:
    get:
      tags:
      - Retention
      summary: Get Retention Settings
      description: Return this account's retention per record class, and the floor.
      operationId: get_retention_settings_api_v1_retention_settings_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RetentionSettingsRead'
      security:
      - bearerAuth: []
    put:
      tags:
      - Retention
      summary: Update Retention Settings
      description: 'Set retention per record class, never below the six month floor.


        The body is the full desired state: a class the caller omits, or sets to

        null, goes back to the deployment default.'
      operationId: update_retention_settings_api_v1_retention_settings_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetentionSettingsUpdate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RetentionSettingsRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/retention/purge-preview:
    get:
      tags:
      - Retention
      summary: Preview Retention Purge
      description: 'Count what the purge would remove today, without removing anything.


        Nobody should discover the effect of a retention setting by watching rows

        disappear. Rows under a legal hold are already excluded by the same

        filters the purge uses, so the count is the count.'
      operationId: preview_retention_purge_api_v1_retention_purge_preview_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RetentionPurgePreview'
      security:
      - bearerAuth: []
  /api/v1/retention/holds:
    get:
      tags:
      - Retention
      summary: List Legal Holds
      description: List this account's legal holds, newest first.
      operationId: list_legal_holds_api_v1_retention_holds_get
      security:
      - bearerAuth: []
      parameters:
      - name: active_only
        in: query
        required: false
        schema:
          type: boolean
          description: Hide released holds
          default: true
          title: Active Only
        description: Hide released holds
      - name: resource_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Resource Type
      - name: resource_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Only holds on this resource id
          title: Resource Id
        description: Only holds on this resource id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LegalHoldRead'
                title: Response List Legal Holds Api V1 Retention Holds Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Retention
      summary: Create Legal Hold
      description: Freeze one execution, approval, evidence pack or runtime session.
      operationId: create_legal_hold_api_v1_retention_holds_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LegalHoldCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LegalHoldRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/retention/holds/{hold_id}/release:
    post:
      tags:
      - Retention
      summary: Release Legal Hold
      description: Lift a hold. The record stays, with who lifted it and why.
      operationId: release_legal_hold_api_v1_retention_holds__hold_id__release_post
      security:
      - bearerAuth: []
      parameters:
      - name: hold_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Hold Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LegalHoldRelease'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LegalHoldRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/retention/exports:
    post:
      tags:
      - Retention
      summary: Create Period Export
      description: 'Build a tar.gz of one period: audit rows, approvals, receipts,
        holds.


        The archive carries ``manifest.json`` with a sha256 per member and a

        digest over the member list, in the same shape an evidence pack manifest

        uses, so one verifier covers both, plus ``signature.json``: a detached

        Ed25519 signature over the manifest bytes, checkable against the account''s

        published public key.'
      operationId: create_period_export_api_v1_retention_exports_post
      security:
      - bearerAuth: []
      parameters:
      - name: start
        in: query
        required: true
        schema:
          type: string
          description: Period start, inclusive (YYYY-MM-DD)
          title: Start
        description: Period start, inclusive (YYYY-MM-DD)
      - name: end
        in: query
        required: true
        schema:
          type: string
          description: Period end, exclusive (YYYY-MM-DD)
          title: End
        description: Period end, exclusive (YYYY-MM-DD)
      responses:
        '200':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/audit/chain/status:
    get:
      tags:
      - Audit chain
      summary: Get Chain Status
      description: Head, purge floor, sealing lag and the newest signed checkpoint.
      operationId: get_chain_status_api_v1_audit_chain_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChainStatusRead'
      security:
      - bearerAuth: []
  /api/v1/audit/chain/verify:
    get:
      tags:
      - Audit chain
      summary: Verify Chain
      description: 'Walk a range of the chain and report the first break.


        The range matters: rows below the purge floor are gone under a stated

        retention policy and rows written since the last seal are not chained

        yet. Claiming to verify either would be a lie of scope.


        The verification is itself audited. A read of the audit trail that leaves

        no trace is a gap in the audit trail.'
      operationId: verify_chain_api_v1_audit_chain_verify_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_seq
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
            minimum: 1
          - type: 'null'
          description: First sequence to check. Defaults to the purge floor.
          title: Start Seq
        description: First sequence to check. Defaults to the purge floor.
      - name: end_seq
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
            minimum: 1
          - type: 'null'
          description: Last sequence to check. Defaults to the head.
          title: End Seq
        description: Last sequence to check. Defaults to the head.
      - name: max_rows
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
            minimum: 1
          - type: 'null'
          description: Cut the range at this many rows and say so.
          title: Max Rows
        description: Cut the range at this many rows and say so.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChainVerifyRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/audit/chain/segment:
    get:
      tags:
      - Audit chain
      summary: Get Chain Segment
      description: Canonical payloads and stored hashes, for a client side walk.
      operationId: get_chain_segment_api_v1_audit_chain_segment_get
      security:
      - bearerAuth: []
      parameters:
      - name: after_seq
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Exclusive lower bound
          default: 0
          title: After Seq
        description: Exclusive lower bound
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 500
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChainSegmentRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/audit/chain/checkpoints:
    get:
      tags:
      - Audit chain
      summary: List Chain Checkpoints
      description: 'Signed checkpoints in sequence order.


        Worth keeping your own copies of. A checkpoint you stored last quarter is

        the only thing here that a rewritten chain cannot reproduce.'
      operationId: list_chain_checkpoints_api_v1_audit_chain_checkpoints_get
      security:
      - bearerAuth: []
      parameters:
      - name: after_seq
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: After Seq
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 50
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ChainCheckpointRead'
                title: Response List Chain Checkpoints Api V1 Audit Chain Checkpoints
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/signing/keys:
    get:
      tags:
      - Signing keys
      summary: List Signing Keys
      description: 'Public halves of every key this account has held.


        Retired keys stay listed: every signature they made is still valid, and a

        verifier holding a bundle from last year needs the key that signed it.'
      operationId: list_signing_keys_api_v1_signing_keys_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SigningKeyListRead'
      security:
      - bearerAuth: []
  /api/v1/signing/keys/rotate:
    post:
      tags:
      - Signing keys
      summary: Rotate Signing Key
      description: 'Retire the active key and mint its replacement.


        Signatures made by the retired key keep verifying. Rotation that

        invalidated them would revoke the customer''s own evidence, which is the

        opposite of the point.'
      operationId: rotate_signing_key_api_v1_signing_keys_rotate_post
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SigningKeyRotateRead'
      security:
      - bearerAuth: []
  /api/v1/notification-preferences/me:
    get:
      tags:
      - Notification Preferences
      summary: Get My Notification Preferences
      description: "Get current user's notification preferences.\n\nReturns:\n   \
        \ Notification preferences."
      operationId: get_my_notification_preferences_api_v1_notification_preferences_me_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreferencesResponse'
      security:
      - bearerAuth: []
    put:
      tags:
      - Notification Preferences
      summary: Update My Notification Preferences
      description: "Update current user's notification preferences.\n\nArgs:\n   \
        \ prefs_in: Preferences update data.\n\nReturns:\n    Updated notification\
        \ preferences."
      operationId: update_my_notification_preferences_api_v1_notification_preferences_me_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationPreferencesUpdate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreferencesResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/notification-preferences/me/register-device:
    post:
      tags:
      - Notification Preferences
      summary: Register Mobile Device
      description: "Register a mobile device for push notifications.\n\nArgs:\n  \
        \  device_in: Device registration data.\n\nReturns:\n    Updated notification\
        \ preferences."
      operationId: register_mobile_device_api_v1_notification_preferences_me_register_device_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobileDeviceRegistration'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreferencesResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/notification-preferences/me/device/{token}:
    delete:
      tags:
      - Notification Preferences
      summary: Unregister Mobile Device
      description: "Unregister a mobile device and delete associated API key.\n\n\
        Args:\n    token: Device token to remove.\n\nReturns:\n    Updated notification\
        \ preferences.\n\nRaises:\n    HTTPException: If preferences not found."
      operationId: unregister_mobile_device_api_v1_notification_preferences_me_device__token__delete
      security:
      - bearerAuth: []
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
          title: Token
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreferencesResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/notification-preferences/me/test-push:
    post:
      tags:
      - Notification Preferences
      summary: Send Test Push Notification
      description: "Send a clearly-labelled TEST notification to the caller's devices.\n\
        \nAdmin-only diagnostic. Exercises the real APNs/FCM/proxy delivery path and\n\
        returns the provider's verbatim result per device, so an otherwise silent\n\
        push failure becomes visible.\n\nThe test is fully isolated from governance:\
        \ no ApprovalRequest row is\ncreated, so nothing waits on it, it appears in\
        \ no approval list, and\napproving or declining it changes no state.\n\nArgs:\n\
        \    request_in: Which kind of test to send ('approval' or 'question').\n\n\
        Returns:\n    Per-device send results.\n\nRaises:\n    HTTPException: 403\
        \ if the caller is not an admin, 404 if no devices are\n        registered,\
        \ 429 if the rate limit is exceeded."
      operationId: send_test_push_notification_api_v1_notification_preferences_me_test_push_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestPushRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestPushResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/notification-preferences/me/qr-code:
    get:
      tags:
      - Notification Preferences
      summary: Get Registration Qr Code
      description: "Generate a QR code for mobile device registration.\n\nReturns:\n\
        \    QR code data with registration token."
      operationId: get_registration_qr_code_api_v1_notification_preferences_me_qr_code_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QRCodeResponse'
      security:
      - bearerAuth: []
  /api/v1/notification-preferences/register-via-token:
    post:
      tags:
      - Notification Preferences
      summary: Register Device Via Token
      description: "Register a mobile device using a QR code token and create an API\
        \ key.\n\nThis endpoint is called by the mobile app after scanning the QR\
        \ code.\nIt registers the device for push notifications AND creates an API\
        \ key\nfor the mobile app to authenticate API requests.\n\nArgs:\n    token:\
        \ Registration token from QR code.\n    device_in: Device registration data.\n\
        \nReturns:\n    Updated notification preferences with API key.\n\nRaises:\n\
        \    HTTPException: If token is invalid or expired."
      operationId: register_device_via_token_api_v1_notification_preferences_register_via_token_post
      parameters:
      - name: token
        in: query
        required: true
        schema:
          type: string
          title: Token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobileDeviceRegistration'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MobileDeviceRegistrationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/organizations:
    post:
      tags:
      - Organizations
      summary: Create Organization
      description: Create a new organization, ensuring it's linked to the current
        user's tracker.
      operationId: create_organization_api_v1_organizations_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Organizations
      summary: List Organizations
      description: List organizations accessible to the current user.
      operationId: list_organizations_api_v1_organizations_get
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 100
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/organizations/{organization_id}:
    get:
      tags:
      - Organizations
      summary: Get Organization
      description: Get an organization by ID, ensuring user has access.
      operationId: get_organization_api_v1_organizations__organization_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Organizations
      summary: Update Organization
      description: Update an organization, ensuring user has access.
      operationId: update_organization_api_v1_organizations__organization_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Organizations
      summary: Delete Organization
      description: Delete an organization, ensuring user has access.
      operationId: delete_organization_api_v1_organizations__organization_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/organizations/by-identifier/{identifier}:
    get:
      tags:
      - Organizations
      summary: Get Organization By Identifier
      description: Get an organization by identifier, ensuring user has access.
      operationId: get_organization_by_identifier_api_v1_organizations_by_identifier__identifier__get
      security:
      - bearerAuth: []
      parameters:
      - name: identifier
        in: path
        required: true
        schema:
          type: string
          title: Identifier
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/projects:
    post:
      tags:
      - Projects
      summary: Create Project
      description: Create a new project, ensuring user has access to the organization.
      operationId: create_project_api_v1_projects_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Projects
      summary: List Projects
      description: List projects accessible to the current user, applying scope rules
        and optionally filtered by organization.
      operationId: list_projects_api_v1_projects_get
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 100
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectResponse'
                title: Response List Projects Api V1 Projects Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/organizations/{organization_id}/projects:
    get:
      tags:
      - Projects
      summary: List Organization Projects
      description: List all projects for an organization, applying scope rules and
        ensuring user has access.
      operationId: list_organization_projects_api_v1_organizations__organization_id__projects_get
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          default: 100
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/projects/{project_id}:
    get:
      tags:
      - Projects
      summary: Get Project
      description: Get a project by ID, ensuring user has access.
      operationId: get_project_api_v1_projects__project_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          title: Project Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Projects
      summary: Update Project
      description: Update a project, ensuring user has access.
      operationId: update_project_api_v1_projects__project_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          title: Project Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Projects
      summary: Delete Project
      description: Delete a project, ensuring user has access.
      operationId: delete_project_api_v1_projects__project_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          title: Project Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/organizations/{organization_id}/projects/{identifier}:
    get:
      tags:
      - Projects
      summary: Get Project By Identifier
      description: Get a project by organization ID and project identifier, ensuring
        user has access.
      operationId: get_project_by_identifier_api_v1_organizations__organization_id__projects__identifier__get
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      - name: identifier
        in: path
        required: true
        schema:
          type: string
          title: Identifier
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/projects/test-connection:
    post:
      tags:
      - Projects
      summary: Test Project Connection
      description: Test the connection to an issue tracker for a project, ensuring
        user has access.
      operationId: test_project_connection_api_v1_projects_test_connection_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestConnectionRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestConnectionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/projects/{project_id}/pull-requests:
    get:
      tags:
      - Projects
      summary: List Project Pull Requests
      description: List open pull requests (GitHub) or merge requests (GitLab) for
        a project.
      operationId: list_project_pull_requests_api_v1_projects__project_id__pull_requests_get
      security:
      - bearerAuth: []
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      - name: state
        in: query
        required: false
        schema:
          const: open
          type: string
          default: open
          title: State
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 50
          minimum: 1
          default: 20
          title: Limit
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: refresh
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Refresh
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PullRequestListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/search:
    get:
      tags:
      - Issues
      summary: Search Issues
      description: 'Search for issues within a project using text query and optional
        similarity search.

        Requires authentication and checks user access.'
      operationId: search_issues_api_v1_issues_search_get
      security:
      - bearerAuth: []
      parameters:
      - name: organization_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Organization ID (UUID)
          title: Organization Id
        description: Organization ID (UUID)
      - name: organization
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Organization name
          title: Organization
        description: Organization name
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Project ID (UUID)
          title: Project Id
        description: Project ID (UUID)
      - name: project
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Project name
          title: Project
        description: Project name
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Search query text
          default: ''
          title: Query
        description: Search query text
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          description: Maximum number of issues to return
          default: 10
          title: Limit
        description: Maximum number of issues to return
      - name: search_type
        in: query
        required: false
        schema:
          type: string
          description: Type of search to perform ('fulltext' or 'similarity')
          enum:
          - fulltext
          - similarity
          default: fulltext
          title: Search Type
        description: Type of search to perform ('fulltext' or 'similarity')
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by issue status
          title: Status
        description: Filter by issue status
      - name: labels
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by comma-separated list of labels
          title: Labels
        description: Filter by comma-separated list of labels
      - name: assignee
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by assignee
          title: Assignee
        description: Filter by assignee
      - name: priority
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by issue priority
          title: Priority
        description: Filter by issue priority
      - name: last_updated_before
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          description: Filter issues updated before this timestamp (ISO 8601 format)
          title: Last Updated Before
        description: Filter issues updated before this timestamp (ISO 8601 format)
      - name: last_updated_after
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          description: Filter issues updated after this timestamp (ISO 8601 format)
          title: Last Updated After
        description: Filter issues updated after this timestamp (ISO 8601 format)
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/IssueResponse'
                title: Response Search Issues Api V1 Issues Search Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues:
    post:
      tags:
      - Issues
      summary: Create Issue
      description: 'Create a new issue in a specified project. Requires authentication
        and checks user access.


        Supports specifying organization/project by:

        - ID (organization_id/project_id)

        - Name (organization_name/project_name)

        - Slug (organization/project)'
      operationId: create_issue_api_v1_issues_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Issues
      summary: List Issues
      description: List issues for a visible project (or tracker) newest updated first.
      operationId: list_issues_api_v1_issues_get
      security:
      - bearerAuth: []
      parameters:
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          description: Project ID (UUID). Required in v1 unless tracker_id is set.
          title: Project Id
        description: Project ID (UUID). Required in v1 unless tracker_id is set.
      - name: tracker_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          description: Optional tracker ID alternative to project_id.
          title: Tracker Id
        description: Optional tracker ID alternative to project_id.
      - name: status
        in: query
        required: false
        schema:
          enum:
          - open
          - closed
          - all
          type: string
          description: Filter by issue status
          default: open
          title: Status
        description: Filter by issue status
      - name: q
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional ILIKE filter on key or title
          title: Q
        description: Optional ILIKE filter on key or title
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of issues to skip
          default: 0
          title: Skip
        description: Number of issues to skip
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          description: Page size
          default: 20
          title: Limit
        description: Page size
      - name: sort
        in: query
        required: false
        schema:
          const: updated_desc
          type: string
          description: Sort order (updated_desc only)
          default: updated_desc
          title: Sort
        description: Sort order (updated_desc only)
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}:
    get:
      tags:
      - Issues
      summary: Get Issue
      description: Get details of a specific issue using its external ID.
      operationId: get_issue_api_v1_issues__issue_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          title: Issue Id
      - name: organization
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization
      - name: project
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Project
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Issues
      summary: Update Issue
      description: Update an existing issue using its internal ID or external key.
      operationId: update_issue_api_v1_issues__issue_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          title: Issue Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues-count:
    get:
      tags:
      - Issues
      summary: Get Issue Count
      description: Get the total number of issues for the current user's account.
      operationId: get_issue_count_api_v1_issues_count_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: integer
                type: object
                title: Response Get Issue Count Api V1 Issues Count Get
      security:
      - bearerAuth: []
  /api/v1/issues/{issue_id}/lifecycle:
    get:
      tags:
      - Issues
      summary: Lifecycle Status
      description: Read live scope plus durable readiness, pickup and audit history.
      operationId: lifecycle_status_api_v1_issues__issue_id__lifecycle_get
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Lifecycle Status Api V1 Issues  Issue Id  Lifecycle
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/lifecycle/refine:
    post:
      tags:
      - Issues
      summary: Refine Issue
      description: Store a reviewable proposal; this operation cannot launch implementation.
      operationId: refine_issue_api_v1_issues__issue_id__lifecycle_refine_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReadinessContract'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Refine Issue Api V1 Issues  Issue Id  Lifecycle Refine
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/lifecycle/ready:
    post:
      tags:
      - Issues
      summary: Ready Issue
      description: Apply the configured readiness policy once to an approved scope.
      operationId: ready_issue_api_v1_issues__issue_id__lifecycle_ready_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReadyRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Ready Issue Api V1 Issues  Issue Id  Lifecycle Ready
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/lifecycle/pickup/reconcile:
    post:
      tags:
      - Issues
      summary: Reconcile Pickup
      description: Authorize and durably dispatch changed scope after the prior run
        ends.
      operationId: reconcile_pickup_api_v1_issues__issue_id__lifecycle_pickup_reconcile_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PickupReconciliationRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Reconcile Pickup Api V1 Issues  Issue Id  Lifecycle
                  Pickup Reconcile Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/lifecycle/audit/reconcile:
    post:
      tags:
      - Issues
      summary: Reconcile Audit
      description: Recover dispatch/publication after a transient provider or worker
        failure.
      operationId: reconcile_audit_api_v1_issues__issue_id__lifecycle_audit_reconcile_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Reconcile Audit Api V1 Issues  Issue Id  Lifecycle
                  Audit Reconcile Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/lifecycle/deployment/verify:
    post:
      tags:
      - Issues
      summary: Verify Deployment
      description: Trigger a separately approved target/scope after recording deployment.
      operationId: verify_deployment_api_v1_issues__issue_id__lifecycle_deployment_verify_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Issue Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeploymentVerificationRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Verify Deployment Api V1 Issues  Issue Id  Lifecycle
                  Deployment Verify Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/releases:
    get:
      tags:
      - Security Maintenance
      summary: List Releases
      description: List this tenant's opted-in supported releases.
      operationId: list_releases_api_v1_security_maintenance_releases_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  additionalProperties: true
                  type: object
                type: array
                title: Response List Releases Api V1 Security Maintenance Releases
                  Get
      security:
      - bearerAuth: []
    post:
      tags:
      - Security Maintenance
      summary: Create Release
      description: Create an explicit opt-in inventory row.
      operationId: create_release_api_v1_security_maintenance_releases_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SupportedReleaseCreate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Create Release Api V1 Security Maintenance Releases
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/security-maintenance/releases/{release_id}:
    get:
      tags:
      - Security Maintenance
      summary: Get Release
      description: Return one inventory row.
      operationId: get_release_api_v1_security_maintenance_releases__release_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: release_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Release Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Release Api V1 Security Maintenance Releases  Release
                  Id  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Security Maintenance
      summary: Update Release
      description: Update mutable inventory fields. Identity stays fixed.
      operationId: update_release_api_v1_security_maintenance_releases__release_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: release_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Release Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SupportedReleaseUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Update Release Api V1 Security Maintenance Releases  Release
                  Id  Patch
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/releases/{release_id}/baseline/audit:
    post:
      tags:
      - Security Maintenance
      summary: Schedule Baseline Audit
      description: Schedule the initial-baseline audit and dispatch it after commit.
      operationId: schedule_baseline_audit_api_v1_security_maintenance_releases__release_id__baseline_audit_post
      security:
      - bearerAuth: []
      parameters:
      - name: release_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Release Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BaselineAuditRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Schedule Baseline Audit Api V1 Security Maintenance
                  Releases  Release Id  Baseline Audit Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/releases/{release_id}/baseline:
    post:
      tags:
      - Security Maintenance
      summary: Accept Baseline
      description: Accept a baseline from a bound audit execution with stored evidence.
      operationId: accept_baseline_api_v1_security_maintenance_releases__release_id__baseline_post
      security:
      - bearerAuth: []
      parameters:
      - name: release_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Release Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BaselineAcceptRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Accept Baseline Api V1 Security Maintenance Releases  Release
                  Id  Baseline Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/scans:
    post:
      tags:
      - Security Maintenance
      summary: Ingest Scan
      description: Ingest a trusted scan. Unsupported product/release names fail closed.
      operationId: ingest_scan_api_v1_security_maintenance_scans_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScanIngestRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Ingest Scan Api V1 Security Maintenance Scans Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/security-maintenance/items:
    get:
      tags:
      - Security Maintenance
      summary: List Items
      description: List durable remediation items.
      operationId: list_items_api_v1_security_maintenance_items_get
      security:
      - bearerAuth: []
      parameters:
      - name: release_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Release Id
      - name: state
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: State
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
                title: Response List Items Api V1 Security Maintenance Items Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}:
    get:
      tags:
      - Security Maintenance
      summary: Get Item
      description: Return one work item. Reconciliation runs in the background, not
        here.
      operationId: get_item_api_v1_security_maintenance_items__item_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Item Api V1 Security Maintenance Items  Item Id  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/decisions:
    get:
      tags:
      - Security Maintenance
      summary: List Decisions
      description: Return append-only decision history.
      operationId: list_decisions_api_v1_security_maintenance_items__item_id__decisions_get
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
                title: Response List Decisions Api V1 Security Maintenance Items  Item
                  Id  Decisions Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/approve:
    post:
      tags:
      - Security Maintenance
      summary: Approve Item
      description: Record a human approval through the platform approval workflow.
      operationId: approve_item_api_v1_security_maintenance_items__item_id__approve_post
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecisionRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Approve Item Api V1 Security Maintenance Items  Item
                  Id  Approve Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/deny:
    post:
      tags:
      - Security Maintenance
      summary: Deny Item
      description: Hold the item after a human denial. History is preserved.
      operationId: deny_item_api_v1_security_maintenance_items__item_id__deny_post
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecisionRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Deny Item Api V1 Security Maintenance Items  Item
                  Id  Deny Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/escalate:
    post:
      tags:
      - Security Maintenance
      summary: Escalate Item
      description: Escalate a held or expired approval. Never auto-releases.
      operationId: escalate_item_api_v1_security_maintenance_items__item_id__escalate_post
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecisionRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Escalate Item Api V1 Security Maintenance Items  Item
                  Id  Escalate Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/resume:
    post:
      tags:
      - Security Maintenance
      summary: Resume Item
      description: Human retry. Prior decisions stay append-only.
      operationId: resume_item_api_v1_security_maintenance_items__item_id__resume_post
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResumeRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Resume Item Api V1 Security Maintenance Items  Item
                  Id  Resume Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/security-maintenance/items/{item_id}/build:
    post:
      tags:
      - Security Maintenance
      summary: Submit Rebuilt Inputs
      description: Submit a rebuilt SBOM for the published commit. The controller
        does not rebuild.
      operationId: submit_rebuilt_inputs_api_v1_security_maintenance_items__item_id__build_post
      security:
      - bearerAuth: []
      parameters:
      - name: item_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Item Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RebuiltInputsRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Submit Rebuilt Inputs Api V1 Security Maintenance
                  Items  Item Id  Build Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/issues/{issue_id}/comments:
    get:
      tags:
      - Comments
      summary: List Issue Comments
      description: Get a list of comments for a specific issue. Requires authentication.
      operationId: list_issue_comments_api_v1_issues__issue_id__comments_get
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          title: Issue Id
      - name: organization
        in: query
        required: true
        schema:
          type: string
          description: Organization identifier
          title: Organization
        description: Organization identifier
      - name: project
        in: query
        required: true
        schema:
          type: string
          description: Project identifier
          title: Project
        description: Project identifier
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          description: Maximum number of comments to return
          default: 20
          title: Limit
        description: Maximum number of comments to return
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of comments to skip
          default: 0
          title: Offset
        description: Number of comments to skip
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentList'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Comments
      summary: Add Issue Comment
      description: Add a new comment to a specific issue. Requires authentication.
      operationId: add_issue_comment_api_v1_issues__issue_id__comments_post
      security:
      - bearerAuth: []
      parameters:
      - name: issue_id
        in: path
        required: true
        schema:
          type: string
          title: Issue Id
      - name: organization
        in: query
        required: true
        schema:
          type: string
          description: Organization identifier
          title: Organization
        description: Organization identifier
      - name: project
        in: query
        required: true
        schema:
          type: string
          description: Project identifier
          title: Project
        description: Project identifier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/comments/search:
    get:
      tags:
      - Comments
      summary: Search Comments
      description: 'Search for comments using full-text or similarity search.

        Requires authentication and checks user access to related issues/projects.'
      operationId: search_comments_api_v1_comments_search_get
      security:
      - bearerAuth: []
      parameters:
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Search query text for comment body or vector search
          title: Query
        description: Search query text for comment body or vector search
      - name: search_type
        in: query
        required: false
        schema:
          type: string
          description: Type of search to perform ('fulltext' or 'similarity')
          enum:
          - fulltext
          - similarity
          default: fulltext
          title: Search Type
        description: Type of search to perform ('fulltext' or 'similarity')
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          description: Maximum number of comments to return
          default: 10
          title: Limit
        description: Maximum number of comments to return
      - name: issue_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by a specific issue ID (UUID)
          title: Issue Id
        description: Filter comments by a specific issue ID (UUID)
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by parent issue's project ID (UUID)
          title: Project Id
        description: Filter comments by parent issue's project ID (UUID)
      - name: organization_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by parent issue's organization ID (UUID)
          title: Organization Id
        description: Filter comments by parent issue's organization ID (UUID)
      - name: author
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by author (username)
          title: Author
        description: Filter comments by author (username)
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentSearchResults'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/search:
    get:
      tags:
      - Search
      summary: Perform Similarity Search
      description: Performs a similarity search across issues and/or comments based
        on a query text and an embedding model.
      operationId: search_all_api_v1_search_get
      security:
      - bearerAuth: []
      parameters:
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: The text query to search for. If not provided, lists all issues
            matching filters.
          title: Query
        description: The text query to search for. If not provided, lists all issues
          matching filters.
      - name: embedding_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Type of items to search: ''issue'', ''comment'', or null for
            both.'
          examples:
          - issue
          - comment
          title: Embedding Type
        description: 'Type of items to search: ''issue'', ''comment'', or null for
          both.'
      - name: search_type
        in: query
        required: false
        schema:
          type: string
          description: Type of search to perform ('fulltext' or 'similarity')
          enum:
          - fulltext
          - similarity
          default: fulltext
          title: Search Type
        description: Type of search to perform ('fulltext' or 'similarity')
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          description: Maximum number of comments to return
          default: 10
          title: Limit
        description: Maximum number of comments to return
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of results to skip for pagination
          default: 0
          title: Skip
        description: Number of results to skip for pagination
      - name: sort
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Sort order. 'newest' sorts by creation date descending.
          enum:
          - newest
          title: Sort
        description: Sort order. 'newest' sorts by creation date descending.
      - name: issue_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by a specific issue ID (UUID)
          title: Issue Id
        description: Filter comments by a specific issue ID (UUID)
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter search results by project ID (UUID).
          title: Project Id
        description: Filter search results by project ID (UUID).
      - name: project
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter search results by project name.
          title: Project
        description: Filter search results by project name.
      - name: organization_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter search results by organization ID (UUID).
          title: Organization Id
        description: Filter search results by organization ID (UUID).
      - name: organization
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter search results by organization name.
          title: Organization
        description: Filter search results by organization name.
      - name: author
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter comments by author (username)
          title: Author
        description: Filter comments by author (username)
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter issues by status ('opened', 'closed', 'all').
          title: Status
        description: Filter issues by status ('opened', 'closed', 'all').
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/embeddings:
    get:
      tags:
      - Embeddings
      - embeddings
      summary: Get all issue embeddings
      description: 'API endpoint to fetch raw embedding vectors for issues, with optional
        filtering.

        This endpoint is designed to provide data for frontend visualizations like
        deck.gl.'
      operationId: get_raw_embeddings_api_v1_embeddings_get
      security:
      - bearerAuth: []
      parameters:
      - name: embedding_model_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: The ID of the embedding model to use.
          title: Embedding Model Id
        description: The ID of the embedding model to use.
      - name: project_ids
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Comma-separated list of project IDs.
          title: Project Ids
        description: Comma-separated list of project IDs.
      - name: project_names
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Comma-separated list of project names.
          title: Project Names
        description: Comma-separated list of project names.
      - name: organization_ids
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Comma-separated list of organization IDs.
          title: Organization Ids
        description: Comma-separated list of organization IDs.
      - name: organization_names
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Comma-separated list of organization names.
          title: Organization Names
        description: Comma-separated list of organization names.
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of records to skip for pagination.
          default: 0
          title: Skip
        description: Number of records to skip for pagination.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 2000
          minimum: 1
          description: Maximum number of records to return.
          default: 1000
          title: Limit
        description: Maximum number of records to return.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmbeddingRawResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models:
    get:
      tags:
      - AI Models
      - AI Models
      summary: List AI Models
      description: List all AI Models associated with the authenticated user's account.
      operationId: list_ai_models_api_v1_ai_models_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/AIModelRead'
                type: array
                title: Response List Ai Models Api V1 Ai Models Get
      security:
      - bearerAuth: []
    post:
      tags:
      - AI Models
      - AI Models
      summary: Create AI Model
      description: Create a new AI Model for the authenticated user's account.
      operationId: create_ai_model_api_v1_ai_models_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AIModelCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/ai-models/overview:
    get:
      tags:
      - AI Models
      - AI Models
      summary: Get AI Models Overview
      description: Return usage, active sessions and price source for every model.
      operationId: get_ai_models_overview_api_v1_ai_models_overview_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: failed_since
        in: query
        required: false
        schema:
          anyOf:
          - type: array
            items:
              type: string
          - type: 'null'
          description: 'Repeatable ''<ai_model_id>:<ISO-8601 timestamp>'' pair. Each
            pair asks for that model''s failed_requests_since: how many of its failures
            in this window are newer than the timestamp. The console passes the moment
            an operator marked the model fixed, so a row can say ''2 failed since
            fix'' instead of repeating the whole window''s total.'
          title: Failed Since
        description: 'Repeatable ''<ai_model_id>:<ISO-8601 timestamp>'' pair. Each
          pair asks for that model''s failed_requests_since: how many of its failures
          in this window are newer than the timestamp. The console passes the moment
          an operator marked the model fixed, so a row can say ''2 failed since fix''
          instead of repeating the whole window''s total.'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelsOverviewResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}:
    get:
      tags:
      - AI Models
      - AI Models
      summary: Get AI Model by ID
      description: Retrieve a specific AI Model by its ID.
      operationId: get_ai_model_api_v1_ai_models__model_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - AI Models
      - AI Models
      summary: Update AI Model
      description: Update an existing AI Model by its ID.
      operationId: update_ai_model_api_v1_ai_models__model_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AIModelUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - AI Models
      - AI Models
      summary: Delete AI Model
      description: Delete an AI Model by its ID.
      operationId: delete_ai_model_api_v1_ai_models__model_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}/summary:
    get:
      tags:
      - AI Models
      - AI Models
      summary: Get AI Model Usage Summary
      description: Return model-scoped gateway usage totals for one AI model.
      operationId: get_ai_model_usage_summary_api_v1_ai_models__model_id__summary_get
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: failed_since
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          description: Count this model's failures newer than this moment into failed_requests_since.
            The console passes the moment the model was marked fixed.
          title: Failed Since
        description: Count this model's failures newer than this moment into failed_requests_since.
          The console passes the moment the model was marked fixed.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelGatewayUsageSummaryResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}/runtime-sessions:
    get:
      tags:
      - AI Models
      - AI Models
      summary: List AI Model Runtime Sessions
      description: List runtime sessions that used one durable AI model.
      operationId: list_ai_model_runtime_sessions_api_v1_ai_models__model_id__runtime_sessions_get
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Query
      - name: session_source_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
      - name: status
        in: query
        required: false
        schema:
          type: string
          pattern: ^(all|active|ended)$
          default: all
          title: Status
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuntimeSessionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}/interactions:
    get:
      tags:
      - AI Models
      - AI Models
      summary: List AI Model Interactions
      description: List indexed gateway interactions scoped to one AI model.
      operationId: list_ai_model_interactions_api_v1_ai_models__model_id__interactions_get
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      - name: query
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Query
      - name: runtime_session_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
      - name: session_source_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGatewayUsageSearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}/pricing:
    get:
      tags:
      - AI Models
      - AI Models
      summary: Get AI Model Pricing
      description: Return the effective price for one model and where it came from.
      operationId: get_ai_model_pricing_api_v1_ai_models__model_id__pricing_get
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelPricingResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/{model_id}/pricing/fetch:
    post:
      tags:
      - AI Models
      - AI Models
      summary: Fetch AI Model Pricing From Provider
      description: 'Read this model''s published price from its provider, without
        saving it.


        A price decides what every past and future request cost, so it is never

        written by a fetch: the numbers come back for a person to confirm, and

        saving one stays with the price override endpoints.'
      operationId: fetch_ai_model_pricing_api_v1_ai_models__model_id__pricing_fetch_post
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelPriceQuote'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/sync:
    post:
      tags:
      - AI Models
      - AI Models
      summary: Sync Provider Model Catalogs
      description: 'Discover newly released provider models and add them to the catalog.


        Runs the existing live provider discovery against credentials the account

        already stores (the same discovery the console model-add flow uses) and

        creates one AI model per newly discovered identifier via the CRUD layer.

        New rows share the seed model''s credential secret and inherit its gateway

        exposure, so authorization semantics are unchanged: API-key models stay

        account-wide, and principal-bound subscription-OAuth models (which cannot

        authenticate server-side discovery) are never created or widened here.


        Backing service: ``preloop.services.ai_model_catalog_sync``. Every added

        model is recorded in the audit trail. Use ``dry_run`` to preview.'
      operationId: sync_ai_model_catalog_api_v1_ai_models_sync_post
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/AIModelCatalogSyncRequest'
              - type: 'null'
              title: Request In
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelCatalogSyncResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/ai-models/{model_id}/credentials/export:
    post:
      tags:
      - AI Models
      - AI Models
      summary: Export Subscription OAuth Credential
      description: 'Export the live subscription-OAuth bundle for an account AI model.


        Only principal-bound subscription OAuth credentials (Claude Code, Codex)

        are exportable: their provider refresh tokens are single-use and rotate on

        every server-side refresh, so once imported the Preloop copy is the only

        live lineage. The CLI calls this at offboard time to restore the agent''s

        local login before the Preloop-held credential is removed. API-key

        credentials are never exportable.'
      operationId: export_ai_model_credentials_api_v1_ai_models__model_id__credentials_export_post
      security:
      - bearerAuth: []
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Model Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIModelCredentialExportResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai-models/providers/{provider}/available-models:
    post:
      tags:
      - AI Models
      - AI Models
      summary: Get Available Models for Provider
      description: 'Fetch available models from the specified AI provider, with provenance.


        Every provider lists live when a key (and endpoint where applicable) is

        present; the response reports ``source`` ("live" or "fallback") and a

        short safe ``error`` reason when a live attempt failed or credentials

        were missing. The reason comes from a fixed vocabulary and never contains

        raw provider error text, endpoint URLs, or key material.


        The provider API key travels in the request BODY, never the query string:

        as a query parameter it was written to access logs in plaintext.


        Edit-mode refresh should send ``ai_model_id`` instead of the stored key.

        The server decrypts the stored secret via CRUD. A typed ``api_key`` in

        the body always wins. Stored secrets are never returned to the client.'
      operationId: list_provider_available_models_api_v1_ai_models_providers__provider__available_models_post
      security:
      - bearerAuth: []
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
          title: Provider
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/AvailableModelsRequest'
              - type: 'null'
              title: Request In
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailableModelsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - AI Models
      - AI Models
      summary: Get Available Models for Provider (deprecated)
      description: 'Deprecated GET form, kept for clients that have not moved to POST
        yet.


        Returns a BARE LIST of model ids, unlike the POST form, which reports

        provenance as ``{models, source, error}``. The bare-list shape is kept

        here on purpose so unknown external callers of the deprecated route do

        not break; new clients should use POST and read the provenance.


        The api_key query parameter this endpoint used to accept has been REMOVED,

        not merely deprecated: it wrote live provider keys into access logs in

        plaintext. Pass the key in the X-Provider-Api-Key header, or use the POST

        form. A key sent as a query parameter is ignored.'
      operationId: get_provider_available_models_api_v1_ai_models_providers__provider__available_models_get
      deprecated: true
      security:
      - bearerAuth: []
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
          title: Provider
      - name: api_endpoint
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Base URL of an OpenAI-compatible endpoint, required for the
            openai-compatible and custom providers.
          title: Api Endpoint
        description: Base URL of an OpenAI-compatible endpoint, required for the openai-compatible
          and custom providers.
      - name: model_kind
        in: query
        required: false
        schema:
          enum:
          - llm
          - stt
          - tts
          type: string
          description: Model service kind to fetch
          default: llm
          title: Model Kind
        description: Model service kind to fetch
      - name: X-Provider-Api-Key
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Provider API key. Headers are not written to access logs.
          title: X-Provider-Api-Key
        description: Provider API key. Headers are not written to access logs.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                title: Response Get Provider Available Models Api V1 Ai Models Providers  Provider  Available
                  Models Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/audio/transcriptions:
    post:
      tags:
      - Audio
      - Audio
      summary: Transcribe Audio
      description: Transcribe an uploaded audio blob using the resolved account STT
        model.
      operationId: transcribe_audio_api_v1_audio_transcriptions_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_transcribe_audio_api_v1_audio_transcriptions_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeechToTextResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/audio/speech:
    post:
      tags:
      - Audio
      - Audio
      summary: Synthesize Speech
      description: Synthesize speech bytes using the resolved account TTS model.
      operationId: synthesize_speech_api_v1_audio_speech_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TextToSpeechRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/budget/policies:
    post:
      tags:
      - Budget
      - Budget
      summary: Create Budget Policy
      description: Create a new budget policy.
      operationId: create_budget_policy_api_v1_budget_policies_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetPolicyCreate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetPolicyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Budget
      - Budget
      summary: Get Budget Policies
      description: Retrieve budget policies for the account, optionally filtered.
      operationId: get_budget_policies_api_v1_budget_policies_get
      security:
      - bearerAuth: []
      parameters:
      - name: subject_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Subject Type
      - name: subject_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Subject Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BudgetPolicyResponse'
                title: Response Get Budget Policies Api V1 Budget Policies Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/budget/policies/{policy_id}:
    put:
      tags:
      - Budget
      - Budget
      summary: Update Budget Policy
      description: Update a budget policy.
      operationId: update_budget_policy_api_v1_budget_policies__policy_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: policy_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Policy Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BudgetPolicyUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetPolicyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Budget
      - Budget
      summary: Delete Budget Policy
      description: Delete a budget policy.
      operationId: delete_budget_policy_api_v1_budget_policies__policy_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: policy_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Policy Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                title: Response Delete Budget Policy Api V1 Budget Policies  Policy
                  Id  Delete
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/cost/summary:
    get:
      tags:
      - Cost Analytics
      - Cost Analytics
      summary: Get Cost Summary
      description: Return the OSS cost overview using gateway usage and pricing metadata.
      operationId: get_cost_summary_api_v1_cost_summary_get
      security:
      - bearerAuth: []
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      - name: runtime_principal_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
      - name: exclude_retries
        in: query
        required: false
        schema:
          type: boolean
          description: Exclude rows marked as retries of an identical earlier request.
            Retries consume real provider tokens, so they count by default.
          default: false
          title: Exclude Retries
        description: Exclude rows marked as retries of an identical earlier request.
          Retries consume real provider tokens, so they count by default.
      - name: include_breakdown
        in: query
        required: false
        schema:
          type: boolean
          description: Include breakdowns; false returns totals and pricing context
            only.
          default: true
          title: Include Breakdown
        description: Include breakdowns; false returns totals and pricing context
          only.
      - name: breakdown
        in: query
        required: false
        schema:
          anyOf:
          - type: array
            items:
              enum:
              - models
              - flows
              - sessions
              - tools
              - days
              - imported
              type: string
          - type: 'null'
          description: Optional breakdown selection. Repeat for multiple sections;
            omitted returns all. Ignored when include_breakdown=false.
          title: Breakdown
        description: Optional breakdown selection. Repeat for multiple sections; omitted
          returns all. Ignored when include_breakdown=false.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnalyticsSummaryResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/cost/health:
    get:
      tags:
      - Cost Analytics
      - Cost Analytics
      summary: Get Cost Health
      description: 'Run the gateway accounting self-check over a lookback window.


        Verifies end-to-end that gateway traffic is recorded, streaming usage is

        captured, requests are priced, usage is provider-reported, and audit

        events are being written — so silent accounting breakage cannot go

        unnoticed.'
      operationId: get_cost_health_api_v1_cost_health_get
      security:
      - bearerAuth: []
      parameters:
      - name: hours
        in: query
        required: false
        schema:
          type: integer
          maximum: 168
          minimum: 1
          description: Lookback window in hours (max 168 = 7 days).
          default: 24
          title: Hours
        description: Lookback window in hours (max 168 = 7 days).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostHealthResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/cost/reprice:
    post:
      tags:
      - Cost Analytics
      - Cost Analytics
      summary: Reprice Account Gateway Usage
      description: 'Re-price historical gateway usage synchronously and report real
        counts.


        Unlike the billing plugin''s ``/billing/cost/reprice`` (which dispatches

        windows over 7 days to a background worker and acknowledges with empty

        counters), this endpoint always scans in-request — keyset-paginated, so

        an operator backfilling weeks of history gets actual examined/updated

        numbers instead of an ambiguous async ack. Budget-spend buckets are never

        rewritten; provider-side actuals (``provider``/``reconciled``/

        ``imported``) and ``subscription`` rows are never touched.'
      operationId: reprice_account_gateway_usage_api_v1_cost_reprice_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RepriceRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepriceResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/cost/ledger-backfill/csv:
    post:
      tags:
      - Cost Analytics
      - Cost Analytics
      summary: Backfill From Provider Ledger Csv
      description: 'Reconcile unpriced gateway rows against a provider daily-usage
        export.


        Distributes each (day x model) ledger total across this account''s

        still-unpriced gateway rows for that day — pro-rata by tokens, equal

        split when the bucket recorded no tokens — and tags them

        ``cost_source=''reconciled''`` so provider actuals never mix with catalog

        estimates. Only rows without a resolved cost are ever touched, which

        also makes re-running the same export idempotent. The export''s "Other"

        bucket is reported as an unallocatable residual, never distributed.


        Defaults to a dry run that returns the full allocation plan; pass

        ``apply=true`` to persist it.'
      operationId: backfill_from_provider_ledger_csv_api_v1_cost_ledger_backfill_csv_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_backfill_from_provider_ledger_csv_api_v1_cost_ledger_backfill_csv_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LedgerBackfillResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/usage/import:
    post:
      tags:
      - Usage Import
      - Usage Import
      summary: Import Usage Events
      description: 'Ingest a batch of normalized usage events into the cost ledger.


        Events are attributed to the given managed agent (default: the account''s

        managed Cursor agent from onboarding) and recorded with

        ``usage_source=''imported''``. Re-sending the same events is idempotent —

        duplicates are counted in ``skipped_duplicates``, never double-billed.'
      operationId: import_usage_events_api_v1_usage_import_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsageImportRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageImportResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/usage/ingest:
    post:
      tags:
      - Usage Import
      - Usage Import
      summary: Ingest Usage Records
      description: 'Push a batch of external usage records into the cost ledger.


        The continuous-push evolution of ``POST /usage/import``: a harness

        posts sanitized spend records — or hook-shaped lifecycle events

        (``event_type``) — as they occur instead of batch CSV. Each record is

        identified by (source, external_id) per account, so replaying a batch

        — e.g. a retry after a network timeout — returns 200 with the

        replayed records flagged ``deduplicated`` and never double-counts

        spend; a replay whose payload differs from the stored record is

        additionally flagged ``conflict`` (first write wins, never a 409).

        Records carry ``conversation_id`` / ``parent_conversation_id`` so

        subagent workers billed on separate conversations can be rolled up

        under their parent thread, and ``cost_basis`` so reconciled

        billing-export records supersede hook-derived estimates in cost

        summaries. Attribution precedence: explicit ``agent_id`` (must belong

        to the account) wins; otherwise the account''s single managed agent

        whose kind equals ``source`` is used, and zero or multiple candidates

        is a 422.'
      operationId: ingest_usage_records_api_v1_usage_ingest_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsageIngestRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageIngestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/usage/import/csv:
    post:
      tags:
      - Usage Import
      - Usage Import
      summary: Import Usage Csv
      description: 'Import a Cursor dashboard Usage CSV export into the cost ledger.


        Parses the export (Date / Kind / Model / Max Mode / Input (w/ Cache

        Write) / Input (w/o Cache Write) / Cache Read / Output Tokens / Total

        Tokens / Cost), normalizes each row, and ingests it through the same

        pipeline as ``POST /usage/import``. Rows whose Cost is "Included" are

        stored with tokens but no charged amount. Re-importing the same file is

        idempotent.'
      operationId: import_usage_csv_api_v1_usage_import_csv_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_import_usage_csv_api_v1_usage_import_csv_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageImportCsvResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/billing/cost/runtime-sessions/example/optimization:
    get:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Get Example Runtime Session Optimization
      description: "Return the bundled example session's optimization suggestions.\n\
        \nLets a brand-new account see what the Optimize tab produces before its own\n\
        agents have generated any traffic. The response is computed live by the\n\
        production deterministic analyzers over a bundled transcript and is flagged\n\
        ``is_example`` so the console can label it as sample data.\n\nDeliberately\
        \ side-effect free: no database writes, no LLM call, and — unlike\n:func:`optimize_account_runtime_session`\
        \ — no ``optimization_result_viewed``\naudit event, because viewing a bundled\
        \ sample is not the account \"reaching\ntheir number\" and must not enter\
        \ launch telemetry.\n\nRaises:\n    HTTPException: 404 if the bundled transcript\
        \ is unavailable, so the\n        console falls back to its normal empty state."
      operationId: get_example_runtime_session_optimization_api_v1_billing_cost_runtime_sessions_example_optimization_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationResponse'
      security:
      - bearerAuth: []
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/optimizations:
    post:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Optimize Account Runtime Session
      description: 'Suggest cost and context optimizations for one runtime session.


        Legacy inline path: runs the shared worker function synchronously and

        returns the results in this response — semantics identical to before the

        async job endpoints existed (no submit-then-poll behind the scenes).'
      operationId: optimize_account_runtime_session_api_v1_billing_cost_runtime_sessions__runtime_session_id__optimizations_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/RuntimeSessionOptimizationRequest'
              - type: 'null'
              title: Request
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/optimizations/jobs:
    post:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Submit Account Runtime Session Optimization Job
      description: 'Queue the (slow, LLM-backed) optimization analysis as a background
        job.


        Idempotent: while a job for this session is still pending or running,

        re-submitting returns THAT job instead of queuing a second model pass, so

        a double-click never spends the model budget twice.'
      operationId: submit_account_runtime_session_optimization_job_api_v1_billing_cost_runtime_sessions__runtime_session_id__optimizations_jobs_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/RuntimeSessionOptimizationRequest'
              - type: 'null'
              title: Request
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationJobSubmitResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/optimizations/jobs/{job_id}:
    get:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Get Account Runtime Session Optimization Job
      description: "Poll one async optimization job's status, result, and error.\n\
        \nA job id belonging to another account, or attached to a different\nsession,\
        \ is indistinguishable from a missing one (404) by design.\n\nRaises:\n  \
        \  HTTPException: 404 when the job is absent, malformed, or out of scope."
      operationId: get_account_runtime_session_optimization_job_api_v1_billing_cost_runtime_sessions__runtime_session_id__optimizations_jobs__job_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          title: Job Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationJobStatusResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/optimizations/apply:
    post:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Apply Account Runtime Session Optimization
      description: 'Apply one optimization suggestion action for a runtime session.


        Applying writes governance/budget configuration only — it spends no model

        budget — so it is never gated behind an entitlement.'
      operationId: apply_account_runtime_session_optimization_api_v1_billing_cost_runtime_sessions__runtime_session_id__optimizations_apply_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionOptimizationApplyRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationAppliedAction'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/replay:
    post:
      tags:
      - Session Optimization
      - Session Optimization
      summary: Replay Account Runtime Session
      description: 'Verify a candidate optimization''s savings by re-executing a stored
        request.


        Re-runs the session''s stored request with and without the candidate applied,

        under a hard spend cap, and records the exact deterministic input-token delta

        together with the noise-banded end-to-end delta. Requires explicit consent.'
      operationId: replay_account_runtime_session_api_v1_billing_cost_runtime_sessions__runtime_session_id__replay_post
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionReplayRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionReplayResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/billing/cost/runtime-sessions/{runtime_session_id}/optimizations/actions:
    get:
      tags:
      - Session Optimization
      - Session Optimization
      summary: List Account Runtime Session Optimization Actions
      description: List applied optimization actions and their measured outcomes.
      operationId: list_account_runtime_session_optimization_actions_api_v1_billing_cost_runtime_sessions__runtime_session_id__optimizations_actions_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeSessionOptimizationActionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/private/webhooks/{tracker_type}/{organization_id}:
    post:
      tags:
      - Webhooks
      summary: Receive Webhook
      description: 'Receive tracker webhooks and queue event processing.


        Return HTTP 503 when task publication is not acknowledged.'
      operationId: receive_webhook_api_v1_private_webhooks__tracker_type___organization_id__post
      parameters:
      - name: tracker_type
        in: path
        required: true
        schema:
          type: string
          title: Tracker Type
      - name: organization_id
        in: path
        required: true
        schema:
          type: string
          title: Organization Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/flows:
    post:
      tags:
      - Flows
      summary: Create Flow
      description: Create new flow.
      operationId: create_flow_api_v1_flows_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowCreate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Flows
      summary: Read Flows
      description: Retrieve flows for the account.
      operationId: read_flows_api_v1_flows_get
      security:
      - bearerAuth: []
      parameters:
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          default: 0
          title: Skip
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 100
          title: Limit
      - name: stats_since
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          description: 'When set, execution_stats also carries the counts for the
            window starting at this time: runs, failed, cost, token_usage and last_run_at.
            The flows list states one period and needs runs and spend measured over
            the same one. Without it, token_usage covers all time, like estimated_cost.'
          title: Stats Since
        description: 'When set, execution_stats also carries the counts for the window
          starting at this time: runs, failed, cost, token_usage and last_run_at.
          The flows list states one period and needs runs and spend measured over
          the same one. Without it, token_usage covers all time, like estimated_cost.'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FlowResponse'
                title: Response Read Flows Api V1 Flows Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/schedule/preview:
    post:
      tags:
      - Flows
      summary: Preview Flow Schedule
      description: 'Preview a schedule trigger configuration.


        Validates the schedule config (invalid configs are rejected with a

        422 by the schema) and returns a human-readable description plus the

        next few run times, for display in the flow editor.'
      operationId: preview_flow_schedule_api_v1_flows_schedule_preview_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchedulePreviewRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchedulePreviewResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/flows/presets:
    get:
      tags:
      - Flows
      summary: Read Presets
      description: 'Retrieve flow presets available to the account.


        Returns global presets (account_id=None) plus any account-specific presets.

        Global rows carry their catalog ``slug``, the stable identifier

        ``POST /flows/run-preset`` takes, so scripted callers do not have to

        match on a display name that can be renamed.'
      operationId: read_presets_api_v1_flows_presets_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FlowResponse'
                type: array
                title: Response Read Presets Api V1 Flows Presets Get
      security:
      - bearerAuth: []
  /api/v1/flows/presets/{flow_id}/clone:
    post:
      tags:
      - Flows
      summary: Clone Preset
      description: 'Clone a flow preset.


        Creates a copy of the preset for the user''s account with template tracking

        enabled. The cloned flow will auto-update when the preset changes, unless

        the user customizes the prompt or tools.'
      operationId: clone_preset_api_v1_flows_presets__flow_id__clone_post
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/run-preset:
    post:
      tags:
      - Flows
      summary: Run Preset
      description: 'Run a catalog preset on an issue or a capped triage batch.


        ``confirm_create=false`` resolves only: 409 ``flow_missing`` when the

        account has no clone, or 200 with flow metadata and no execution when

        one exists. ``confirm_create=true`` creates if needed (requires

        ``create_flows``) and starts the run. ``targets`` is a 1-25 issue

        list for ``issue-triage-assistant`` only.'
      operationId: run_preset_api_v1_flows_run_preset_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunPresetRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunPresetResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/flows/executions:
    get:
      tags:
      - Flows
      summary: Read Flow Executions
      description: 'Retrieve lightweight flow execution summaries for the account.


        `X-Total-Count` carries how many executions the filters matched, so a

        console page of 25 can say "25 of 1,412 executions" honestly.'
      operationId: read_flow_executions_api_v1_flows_executions_get
      security:
      - bearerAuth: []
      parameters:
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          default: 0
          title: Skip
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 25
          title: Limit
      - name: flow_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Flow Id
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: array
            items:
              type: string
          - type: 'null'
          title: Status
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: started_after
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Started After
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FlowExecutionListResponse'
                title: Response Read Flow Executions Api V1 Flows Executions Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/batches/{batch_id}/executions:
    get:
      tags:
      - Flows
      summary: Read Batch Executions
      description: 'List all executions of one matrix/batch trigger with a rollup.


        The rollup aggregates status counts, tokens, tool calls and estimated

        cost across the batch so an eval matrix can be observed as a unit.'
      operationId: read_batch_executions_api_v1_flows_batches__batch_id__executions_get
      security:
      - bearerAuth: []
      parameters:
      - name: batch_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Batch Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchExecutionsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/tree:
    get:
      tags:
      - Flows
      summary: Read Execution Tree
      description: 'List what one execution delegated, with a rollup over the subtree.


        The whole lineage is read in one indexed query (every descendant carries

        the root''s id) and the subtree under the requested execution is walked

        from that in memory, so asking a child about its own children costs the

        same as asking the root. A run that delegated nothing answers with an

        empty list and a zeroed rollup rather than a 404: "nothing here" is the

        common case and the page renders it as an empty state.


        The rollup covers the descendants only. The requested execution''s own

        cost and tokens come back on ``execution``, so the console can show "this

        run cost X, what it started cost Y" without adding the two numbers.'
      operationId: read_execution_tree_api_v1_flows_executions__execution_id__tree_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionTreeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}:
    get:
      tags:
      - Flows
      summary: Read Flow Execution
      description: Get flow execution by ID.
      operationId: read_flow_execution_api_v1_flows_executions__execution_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowExecutionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/continuation:
    get:
      tags:
      - Flows
      summary: Preview Execution Continuation
      description: Inspect one publication without enabling repairs or holding DB
        during I/O.
      operationId: preview_execution_continuation_api_v1_flows_executions__execution_id__continuation_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      - name: pr_url
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Pr Url
      - name: branch
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Branch
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuationPreview'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Flows
      summary: Adopt Execution Continuation
      description: Explicitly subscribe one previously published PR to bounded repairs.
      operationId: adopt_execution_continuation_api_v1_flows_executions__execution_id__continuation_post
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuationAdoptRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuationAdoptResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/result:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Result
      description: 'Get the structured result artifact reported by a flow execution.


        Eval/observe flows write ``/workspace/result.json`` as their final

        report; the runner captures it as a first-class execution artifact.

        Returns 404 if the execution does not exist or reported no result.


        ``evidence`` is the persisted receipt only: bounded, account-scoped,

        no decrypt. Availability is not integrity proof; download

        ``GET .../evidence`` to verify digest.'
      operationId: get_flow_execution_result_api_v1_flows_executions__execution_id__result_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Result Api V1 Flows Executions  Execution
                  Id  Result Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/evidence-status:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Evidence Status
      description: 'Report evidence availability without downloading the pack.


        Status is one of ``available``, ``missing``, ``expired``, or ``failed``.

        Served from the persisted execution receipt (no decrypt). Availability

        is not integrity proof. ``object_lock`` is always false: this API does

        not implement WORM retention and cannot verify a property of the storage

        layer beneath it. ``legal_hold`` is true while a hold covers the pack or

        its execution, which blocks payload expiry and purge inside Preloop.


        ``signature`` is the detached Ed25519 signature made when the pack was

        captured, with the payload it covers, or null for a pack captured before

        signing existed. ``preloop evidence verify`` checks it against the

        account''s public key.'
      operationId: get_flow_execution_evidence_status_api_v1_flows_executions__execution_id__evidence_status_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Evidence Status Api V1 Flows Executions  Execution
                  Id  Evidence Status Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/evidence:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Evidence
      description: 'Download the evidence pack captured for a flow execution.


        Audit-style flows write an evidence pack under ``/workspace/evidence``;

        the runner captures it as a tar.gz archive after the agent finishes.

        Durable artifacts are served after digest verification. Distinct

        outcomes: 404 missing, 410 expired, 409 failed. Legacy executions that

        only stored ``evidence_archive`` remain readable.'
      operationId: get_flow_execution_evidence_api_v1_flows_executions__execution_id__evidence_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/evidence/members:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Evidence Members
      description: 'List a pack''s manifest members, or return one member when ``path``
        is set.


        Auth, decryption, digest verification and legal hold follow

        ``GET .../evidence``. Only a path that the manifest lists is readable.

        Absolute paths and ``..`` are refused. A member larger than 8 MiB is

        refused; download the pack for that file. Markdown, JSON and plain text

        are served with those content types.


        The full download is not audited. This read is not audited either.'
      operationId: get_flow_execution_evidence_members_api_v1_flows_executions__execution_id__evidence_members_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      - name: path
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Path
      responses:
        '200':
          description: JSON member list, or one member's bytes when path is set.
          content:
            application/json:
              schema:
                type: object
            text/markdown:
              schema:
                type: string
            text/plain:
              schema:
                type: string
            application/octet-stream:
              schema:
                type: string
                format: binary
          headers:
            X-Preloop-Evidence-Integrity:
              schema:
                type: string
            X-Preloop-Evidence-SHA256:
              schema:
                type: string
            X-Preloop-Evidence-Member:
              schema:
                type: string
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/workspace:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Workspace
      description: 'Download the workspace snapshot captured for a flow execution.


        Every hosted run leaves a size-capped tar.gz of ``/workspace`` (``.git``

        included, so commits that were never pushed are in it) behind. Returns 404

        if the execution does not exist, if the workspace was too large to keep,

        or if the retention window has passed and the janitor reaped it.'
      operationId: get_flow_execution_workspace_api_v1_flows_executions__execution_id__workspace_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Size-capped tar.gz of /workspace from this execution.
          content:
            application/json:
              schema: {}
            application/gzip:
              schema:
                type: string
                format: binary
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/logs:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Logs
      description: "Get execution logs from the live container or from persisted database\
        \ rows.\n\nFor running container-backed executions, fetches logs directly\
        \ from the\nDocker/Kubernetes container. Runner-backed executions (lease references\n\
        such as ``runner:{runner_id}:{execution_id}``) always return persisted\ndatabase\
        \ logs, even while they are still running, because their output\narrives over\
        \ the runner WebSocket rather than a container log stream.\nFor finished executions,\
        \ returns persisted logs from the database.\n\nArgs:\n    execution_id: ID\
        \ of the execution\n    tail: Number of recent log lines to retrieve, or None\
        \ for all logs\n    skip: Number of logs to skip (for pagination)\n    limit:\
        \ Number of logs to return (for pagination)\n\nReturns:\n    Dictionary with:\n\
        \    - logs: List of log lines\n    - source: Where logs were fetched from\
        \ (\"container\" or \"database\")\n    - has_more: Boolean indicating if there\
        \ are more logs (if paginated)"
      operationId: get_flow_execution_logs_api_v1_flows_executions__execution_id__logs_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      - name: tail
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tail
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          default: 0
          title: Skip
      - name: limit
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Logs Api V1 Flows Executions  Execution
                  Id  Logs Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/gateway-events:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Gateway Events
      description: Get normalized model gateway events for a flow execution.
      operationId: get_flow_execution_gateway_events_api_v1_flows_executions__execution_id__gateway_events_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      - name: tail
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tail
      - name: metadata_only
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Metadata Only
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Gateway Events Api V1 Flows Executions  Execution
                  Id  Gateway Events Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/gateway-events/{event_id}:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Gateway Event
      description: Get a single normalized model gateway event.
      operationId: get_flow_execution_gateway_event_api_v1_flows_executions__execution_id__gateway_events__event_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      - name: event_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Event Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Gateway Event Api V1 Flows Executions  Execution
                  Id  Gateway Events  Event Id  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/metrics:
    get:
      tags:
      - Flows
      summary: Get Flow Execution Metrics
      description: "Get execution metrics including tool calls, API usage, and costs.\n\
        \nReturns:\n    Dictionary with:\n    - tool_calls: Number of MCP tool calls\
        \ made\n    - api_requests: Number of API requests made during execution\n\
        \    - token_usage: Token usage statistics\n    - estimated_cost: Estimated\
        \ cost in USD, or null when no usage could\n      be priced (never a placeholder\
        \ 0.0, which would misreport real\n      spend as free)\n    - has_pricing:\
        \ Whether any request could be priced\n    - cost_is_partial: Whether estimated_cost\
        \ excludes unpriced requests\n    - unpriced_requests: Requests that could\
        \ not be priced\n    - unpriced_tokens: Token volume behind the unpriced requests\n\
        \    - limits: Configured per-execution ceilings (only keys that are set)\n\
        \    - limit_status: Current usage against those ceilings\n      (``total_tokens``,\
        \ ``estimated_cost_usd``, ``turns``)"
      operationId: get_flow_execution_metrics_api_v1_flows_executions__execution_id__metrics_get
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Get Flow Execution Metrics Api V1 Flows Executions  Execution
                  Id  Metrics Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/{flow_id}/gateway-usage/summary:
    get:
      tags:
      - Flows
      summary: Get Flow Gateway Usage Summary
      description: Get model gateway usage summary for a flow.
      operationId: get_flow_gateway_usage_summary_api_v1_flows__flow_id__gateway_usage_summary_get
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      - name: start_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
      - name: end_date
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowGatewayUsageSummaryResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/command:
    post:
      tags:
      - Flows
      summary: Send Execution Command
      description: Send a command to a running flow execution.
      operationId: send_execution_command_api_v1_flows_executions__execution_id__command_post
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowExecutionCommand'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/{flow_id}/trigger:
    post:
      tags:
      - Flows
      summary: Trigger Flow Execution
      description: "Trigger a test execution for a flow.\n\nThe body is free-form\
        \ trigger event data, except for the reserved key\n``matrix``: when present\
        \ it must be a list of up to 25\n``{\"agent_type\"?, \"ai_model_id\"?}`` objects\
        \ and the trigger fans out to\none execution per entry, all sharing a ``batch_id``\
        \ (an empty object runs\nthe flow defaults). Without ``matrix`` the behaviour\
        \ is unchanged.\n\nArgs:\n    flow_id: Flow to trigger\n    trigger_event_data:\
        \ Optional custom trigger event data for testing\n        template variables,\
        \ plus the optional reserved ``matrix`` key\n\nReturns:\n    Execution details,\
        \ or batch details (``batch_id`` + per-cell\n    ``execution_id``) when a\
        \ matrix was given"
      operationId: trigger_flow_execution_api_v1_flows__flow_id__trigger_post
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - type: object
                additionalProperties: true
              - type: 'null'
              title: Trigger Event Data
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/executions/{execution_id}/retry:
    post:
      tags:
      - Flows
      summary: Retry Flow Execution
      description: "Retry a failed, stopped, or cancelled flow execution.\n\nCreates\
        \ a new execution with the same trigger event data as the original.\nThe new\
        \ execution is linked to the original via retry_of_execution_id.\n\nArgs:\n\
        \    execution_id: The execution to retry\n\nReturns:\n    New execution details"
      operationId: retry_flow_execution_api_v1_flows_executions__execution_id__retry_post
      security:
      - bearerAuth: []
      parameters:
      - name: execution_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Execution Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/{flow_id}:
    get:
      tags:
      - Flows
      summary: Read Flow
      description: Get flow by ID.
      operationId: read_flow_api_v1_flows__flow_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Flows
      summary: Update Flow
      description: Update a flow.
      operationId: update_flow_api_v1_flows__flow_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Flows
      summary: Delete Flow
      description: Delete a flow.
      operationId: delete_flow_api_v1_flows__flow_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/{flow_id}/apply-preset-update:
    post:
      tags:
      - Flows
      summary: Apply Preset Update
      description: 'Apply pending preset update to a flow.


        This overwrites the flow''s prompt and tools with the latest version

        from its source preset. Any customizations will be lost.


        Returns the updated flow.'
      operationId: apply_preset_update_api_v1_flows__flow_id__apply_preset_update_post
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/flows/{flow_id}/dismiss-preset-update:
    post:
      tags:
      - Flows
      summary: Dismiss Preset Update
      description: 'Dismiss the preset update notification for a flow.


        The notification won''t reappear until the source preset changes again.


        Returns the updated flow.'
      operationId: dismiss_preset_update_api_v1_flows__flow_id__dismiss_preset_update_post
      security:
      - bearerAuth: []
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/webhooks/flows/{flow_id}/{webhook_secret}:
    post:
      tags:
      - Flows
      summary: Trigger Flow Via Webhook
      description: 'Trigger a flow via webhook (no authentication required - uses
        secret token in URL).


        This endpoint allows external services to trigger flows without authentication.

        Security is provided by the unguessable webhook_secret in the URL.


        Success responses carry a nested ``execution`` object with the same

        ``{id, status, flow_id}`` shape as ``POST /flows/{flow_id}/trigger``, plus

        flat ``execution_id``/``execution_status``/``execution_url`` fields and

        ``"status": "triggered"`` for backwards compatibility.


        Deduplication: redelivered payloads carrying the same commit SHA as a

        still-running execution of this flow return that execution with

        ``"deduplicated": true`` instead of creating a duplicate. Commit-less

        payloads (e.g. GlitchTip alerts) are coalesced on a resource key derived

        from ``webhook_config.dedupe_path`` or the default paths

        ``attachments.0.title_link`` / ``data.issue.id``; payloads with neither

        identity are never deduplicated.'
      operationId: trigger_flow_via_webhook_api_v1_webhooks_flows__flow_id___webhook_secret__post
      parameters:
      - name: flow_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Flow Id
      - name: webhook_secret
        in: path
        required: true
        schema:
          type: string
          title: Webhook Secret
      responses:
        '200':
          description: Execution triggered (or deduplicated to an existing running
            execution for the same repo + commit, see 'deduplicated').
          content:
            application/json:
              schema: {}
        '202':
          description: Execution row was created but dispatch failed; poll execution_url
            or retry from the console. Do not re-send the webhook.
        '422':
          description: Payload does not match the flow's trigger_config; no execution
            was created. Also returned for invalid request input (e.g. malformed flow_id).
        '500':
          description: No execution could be created (failure at/before the insert).
  /api/v1/runners/register:
    post:
      tags:
      - Runners
      summary: Register Runner
      description: Register a new runner or resume an existing one for this account.
      operationId: register_runner_api_v1_runners_register_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunnerRegisterRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunnerRegisterResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/runners:
    get:
      tags:
      - Runners
      summary: List Runners
      operationId: list_runners_api_v1_runners_get
      security:
      - bearerAuth: []
      parameters:
      - name: skip
        in: query
        required: false
        schema:
          type: integer
          default: 0
          title: Skip
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 100
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RunnerResponse'
                title: Response List Runners Api V1 Runners Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runners/fleet-summary:
    get:
      tags:
      - Runners
      summary: Runner Fleet Summary
      operationId: runner_fleet_summary_api_v1_runners_fleet_summary_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunnerFleetSummary'
      security:
      - bearerAuth: []
  /api/v1/runners/{runner_id}/concurrency:
    patch:
      tags:
      - Runners
      summary: Update Runner Concurrency
      description: 'Set how many executions this runner may hold at once.


        The stored value is a ceiling. A runner process started with a lower

        ``--concurrency`` still lowers it while connected; raising it here does

        not make someone''s laptop run more than that process agreed to.'
      operationId: update_runner_concurrency_api_v1_runners__runner_id__concurrency_patch
      security:
      - bearerAuth: []
      parameters:
      - name: runner_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Runner Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunnerConcurrencyUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunnerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runners/{runner_id}:
    get:
      tags:
      - Runners
      summary: Get Runner
      operationId: get_runner_api_v1_runners__runner_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: runner_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Runner Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunnerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/validate:
    post:
      tags:
      - Policies
      summary: Validate a policy file
      description: Validate a YAML/JSON policy file without applying any changes.
      operationId: validate_policy_api_v1_policies_validate_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_validate_policy_api_v1_policies_validate_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyValidationResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/upload:
    post:
      tags:
      - Policies
      summary: Upload and apply a policy file
      description: Upload a YAML/JSON policy file and apply it to your account.
      operationId: upload_policy_api_v1_policies_upload_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_upload_policy_api_v1_policies_upload_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyImportResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/model-io-rules:
    get:
      tags:
      - Policies
      summary: List model I/O content policy rules
      description: Return model.request and model.response rules for the console.
      operationId: list_model_io_rules_api_v1_policies_model_io_rules_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelIORuleListResponse'
      security:
      - bearerAuth: []
    post:
      tags:
      - Policies
      summary: Create or replace a model I/O content policy rule
      description: Save one model I/O rule from the Policies console form.
      operationId: create_model_io_rule_api_v1_policies_model_io_rules_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ModelIORule'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Create Model Io Rule Api V1 Policies Model Io Rules
                  Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/model-io-rules/{rule_id}:
    put:
      tags:
      - Policies
      summary: Replace a model I/O content policy rule
      description: Replace an existing model I/O rule. The path id wins.
      operationId: update_model_io_rule_api_v1_policies_model_io_rules__rule_id__put
      security:
      - bearerAuth: []
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          title: Rule Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ModelIORule'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Update Model Io Rule Api V1 Policies Model Io Rules  Rule
                  Id  Put
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Policies
      summary: Enable or disable a model I/O content policy rule
      description: Toggle enabled without rewriting the rest of the rule.
      operationId: patch_model_io_rule_api_v1_policies_model_io_rules__rule_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          title: Rule Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ModelIORulePatchRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Patch Model Io Rule Api V1 Policies Model Io Rules  Rule
                  Id  Patch
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Policies
      summary: Delete a model I/O content policy rule
      description: Delete one model I/O rule.
      operationId: remove_model_io_rule_api_v1_policies_model_io_rules__rule_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          title: Rule Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/export:
    get:
      tags:
      - Policies
      summary: Export current configuration as policy
      description: Export your current MCP servers, approval workflows, and tool configurations
        as a YAML or JSON policy file.
      operationId: export_policy_api_v1_policies_export_get
      security:
      - bearerAuth: []
      parameters:
      - name: format
        in: query
        required: false
        schema:
          enum:
          - yaml
          - json
          type: string
          default: yaml
          title: Format
      - name: policy_name
        in: query
        required: false
        schema:
          type: string
          default: Exported Policy
          title: Policy Name
      - name: include_mcp_servers
        in: query
        required: false
        schema:
          type: boolean
          default: true
          title: Include Mcp Servers
      - name: include_credentials
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Include Credentials
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/diff:
    post:
      tags:
      - Policies
      summary: Preview changes from a policy file
      description: Compare an uploaded policy file with your current configuration
        to see what would change.
      operationId: diff_policy_api_v1_policies_diff_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_diff_policy_api_v1_policies_diff_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyDiffResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/schema:
    get:
      tags:
      - Policies
      summary: Get policy schema documentation
      description: Get the JSON schema for policy files with documentation.
      operationId: get_policy_schema_api_v1_policies_schema_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Get Policy Schema Api V1 Policies Schema Get
      security:
      - bearerAuth: []
  /api/v1/policies/versions:
    get:
      tags:
      - Policies
      summary: List policy versions
      description: List all policy versions for the account with optional pagination.
      operationId: list_policy_versions_api_v1_policies_versions_get
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          description: Maximum number of versions to return
          default: 100
          title: Limit
        description: Maximum number of versions to return
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of versions to skip
          default: 0
          title: Offset
        description: Number of versions to skip
      - name: include_snapshots
        in: query
        required: false
        schema:
          type: boolean
          description: Include full snapshot data
          default: false
          title: Include Snapshots
        description: Include full snapshot data
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyVersionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - Policies
      summary: Create a new policy version
      description: Create a snapshot of the current policy state.
      operationId: create_policy_version_api_v1_policies_versions_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateVersionRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyVersionFull'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/versions/{version_id}:
    get:
      tags:
      - Policies
      summary: Get a specific policy version
      description: Get a specific policy version with full snapshot data.
      operationId: get_policy_version_api_v1_policies_versions__version_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Version Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyVersionFull'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Policies
      summary: Delete a policy version
      description: Delete a policy version. Cannot delete the active version.
      operationId: delete_policy_version_api_v1_policies_versions__version_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Version Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/versions/{version_id}/tag:
    put:
      tags:
      - Policies
      summary: Add or update tag on a version
      description: Add or update the tag on a policy version.
      operationId: update_version_tag_api_v1_policies_versions__version_id__tag_put
      security:
      - bearerAuth: []
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Version Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTagRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyVersionMetadata'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Policies
      summary: Remove tag from a version
      description: Remove the tag from a policy version.
      operationId: remove_version_tag_api_v1_policies_versions__version_id__tag_delete
      security:
      - bearerAuth: []
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Version Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyVersionMetadata'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/versions/{version_id}/rollback:
    post:
      tags:
      - Policies
      summary: Rollback to a previous version
      description: Apply a previous policy version to restore that configuration.
      operationId: rollback_to_version_api_v1_policies_versions__version_id__rollback_post
      security:
      - bearerAuth: []
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Version Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/versions/prune:
    post:
      tags:
      - Policies
      summary: Prune old policy versions
      description: Delete old unused policy versions based on age and count criteria.
      operationId: prune_policy_versions_api_v1_policies_versions_prune_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PruneRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PruneResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/generate:
    post:
      tags:
      - Policies
      summary: Generate a policy from a description
      description: Use an AI model to generate a valid Preloop policy YAML from a
        natural-language description. Requires at least one AI model configured on
        the account.
      operationId: generate_policy_api_v1_policies_generate_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GeneratePolicyRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GeneratePolicyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/policies/generate-from-audit:
    post:
      tags:
      - Policies
      summary: Generate a policy from audit-log patterns
      description: Analyse historical MCP tool-call audit logs and generate a policy
        that allows observed-normal calls and requires approval for outliers. Requires
        at least one AI model configured on the account.
      operationId: generate_policy_from_audit_api_v1_policies_generate_from_audit_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GeneratePolicyFromAuditRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GeneratePolicyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/security-screen/score:
    post:
      tags:
      - Security Screen
      summary: Score screened content (QM security-screen proxy contract)
      description: "Score one screened content chunk with the deterministic rule engine.\n\
        \nArgs:\n    payload: Screened chunk (text, hook, opaque metadata).\n    auth_context:\
        \ Authenticated request context.\n\nReturns:\n    The verdict in QM's expected\
        \ shape; ``primary_outcome`` is omitted\n    for benign content."
      operationId: score_screened_content_api_v1_security_screen_score_post
      parameters:
      - name: x-api-key
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: X-Api-Key
      - name: authorization
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecurityScreenRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityScreenResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/agents/{agent_id}/control/commands:
    post:
      tags:
      - Agent Control
      summary: Send Managed Agent Command
      description: Backward-compatible route for sending text commands to an agent.
      operationId: send_managed_agent_command_api_v1_agents__agent_id__control_commands_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentControlSendMessageRequest'
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentControlCommandResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/control/prompts:
    post:
      tags:
      - Agent Control
      summary: Send Managed Agent Prompt
      description: Route a text prompt to the current, existing, or next agent session.
      operationId: send_managed_agent_prompt_api_v1_agents__agent_id__control_prompts_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentControlSendMessageRequest'
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentControlCommandResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/control/voice-transcripts:
    post:
      tags:
      - Agent Control
      summary: Send Managed Agent Voice Transcript
      description: Mobile-friendly alias that routes a voice transcript as a prompt.
      operationId: send_managed_agent_voice_transcript_api_v1_agents__agent_id__control_voice_transcripts_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentControlVoiceTranscriptRequest'
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentControlCommandResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/control/takeover:
    post:
      tags:
      - Agent Control
      summary: Request Managed Agent Takeover
      description: Ask the sidecar to switch a local TUI session into remote SDK mode.
      operationId: request_managed_agent_takeover_api_v1_agents__agent_id__control_takeover_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentControlSessionActionRequest'
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentControlCommandResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/{agent_id}/control/release:
    post:
      tags:
      - Agent Control
      summary: Release Managed Agent Session
      description: Release a remote SDK session back to the local `preloop claude`
        TUI.
      operationId: release_managed_agent_session_api_v1_agents__agent_id__control_release_post
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: path
        required: true
        schema:
          type: string
          title: Agent Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentControlSessionActionRequest'
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentControlCommandResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/permission-check:
    post:
      tags:
      - Agent Permissions
      - Agent Permissions
      summary: Agent Permission Check
      description: Decide whether an onboarded agent's native tool call may proceed.
      operationId: agent_permission_check_api_v1_agents_permission_check_post
      parameters:
      - name: authorization
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentPermissionCheckRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentPermissionCheckResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/operator-notes:
    post:
      tags:
      - Operator Notes
      - Operator Notes
      summary: Create Operator Note
      description: Send a note to a running agent, delivered at its next turn boundary.
      operationId: create_operator_note_api_v1_operator_notes_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OperatorNoteCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperatorNoteResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Operator Notes
      - Operator Notes
      summary: List Operator Notes
      description: List notes for one agent, session or execution, newest first.
      operationId: list_operator_notes_api_v1_operator_notes_get
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
      - name: runtime_session_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runtime Session Id
      - name: execution_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Execution Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          minimum: 1
          default: 50
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperatorNoteList'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operator-notes/{note_id}/cancel:
    post:
      tags:
      - Operator Notes
      - Operator Notes
      summary: Cancel Operator Note
      description: 'Withdraw a note that has not been delivered yet.


        Cancelling is a state, never a delete. A delivered note cannot be unsent,

        so this returns it unchanged and the caller can see why.'
      operationId: cancel_operator_note_api_v1_operator_notes__note_id__cancel_post
      security:
      - bearerAuth: []
      parameters:
      - name: note_id
        in: path
        required: true
        schema:
          type: string
          title: Note Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperatorNoteResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/agents/notes/pending:
    post:
      tags:
      - Operator Notes
      - Operator Notes
      summary: Pending Operator Notes
      description: 'Hand a harness the notes its session has been given.


        Claiming is the delivery: what this returns is marked delivered on the

        named channel, audited and evented before it leaves. Call it from the hook

        process, not from the model.'
      operationId: pending_operator_notes_api_v1_agents_notes_pending_post
      parameters:
      - name: authorization
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OperatorNotePendingRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperatorNotePendingResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/runtime-sessions/search:
    post:
      tags:
      - Runtime Sessions
      summary: Search session content by relevance
      description: 'Rank the caller''s runtime sessions by relevance to a query.


        Returns sessions ordered by relevance, each with snippets that name the

        turn the match came from and a reason saying whether the words, the

        vector or both put it there. ``semantic`` and ``hybrid`` embed the query

        and search the corpus vectors; when that half cannot run, the answer is

        keyword results with a degraded marker naming the cause, never an error.


        The call is audited either way. A refusal raised inside the handler is

        recorded as denied and a broken search as failed, so a trail with no row

        for a search means no search ran, not that one was hidden.'
      operationId: search_runtime_sessions_api_v1_runtime_sessions_search_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionSearchRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/runtime-sessions/{runtime_session_id}/similar:
    get:
      tags:
      - Runtime Sessions
      summary: List sessions similar to this one
      description: "Rank other sessions of this account by similarity to this one.\n\
        \nThe comparison reads vectors the indexing worker already wrote: nothing\
        \ is\nembedded, no provider is called and no spend is recorded, so an account\
        \ at\nits daily embedding cap still gets this list. A session with no vectors\
        \ of\nits own, or an account whose corpus holds no other session embedded\
        \ with\nthe same model, is answered with an empty list and a degraded reason\n\
        naming which of those it was, never with an error.\n\nRaises:\n    HTTPException:\
        \ 404 when the session does not belong to this account,\n        so a probe\
        \ cannot tell an existing session of another account\n        apart from one\
        \ that never existed."
      operationId: similar_runtime_sessions_api_v1_runtime_sessions__runtime_session_id__similar_get
      security:
      - bearerAuth: []
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Runtime Session Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 20
          minimum: 1
          description: Similar sessions to return.
          default: 5
          title: Limit
        description: Similar sessions to return.
      - name: max_matches_per_session
        in: query
        required: false
        schema:
          type: integer
          maximum: 5
          minimum: 0
          description: Matching passages returned per session. Zero returns the ranking
            with no captured content at all.
          default: 2
          title: Max Matches Per Session
        description: Matching passages returned per session. Zero returns the ranking
          with no captured content at all.
      - name: window_days
        in: query
        required: false
        schema:
          anyOf:
          - type: integer
            maximum: 365
            minimum: 1
          - type: 'null'
          description: 'Only compare content this recent. Unset compares the whole
            corpus, which is the default: the session worth finding is often an old
            one.'
          title: Window Days
        description: 'Only compare content this recent. Unset compares the whole corpus,
          which is the default: the session worth finding is often an old one.'
      - name: include_match_text
        in: query
        required: false
        schema:
          type: boolean
          description: When false no chunk text is read, so no captured content leaves
            the database; matches still name the turn they came from.
          default: true
          title: Include Match Text
        description: When false no chunk text is read, so no captured content leaves
          the database; matches still name the turn they came from.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimilarSessionsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/{runtime_session_id}/browser-steps:
    post:
      tags:
      - Runtime Sessions
      summary: Ingest Runtime Session Browser Steps
      description: "Store a batch of browser step observations on a runtime session.\n\
        \nArgs:\n    runtime_session_id: Session the steps attach to.\n    batch:\
        \ One to 200 steps.\n    db: Database session.\n    auth: Agent credential\
        \ from the bearer token.\n\nReturns:\n    How many steps were stored, repeated,\
        \ or refused.\n\nRaises:\n    HTTPException: 403 when the credential is pinned\
        \ to another session,\n        404 when the session is not in the credential's\
        \ account."
      operationId: ingest_runtime_session_browser_steps_api_v1_runtime_sessions__runtime_session_id__browser_steps_post
      parameters:
      - name: runtime_session_id
        in: path
        required: true
        schema:
          type: string
          title: Runtime Session Id
      - name: authorization
        in: header
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BrowserStepBatchIn'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrowserStepBatchOut'
        '401':
          description: Missing or invalid bearer token
        '403':
          description: Credential is bound to a different runtime session
        '404':
          description: Runtime session not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/runtime-sessions/search/saved:
    post:
      tags:
      - Runtime Sessions
      summary: Save a session search under a name
      description: 'Save a query, mode, filters and snippet preferences under a name.


        The filters are validated here, so a saved search that cannot run cannot

        be stored. Visibility defaults to private; sharing is a separate edit.'
      operationId: create_saved_session_search_api_v1_runtime_sessions_search_saved_post
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionSavedSearchCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSavedSearchRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - Runtime Sessions
      summary: List saved session searches
      description: 'Return the caller''s saved searches plus the ones shared with
        the account.


        Most recently run first, so the searches somebody actually repeats are at

        the top of the list.'
      operationId: list_saved_session_searches_api_v1_runtime_sessions_search_saved_get
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSavedSearchList'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/search/saved/{saved_search_id}:
    get:
      tags:
      - Runtime Sessions
      summary: Read one saved session search
      description: Return one saved search the caller may see.
      operationId: get_saved_session_search_api_v1_runtime_sessions_search_saved__saved_search_id__get
      security:
      - bearerAuth: []
      parameters:
      - name: saved_search_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Saved Search Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSavedSearchRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Runtime Sessions
      summary: Rename, edit or share a saved session search
      description: 'Change a saved search the caller wrote.


        Renaming, changing the question and sharing are one route because they are

        one thing: this saved question is not what it was. Only the author may do

        any of them.'
      operationId: update_saved_session_search_api_v1_runtime_sessions_search_saved__saved_search_id__patch
      security:
      - bearerAuth: []
      parameters:
      - name: saved_search_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Saved Search Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionSavedSearchUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSavedSearchRead'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Runtime Sessions
      summary: Delete a saved session search
      description: Delete a saved search the caller wrote. The sessions are untouched.
      operationId: delete_saved_session_search_api_v1_runtime_sessions_search_saved__saved_search_id__delete
      security:
      - bearerAuth: []
      parameters:
      - name: saved_search_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Saved Search Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/search/saved/{saved_search_id}/run:
    post:
      tags:
      - Runtime Sessions
      summary: Run a saved session search
      description: 'Run a saved search against the corpus as it is now.


        The answer is the search endpoint''s answer, degraded block and all: a

        saved semantic search on a deployment whose semantic half cannot run comes

        back as keyword results with a reason, not as an error. Alongside it the

        response names every saved filter that no longer resolves, and says

        whether the ranking constants have moved since the search was saved.


        The body is optional: it carries paging only, because the saved search

        owns everything else about the question.'
      operationId: run_saved_session_search_api_v1_runtime_sessions_search_saved__saved_search_id__run_post
      security:
      - bearerAuth: []
      parameters:
      - name: saved_search_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Saved Search Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/SessionSavedSearchRunRequest'
              - type: 'null'
              title: Paging
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionSavedSearchRunResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/runtime-sessions/settings/embedding:
    get:
      tags:
      - Runtime Sessions
      summary: Read this account's session embedding setting
      description: 'Return the setting, creating the shipped default row if there
        is none.


        An account that never opted in reads ``enabled = false`` and

        ``scope = summaries_only``, which is what it would get if it opted in

        without saying anything else.'
      operationId: read_session_embedding_setting_api_v1_runtime_sessions_settings_embedding_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionEmbeddingSettingResponse'
      security:
      - bearerAuth: []
    put:
      tags:
      - Runtime Sessions
      summary: Set how much of a session this account embeds
      description: 'Change the scope. Existing vectors are never touched by this call.


        Narrowing to ``summaries_only`` stops new transcript chunks being

        embedded from the next worker pass; widening to ``full`` gives the

        untouched backlog back to the worker, still under the daily cap.'
      operationId: update_session_embedding_setting_api_v1_runtime_sessions_settings_embedding_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionEmbeddingSettingUpdate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionEmbeddingSettingResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      security:
      - bearerAuth: []
  /api/v1/roles:
    get:
      tags:
      - Roles
      summary: List Roles
      description: "List all available roles.\n\nArgs:\n    current_user: Current\
        \ authenticated user\n    db: Database session\n\nReturns:\n    dict: List\
        \ of roles with their permissions"
      operationId: list_roles_api_v1_roles_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      security:
      - bearerAuth: []
components:
  schemas:
    AIModelAliasFailure:
      properties:
        alias:
          type: string
          title: Alias
          description: Alias the failing calls carried (the provider name when they
            carried none), which is how the console groups gateway failures.
        last_failure_at:
          type: string
          format: date-time
          title: Last Failure At
          description: Newest failed request for this alias in the window. The console
            fingerprints a dismissed attention item with it.
        failed_requests:
          type: integer
          title: Failed Requests
          description: Failed requests for this alias in the window
          default: 0
        failed_requests_since:
          anyOf:
          - type: integer
          - type: 'null'
          title: Failed Requests Since
          description: Failures for this alias newer than this model's failed_since
            query parameter. Null when the caller asked for no such moment.
      type: object
      required:
      - alias
      - last_failure_at
      title: AIModelAliasFailure
      description: One alias-group of failures, matching how the inbox keys a model.
    AIModelCatalogSyncProviderResult:
      properties:
        provider:
          type: string
          title: Provider
          description: Provider name, e.g. 'anthropic'
        source:
          type: string
          enum:
          - live
          - fallback
          title: Source
          description: '''live'' when the provider''s listing endpoint answered; ''fallback''
            when discovery failed or was impossible'
          default: fallback
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: Short safe reason when nothing could be discovered (fixed vocabulary;
            never raw provider error text)
        discovered:
          type: integer
          title: Discovered
          description: Total model identifiers the provider listed
          default: 0
        added:
          items:
            type: string
          type: array
          title: Added
          description: Gateway aliases of newly added catalog models
        skipped_existing:
          type: integer
          title: Skipped Existing
          description: Discovered identifiers already present in the catalog
          default: 0
        note:
          anyOf:
          - type: string
          - type: 'null'
          title: Note
          description: Human-readable context for skips and errors
      type: object
      required:
      - provider
      title: AIModelCatalogSyncProviderResult
      description: Sync outcome for one provider.
    AIModelCatalogSyncRequest:
      properties:
        provider:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider
          description: Optional provider filter, e.g. 'anthropic'. When omitted, every
            provider the account has credentialed models for is synced.
        dry_run:
          type: boolean
          title: Dry Run
          description: Report what would be added without creating any models
          default: false
      type: object
      title: AIModelCatalogSyncRequest
      description: Request body for the account model-catalog sync.
    AIModelCatalogSyncResponse:
      properties:
        providers:
          items:
            $ref: '#/components/schemas/AIModelCatalogSyncProviderResult'
          type: array
          title: Providers
        dry_run:
          type: boolean
          title: Dry Run
          default: false
      type: object
      title: AIModelCatalogSyncResponse
      description: Per-provider results of one model-catalog sync run.
    AIModelCreate:
      properties:
        name:
          type: string
          title: Name
          description: User-defined name for this model configuration
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description
        provider_name:
          type: string
          title: Provider Name
          description: e.g., 'openai', 'anthropic'
        model_identifier:
          type: string
          title: Model Identifier
          description: Standardized identifier, e.g., 'gpt-5.4-turbo'
        model_kind:
          type: string
          enum:
          - llm
          - stt
          - tts
          title: Model Kind
          description: 'Service kind for this model configuration: inference, STT,
            or TTS'
          default: llm
        api_endpoint:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Endpoint
          description: URL for the model's API, if not standard
        api_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key
          description: API key for the model provider
        credential_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Type
          description: Optional inline credential envelope type, e.g. 'oauth_openai_codex'
        credential_payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Credential Payload
          description: Optional inline credential payload for non-API-key auth, stored
            encrypted when provided
        credentials_backend_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Backend Type
          description: Optional external credential backend type, e.g. 'vault_kv_v2'
        credentials_external_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials External Ref
          description: Optional external secret reference for provider credentials
        credentials_meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Credentials Meta Data
          description: Optional metadata for external secret backends, e.g. field/version
        is_default:
          type: boolean
          title: Is Default
          description: Indicates if this is the default model for the account
          default: false
        model_parameters:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Model Parameters
          description: Optional, for model-specific parameters like temperature, max_tokens
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Optional, for custom fields, labels, etc.
        credentials_secret_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Credentials Secret Id
          description: Reuse an existing SecretReference instead of minting a new
            one. Used to create several models that share a single provider key. The
            secret must already belong to the caller's account.
      type: object
      required:
      - name
      - provider_name
      - model_identifier
      title: AIModelCreate
      description: Schema for creating a new AIModel entry.
    AIModelCredentialExportResponse:
      properties:
        credential_type:
          type: string
          title: Credential Type
        access:
          type: string
          title: Access
        refresh:
          anyOf:
          - type: string
          - type: 'null'
          title: Refresh
        expires:
          anyOf:
          - type: integer
          - type: 'null'
          title: Expires
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
      type: object
      required:
      - credential_type
      - access
      title: AIModelCredentialExportResponse
      description: 'Live subscription-OAuth bundle exported to the owning operator.


        Returned once over the authenticated API so the CLI can restore an

        agent''s local login at offboard time (subscription refresh tokens are

        single-use, so the Preloop-held copy is the only live lineage after a

        server-side refresh). Token material must never be logged.'
    AIModelGatewayUsageSummaryResponse:
      properties:
        ai_model_id:
          type: string
          title: Ai Model Id
        model_name:
          type: string
          title: Model Name
        provider_name:
          type: string
          title: Provider Name
        model_identifier:
          type: string
          title: Model Identifier
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        last_failure_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Failure At
          description: Timestamp of the most recent failed gateway request in the
            window, which the console fingerprints a dismissed 'needs attention' item
            with
        last_failure_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Failure Alias
          description: Alias the most recent failed request carried (the provider
            name when it carried none)
        failed_requests_since:
          anyOf:
          - type: integer
          - type: 'null'
          title: Failed Requests Since
          description: Failures newer than the failed_since query parameter. Null
            when the caller asked for no such moment.
        alias_failures:
          items:
            $ref: '#/components/schemas/AIModelAliasFailure'
          type: array
          title: Alias Failures
          description: Per-alias failure groups for this model, one item per inbox
            key. The page is Attention if any group is unacknowledged.
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        requests_by_day:
          items:
            $ref: '#/components/schemas/GatewayUsageByDay'
          type: array
          title: Requests By Day
        usage_by_session:
          items:
            $ref: '#/components/schemas/GatewayUsageBySession'
          type: array
          title: Usage By Session
      type: object
      required:
      - ai_model_id
      - model_name
      - provider_name
      - model_identifier
      - period_start
      - period_end
      - token_usage
      title: AIModelGatewayUsageSummaryResponse
      description: Gateway usage summary for one durable AI model.
    AIModelOverviewItem:
      properties:
        ai_model_id:
          type: string
          title: Ai Model Id
        model_name:
          type: string
          title: Model Name
        provider_name:
          type: string
          title: Provider Name
        model_identifier:
          type: string
          title: Model Identifier
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Gateway alias clients call this model by, if configured
        is_default:
          type: boolean
          title: Is Default
          default: false
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        unpriced_request_count:
          type: integer
          title: Unpriced Request Count
          description: Requests with tokens but no price to apply
          default: 0
        active_session_count:
          type: integer
          title: Active Session Count
          description: Runtime sessions still open that used this model
          default: 0
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
          description: Timestamp of the most recent gateway request
        last_failure_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Failure At
          description: Timestamp of the most recent failed gateway request in the
            window. The console fingerprints a dismissed 'needs attention' item with
            it, so one more failure brings the item back.
        last_failure_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Failure Alias
          description: Alias the most recent failed request carried (the provider
            name when it carried none), which is how the console groups gateway failures.
        failed_requests_since:
          anyOf:
          - type: integer
          - type: 'null'
          title: Failed Requests Since
          description: Failures newer than this model's failed_since query parameter.
            Null when the caller asked for no such moment.
        alias_failures:
          items:
            $ref: '#/components/schemas/AIModelAliasFailure'
          type: array
          title: Alias Failures
          description: Per-alias failure groups for this model, one item per inbox
            key. The row is Attention if any group is unacknowledged.
        pricing_source:
          type: string
          enum:
          - override
          - model_config
          - catalog
          - none
          title: Pricing Source
          description: Where this model's effective price comes from
          default: none
      type: object
      required:
      - ai_model_id
      - model_name
      - provider_name
      - model_identifier
      title: AIModelOverviewItem
      description: 'One row of the Models page: what this model did in the window.'
    AIModelPrice:
      properties:
        input_per_1m:
          anyOf:
          - type: number
          - type: 'null'
          title: Input Per 1M
          description: Price of a million input tokens, in USD.
        output_per_1m:
          anyOf:
          - type: number
          - type: 'null'
          title: Output Per 1M
          description: Price of a million output tokens, in USD.
        cached_input_per_1m:
          anyOf:
          - type: number
          - type: 'null'
          title: Cached Input Per 1M
          description: Price of a million cached input tokens, in USD. Absent when
            the source says nothing about caching, which is not the same as free.
        blended_per_1m:
          anyOf:
          - type: number
          - type: 'null'
          title: Blended Per 1M
          description: Single price applied to every token, in USD per million. Set
            by overrides that do not separate input from output.
        request_price:
          anyOf:
          - type: number
          - type: 'null'
          title: Request Price
          description: Flat price added per request, in USD.
      type: object
      title: AIModelPrice
      description: One price, in USD per million tokens.
    AIModelPriceQuote:
      properties:
        ai_model_id:
          type: string
          title: Ai Model Id
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        source_url:
          type: string
          title: Source Url
        model_key:
          type: string
          title: Model Key
          description: The provider's own identifier for the model.
        price:
          $ref: '#/components/schemas/AIModelPrice'
        currency:
          type: string
          title: Currency
          default: USD
        fetched_at:
          type: string
          format: date-time
          title: Fetched At
      type: object
      required:
      - ai_model_id
      - source_url
      - model_key
      - price
      - fetched_at
      title: AIModelPriceQuote
      description: A price read from the provider, for confirmation. Nothing is saved.
    AIModelPricingResponse:
      properties:
        ai_model_id:
          type: string
          title: Ai Model Id
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        source:
          type: string
          enum:
          - override
          - model_config
          - catalog
          - none
          title: Source
          description: 'override: an account price override. model_config: pricing
            configured on the model itself. catalog: the bundled or live provider
            price list. none: nothing prices this model, so its requests land unpriced.'
        price:
          $ref: '#/components/schemas/AIModelPrice'
        currency:
          type: string
          title: Currency
          default: USD
        override_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Override Id
        effective_from:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Effective From
        effective_until:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Effective Until
        catalog_provenance:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Catalog Provenance
          description: Published-price provenance and schedule estimation assumptions.
        catalog_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Catalog Key
          description: The price list entry that matched, when the source is the catalog.
        fetch_supported:
          type: boolean
          title: Fetch Supported
          description: True when this provider publishes prices Preloop can read.
          default: false
        fetch_provider_label:
          anyOf:
          - type: string
          - type: 'null'
          title: Fetch Provider Label
          description: Provider name to show when a price fetch is not offered.
      type: object
      required:
      - ai_model_id
      - source
      title: AIModelPricingResponse
      description: Effective list or configured tariff estimate, not a settled invoice
        charge.
    AIModelRead:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Primary key
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        provider_name:
          type: string
          title: Provider Name
        model_identifier:
          type: string
          title: Model Identifier
        model_kind:
          type: string
          enum:
          - llm
          - stt
          - tts
          title: Model Kind
          default: llm
        api_endpoint:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Endpoint
        is_default:
          type: boolean
          title: Is Default
          default: false
        model_parameters:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Model Parameters
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
          description: Account this model belongs to
        credentials_secret_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Credentials Secret Id
          description: Secret reference ID for model credentials
        credentials_backend_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Backend Type
          description: Backend type used for credential storage
        credentials_external_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials External Ref
          description: External secret reference when using a non-local backend
        credential_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Type
          description: Logical credential type stored for the model
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
          description: When this AI model row was last updated
        credentials_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Status
          description: Status of the model's credential secret
        credentials_last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Last Error
          description: Summary of the last credential refresh error
        credentials_last_error_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Last Error Code
          description: Error code from the last credential refresh attempt
        credentials_last_failed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Credentials Last Failed At
          description: Timestamp of the last failed credential refresh
        credentials_last_verified_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Credentials Last Verified At
          description: Timestamp when credentials were last verified or refreshed
        has_api_key:
          type: boolean
          title: Has Api Key
          description: Whether this model has credentials configured
          default: false
        supports_server_side_generation:
          type: boolean
          title: Supports Server Side Generation
          description: Whether Preloop can run its own generation calls with this
            model. False for principal-bound OAuth (Claude Code / Codex subscription)
            credentials, which only authorize their owner's interactive traffic and
            must never be auto-selected as a default.
          default: false
      type: object
      required:
      - id
      - name
      - provider_name
      - model_identifier
      title: AIModelRead
      description: Schema for reading AIModel entries, including timestamps.
    AIModelUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        model_identifier:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Identifier
        model_kind:
          anyOf:
          - type: string
            enum:
            - llm
            - stt
            - tts
          - type: 'null'
          title: Model Kind
        api_endpoint:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Endpoint
        api_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key
        credential_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Type
        credential_payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Credential Payload
        credentials_backend_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials Backend Type
        credentials_external_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Credentials External Ref
        credentials_meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Credentials Meta Data
        credentials_secret_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Credentials Secret Id
          description: Repoint this model at an existing SecretReference instead of
            minting a new one. Used to keep several models on one provider key or
            OAuth lineage. The secret must already belong to the caller's account.
        is_default:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Default
        model_parameters:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Model Parameters
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
      type: object
      title: AIModelUpdate
      description: Schema for updating an existing AIModel entry. All fields are optional.
    AIModelsOverviewResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        models:
          items:
            $ref: '#/components/schemas/AIModelOverviewItem'
          type: array
          title: Models
      type: object
      required:
      - period_start
      - period_end
      title: AIModelsOverviewResponse
      description: 'Batch answer for the Models page and the dashboard inventory tab.


        Replaces one request per model per panel with a single response, so

        opening the page costs a fixed number of queries no matter how many

        models an account has configured.'
    AccessRuleCreate:
      properties:
        action:
          type: string
          title: Action
          description: 'Action: ''allow'', ''deny'', ''require_approval'''
        condition_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Expression
          description: CEL or simple expression
        condition_type:
          type: string
          title: Condition Type
          description: 'Type: ''simple'' or ''cel'''
          default: cel
        priority:
          type: integer
          title: Priority
          description: Evaluation order (lower = first)
          default: 0
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Description or denial message
        is_enabled:
          type: boolean
          title: Is Enabled
          description: Whether the rule is active
          default: true
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
          description: Approval workflow ID (for 'require_approval' action)
      type: object
      required:
      - action
      title: AccessRuleCreate
      description: Schema for creating an access rule.
    AccessRuleResponse:
      properties:
        id:
          type: string
          title: Id
        account_id:
          type: string
          title: Account Id
        tool_configuration_id:
          type: string
          title: Tool Configuration Id
        action:
          type: string
          title: Action
        condition_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Expression
        condition_type:
          type: string
          title: Condition Type
        priority:
          type: integer
          title: Priority
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        is_enabled:
          type: boolean
          title: Is Enabled
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
      type: object
      required:
      - id
      - account_id
      - tool_configuration_id
      - action
      - condition_expression
      - condition_type
      - priority
      - description
      - is_enabled
      title: AccessRuleResponse
      description: Schema for access rule response.
    AccessRuleUpdate:
      properties:
        action:
          anyOf:
          - type: string
          - type: 'null'
          title: Action
        condition_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Expression
        condition_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Type
        priority:
          anyOf:
          - type: integer
          - type: 'null'
          title: Priority
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
      type: object
      title: AccessRuleUpdate
      description: Schema for updating an access rule.
    AccountDeletionRequest:
      properties:
        email:
          type: string
          format: email
          title: Email
        username:
          type: string
          title: Username
        account_id:
          type: string
          title: Account Id
        org_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Org Name
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
      type: object
      required:
      - email
      - username
      - account_id
      title: AccountDeletionRequest
      description: Account deletion request from user.
    AccountDetailsResponse:
      properties:
        id:
          type: string
          title: Id
        organization_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Name
        default_runner_pool:
          anyOf:
          - type: string
          - type: 'null'
          title: Default Runner Pool
          description: 'Account default runner pool: a runner id, name, or label;
            the literal ''server'' for Preloop hosted; null for any online private
            runner.'
        hosted_minutes_remaining:
          anyOf:
          - type: integer
          - type: 'null'
          title: Hosted Minutes Remaining
          description: Hosted runner minutes remaining for this account. Null when
            quota is not configured.
        created_at:
          type: string
          title: Created At
        updated_at:
          type: string
          title: Updated At
      type: object
      required:
      - id
      - created_at
      - updated_at
      title: AccountDetailsResponse
      description: Account details response.
    AccountDetailsUpdate:
      properties:
        organization_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Name
        default_runner_pool:
          anyOf:
          - type: string
            maxLength: 200
          - type: 'null'
          title: Default Runner Pool
          description: 'Account default runner pool: a runner id, name, or label;
            the literal ''server'' for Preloop hosted; null for any online private
            runner.'
      type: object
      title: AccountDetailsUpdate
      description: Account details update request.
    AccountGatewayUsageSearchResponse:
      properties:
        period_start:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period Start
        period_end:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period End
        query:
          anyOf:
          - type: string
          - type: 'null'
          title: Query
        total:
          type: integer
          title: Total
          default: 0
        limit:
          type: integer
          title: Limit
          default: 20
        offset:
          type: integer
          title: Offset
          default: 0
        items:
          items:
            $ref: '#/components/schemas/GatewayUsageSearchResultItem'
          type: array
          title: Items
      type: object
      title: AccountGatewayUsageSearchResponse
      description: Account-scoped gateway interaction search results.
    AccountGatewayUsageSummaryResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        unpriced_requests:
          type: integer
          title: Unpriced Requests
          default: 0
        unpriced_tokens:
          type: integer
          title: Unpriced Tokens
          default: 0
        budget:
          $ref: '#/components/schemas/GatewayBudgetSummary'
        requests_by_day:
          items:
            $ref: '#/components/schemas/GatewayUsageByDay'
          type: array
          title: Requests By Day
        usage_by_model:
          items:
            $ref: '#/components/schemas/GatewayUsageByModel'
          type: array
          title: Usage By Model
        usage_by_flow:
          items:
            $ref: '#/components/schemas/GatewayUsageByFlow'
          type: array
          title: Usage By Flow
        usage_by_session:
          items:
            $ref: '#/components/schemas/GatewayUsageBySession'
          type: array
          title: Usage By Session
        usage_by_tool:
          items:
            $ref: '#/components/schemas/GatewayUsageByTool'
          type: array
          title: Usage By Tool
      type: object
      required:
      - period_start
      - period_end
      - token_usage
      - budget
      title: AccountGatewayUsageSummaryResponse
      description: Account-scoped gateway usage summary.
    AccountGovernanceDefaults:
      properties:
        native_tool_approvals:
          anyOf:
          - type: string
            enum:
            - enforce
            - 'off'
          - type: 'null'
          title: Native Tool Approvals
          description: Account default for native tool-call approvals. Agents without
            an explicit per-agent setting inherit this; absent/None means "enforce".
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
          description: Account default approval workflow for native tool-call approvals.
            Agents without a per-agent pin inherit this; absent falls back to the
            account's default workflow.
      type: object
      title: AccountGovernanceDefaults
      description: 'Account-wide governance defaults inherited by every managed agent.


        A per-agent subject-governance config with an explicit value overrides

        these; an absent/None per-agent value inherits. The final fallback when

        both are unset is "enforce" (fail safe).'
    AccountGovernanceDefaultsResponse:
      properties:
        defaults:
          $ref: '#/components/schemas/AccountGovernanceDefaults'
        override_agent_ids:
          items:
            type: string
          type: array
          title: Override Agent Ids
      type: object
      required:
      - defaults
      title: AccountGovernanceDefaultsResponse
    AccountManagedAgentListResponse:
      properties:
        query:
          anyOf:
          - type: string
          - type: 'null'
          title: Query
        agent_kind:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Kind
        last_seen_after:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Seen After
        status:
          type: string
          title: Status
          default: all
        total:
          type: integer
          title: Total
          default: 0
        limit:
          type: integer
          title: Limit
          default: 20
        offset:
          type: integer
          title: Offset
          default: 0
        items:
          items:
            $ref: '#/components/schemas/ManagedAgentSummary'
          type: array
          title: Items
      type: object
      title: AccountManagedAgentListResponse
      description: Account-scoped managed-agent registry response.
    AccountRateLimitReportResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        totals:
          $ref: '#/components/schemas/RateLimitTotals'
        by_model:
          items:
            $ref: '#/components/schemas/RateLimitByModel'
          type: array
          title: By Model
        by_session:
          items:
            $ref: '#/components/schemas/RateLimitBySession'
          type: array
          title: By Session
        latest_snapshots:
          items:
            $ref: '#/components/schemas/RateLimitSnapshotItem'
          type: array
          title: Latest Snapshots
      type: object
      required:
      - period_start
      - period_end
      - totals
      title: AccountRateLimitReportResponse
      description: Account-scoped rate-limit telemetry and observed headroom report.
    AccountRuntimeSessionDetailResponse:
      properties:
        period_start:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period Start
        period_end:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period End
        session:
          $ref: '#/components/schemas/RuntimeSessionSummary'
        usage_by_model:
          items:
            $ref: '#/components/schemas/GatewayUsageByModel'
          type: array
          title: Usage By Model
      type: object
      required:
      - session
      title: AccountRuntimeSessionDetailResponse
      description: One runtime session detail summary. (Interactions and activity
        moved to sub-endpoints).
    AccountRuntimeSessionListResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        query:
          anyOf:
          - type: string
          - type: 'null'
          title: Query
        session_source_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
        status:
          type: string
          title: Status
          default: all
        total:
          type: integer
          title: Total
          default: 0
        limit:
          type: integer
          title: Limit
          default: 20
        offset:
          type: integer
          title: Offset
          default: 0
        items:
          items:
            $ref: '#/components/schemas/RuntimeSessionSummary'
          type: array
          title: Items
      type: object
      required:
      - period_start
      - period_end
      title: AccountRuntimeSessionListResponse
      description: Account-scoped runtime session explorer list response.
    AgentControlCommandResponse:
      properties:
        command_id:
          type: string
          title: Command Id
        managed_agent_id:
          type: string
          format: uuid
          title: Managed Agent Id
        runtime_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runtime Session Id
        target_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Target Session Id
        session_source_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        session_mode:
          type: string
          enum:
          - existing
          - new
          - current
          title: Session Mode
        subject:
          anyOf:
          - type: string
          - type: 'null'
          title: Subject
        local_delivery:
          type: boolean
          title: Local Delivery
          default: false
        published:
          type: boolean
          title: Published
          default: false
        command_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Command Status
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        command_ttl_seconds:
          anyOf:
          - type: integer
          - type: 'null'
          title: Command Ttl Seconds
        command_envelope:
          $ref: '#/components/schemas/AgentControlEnvelope'
      type: object
      required:
      - command_id
      - managed_agent_id
      - session_mode
      - command_envelope
      title: AgentControlCommandResponse
      description: Result of routing an operator command to a managed agent.
    AgentControlEnvelope:
      properties:
        type:
          type: string
          enum:
          - command
          - event
          - presence
          - ack
          - error
          title: Type
        name:
          type: string
          title: Name
        message_id:
          type: string
          title: Message Id
        account_id:
          type: string
          format: uuid
          title: Account Id
        managed_agent_id:
          type: string
          format: uuid
          title: Managed Agent Id
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
        session_source_type:
          type: string
          title: Session Source Type
        session_source_id:
          type: string
          title: Session Source Id
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        payload:
          additionalProperties: true
          type: object
          title: Payload
      type: object
      required:
      - type
      - name
      - message_id
      - account_id
      - managed_agent_id
      - runtime_session_id
      - session_source_type
      - session_source_id
      - timestamp
      title: AgentControlEnvelope
      description: Typed envelope exchanged over the shared managed-agent control
        plane.
    AgentControlSendMessageRequest:
      properties:
        message:
          type: string
          minLength: 1
          title: Message
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        target_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Target Session Id
        start_new_session:
          type: boolean
          title: Start New Session
          default: false
        input_mode:
          type: string
          enum:
          - text
          - voice_transcript
          title: Input Mode
          default: text
        voice:
          additionalProperties: true
          type: object
          title: Voice
        spawn_worktree:
          type: boolean
          title: Spawn Worktree
          default: false
        interrupt:
          type: boolean
          title: Interrupt
          default: false
      type: object
      required:
      - message
      title: AgentControlSendMessageRequest
      description: Operator request for routing a prompt to an online managed agent.
    AgentControlSessionActionRequest:
      properties:
        target_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Target Session Id
        start_new_session:
          type: boolean
          title: Start New Session
          default: false
        spawn_worktree:
          type: boolean
          title: Spawn Worktree
          default: false
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      type: object
      title: AgentControlSessionActionRequest
      description: Takeover or release a Claude Code (or other) session.
    AgentControlVoiceTranscriptRequest:
      properties:
        transcript:
          type: string
          minLength: 1
          title: Transcript
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        voice:
          additionalProperties: true
          type: object
          title: Voice
        target_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Target Session Id
        start_new_session:
          type: boolean
          title: Start New Session
          default: false
      type: object
      required:
      - transcript
      title: AgentControlVoiceTranscriptRequest
      description: Mobile-friendly request for routing a spoken prompt transcript.
    AgentDeploymentRequest:
      properties:
        idempotency_key:
          type: string
          format: uuid
          title: Idempotency Key
        runtime:
          type: string
          enum:
          - hermes
          - openclaw
          title: Runtime
        model_id:
          type: string
          format: uuid
          title: Model Id
        target:
          type: string
          enum:
          - ssh
          - gcp
          title: Target
        ssh:
          anyOf:
          - $ref: '#/components/schemas/AgentDeploymentSSH'
          - type: 'null'
        compute_size:
          type: string
          enum:
          - standard
          - performance
          - high-mem
          title: Compute Size
          default: standard
        desktop:
          type: boolean
          title: Desktop
          default: false
      type: object
      required:
      - idempotency_key
      - runtime
      - model_id
      - target
      title: AgentDeploymentRequest
      description: A single deployment; reuse its idempotency key when retrying.
    AgentDeploymentSSH:
      properties:
        host:
          type: string
          maxLength: 253
          minLength: 1
          title: Host
        port:
          type: integer
          maximum: 65535.0
          minimum: 1.0
          title: Port
          default: 22
        username:
          type: string
          maxLength: 64
          minLength: 1
          pattern: ^[a-zA-Z_][a-zA-Z0-9_-]*$
          title: Username
        host_key:
          type: string
          maxLength: 16384
          minLength: 20
          title: Host Key
        password:
          anyOf:
          - type: string
            format: password
            writeOnly: true
          - type: 'null'
          title: Password
        private_key:
          anyOf:
          - type: string
            format: password
            writeOnly: true
          - type: 'null'
          title: Private Key
      type: object
      required:
      - host
      - username
      - host_key
      title: AgentDeploymentSSH
      description: An explicit SSH destination and its independently verified host
        key.
    AgentNameExtractionRequest:
      properties:
        identity_content:
          type: string
          title: Identity Content
      type: object
      required:
      - identity_content
      title: AgentNameExtractionRequest
      description: Request to extract an agent's name from IDENTITY.md content.
    AgentNameExtractionResponse:
      properties:
        name:
          type: string
          title: Name
      type: object
      required:
      - name
      title: AgentNameExtractionResponse
      description: Extracted agent name.
    AgentPermissionCheckRequest:
      properties:
        tool_name:
          type: string
          title: Tool Name
          description: Native tool name, e.g. 'Bash'
        tool_input:
          additionalProperties: true
          type: object
          title: Tool Input
          description: Tool arguments (stored as tool_args)
        source:
          anyOf:
          - type: string
          - type: 'null'
          title: Source
          description: 'Originating agent adapter: ''claude_code'', ''codex_cli'',
            ''cursor'', ''opencode'', ''openclaw'', ''hermes'', ''pi'', or ''deepseek''.
            Stored as the ''_preloop_source'' marker inside tool_args so approver
            surfaces can label the requester.'
        session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Id
          description: Agent session id
        cwd:
          anyOf:
          - type: string
          - type: 'null'
          title: Cwd
          description: Working directory
        repository:
          anyOf:
          - $ref: '#/components/schemas/AgentPermissionRepository'
          - type: 'null'
          description: Trusted repository identity the hook observed from its cwd.
            Stored as the '_preloop_repository' marker inside tool_args, next to '_preloop_source',
            so approvals and the session timeline can label which repository the call
            ran in.
        agent_reasoning:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Reasoning
          description: Why the agent wants this call (shown to the approver)
        client_decision:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Decision
          description: 'What the client''s own policy decided: ''allow'' | ''deny''
            | ''ask''. Absent/''ask'' escalates to a human approver.'
        evaluation_phase:
          type: string
          enum:
          - permission_request
          - pre_tool_use
          title: Evaluation Phase
          description: '''pre_tool_use'' checks central native rules without assuming
            a host permission decision: no matching rule continues without automatic
            human escalation. ''permission_request'' retains normal remote escalation.
            A pre-tool allow never grants the host''s permission.'
          default: permission_request
      type: object
      required:
      - tool_name
      title: AgentPermissionCheckRequest
      description: A native tool-call permission check from an onboarded agent.
    AgentPermissionCheckResponse:
      properties:
        decision:
          type: string
          title: Decision
          description: '''allow'' or ''deny'''
        reason:
          type: string
          title: Reason
          default: ''
        request_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Request Id
        operator_note:
          anyOf:
          - type: string
          - type: 'null'
          title: Operator Note
          description: 'A pending operator note, rendered for the model, to surface
            as additional context alongside this decision. Null when there is none,
            which is almost always: the PreToolUse call the agent was making anyway
            carries the note, so a note costs no extra round trip and no note costs
            nothing at all.'
        timed_out:
          type: boolean
          title: Timed Out
          description: True when the deny is only the expiry of an unanswered approval
            request, not a human decision. It remains a denial; adapters must not
            replace required central approval with a local prompt.
          default: false
      type: object
      required:
      - decision
      title: AgentPermissionCheckResponse
      description: Allow/deny decision for the agent's native tool call.
    AgentPermissionRepository:
      properties:
        remote:
          type: string
          maxLength: 512
          title: Remote
          description: Normalized 'host/owner/repo' with credentials stripped; empty
            when the work tree has no origin remote.
          default: ''
        toplevel:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Toplevel
          description: Absolute work-tree root.
        relative_path:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Relative Path
          description: cwd relative to the work-tree root.
        source:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Source
          description: How the identity was observed, e.g. 'hook_cwd'.
        no_remote:
          type: boolean
          title: No Remote
          description: True when the work tree has no origin, or the origin is not
            a host/owner/repo identity (a local path or file:// remote).
          default: false
      additionalProperties: false
      type: object
      title: AgentPermissionRepository
      description: 'Trusted hook observation of the repository a native call ran in.


        The hook resolves its own ``cwd`` against git and sends the result; the

        caller-supplied tool arguments are never used, so an MCP tool cannot

        spoof the repository. Extra keys are forbidden and every string is

        bounded: the object travels into approval ``tool_args`` and onto the

        session timeline, and this is untrusted network input.'
    ApiKeyCreate:
      properties:
        name:
          type: string
          maxLength: 100
          minLength: 1
          title: Name
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        scopes:
          items:
            type: string
          type: array
          title: Scopes
      type: object
      required:
      - name
      title: ApiKeyCreate
      description: Model for API key creation.
    ApiKeyResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        key:
          type: string
          title: Key
        created_at:
          type: string
          format: date-time
          title: Created At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        scopes:
          items: {}
          type: array
          title: Scopes
          default: []
        user_id:
          type: string
          format: uuid
          title: User Id
        last_used_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Used At
      type: object
      required:
      - id
      - name
      - key
      - created_at
      - user_id
      title: ApiKeyResponse
      description: Response model for API key data.
    ApiKeySummary:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        created_at:
          type: string
          format: date-time
          title: Created At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        scopes:
          items: {}
          type: array
          title: Scopes
          default: []
        last_used_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Used At
        managed_agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Managed Agent Id
        runtime_principal_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Type
        runtime_principal_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        activity_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Activity Status
        recent_model_calls:
          type: integer
          title: Recent Model Calls
          default: 0
        recent_tool_calls:
          type: integer
          title: Recent Tool Calls
          default: 0
      type: object
      required:
      - id
      - name
      - created_at
      title: ApiKeySummary
      description: Summary model for API key data (without the key itself).
    ApiUsageStatistics:
      properties:
        total_requests:
          type: integer
          title: Total Requests
        requests_by_date:
          additionalProperties:
            type: integer
          type: object
          title: Requests By Date
        issues_created:
          type: integer
          title: Issues Created
        issues_updated:
          type: integer
          title: Issues Updated
        issues_closed:
          type: integer
          title: Issues Closed
        requests_by_endpoint:
          additionalProperties:
            type: integer
          type: object
          title: Requests By Endpoint
      type: object
      required:
      - total_requests
      - requests_by_date
      - issues_created
      - issues_updated
      - issues_closed
      - requests_by_endpoint
      title: ApiUsageStatistics
      description: Model for API usage statistics.
    ApprovalAgentSummary:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        kind:
          anyOf:
          - type: string
          - type: 'null'
          title: Kind
          description: Agent kind, e.g. 'claude_code' or 'cursor'
      type: object
      required:
      - id
      - name
      title: ApprovalAgentSummary
      description: The managed agent that raised the request.
    ApprovalApiKeySummary:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
      type: object
      required:
      - id
      - name
      title: ApprovalApiKeySummary
      description: The API key the requesting caller authenticated with.
    ApprovalBatchDecision:
      properties:
        ids:
          items:
            type: string
            format: uuid
          type: array
          maxItems: 100
          minItems: 1
          title: Ids
          description: Approval request ids to decide, in the order they were picked
        approved:
          type: boolean
          title: Approved
          description: Whether every listed request is approved or declined
        comment:
          anyOf:
          - type: string
          - type: 'null'
          title: Comment
          description: Comment recorded on every request in the batch
      type: object
      required:
      - ids
      - approved
      title: ApprovalBatchDecision
      description: One decision applied to several pending requests.
    ApprovalBatchItemResult:
      properties:
        id:
          type: string
          title: Id
          description: The approval request this result is for
        ok:
          type: boolean
          title: Ok
          description: True when the decision was recorded
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: Status of the request after the decision, when known
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: 'Why this request was not decided: not found, not yours, or
            already resolved. Null when ok is true.'
      type: object
      required:
      - id
      - ok
      title: ApprovalBatchItemResult
      description: What happened to one request in a batch decision.
    ApprovalBatchResponse:
      properties:
        results:
          items:
            $ref: '#/components/schemas/ApprovalBatchItemResult'
          type: array
          title: Results
          description: One result per requested id, in request order
        succeeded:
          type: integer
          title: Succeeded
          description: How many requests took the decision.
          readOnly: true
        failed:
          type: integer
          title: Failed
          description: How many requests did not.
          readOnly: true
      type: object
      required:
      - results
      - succeeded
      - failed
      title: ApprovalBatchResponse
      description: 'Per-request results for a batch decision.


        A batch never fails as a whole. One request that is already resolved (or

        belongs to somebody else) must not cost the operator the other nineteen

        decisions they just made, so every id gets its own result and the caller

        reports the failures by name.'
    ApprovalBypassCreate:
      properties:
        mode:
          type: string
          enum:
          - mute_notifications
          - auto_approve
          title: Mode
          description: '''mute_notifications'' silences alerts but still blocks the
            agent; ''auto_approve'' also approves automatically (governance bypass)'
        duration_minutes:
          type: integer
          maximum: 480.0
          minimum: 1.0
          title: Duration Minutes
          description: How long the bypass lasts (1-480 minutes)
        managed_agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Managed Agent Id
          description: Scope to one agent. Omit for an account-wide bypass.
        reason:
          anyOf:
          - type: string
            maxLength: 500
          - type: 'null'
          title: Reason
          description: Why the bypass was opened (recorded for audit)
        created_via:
          anyOf:
          - type: string
          - type: 'null'
          title: Created Via
          description: 'Originating surface: console|mobile|watch|api|cli'
      type: object
      required:
      - mode
      - duration_minutes
      title: ApprovalBypassCreate
      description: 'Request body for opening a bypass.


        ``duration_minutes`` is required and bounded — there is deliberately no way

        to express "forever". An indefinite bypass is the failure mode this feature

        exists to prevent.'
    ApprovalBypassResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        account_id:
          type: string
          format: uuid
          title: Account Id
        user_id:
          type: string
          format: uuid
          title: User Id
        managed_agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Managed Agent Id
        mode:
          type: string
          title: Mode
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
        created_by_user_id:
          type: string
          format: uuid
          title: Created By User Id
        created_via:
          anyOf:
          - type: string
          - type: 'null'
          title: Created Via
        created_at:
          type: string
          format: date-time
          title: Created At
        expires_at:
          type: string
          format: date-time
          title: Expires At
        revoked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Revoked At
        revoked_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Revoked By User Id
        auto_approved_count:
          type: integer
          title: Auto Approved Count
          default: 0
        is_account_wide:
          type: boolean
          title: Is Account Wide
          description: True when this bypass applies to every agent for the user.
          readOnly: true
        suppresses_approvals:
          type: boolean
          title: Suppresses Approvals
          description: True when agents are auto-approved rather than merely unmuted.
          readOnly: true
      type: object
      required:
      - id
      - account_id
      - user_id
      - mode
      - created_by_user_id
      - created_at
      - expires_at
      - is_account_wide
      - suppresses_approvals
      title: ApprovalBypassResponse
      description: A bypass as returned to console and mobile surfaces.
    ApprovalBypassStatus:
      properties:
        active:
          type: boolean
          title: Active
          description: Whether any bypass is currently in force
        auto_approve_active:
          type: boolean
          title: Auto Approve Active
          description: Whether any active bypass is auto-approving (unsupervised)
        bypasses:
          items:
            $ref: '#/components/schemas/ApprovalBypassResponse'
          type: array
          title: Bypasses
        soonest_expiry:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Soonest Expiry
          description: Earliest expiry among active bypasses, for a countdown.
          readOnly: true
      type: object
      required:
      - active
      - auto_approve_active
      - soonest_expiry
      title: ApprovalBypassStatus
      description: 'Aggregate ''is anything bypassed right now?'' answer for banners.


        Console and mobile poll this to decide whether to show the persistent

        warning banner, so it is intentionally cheap and shaped for direct

        rendering.'
    ApprovalDecision:
      properties:
        approved:
          type: boolean
          title: Approved
          description: Whether the request is approved or declined
        comment:
          anyOf:
          - type: string
          - type: 'null'
          title: Comment
          description: Comment from the approver
        selected_option:
          anyOf:
          - type: string
          - type: 'null'
          title: Selected Option
          description: 'For ask_user questions: the option the user chose'
        answer_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Answer Text
          description: 'For ask_user questions: a free-text answer the user typed'
        answer:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Answer
          description: 'For questions carrying an input_schema: the filled form, as
            JSON. Validated against that schema server-side; x-autofill fields (author,
            date) are stamped by the platform and anything sent for them is ignored.'
      type: object
      required:
      - approved
      title: ApprovalDecision
      description: Schema for an approval decision or a question answer.
    ApprovalDecisionRequest:
      properties:
        reason:
          type: string
          maxLength: 2000
          minLength: 1
          title: Reason
      additionalProperties: false
      type: object
      required:
      - reason
      title: ApprovalDecisionRequest
      description: Human decision. Agent-asserted approval in result JSON is ignored.
    ApprovalEventResponse:
      properties:
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
          description: Event ID
        event_type:
          type: string
          title: Event Type
          description: approval_requested, notification_sent, viewed, vote_received,
            escalation_triggered, approval_complete, expired, ...
        detail:
          type: string
          title: Detail
          description: Human-readable description of the event
        comment:
          anyOf:
          - type: string
          - type: 'null'
          title: Comment
          description: Approver comment attached to the event
        actor_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
          description: User who triggered the event (None for system/token)
        actor_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Email
          description: Resolved display identity of the actor, when known
        timestamp:
          type: string
          format: date-time
          title: Timestamp
          description: When the event occurred
      type: object
      required:
      - id
      - event_type
      - detail
      - timestamp
      title: ApprovalEventResponse
      description: One entry in an approval request's workflow-history timeline.
    ApprovalFlowExecutionSummary:
      properties:
        id:
          type: string
          title: Id
        flow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
      type: object
      required:
      - id
      title: ApprovalFlowExecutionSummary
      description: The flow run the gated call belonged to.
    ApprovalRequestResponse:
      properties:
        tool_name:
          type: string
          title: Tool Name
          description: Name of the tool being executed
        tool_args:
          additionalProperties: true
          type: object
          title: Tool Args
          description: Arguments passed to the tool
        agent_reasoning:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Reasoning
          description: Agent's reasoning for the tool call
        summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Summary
          description: User-facing plain-language ask (LLM or ask_user question)
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
          description: Flow execution ID (if applicable)
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
        tool_configuration_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Configuration Id
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
        status:
          type: string
          title: Status
        requested_at:
          type: string
          format: date-time
          title: Requested At
        resolved_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Resolved At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        approver_comment:
          anyOf:
          - type: string
          - type: 'null'
          title: Approver Comment
        structured_answer:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Structured Answer
        webhook_posted_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Webhook Posted At
        webhook_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Webhook Error
        managed_agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Managed Agent Id
        runtime_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runtime Session Id
        managed_agent_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Managed Agent Name
        api_key_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Api Key Id
        decided_by_ai:
          type: boolean
          title: Decided By Ai
          default: false
        ai_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model
        ai_confidence:
          anyOf:
          - type: number
          - type: 'null'
          title: Ai Confidence
        ai_reasoning:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Reasoning
        auto_approved_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Auto Approved Reason
        auto_approval_bypass_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Auto Approval Bypass Id
        rule_context:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Rule Context
        agent:
          anyOf:
          - $ref: '#/components/schemas/ApprovalAgentSummary'
          - type: 'null'
        api_key:
          anyOf:
          - $ref: '#/components/schemas/ApprovalApiKeySummary'
          - type: 'null'
        session:
          anyOf:
          - $ref: '#/components/schemas/ApprovalSessionSummary'
          - type: 'null'
        flow_execution:
          anyOf:
          - $ref: '#/components/schemas/ApprovalFlowExecutionSummary'
          - type: 'null'
        risk_level:
          type: string
          title: Risk Level
          description: 'danger for destructive/spend tools, otherwise low.


            Unknown is treated as danger so notification actions stay fail-closed.'
          readOnly: true
        was_bypassed:
          type: boolean
          title: Was Bypassed
          description: True when a bypass auto-approved this request without a human.
          readOnly: true
        decided_by_human:
          type: boolean
          title: Decided By Human
          description: 'True only when a person actually made this decision.


            The single field every statistic should filter on. A request is a human

            decision only if it reached a terminal decided state without an AI

            judging it and without a bypass skipping it.'
          readOnly: true
        approval_policy_id:
          type: string
          title: Approval Policy Id
          description: Alias for backward compatibility with older mobile app versions.
          readOnly: true
        is_question:
          type: boolean
          title: Is Question
          description: True when this request is an ask_user question, not a gate.
          readOnly: true
        question:
          anyOf:
          - type: string
          - type: 'null'
          title: Question
          description: The question text the agent is asking (ask_user only).
          readOnly: true
        question_options:
          items:
            type: string
          type: array
          title: Question Options
          description: Ordered answer options the agent offered (may be empty).
          readOnly: true
        allow_free_text:
          type: boolean
          title: Allow Free Text
          description: Whether the user may type a free-text answer (default True).
          readOnly: true
        question_items:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Question Items
          description: 'Rows the question is about, e.g. one per unwaived finding.


            Each row is {id, title, description?, severity?, badges?, href?}. The

            console renders them as a table with a checkbox per row, so a human

            picks findings instead of transcribing ids. Empty for every question

            that has no rows, which surfaces render as before.'
          readOnly: true
        question_schema:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Question Schema
          description: 'The answer form, in the subset documented in question_schema.py.


            None means there is no form and the legacy options + free-text path

            applies. Present on questions AND on approvals whose decision needs

            structured input (request_approval also accepts input_schema).'
          readOnly: true
        has_answer_form:
          type: boolean
          title: Has Answer Form
          description: True when this request must be decided through a form.
          readOnly: true
      type: object
      required:
      - tool_name
      - id
      - account_id
      - tool_configuration_id
      - approval_workflow_id
      - status
      - requested_at
      - resolved_at
      - expires_at
      - approver_comment
      - webhook_posted_at
      - webhook_error
      - risk_level
      - was_bypassed
      - decided_by_human
      - approval_policy_id
      - is_question
      - question
      - question_options
      - allow_free_text
      - question_items
      - question_schema
      - has_answer_form
      title: ApprovalRequestResponse
      description: Schema for approval request response.
    ApprovalSessionSummary:
      properties:
        id:
          type: string
          title: Id
        subject:
          anyOf:
          - type: string
          - type: 'null'
          title: Subject
          description: What the session is about (title or reference)
      type: object
      required:
      - id
      title: ApprovalSessionSummary
      description: The runtime session the gated call was made in.
    ApprovalWorkflowCreate:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description of what this workflow does
        approval_type:
          type: string
          title: Approval Type
          default: slack
        channel:
          anyOf:
          - type: string
          - type: 'null'
          title: Channel
          description: Channel for approval requests
        user:
          anyOf:
          - type: string
          - type: 'null'
          title: User
          description: Specific user to request approval from
        approval_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Approval Config
          description: Generic configuration for approval mechanism
        timeout_seconds:
          anyOf:
          - type: integer
          - type: 'null'
          title: Timeout Seconds
          description: 'How long to wait for approval (default: 5 minutes)'
          default: 300
        require_reason:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Require Reason
          description: Whether approver must provide a reason
          default: false
        async_approval_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Async Approval Enabled
          description: When enabled, tool calls return immediately and agents poll
            for status
          default: false
        is_default:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Default
          description: Whether this is the default workflow for the account
          default: false
        workflow_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Workflow Type
          description: 'Type of approval workflow: ''simple'', ''multi_stage'', ''consensus'''
          default: simple
        workflow_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Workflow Config
          description: Workflow configuration
        approver_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Approver User Ids
          description: List of user IDs who can approve (proprietary)
        approver_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Approver Team Ids
          description: List of team IDs whose members can approve (proprietary)
        approvals_required:
          anyOf:
          - type: integer
          - type: 'null'
          title: Approvals Required
          description: Number of approvals required (quorum) - proprietary
          default: 1
        escalation_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Escalation User Ids
          description: List of user IDs to escalate to on timeout (proprietary)
        escalation_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Escalation Team Ids
          description: List of team IDs to escalate to on timeout (proprietary)
        channel_configs:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Channel Configs
          description: Configuration for notification channels (Slack/Mattermost/webhook
            settings)
        approval_mode:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Mode
          description: 'Approval mode: ''standard'' (human) or ''ai_driven'' (AI makes
            decision)'
          default: standard
        ai_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model
          description: AI model for approval decisions (e.g., 'gpt-5.4', 'claude-sonnet-4.7')
        ai_guidelines:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Guidelines
          description: User-defined guidelines for AI approval decisions
        ai_context:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Ai Context
          description: Additional context for AI decisions (e.g., examples, constraints)
        ai_confidence_threshold:
          anyOf:
          - type: number
          - type: 'null'
          title: Ai Confidence Threshold
          description: Minimum confidence score required for AI to make a decision
          default: 0.8
        ai_fallback_behavior:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Fallback Behavior
          description: 'Behavior when AI confidence is below threshold: ''escalate'',
            ''approve'', ''deny'''
          default: escalate
        escalation_workflow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Escalation Workflow Id
          description: workflow to escalate to when fallback_behavior='escalate'
      type: object
      required:
      - name
      title: ApprovalWorkflowCreate
      description: Schema for creating approval workflow.
    ApprovalWorkflowResponse:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description of what this workflow does
        approval_type:
          type: string
          title: Approval Type
        channel:
          anyOf:
          - type: string
          - type: 'null'
          title: Channel
          description: Channel for approval requests
        user:
          anyOf:
          - type: string
          - type: 'null'
          title: User
          description: Specific user to request approval from
        approval_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Approval Config
          description: Generic configuration for approval mechanism
        timeout_seconds:
          anyOf:
          - type: integer
          - type: 'null'
          title: Timeout Seconds
          description: 'How long to wait for approval (default: 5 minutes)'
          default: 300
        require_reason:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Require Reason
          description: Whether approver must provide a reason
          default: false
        async_approval_enabled:
          type: boolean
          title: Async Approval Enabled
          default: false
        is_default:
          type: boolean
          title: Is Default
        workflow_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Workflow Type
          description: 'Type of approval workflow: ''simple'', ''multi_stage'', ''consensus'''
          default: simple
        workflow_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Workflow Config
          description: Workflow configuration
        approver_user_ids:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Approver User Ids
          description: List of user IDs who can approve (proprietary)
        approver_team_ids:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Approver Team Ids
          description: List of team IDs whose members can approve (proprietary)
        approvals_required:
          anyOf:
          - type: integer
          - type: 'null'
          title: Approvals Required
          description: Number of approvals required (quorum) - proprietary
          default: 1
        escalation_user_ids:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Escalation User Ids
          description: List of user IDs to escalate to on timeout (proprietary)
        escalation_team_ids:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Escalation Team Ids
          description: List of team IDs to escalate to on timeout (proprietary)
        channel_configs:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Channel Configs
          description: Configuration for notification channels (Slack/Mattermost/webhook
            settings)
        approval_mode:
          type: string
          title: Approval Mode
        ai_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model
        ai_guidelines:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Guidelines
        ai_context:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Ai Context
        ai_confidence_threshold:
          type: number
          title: Ai Confidence Threshold
        ai_fallback_behavior:
          type: string
          title: Ai Fallback Behavior
        escalation_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Escalation Workflow Id
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - name
      - approval_type
      - is_default
      - approval_mode
      - ai_confidence_threshold
      - ai_fallback_behavior
      - id
      - account_id
      - created_at
      - updated_at
      title: ApprovalWorkflowResponse
      description: Schema for approval workflow response.
    ApprovalWorkflowUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Human-readable name for the workflow
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description of what this workflow does
        approval_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Type
          description: 'Type of approval: ''slack'', ''mattermost'', ''webhook'',
            ''manual'''
          default: slack
        channel:
          anyOf:
          - type: string
          - type: 'null'
          title: Channel
          description: Channel for approval requests
        user:
          anyOf:
          - type: string
          - type: 'null'
          title: User
          description: Specific user to request approval from
        approval_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Approval Config
          description: Generic configuration for approval mechanism
        timeout_seconds:
          anyOf:
          - type: integer
          - type: 'null'
          title: Timeout Seconds
          description: 'How long to wait for approval (default: 5 minutes)'
          default: 300
        require_reason:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Require Reason
          description: Whether approver must provide a reason
          default: false
        async_approval_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Async Approval Enabled
          description: When enabled, tool calls return immediately and agents poll
            for status
          default: false
        is_default:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Default
          description: Whether this is the default workflow for the account
          default: false
        workflow_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Workflow Type
          description: 'Type of approval workflow: ''simple'', ''multi_stage'', ''consensus'''
          default: simple
        workflow_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Workflow Config
          description: Workflow configuration
        approver_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Approver User Ids
          description: List of user IDs who can approve (proprietary)
        approver_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Approver Team Ids
          description: List of team IDs whose members can approve (proprietary)
        approvals_required:
          anyOf:
          - type: integer
          - type: 'null'
          title: Approvals Required
          description: Number of approvals required (quorum) - proprietary
          default: 1
        escalation_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Escalation User Ids
          description: List of user IDs to escalate to on timeout (proprietary)
        escalation_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Escalation Team Ids
          description: List of team IDs to escalate to on timeout (proprietary)
        channel_configs:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Channel Configs
          description: Configuration for notification channels (Slack/Mattermost/webhook
            settings)
        approval_mode:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Mode
          description: 'Approval mode: ''standard'' (human) or ''ai_driven'' (AI makes
            decision)'
          default: standard
        ai_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model
          description: AI model for approval decisions (e.g., 'gpt-5.4', 'claude-sonnet-4.7')
        ai_guidelines:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Guidelines
          description: User-defined guidelines for AI approval decisions
        ai_context:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Ai Context
          description: Additional context for AI decisions (e.g., examples, constraints)
        ai_confidence_threshold:
          anyOf:
          - type: number
          - type: 'null'
          title: Ai Confidence Threshold
          description: Minimum confidence score required for AI to make a decision
          default: 0.8
        ai_fallback_behavior:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Fallback Behavior
          description: 'Behavior when AI confidence is below threshold: ''escalate'',
            ''approve'', ''deny'''
          default: escalate
        escalation_workflow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Escalation Workflow Id
          description: workflow to escalate to when fallback_behavior='escalate'
      type: object
      title: ApprovalWorkflowUpdate
      description: Schema for updating approval workflow.
    AttentionDismissalListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/AttentionDismissalResponse'
          type: array
          title: Items
        total:
          type: integer
          title: Total
          default: 0
      type: object
      title: AttentionDismissalListResponse
      description: Every dismissal in force for the account.
    AttentionDismissalResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        item_id:
          type: string
          title: Item Id
        fingerprint:
          type: string
          title: Fingerprint
        reason:
          type: string
          title: Reason
        snooze_until:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Snooze Until
        dismissed_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Dismissed By User Id
        dismissed_by_username:
          anyOf:
          - type: string
          - type: 'null'
          title: Dismissed By Username
        created_at:
          type: string
          format: date-time
          title: Created At
      type: object
      required:
      - id
      - item_id
      - fingerprint
      - reason
      - created_at
      title: AttentionDismissalResponse
      description: One dismissal that is still in force.
    AttentionDismissalUpsertRequest:
      properties:
        fingerprint:
          type: string
          maxLength: 4096
          title: Fingerprint
          description: Why the item is showing (latest failed run id, unpriced model
            aliases, agent onboarding and validation state). The item comes back when
            this changes.
        reason:
          type: string
          enum:
          - expected
          - snoozed
          - fixed
          title: Reason
          description: 'expected: this is how the account is configured. snoozed:
            hide it for snooze_days. fixed: it has been dealt with.'
        snooze_days:
          anyOf:
          - type: integer
            maximum: 90.0
            minimum: 1.0
          - type: 'null'
          title: Snooze Days
          description: Required for reason='snoozed'; ignored otherwise.
      type: object
      required:
      - fingerprint
      - reason
      title: AttentionDismissalUpsertRequest
      description: Body of ``PUT /attention/dismissals/{item_id}``.
    AuthUserCreate:
      properties:
        username:
          type: string
          maxLength: 50
          minLength: 3
          title: Username
        email:
          type: string
          format: email
          title: Email
        password:
          type: string
          maxLength: 72
          minLength: 8
          title: Password
        full_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Full Name
        bootstrap_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Bootstrap Token
          description: First-user setup token from the install (required while the
            instance has zero users and PRELOOP_BOOTSTRAP_TOKEN is set).
        plan_choice:
          anyOf:
          - type: string
            maxLength: 64
          - type: 'null'
          title: Plan Choice
          description: 'The plan this person already picked on the pricing page before
            they got here. Its presence, not its value, is what matters: it records
            that the choice has been made, so the console never asks again. Paid plans
            do not arrive this way (they go through checkout first), so in practice
            this is the free plan''s id.'
      type: object
      required:
      - username
      - email
      - password
      title: AuthUserCreate
      description: User creation model.
    AuthUserResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: The caller's user id. Clients compare it against an approval
            workflow's approver_user_ids to tell whether a pending approval is waiting
            for this person.
        account_id:
          type: string
          format: uuid
          title: Account Id
          description: The account this user belongs to.
        username:
          type: string
          title: Username
        email:
          type: string
          format: email
          title: Email
        full_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Full Name
        email_verified:
          type: boolean
          title: Email Verified
        is_superuser:
          type: boolean
          title: Is Superuser
          default: false
        permissions:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Permissions
        avatar_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Avatar Url
        avatar_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Avatar Source
        plan_choice_made:
          type: boolean
          title: Plan Choice Made
          description: Whether this person has already chosen a plan. False is the
            console's cue to ask the billing plugin whether to show the first-login
            plan choice; true means it never asks. It defaults to true so a client
            talking to an older server, or any caller reading a profile built without
            a user row, is never dragged into an onboarding step it cannot reason
            about.
          default: true
        team_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Team Ids
          description: Ids of the teams the caller belongs to inside account_id. Clients
            intersect these with an approval workflow's approver_team_ids to tell
            whether a pending approval is waiting for this person. Empty when the
            user is in no team.
      type: object
      required:
      - id
      - account_id
      - username
      - email
      - email_verified
      - team_ids
      title: AuthUserResponse
      description: Response model for user data.
    AuthUserUpdate:
      properties:
        full_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Full Name
      type: object
      title: AuthUserUpdate
      description: User update model.
    AuthenticationOptionsResponse:
      properties:
        options:
          additionalProperties: true
          type: object
          title: Options
        challenge_token:
          type: string
          title: Challenge Token
      type: object
      required:
      - options
      - challenge_token
      title: AuthenticationOptionsResponse
      description: Options for navigator.credentials.get plus the challenge token.
    AuthenticationVerifyRequest:
      properties:
        credential:
          additionalProperties: true
          type: object
          title: Credential
        challenge_token:
          type: string
          title: Challenge Token
      type: object
      required:
      - credential
      - challenge_token
      title: AuthenticationVerifyRequest
      description: Authenticator response for the authentication ceremony.
    AvailableModelsRequest:
      properties:
        api_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key
          description: Provider API key used to list models. Never logged and never
            persisted by this endpoint. When omitted, pass ai_model_id so the server
            can decrypt the stored key; the stored key is never returned to the client.
            A typed key always wins over the stored one.
        ai_model_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Ai Model Id
          description: Existing AI model id. When no typed api_key is sent, the server
            decrypts the stored credentials via CRUD and lists live. The plaintext
            key is never returned in the response.
        api_endpoint:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Endpoint
          description: Base URL of an OpenAI-compatible endpoint, e.g. https://openrouter.ai/api/v1.
            Required for the 'openai-compatible' and 'custom' providers, which have
            no fixed model catalog.
        model_kind:
          type: string
          enum:
          - llm
          - stt
          - tts
          title: Model Kind
          description: Model service kind to fetch
          default: llm
        aws_access_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Access Key Id
          description: AWS access key id for the bedrock provider. Never logged and
            never persisted by this endpoint.
        aws_secret_access_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Secret Access Key
          description: AWS secret access key for the bedrock provider. Never logged
            and never persisted by this endpoint.
        aws_session_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Session Token
          description: Optional temporary-session token for the bedrock provider.
            Never logged and never persisted by this endpoint.
        aws_region_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Region Name
          description: AWS region for the bedrock provider, e.g. us-east-1. Falls
            back to boto3's default region chain when omitted.
      type: object
      title: AvailableModelsRequest
      description: 'Discovery request for a provider''s model catalog.


        The API key is carried in the body rather than the query string on

        purpose: as a query parameter it was written to access logs in plaintext

        and leaked live provider keys.'
    AvailableModelsResponse:
      properties:
        models:
          items:
            type: string
          type: array
          title: Models
          description: Model identifiers to offer in the picker
        source:
          type: string
          enum:
          - live
          - fallback
          title: Source
          description: '''live'' when the provider''s listing endpoint answered; ''fallback''
            when an empty list was returned instead of a live catalog'
          default: fallback
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: Short safe reason for a fallback after a failed or impossible
            live attempt (fixed vocabulary, never raw provider error text). None for
            a clean live result.
      type: object
      title: AvailableModelsResponse
      description: 'A provider''s model listing plus the provenance of the result.


        ``source`` reports whether the ids came from the provider''s live listing

        endpoint ("live") or an empty fallback ("fallback"). There is no bundled

        picker catalog. ``error`` is a short machine-readable reason drawn from a

        fixed vocabulary (e.g. "timeout", "network", "empty_response",

        "unsupported", "missing_endpoint", "sdk_missing", "missing_key", "auth",

        "unknown", "subscription_oauth") when a live attempt failed or was

        impossible; it never carries raw exception text, which can embed endpoint

        URLs or key material. "subscription_oauth" marks a stored principal-bound

        subscription-OAuth credential (e.g. Claude Code): the server never

        queries the provider with such a token, and the models list is the

        account''s own catalog for the provider instead.'
    AvatarResponse:
      properties:
        avatar_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Avatar Url
        avatar_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Avatar Source
      type: object
      title: AvatarResponse
      description: Response after avatar upload or deletion.
    BaselineAcceptRequest:
      properties:
        audit_execution_id:
          type: string
          format: uuid
          title: Audit Execution Id
        evidence_ref:
          additionalProperties: true
          type: object
          title: Evidence Ref
      additionalProperties: false
      type: object
      required:
      - audit_execution_id
      title: BaselineAcceptRequest
      description: Record an initial or replacement baseline from a bound audit execution.
    BaselineAuditRequest:
      properties:
        sbom_content_base64:
          type: string
          minLength: 1
          title: Sbom Content Base64
      additionalProperties: false
      type: object
      required:
      - sbom_content_base64
      title: BaselineAuditRequest
      description: Authenticated source SBOM for the initial-baseline audit of one
        release.
    BatchExecutionListItem:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Alias of the model that served most of this execution's gateway
            requests. Null when the execution has no attributable gateway usage.
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
          description: Provider behind ``model_alias``, when recorded
        models_used:
          items:
            $ref: '#/components/schemas/ExecutionModelUsage'
          type: array
          title: Models Used
          description: Every distinct model alias this execution used with its request
            count, most used first.
        token_usage:
          anyOf:
          - $ref: '#/components/schemas/GatewayTokenUsage'
          - type: 'null'
          description: Tokens this execution's gateway traffic consumed, split by
            direction and cache participation. Null when the run has no attributable
            gateway usage, which is not the same as zero.
        id:
          type: string
          format: uuid
          title: Id
        flow_id:
          type: string
          format: uuid
          title: Flow Id
        status:
          type: string
          title: Status
        start_time:
          type: string
          format: date-time
          title: Start Time
        end_time:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Time
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        failure_category:
          anyOf:
          - type: string
          - type: 'null'
          title: Failure Category
          description: Coarse machine-readable failure class for terminal executions
            (runner_conflict, model_transient, agent_error, ...). Null when the execution
            succeeded, is still running, or predates the field.
        queued_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Queued Reason
          description: Why a PENDING execution has not been admitted yet (account_concurrency_cap).
            Null for every other row.
        retry_of_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Retry Of Execution Id
        parent_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Parent Execution Id
          description: Execution that directly started this run; null for a root.
        root_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Root Execution Id
          description: First execution of this lineage; null on the root itself.
        delegation_depth:
          type: integer
          title: Delegation Depth
          description: Distance from the lineage root; 0 for roots and existing rows.
          default: 0
        batch_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Batch Id
        tool_calls_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tool Calls Count
          default: 0
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
          default: 0
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
          default: 0.0
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        parked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Parked At
          description: When this execution was parked waiting for a human decision
            (status WAITING_FOR_HUMAN). Null for every other row.
        park_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Park Expires At
          description: When the pending approval window closes
        trigger_subject:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject
          description: 'Short human-readable description of what triggered this execution,
            e.g. ''preloop/preloop #78 · Pull Request Updated · 5167595c''. Null for
            executions created before subjects were recorded, or where no identifying
            detail could be derived.'
        trigger_subject_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject Url
          description: Link to the resource that triggered this execution (e.g. the
            pull request or merge request), when the trigger payload carries one.
        runner:
          $ref: '#/components/schemas/ExecutionRunnerSummary'
          description: Where this execution ran. List rows carry kind and name; detail
            adds id and pool.
        matrix:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Matrix
          description: Matrix cell overrides this execution was created with (index,
            agent_type, ai_model_id)
      type: object
      required:
      - id
      - flow_id
      - status
      - start_time
      - created_at
      - updated_at
      title: BatchExecutionListItem
      description: Execution row in a batch listing, annotated with its matrix cell.
    BatchExecutionsResponse:
      properties:
        batch_id:
          type: string
          format: uuid
          title: Batch Id
        flow_id:
          type: string
          format: uuid
          title: Flow Id
        rollup:
          $ref: '#/components/schemas/BatchRollup'
        executions:
          items:
            $ref: '#/components/schemas/BatchExecutionListItem'
          type: array
          title: Executions
      type: object
      required:
      - batch_id
      - flow_id
      - rollup
      - executions
      title: BatchExecutionsResponse
      description: Executions of one batch plus a status/cost/token rollup.
    BatchRollup:
      properties:
        total:
          type: integer
          title: Total
        by_status:
          additionalProperties:
            type: integer
          type: object
          title: By Status
        completed:
          type: integer
          title: Completed
          description: Executions in a terminal state (succeeded/failed/etc.)
          default: 0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        total_estimated_cost:
          type: number
          title: Total Estimated Cost
          default: 0.0
        total_tool_calls:
          type: integer
          title: Total Tool Calls
          default: 0
      type: object
      required:
      - total
      - by_status
      title: BatchRollup
      description: Aggregate metrics over the executions of one batch.
    Body_backfill_from_provider_ledger_csv_api_v1_cost_ledger_backfill_csv_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: 'OpenRouter Activity -> Explore daily export (columns: date__day,
            model, total_usage)'
        provider:
          type: string
          title: Provider
          default: openrouter
        start:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Start
          description: 'First UTC day (inclusive); default: CSV min.'
        end:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: End
          description: 'Last UTC day (inclusive); default: CSV max.'
        apply:
          type: boolean
          title: Apply
          description: Persist the allocation. Default is a dry run.
          default: false
      type: object
      required:
      - file
      title: Body_backfill_from_provider_ledger_csv_api_v1_cost_ledger_backfill_csv_post
    Body_diff_policy_api_v1_policies_diff_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: YAML or JSON policy file to compare
      type: object
      required:
      - file
      title: Body_diff_policy_api_v1_policies_diff_post
    Body_import_usage_csv_api_v1_usage_import_csv_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: Cursor dashboard Usage CSV export
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
        source:
          type: string
          title: Source
          default: cursor
        column_map:
          anyOf:
          - type: string
          - type: 'null'
          title: Column Map
          description: Optional JSON object mapping logical fields (date, model, cost,
            kind, max_mode, input_with_cache_write, input_without_cache_write, cache_read,
            output_tokens, total_tokens) to your export's exact column headers, for
            CSV shapes the built-in matcher does not recognize.
      type: object
      required:
      - file
      title: Body_import_usage_csv_api_v1_usage_import_csv_post
    Body_login_form_api_v1_auth_token_post:
      properties:
        grant_type:
          anyOf:
          - type: string
            pattern: ^password$
          - type: 'null'
          title: Grant Type
        username:
          type: string
          title: Username
        password:
          type: string
          format: password
          title: Password
        scope:
          type: string
          title: Scope
          default: ''
        client_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Id
        client_secret:
          anyOf:
          - type: string
          - type: 'null'
          format: password
          title: Client Secret
      type: object
      required:
      - username
      - password
      title: Body_login_form_api_v1_auth_token_post
    Body_transcribe_audio_api_v1_audio_transcriptions_post:
      properties:
        audio:
          type: string
          contentMediaType: application/octet-stream
          title: Audio
        ai_model_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Ai Model Id
      type: object
      required:
      - audio
      title: Body_transcribe_audio_api_v1_audio_transcriptions_post
    Body_upload_avatar_api_v1_users_me_avatar_put:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
      type: object
      required:
      - file
      title: Body_upload_avatar_api_v1_users_me_avatar_put
    Body_upload_policy_api_v1_policies_upload_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: YAML or JSON policy file to apply
        dry_run:
          type: boolean
          title: Dry Run
          description: If true, validate only without making changes
          default: false
        resolve_env:
          type: boolean
          title: Resolve Env
          description: If true, resolve ${VAR} environment variable references
          default: true
        skip_missing_servers:
          type: boolean
          title: Skip Missing Servers
          description: If true, skip tools that reference MCP servers not configured
            in your account instead of failing. Skipped tools are reported as warnings.
          default: false
      type: object
      required:
      - file
      title: Body_upload_policy_api_v1_policies_upload_post
    Body_validate_policy_api_v1_policies_validate_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: YAML or JSON policy file to validate
        check_server_references:
          type: boolean
          title: Check Server References
          description: If true, validate that MCP server references exist in your
            account. Set to false for standalone schema validation.
          default: true
      type: object
      required:
      - file
      title: Body_validate_policy_api_v1_policies_validate_post
    BrowserStepBatchIn:
      properties:
        steps:
          items:
            $ref: '#/components/schemas/BrowserStepIn'
          type: array
          maxItems: 200
          minItems: 1
          title: Steps
      type: object
      required:
      - steps
      title: BrowserStepBatchIn
      description: A batch of browser steps from one agent flush.
    BrowserStepBatchOut:
      properties:
        accepted:
          type: integer
          title: Accepted
        duplicates:
          type: integer
          title: Duplicates
        rejected:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Rejected
      type: object
      required:
      - accepted
      - duplicates
      - rejected
      title: BrowserStepBatchOut
      description: How many steps in a batch were stored, repeated, or refused.
    BrowserStepIn:
      properties:
        source:
          type: string
          enum:
          - api
          - browser_use
          - skyvern
          - playwright_mcp
          title: Source
          default: api
        source_step_id:
          type: string
          maxLength: 200
          minLength: 1
          title: Source Step Id
        step_index:
          type: integer
          minimum: 0.0
          title: Step Index
        action:
          type: string
          enum:
          - navigate
          - click
          - type
          - select
          - scroll
          - screenshot
          - extract
          - wait
          - done
          - other
          title: Action
        url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Url
        target:
          anyOf:
          - type: string
            maxLength: 1024
          - type: 'null'
          title: Target
        reasoning:
          anyOf:
          - type: string
            maxLength: 8000
          - type: 'null'
          title: Reasoning
        status:
          type: string
          enum:
          - success
          - failed
          - unknown
          title: Status
          default: success
        occurred_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Occurred At
        extra:
          additionalProperties: true
          type: object
          title: Extra
      type: object
      required:
      - source_step_id
      - step_index
      - action
      title: BrowserStepIn
      description: One browser action observed by an agent.
    BudgetPeriod:
      type: string
      enum:
      - hourly
      - daily
      - weekly
      - monthly
      - yearly
      - all_time
      title: BudgetPeriod
    BudgetPolicyCreate:
      properties:
        subject_type:
          type: string
          title: Subject Type
          description: 'Subject type: account, api_key, managed_agent, ai_model, or
            user'
        subject_id:
          anyOf:
          - type: string
            format: uuid
          - type: string
          - type: 'null'
          title: Subject Id
          description: UUID of the subject. None if subject_type is account
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Model alias to restrict. None for all models.
        period:
          $ref: '#/components/schemas/BudgetPeriod'
        hard_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Hard Limit Usd
          description: Hard limit in USD
        soft_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Soft Limit Usd
          description: Soft limit in USD
        notify_on_soft:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Soft
          default: false
        notify_on_hard:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Hard
          default: false
        notification_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification User Ids
          description: Users to notify using their notification preferences
        notification_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification Team Ids
          description: Teams to notify using member notification preferences
        notification_emails:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Notification Emails
          description: Additional custom email addresses to notify
      type: object
      required:
      - subject_type
      - period
      title: BudgetPolicyCreate
    BudgetPolicyResponse:
      properties:
        subject_type:
          type: string
          title: Subject Type
          description: 'Subject type: account, api_key, managed_agent, ai_model, or
            user'
        subject_id:
          anyOf:
          - type: string
            format: uuid
          - type: string
          - type: 'null'
          title: Subject Id
          description: UUID of the subject. None if subject_type is account
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Model alias to restrict. None for all models.
        period:
          $ref: '#/components/schemas/BudgetPeriod'
        hard_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Hard Limit Usd
          description: Hard limit in USD
        soft_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Soft Limit Usd
          description: Soft limit in USD
        notify_on_soft:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Soft
          default: false
        notify_on_hard:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Hard
          default: false
        notification_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification User Ids
          description: Users to notify using their notification preferences
        notification_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification Team Ids
          description: Teams to notify using member notification preferences
        notification_emails:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Notification Emails
          description: Additional custom email addresses to notify
        id:
          type: string
          format: uuid
          title: Id
        current_spend_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Current Spend Usd
        period_start:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period Start
        period_end:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Period End
      type: object
      required:
      - subject_type
      - period
      - id
      title: BudgetPolicyResponse
    BudgetPolicyUpdate:
      properties:
        hard_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Hard Limit Usd
        soft_limit_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Soft Limit Usd
        notify_on_soft:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Soft
        notify_on_hard:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify On Hard
        notification_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification User Ids
        notification_team_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Notification Team Ids
        notification_emails:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Notification Emails
      type: object
      title: BudgetPolicyUpdate
    CallableFlowEntry:
      properties:
        flow:
          type: string
          maxLength: 255
          minLength: 1
          title: Flow
          description: Slug or name of the flow that may be called, resolved inside
            the calling account. A reference that names no flow in the account is
            rejected on write.
        max_children:
          anyOf:
          - type: integer
          - type: 'null'
          title: Max Children
          description: Maximum number of children one execution of the calling flow
            may start through this entry. Unset means no per entry count ceiling.
        max_usd_per_child:
          anyOf:
          - type: number
          - type: 'null'
          title: Max Usd Per Child
          description: Maximum spend in USD for each child started through this entry.
            Unset means no per child cost ceiling.
        allow_self:
          type: boolean
          title: Allow Self
          description: Explicit opt in to a self reference. A flow calling itself
            is a recursion risk, so it has to be asked for by name.
          default: false
      additionalProperties: false
      type: object
      required:
      - flow
      title: CallableFlowEntry
      description: 'One delegation allowlist entry: a flow this flow may run, with
        ceilings.


        ``extra=''forbid''`` is deliberate. A misspelled key in an allowlist is a

        ceiling that silently does not apply, so an unknown key is rejected on

        write rather than stored and ignored.'
    ChainBreakRead:
      properties:
        kind:
          type: string
          title: Kind
        seq:
          type: integer
          title: Seq
        row_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Row Id
        detail:
          type: string
          title: Detail
      type: object
      required:
      - kind
      - seq
      - detail
      title: ChainBreakRead
      description: The first place the walk stopped agreeing with the chain.
    ChainCheckpointRead:
      properties:
        seq:
          type: integer
          title: Seq
        chain_hash:
          type: string
          title: Chain Hash
          description: row_hash of the row at this sequence
        row_count:
          type: integer
          title: Row Count
        checkpointed_at:
          type: string
          title: Checkpointed At
        signing_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Signing Key Id
        signature:
          anyOf:
          - type: string
          - type: 'null'
          title: Signature
          description: Base64 Ed25519 signature, null when signing failed
        signed_payload:
          additionalProperties: true
          type: object
          title: Signed Payload
          description: Canonical payload the digest was taken over
        digest:
          type: string
          title: Digest
        signature_document:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Signature Document
          description: Detached signature document, ready to verify
      type: object
      required:
      - seq
      - chain_hash
      - row_count
      - checkpointed_at
      - signed_payload
      - digest
      title: ChainCheckpointRead
      description: One signed checkpoint over the chain head at a sequence.
    ChainSegmentEntry:
      properties:
        seq:
          type: integer
          title: Seq
        row_id:
          type: string
          title: Row Id
        prev_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Prev Hash
        row_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Row Hash
        payload:
          additionalProperties: true
          type: object
          title: Payload
      type: object
      required:
      - seq
      - row_id
      - payload
      title: ChainSegmentEntry
      description: One row's chain material, enough to recompute its hash.
    ChainSegmentRead:
      properties:
        account_id:
          type: string
          title: Account Id
        row_domain:
          type: string
          title: Row Domain
        after_seq:
          type: integer
          title: After Seq
        head_seq:
          type: integer
          title: Head Seq
        pruned_below_seq:
          type: integer
          title: Pruned Below Seq
        genesis_hash:
          type: string
          title: Genesis Hash
        entries:
          items:
            $ref: '#/components/schemas/ChainSegmentEntry'
          type: array
          title: Entries
        has_more:
          type: boolean
          title: Has More
        note:
          type: string
          title: Note
      type: object
      required:
      - account_id
      - row_domain
      - after_seq
      - head_seq
      - pruned_below_seq
      - genesis_hash
      - entries
      - has_more
      - note
      title: ChainSegmentRead
      description: Material for a client side walk. The client decides, not the server.
    ChainStatusRead:
      properties:
        enabled:
          type: boolean
          title: Enabled
        head_seq:
          type: integer
          title: Head Seq
        head_hash:
          type: string
          title: Head Hash
        last_sealed_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Sealed At
        pruned_below_seq:
          type: integer
          title: Pruned Below Seq
          description: Rows below this sequence were removed by the retention purge.
            Their absence is policy, not tampering.
        sealed_rows:
          type: integer
          title: Sealed Rows
        unsealed_rows:
          type: integer
          title: Unsealed Rows
          description: Written but not yet chained; sealing is a background pass
        seal_lag_seconds:
          type: integer
          title: Seal Lag Seconds
        checkpoint_interval:
          type: integer
          title: Checkpoint Interval
        latest_checkpoint:
          anyOf:
          - $ref: '#/components/schemas/ChainCheckpointRead'
          - type: 'null'
        active_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Active Key Id
      type: object
      required:
      - enabled
      - head_seq
      - head_hash
      - pruned_below_seq
      - sealed_rows
      - unsealed_rows
      - seal_lag_seconds
      - checkpoint_interval
      title: ChainStatusRead
      description: Where this account's chain is, and how far behind sealing runs.
    ChainVerifyRead:
      properties:
        account_id:
          type: string
          title: Account Id
        status:
          type: string
          title: Status
          description: ok, broken or empty
        checked_rows:
          type: integer
          title: Checked Rows
        start_seq:
          type: integer
          title: Start Seq
        end_seq:
          type: integer
          title: End Seq
        head_seq:
          type: integer
          title: Head Seq
        pruned_below_seq:
          type: integer
          title: Pruned Below Seq
        unsealed_rows:
          type: integer
          title: Unsealed Rows
        truncated:
          type: boolean
          title: Truncated
          description: True when the range was cut to the row limit
        out_of_order_rows:
          type: integer
          title: Out Of Order Rows
          description: Rows whose timestamp precedes the row before them. Concurrency,
            not evidence of tampering on its own.
        first_break:
          anyOf:
          - $ref: '#/components/schemas/ChainBreakRead'
          - type: 'null'
        checkpoints_verified:
          type: integer
          title: Checkpoints Verified
        checkpoint_failures:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Checkpoint Failures
      type: object
      required:
      - account_id
      - status
      - checked_rows
      - start_seq
      - end_seq
      - head_seq
      - pruned_below_seq
      - unsealed_rows
      - truncated
      - out_of_order_rows
      - checkpoints_verified
      title: ChainVerifyRead
      description: Verdict of a server side walk over a range of the chain.
    CommentCreate:
      properties:
        body:
          type: string
          title: Body
          description: Comment text
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional comment metadata
      type: object
      required:
      - body
      title: CommentCreate
      description: Model for creating a new comment.
    CommentList:
      properties:
        items:
          items:
            $ref: '#/components/schemas/CommentResponse'
          type: array
          title: Items
        total:
          type: integer
          title: Total
        limit:
          type: integer
          title: Limit
        offset:
          type: integer
          title: Offset
      type: object
      required:
      - items
      - total
      - limit
      - offset
      title: CommentList
      description: Response model for a list of comments with pagination details.
    CommentResponse:
      properties:
        body:
          type: string
          title: Body
          description: Comment text
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional comment metadata
        id:
          type: string
          title: Id
          description: Comment unique identifier
        issue_id:
          type: string
          title: Issue Id
          description: Issue ID this comment belongs to
        author:
          anyOf:
          - type: string
          - type: 'null'
          title: Author
          description: Comment author
        created_at:
          type: string
          title: Created At
          description: Creation timestamp
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
          description: Last update timestamp
        score:
          anyOf:
          - type: number
          - type: 'null'
          title: Score
          description: Similarity score for search results (if applicable)
      type: object
      required:
      - body
      - id
      - issue_id
      - created_at
      title: CommentResponse
      description: Response model for comment data.
    CommentSearchResults:
      properties:
        items:
          items:
            $ref: '#/components/schemas/CommentResponse'
          type: array
          title: Items
          description: Search result items
        total:
          type: integer
          title: Total
          description: Total number of matching comments
        query:
          type: string
          title: Query
          description: Search query used
      type: object
      required:
      - items
      - total
      - query
      title: CommentSearchResults
      description: Response model for comment search results.
    ConditionAction:
      type: string
      enum:
      - require_approval
      - deny
      - allow
      title: ConditionAction
      description: Actions to take when a condition matches.
    ConditionTestRequest:
      properties:
        expression:
          type: string
          title: Expression
          description: CEL expression to test
        sample_args:
          additionalProperties: true
          type: object
          title: Sample Args
          description: Sample tool arguments for testing
      type: object
      required:
      - expression
      - sample_args
      title: ConditionTestRequest
      description: Schema for testing a condition expression.
    ConditionTestResponse:
      properties:
        matches:
          type: boolean
          title: Matches
          description: Whether the condition matched
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: Error message if evaluation failed
        evaluation_context:
          additionalProperties: true
          type: object
          title: Evaluation Context
          description: Context used for evaluation
      type: object
      required:
      - matches
      - evaluation_context
      title: ConditionTestResponse
      description: Schema for condition test result.
    ConditionType:
      type: string
      enum:
      - simple
      - cel
      title: ConditionType
      description: "Type of condition expression.\n\nThis enum supports the open core\
        \ licensing model:\n\n- SIMPLE (open source): Basic comparisons using Python-like\
        \ syntax.\n  Supports operators: ==, !=, >, <, >=, <=\n  Examples:\n    -\
        \ \"args.amount > 500\"\n    - \"args.recipient == 'bob'\"\n    - \"args.priority\
        \ != 'low'\"\n\n- CEL (enterprise): Full CEL (Common Expression Language)\
        \ expressions\n  with advanced functions and capabilities.\n  Examples:\n\
        \    - \"args.command.contains('rm -rf')\"\n    - \"args.path.startsWith('/etc/')\"\
        \n    - \"args.tags.exists(t, t == 'production')\"\n    - \"args.amount >\
        \ 1000 && args.approved == false\""
    ContinuationAdoptRequest:
      properties:
        recovery_mode:
          type: string
          enum:
          - native_resume
          - published_branch_handoff
          title: Recovery Mode
        expected_head_sha:
          type: string
          pattern: ^[0-9a-fA-F]{40,64}$
          title: Expected Head Sha
        acknowledge_fresh_conversation:
          type: boolean
          title: Acknowledge Fresh Conversation
          default: false
        pr_url:
          anyOf:
          - type: string
            maxLength: 2048
            minLength: 1
          - type: 'null'
          title: Pr Url
        branch:
          anyOf:
          - type: string
            maxLength: 255
            minLength: 1
          - type: 'null'
          title: Branch
      type: object
      required:
      - recovery_mode
      - expected_head_sha
      title: ContinuationAdoptRequest
    ContinuationAdoptResponse:
      properties:
        thread_id:
          type: string
          format: uuid
          title: Thread Id
        state:
          type: string
          title: State
        pr_url:
          type: string
          title: Pr Url
        recovery_mode:
          type: string
          enum:
          - native_resume
          - published_branch_handoff
          title: Recovery Mode
      type: object
      required:
      - thread_id
      - state
      - pr_url
      - recovery_mode
      title: ContinuationAdoptResponse
    ContinuationPreview:
      properties:
        execution_id:
          type: string
          format: uuid
          title: Execution Id
        flow_id:
          type: string
          format: uuid
          title: Flow Id
        pr_url:
          type: string
          title: Pr Url
        branch:
          type: string
          title: Branch
        head_sha:
          type: string
          title: Head Sha
        feedback_enabled:
          type: boolean
          title: Feedback Enabled
        artifact_upload_enabled:
          type: boolean
          title: Artifact Upload Enabled
        feedback_readable:
          type: boolean
          title: Feedback Readable
          default: false
        feedback_blocked_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Feedback Blocked Reason
        native_resume_available:
          type: boolean
          title: Native Resume Available
        native_resume_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Native Resume Expires At
        existing_thread_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Existing Thread Id
        existing_thread_state:
          anyOf:
          - type: string
          - type: 'null'
          title: Existing Thread State
        allowed_recovery_modes:
          items:
            type: string
            enum:
            - native_resume
            - published_branch_handoff
          type: array
          title: Allowed Recovery Modes
        warnings:
          items:
            type: string
          type: array
          title: Warnings
      type: object
      required:
      - execution_id
      - flow_id
      - pr_url
      - branch
      - head_sha
      - feedback_enabled
      - artifact_upload_enabled
      - native_resume_available
      - allowed_recovery_modes
      - warnings
      title: ContinuationPreview
    CostAnalyticsSummaryResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        unpriced_requests:
          type: integer
          title: Unpriced Requests
          default: 0
        unpriced_tokens:
          type: integer
          title: Unpriced Tokens
          default: 0
        unpriced_models:
          items:
            $ref: '#/components/schemas/UnpricedModelUsage'
          type: array
          title: Unpriced Models
        price_catalog:
          anyOf:
          - $ref: '#/components/schemas/PriceCatalogInfo'
          - type: 'null'
        budget:
          $ref: '#/components/schemas/GatewayBudgetSummary'
        requests_by_day:
          items:
            $ref: '#/components/schemas/GatewayUsageByDay'
          type: array
          title: Requests By Day
        usage_by_model:
          items:
            $ref: '#/components/schemas/GatewayUsageByModel'
          type: array
          title: Usage By Model
        usage_by_flow:
          items:
            $ref: '#/components/schemas/GatewayUsageByFlow'
          type: array
          title: Usage By Flow
        usage_by_session:
          items:
            $ref: '#/components/schemas/GatewayUsageBySession'
          type: array
          title: Usage By Session
        usage_by_tool:
          items:
            $ref: '#/components/schemas/GatewayUsageByTool'
          type: array
          title: Usage By Tool
        imported_usage:
          anyOf:
          - $ref: '#/components/schemas/ImportedUsageSummary'
          - type: 'null'
      type: object
      required:
      - period_start
      - period_end
      - token_usage
      - budget
      title: CostAnalyticsSummaryResponse
      description: Open-source cost overview response.
    CostHealthCheck:
      properties:
        key:
          type: string
          title: Key
        status:
          type: string
          pattern: ^(pass|fail|skip)$
          title: Status
        detail:
          type: string
          title: Detail
      type: object
      required:
      - key
      - status
      - detail
      title: CostHealthCheck
      description: One entry in the gateway accounting self-check checklist.
    CostHealthResponse:
      properties:
        window_hours:
          type: integer
          title: Window Hours
        checks:
          items:
            $ref: '#/components/schemas/CostHealthCheck'
          type: array
          title: Checks
        status:
          type: string
          pattern: ^(pass|fail|skip)$
          title: Status
      type: object
      required:
      - window_hours
      - status
      title: CostHealthResponse
      description: Account-scoped gateway accounting self-check result.
    CreateVersionRequest:
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Description of the version
        tag:
          anyOf:
          - type: string
          - type: 'null'
          title: Tag
          description: Tag for the version (e.g., 'production')
      type: object
      title: CreateVersionRequest
      description: Request to create a new policy version.
    Criterion:
      properties:
        id:
          type: string
          maxLength: 80
          minLength: 1
          title: Id
        outcome:
          type: string
          maxLength: 2000
          minLength: 1
          title: Outcome
        verification:
          type: string
          maxLength: 2000
          minLength: 1
          title: Verification
        deployment_required:
          type: boolean
          title: Deployment Required
          default: false
      type: object
      required:
      - id
      - outcome
      - verification
      title: Criterion
      description: Observable acceptance, independently addressable by evidence.
    CronSchedule:
      properties:
        timezone:
          type: string
          title: Timezone
          description: IANA timezone name the schedule is evaluated in
          default: UTC
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Static trigger payload merged into every scheduled run, for
            options a schedule has no other way to state (e.g. previous_result_execution_id).
            Bounded and inline only.
        type:
          type: string
          const: cron
          title: Type
          default: cron
        expr:
          type: string
          title: Expr
          description: 5-field crontab expression (minute hour day month day_of_week)
      type: object
      required:
      - expr
      title: CronSchedule
      description: 'Advanced schedule form: a raw 5-field crontab expression.'
    CustomCommands:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether custom commands are enabled
          default: false
        commands:
          items:
            type: string
          type: array
          title: Commands
          description: List of shell commands to execute before agent starts
      type: object
      title: CustomCommands
      description: Configuration for custom commands (admin-only).
    DailySchedule:
      properties:
        timezone:
          type: string
          title: Timezone
          description: IANA timezone name the schedule is evaluated in
          default: UTC
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Static trigger payload merged into every scheduled run, for
            options a schedule has no other way to state (e.g. previous_result_execution_id).
            Bounded and inline only.
        type:
          type: string
          const: daily
          title: Type
          default: daily
        at:
          type: string
          title: At
          description: Time of day in 24h 'HH:MM' format
      type: object
      required:
      - at
      title: DailySchedule
      description: 'Friendly schedule form: run once a day at a fixed local time.'
    DashboardTelemetryResponse:
      properties:
        active_agents:
          type: integer
          title: Active Agents
          default: 0
        total_tool_calls:
          type: integer
          title: Total Tool Calls
          default: 0
        daily_cost:
          type: number
          title: Daily Cost
          default: 0.0
        success_rate:
          type: number
          title: Success Rate
          default: 0.0
      type: object
      title: DashboardTelemetryResponse
      description: Aggregate high-level metrics for the global dashboard control plane.
    DeploymentVerificationRequest:
      properties:
        merge_sha:
          type: string
          title: Merge Sha
        target:
          type: string
          title: Target
        deployed_revision:
          type: string
          title: Deployed Revision
        deployment_evidence:
          type: string
          title: Deployment Evidence
      type: object
      required:
      - merge_sha
      - target
      - deployed_revision
      - deployment_evidence
      title: DeploymentVerificationRequest
      description: An authenticated deployment record, never inferred from issue closure.
    DetectorTimeoutFailMode:
      type: string
      enum:
      - allow
      - deny
      title: DetectorTimeoutFailMode
      description: 'Fail mode when a detector exceeds its hard timeout.


        Default is ``deny`` (fail closed). Set ``allow`` to skip the timed-out

        rule and continue evaluation.'
    EffectivePublicationPolicy:
      properties:
        mode:
          type: string
          enum:
          - disabled
          - ungated
          - sandbox_gated
          - isolated
          - blocked
          title: Mode
        reason:
          type: string
          title: Reason
        verification:
          anyOf:
          - $ref: '#/components/schemas/ResolvedVerificationPolicy'
          - type: 'null'
        configured_check_ids:
          items:
            type: string
          type: array
          title: Configured Check Ids
        blockers:
          items:
            type: string
          type: array
          title: Blockers
        runtime_validation_required:
          type: boolean
          title: Runtime Validation Required
          default: false
      type: object
      required:
      - mode
      - reason
      title: EffectivePublicationPolicy
      description: Saved configuration summary, never an attestation that checks passed.
    EmailVerificationRequest:
      properties:
        token:
          type: string
          title: Token
      type: object
      required:
      - token
      title: EmailVerificationRequest
      description: Model for email verification requests.
    EmailVerificationResendRequest:
      properties:
        email:
          type: string
          format: email
          title: Email
      type: object
      required:
      - email
      title: EmailVerificationResendRequest
      description: Model for asking for another verification email.
    EmbeddingRawDataItem:
      properties:
        issue_id:
          type: string
          title: Issue Id
        project_id:
          type: string
          title: Project Id
        embedding:
          items:
            type: number
          type: array
          title: Embedding
        issue_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Issue Title
        issue_labels:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Issue Labels
        issue_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Issue Type
        issue_created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Issue Created At
      type: object
      required:
      - issue_id
      - project_id
      - embedding
      title: EmbeddingRawDataItem
      description: Individual data item in the raw embedding response.
    EmbeddingRawResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/EmbeddingRawDataItem'
          type: array
          title: Data
      type: object
      required:
      - data
      title: EmbeddingRawResponse
      description: Response model for the raw embeddings endpoint.
    ExecutionModelUsage:
      properties:
        model_alias:
          type: string
          title: Model Alias
          description: Gateway model alias, e.g. 'openai/gpt-5'
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
          description: Provider that served the requests, when recorded
        request_count:
          type: integer
          title: Request Count
          description: Gateway requests this execution sent to that alias
          default: 0
      type: object
      required:
      - model_alias
      title: ExecutionModelUsage
      description: One model alias that served requests during an execution.
    ExecutionPark:
      properties:
        request_id:
          type: string
          format: uuid
          title: Request Id
          description: The pending approval request
        since:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Since
          description: When the execution was parked
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
          description: When the approval window closes
        waiting_for:
          anyOf:
          - type: string
          - type: 'null'
          title: Waiting For
          description: 'Who the run is waiting on: the approval workflow''s name,
            which is the routing decision an operator actually made.'
        tool_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Name
          description: The gated call that raised the question
        question:
          anyOf:
          - type: string
          - type: 'null'
          title: Question
          description: The question text, for ask_user requests
      type: object
      required:
      - request_id
      title: ExecutionPark
      description: 'Why a ``WAITING_FOR_HUMAN`` execution is waiting, and until when.


        Projected onto the detail response so the console can render "waiting for

        <who> since <when>, expires <when>" without the page having to know how

        approvals are stored. A parked run holds no container and no runner: this

        is a governance object, not a spinner.'
    ExecutionRunner:
      properties:
        kind:
          type: string
          enum:
          - private
          - hosted
          - unknown
          title: Kind
          description: private when the run was leased to a self-hosted CLI runner;
            hosted when it used the built-in Preloop executor; unknown when no runtime
            assignment is recorded
        id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Id
          description: FlowRunner id when kind is private and a runner was assigned
        name:
          type: string
          title: Name
          description: Display name. "Preloop hosted" when kind is hosted.
        pool:
          anyOf:
          - type: string
          - type: 'null'
          title: Pool
          description: Resolved runner pool string, when known
      type: object
      required:
      - kind
      - name
      title: ExecutionRunner
      description: 'Where an execution ran: a private CLI runner or Preloop hosted.


        Derived at read time from ``FlowExecution.runner_id`` and

        ``agent_session_reference`` ``runner:...`` forms. An absent runtime

        assignment is unknown until an executor actually starts.'
    ExecutionRunnerSummary:
      properties:
        kind:
          type: string
          enum:
          - private
          - hosted
          - unknown
          title: Kind
          description: private for a CLI runner; hosted for built-in compute; unknown
            when unassigned
        name:
          type: string
          title: Name
          description: Display name. "Preloop hosted" when kind is hosted.
      type: object
      required:
      - kind
      - name
      title: ExecutionRunnerSummary
      description: 'List-row runner: kind and name only.'
    ExecutionTreeNode:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Alias of the model that served most of this execution's gateway
            requests. Null when the execution has no attributable gateway usage.
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
          description: Provider behind ``model_alias``, when recorded
        models_used:
          items:
            $ref: '#/components/schemas/ExecutionModelUsage'
          type: array
          title: Models Used
          description: Every distinct model alias this execution used with its request
            count, most used first.
        token_usage:
          anyOf:
          - $ref: '#/components/schemas/GatewayTokenUsage'
          - type: 'null'
          description: Tokens this execution's gateway traffic consumed, split by
            direction and cache participation. Null when the run has no attributable
            gateway usage, which is not the same as zero.
        id:
          type: string
          format: uuid
          title: Id
        flow_id:
          type: string
          format: uuid
          title: Flow Id
        status:
          type: string
          title: Status
        start_time:
          type: string
          format: date-time
          title: Start Time
        end_time:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Time
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        failure_category:
          anyOf:
          - type: string
          - type: 'null'
          title: Failure Category
          description: Coarse machine-readable failure class for terminal executions
            (runner_conflict, model_transient, agent_error, ...). Null when the execution
            succeeded, is still running, or predates the field.
        queued_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Queued Reason
          description: Why a PENDING execution has not been admitted yet (account_concurrency_cap).
            Null for every other row.
        retry_of_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Retry Of Execution Id
        parent_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Parent Execution Id
          description: Execution that directly started this run; null for a root.
        root_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Root Execution Id
          description: First execution of this lineage; null on the root itself.
        delegation_depth:
          type: integer
          title: Delegation Depth
          description: Distance from the lineage root; 0 for roots and existing rows.
          default: 0
        batch_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Batch Id
        tool_calls_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tool Calls Count
          default: 0
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
          default: 0
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
          default: 0.0
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        parked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Parked At
          description: When this execution was parked waiting for a human decision
            (status WAITING_FOR_HUMAN). Null for every other row.
        park_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Park Expires At
          description: When the pending approval window closes
        trigger_subject:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject
          description: 'Short human-readable description of what triggered this execution,
            e.g. ''preloop/preloop #78 · Pull Request Updated · 5167595c''. Null for
            executions created before subjects were recorded, or where no identifying
            detail could be derived.'
        trigger_subject_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject Url
          description: Link to the resource that triggered this execution (e.g. the
            pull request or merge request), when the trigger payload carries one.
        runner:
          $ref: '#/components/schemas/ExecutionRunnerSummary'
          description: Where this execution ran. List rows carry kind and name; detail
            adds id and pool.
        label:
          anyOf:
          - type: string
          - type: 'null'
          title: Label
          description: Label the caller passed to run_flow for this child, e.g. 'lint
            the diff'. Null on a run that was not delegated and on a delegation that
            passed no label.
        stop_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Stop Reason
          description: 'Why this execution was stopped, when something other than
            the run itself ended it: an account kill switch, or the stop of the execution
            that started it. Null for every row nobody stopped, which lets the tree
            show which children a parent''s stop ended and which had finished on their
            own.'
      type: object
      required:
      - id
      - flow_id
      - status
      - start_time
      - created_at
      - updated_at
      title: ExecutionTreeNode
      description: 'One execution in a delegation tree, as the console draws the row.


        A list row plus the two things only a tree knows: who started it

        (``parent_execution_id``, already on the list row) and what it was asked

        to do (``label``).'
    ExecutionTreeResponse:
      properties:
        execution_id:
          type: string
          format: uuid
          title: Execution Id
          description: The execution asked about
        root_execution_id:
          type: string
          format: uuid
          title: Root Execution Id
          description: Root of the lineage this execution belongs to; the execution's
            own id when it is the root.
        execution:
          $ref: '#/components/schemas/ExecutionTreeNode'
          description: The execution asked about, with its own cost and tokens
        executions:
          items:
            $ref: '#/components/schemas/ExecutionTreeNode'
          type: array
          title: Executions
          description: Every descendant of this execution, parents before children.
            Empty for the overwhelming majority of runs, which delegate nothing.
        rollup:
          $ref: '#/components/schemas/BatchRollup'
          description: Status counts, tokens, tool calls and estimated cost summed
            over the descendants, in the same shape the batch listing uses. Excludes
            the execution itself.
        truncated:
          type: boolean
          title: Truncated
          description: True when the lineage holds more executions than one read returns,
            so the tree and the rollup are partial.
          default: false
      type: object
      required:
      - execution_id
      - root_execution_id
      - execution
      - rollup
      title: ExecutionTreeResponse
      description: 'One execution''s subtree plus the rollup over it.


        ``execution`` is the run that was asked about and its rollup fields are

        its own; ``rollup`` covers the descendants only. The two are deliberately

        never added together: "this run cost X, the work it delegated cost Y" is

        the question the tree exists to answer, and one number would hide it.'
    FlowCreate:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        icon:
          anyOf:
          - type: string
          - type: 'null'
          title: Icon
        trigger_event_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Event Source
        trigger_event_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Trigger Event Types
        trigger_organization_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Trigger Organization Id
        trigger_project_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Trigger Project Ids
        trigger_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Trigger Config
        webhook_config:
          anyOf:
          - $ref: '#/components/schemas/WebhookConfig'
          - type: 'null'
        schedule_config:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/CronSchedule'
            - $ref: '#/components/schemas/IntervalSchedule'
            - $ref: '#/components/schemas/DailySchedule'
            - $ref: '#/components/schemas/WeeklySchedule'
            discriminator:
              propertyName: type
              mapping:
                cron: '#/components/schemas/CronSchedule'
                daily: '#/components/schemas/DailySchedule'
                interval: '#/components/schemas/IntervalSchedule'
                weekly: '#/components/schemas/WeeklySchedule'
          - type: 'null'
          title: Schedule Config
        prompt_template:
          type: string
          title: Prompt Template
        ai_model_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Ai Model Id
        agent_type:
          type: string
          title: Agent Type
          default: openhands
        agent_config:
          additionalProperties: true
          type: object
          title: Agent Config
        allowed_mcp_servers:
          items:
            type: string
          type: array
          title: Allowed Mcp Servers
          default: []
        allowed_mcp_tools:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Allowed Mcp Tools
          default: []
        callable_flows:
          anyOf:
          - items:
              $ref: '#/components/schemas/CallableFlowEntry'
            type: array
          - type: 'null'
          title: Callable Flows
          description: 'Delegation allowlist: the flows an execution of this flow
            is permitted to run, each with optional per child ceilings. Unset or an
            empty list means no delegation, which is the default for every flow. Nothing
            enforces the list yet.'
        git_clone_config:
          anyOf:
          - $ref: '#/components/schemas/GitCloneConfig-Input'
          - type: 'null'
        custom_commands:
          anyOf:
          - $ref: '#/components/schemas/CustomCommands'
          - type: 'null'
        is_preset:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Preset
          default: false
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
          default: true
        account_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Account Id
        source_preset_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Source Preset Id
        source_prompt_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Prompt Hash
        source_tools_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Tools Hash
        prompt_customized:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Prompt Customized
          default: false
        tools_customized:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Tools Customized
          default: false
        preset_update_available:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Preset Update Available
          default: false
        runner_pool:
          anyOf:
          - type: string
          - type: 'null'
          title: Runner Pool
          description: Runner pool for executions of this flow. Accepts a runner id,
            name, or label; the literal 'auto' for any online private runner; or the
            literal 'server' for the hosted executor. When unset, the account default_runner_pool
            applies, then any online private runner, then the hosted executor. A trigger-time
            `--runner` / `_runner` override takes precedence. If no runner in a chosen
            private pool has a free slot the job queues for 15 minutes then FAILS.
        timeout_seconds:
          anyOf:
          - type: integer
            maximum: 86400.0
            minimum: 60.0
          - type: 'null'
          title: Timeout Seconds
          description: Wall-clock budget for one execution of this flow, in seconds.
            Leave unset to use the deployment default (3600). A run that exceeds the
            budget is stopped and fails with the timeout category, and the failure
            message names the budget that expired.
        approval_window_seconds:
          anyOf:
          - type: integer
            maximum: 2592000.0
            minimum: 60.0
          - type: 'null'
          title: Approval Window Seconds
          description: 'How long a human has to answer an approval or question raised
            by an execution of this flow, in seconds (60s to 30 days). Leave unset
            to use the approval workflow''s timeout, then the deployment default (300s).
            While the request is outstanding the execution is parked (WAITING_FOR_HUMAN):
            it holds no container and no runner, and the decision resumes it, so a
            window measured in days costs calendar time only.'
        notifications:
          anyOf:
          - $ref: '#/components/schemas/FlowNotifications'
          - type: 'null'
          description: When to comment on the triggering issue and raise a console
            attention item after a terminal execution. Leave unset for no notifications.
      type: object
      required:
      - name
      - prompt_template
      - agent_config
      title: FlowCreate
    FlowExecutionCommand:
      properties:
        command:
          type: string
          title: Command
          description: Command to send (e.g., 'stop', 'pause')
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Optional payload for the command
      type: object
      required:
      - command
      title: FlowExecutionCommand
    FlowExecutionListResponse:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Alias of the model that served most of this execution's gateway
            requests. Null when the execution has no attributable gateway usage.
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
          description: Provider behind ``model_alias``, when recorded
        models_used:
          items:
            $ref: '#/components/schemas/ExecutionModelUsage'
          type: array
          title: Models Used
          description: Every distinct model alias this execution used with its request
            count, most used first.
        token_usage:
          anyOf:
          - $ref: '#/components/schemas/GatewayTokenUsage'
          - type: 'null'
          description: Tokens this execution's gateway traffic consumed, split by
            direction and cache participation. Null when the run has no attributable
            gateway usage, which is not the same as zero.
        id:
          type: string
          format: uuid
          title: Id
        flow_id:
          type: string
          format: uuid
          title: Flow Id
        status:
          type: string
          title: Status
        start_time:
          type: string
          format: date-time
          title: Start Time
        end_time:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Time
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        failure_category:
          anyOf:
          - type: string
          - type: 'null'
          title: Failure Category
          description: Coarse machine-readable failure class for terminal executions
            (runner_conflict, model_transient, agent_error, ...). Null when the execution
            succeeded, is still running, or predates the field.
        queued_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Queued Reason
          description: Why a PENDING execution has not been admitted yet (account_concurrency_cap).
            Null for every other row.
        retry_of_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Retry Of Execution Id
        parent_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Parent Execution Id
          description: Execution that directly started this run; null for a root.
        root_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Root Execution Id
          description: First execution of this lineage; null on the root itself.
        delegation_depth:
          type: integer
          title: Delegation Depth
          description: Distance from the lineage root; 0 for roots and existing rows.
          default: 0
        batch_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Batch Id
        tool_calls_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tool Calls Count
          default: 0
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
          default: 0
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
          default: 0.0
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        parked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Parked At
          description: When this execution was parked waiting for a human decision
            (status WAITING_FOR_HUMAN). Null for every other row.
        park_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Park Expires At
          description: When the pending approval window closes
        trigger_subject:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject
          description: 'Short human-readable description of what triggered this execution,
            e.g. ''preloop/preloop #78 · Pull Request Updated · 5167595c''. Null for
            executions created before subjects were recorded, or where no identifying
            detail could be derived.'
        trigger_subject_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Subject Url
          description: Link to the resource that triggered this execution (e.g. the
            pull request or merge request), when the trigger payload carries one.
        runner:
          $ref: '#/components/schemas/ExecutionRunnerSummary'
          description: Where this execution ran. List rows carry kind and name; detail
            adds id and pool.
      type: object
      required:
      - id
      - flow_id
      - status
      - start_time
      - created_at
      - updated_at
      title: FlowExecutionListResponse
      description: Lightweight flow execution row for list views.
    FlowExecutionResponse:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
          description: Alias of the model that served most of this execution's gateway
            requests. Null when the execution has no attributable gateway usage.
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
          description: Provider behind ``model_alias``, when recorded
        models_used:
          items:
            $ref: '#/components/schemas/ExecutionModelUsage'
          type: array
          title: Models Used
          description: Every distinct model alias this execution used with its request
            count, most used first.
        token_usage:
          anyOf:
          - $ref: '#/components/schemas/GatewayTokenUsage'
          - type: 'null'
          description: Tokens this execution's gateway traffic consumed, split by
            direction and cache participation. Null when the run has no attributable
            gateway usage, which is not the same as zero.
        flow_id:
          type: string
          format: uuid
          title: Flow Id
          description: Foreign Key to Flows.id
        trigger_event_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Event Id
          description: Identifier for the specific event that triggered this execution
        trigger_event_details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Trigger Event Details
          description: A snapshot of the payload of the triggering event
        status:
          type: string
          title: Status
          description: Status of the execution (e.g., PENDING, RUNNING, SUCCEEDED,
            FAILED)
          default: PENDING
        start_time:
          type: string
          format: date-time
          title: Start Time
          description: Timestamp when the execution started
        end_time:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Time
          description: Timestamp when the execution ended
        resolved_input_prompt:
          anyOf:
          - type: string
          - type: 'null'
          title: Resolved Input Prompt
          description: The full prompt after placeholder resolution
        model_output_summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Output Summary
          description: A concise summary of the AI model's final output or key findings
        actions_taken_summary:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Actions Taken Summary
          description: A structured log of significant actions performed by the agent
        mcp_usage_logs:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Mcp Usage Logs
          description: Detailed log of each MCP tool call
        result:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Result
          description: Structured result artifact reported by the agent (parsed /workspace/result.json,
            eval/observe runs)
        launch_requested_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Launch Requested At
        parked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Parked At
          description: When this execution was parked waiting for a human decision
            (status WAITING_FOR_HUMAN) or for the flows it started (status WAITING_FOR_CHILDREN).
            Cleared when the run is resumed.
        park_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Park Expires At
          description: When the pending approval window closes, or when a run parked
            on its children is resumed anyway
        park_request_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Park Request Id
          description: The approval request this execution is parked on, or the wait
            grouping the children it is parked on
        stop_requested_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Stop Requested At
        stop_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Stop Reason
        stop_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Stop Source
        stop_confirmed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Stop Confirmed At
        agent_session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Session Reference
          description: Reference to agent session (e.g., session ID, K8s job ID, container
            ID, process ID)
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
          description: Error message if the execution failed
        failure_category:
          anyOf:
          - type: string
          - type: 'null'
          title: Failure Category
          description: 'Coarse machine-readable reason a terminal execution did not
            succeed: one of runner_conflict, runner_error, model_transient, model_auth,
            provider_billing, model_quota (legacy, superseded by provider_billing),
            budget_exceeded, model_config, no_confirmation, agent_no_progress, setup_failed,
            tool_error, agent_error, timeout, cancelled, unknown. Null for successful
            or still-running executions, and for executions that predate this field.'
        queued_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Queued Reason
          description: 'Why a PENDING execution has not been admitted yet: account_concurrency_cap
            when the account already has its maximum number of executions running.
            Cleared when the execution is claimed; null for every other row.'
        retry_of_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Retry Of Execution Id
          description: ID of the original execution if this is a retry
        batch_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Batch Id
          description: Shared ID linking executions created by one matrix/batch trigger
        parent_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Parent Execution Id
          description: Execution that directly started this one (the delegation tree
            edge). Null for a root run and for executions created before lineage was
            recorded.
        root_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Root Execution Id
          description: First execution of this lineage. Null on the root itself and
            on executions created before lineage was recorded. The whole tree is the
            root row matched by its own id plus rows whose root_execution_id points
            at that root.
        delegation_depth:
          type: integer
          title: Delegation Depth
          description: 'Distance from the root of the lineage: 0 for a root run, 1
            for a direct child, and so on. Existing rows backfilled by the migration
            read back as 0.'
          default: 0
        tool_calls_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tool Calls Count
          description: Total number of tool/MCP calls made during execution
          default: 0
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
          description: Total tokens used (input + output) during execution
          default: 0
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
          description: Estimated cost in USD for this execution
          default: 0.0
        id:
          type: string
          format: uuid
          title: Id
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        park:
          anyOf:
          - $ref: '#/components/schemas/ExecutionPark'
          - type: 'null'
          description: Present while the execution is parked on a human decision (status
            WAITING_FOR_HUMAN).
        runner:
          $ref: '#/components/schemas/ExecutionRunner'
          description: Where this execution ran. Hosted when the built-in executor
            ran it; private when a self-hosted CLI runner did; unknown until assigned.
      type: object
      required:
      - flow_id
      - id
      - created_at
      - updated_at
      title: FlowExecutionResponse
    FlowFailureNotifications:
      properties:
        comment_on_trigger_issue:
          type: boolean
          title: Comment On Trigger Issue
          description: Ignored since 2026-09. Used to post a failure comment with
            a redacted log tail on the triggering issue; a failed execution is an
            attention item on Overview instead. Kept so stored JSON and older clients
            still parse.
          default: false
        attention_item:
          type: boolean
          title: Attention Item
          description: Ignored. Failed executions always appear as console attention
            items of kind ``flow`` on Overview and /console/attention. Kept so stored
            JSON that set this flag still parses.
          default: false
      type: object
      title: FlowFailureNotifications
      description: 'Failure-side keys, all ignored.


        Both options were removed. The block itself is kept so a flow stored

        before the removal, or a client that still sends the keys, parses instead

        of failing with a 422.'
    FlowGatewayUsageSummaryResponse:
      properties:
        flow_id:
          type: string
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        budget:
          $ref: '#/components/schemas/GatewayBudgetSummary'
        usage_by_model:
          items:
            $ref: '#/components/schemas/GatewayUsageByModel'
          type: array
          title: Usage By Model
        usage_by_execution:
          items:
            $ref: '#/components/schemas/GatewayUsageByExecution'
          type: array
          title: Usage By Execution
      type: object
      required:
      - flow_id
      - period_start
      - period_end
      - token_usage
      - budget
      title: FlowGatewayUsageSummaryResponse
      description: Flow-scoped gateway usage summary.
    FlowNotifications:
      properties:
        on_failure:
          $ref: '#/components/schemas/FlowFailureNotifications'
          description: Actions to take when the execution fails or times out.
        on_success:
          $ref: '#/components/schemas/FlowSuccessNotifications'
          description: Actions to take when the execution succeeds.
      type: object
      title: FlowNotifications
      description: Per-flow terminal notifications. NULL on the row means none.
    FlowResponse:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        icon:
          anyOf:
          - type: string
          - type: 'null'
          title: Icon
        trigger_event_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Event Source
        trigger_event_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Trigger Event Types
        trigger_organization_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Organization Id
        trigger_project_ids:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Trigger Project Ids
        trigger_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Trigger Config
        webhook_config:
          anyOf:
          - $ref: '#/components/schemas/WebhookConfig'
          - type: 'null'
        schedule_config:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/CronSchedule'
            - $ref: '#/components/schemas/IntervalSchedule'
            - $ref: '#/components/schemas/DailySchedule'
            - $ref: '#/components/schemas/WeeklySchedule'
            discriminator:
              propertyName: type
              mapping:
                cron: '#/components/schemas/CronSchedule'
                daily: '#/components/schemas/DailySchedule'
                interval: '#/components/schemas/IntervalSchedule'
                weekly: '#/components/schemas/WeeklySchedule'
          - type: 'null'
          title: Schedule Config
        prompt_template:
          anyOf:
          - type: string
          - type: 'null'
          title: Prompt Template
        ai_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Id
        agent_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Type
          default: openhands
        agent_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Agent Config
        allowed_mcp_servers:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Allowed Mcp Servers
        allowed_mcp_tools:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Allowed Mcp Tools
        callable_flows:
          anyOf:
          - items:
              $ref: '#/components/schemas/CallableFlowEntry'
            type: array
          - type: 'null'
          title: Callable Flows
          description: 'Delegation allowlist: the flows an execution of this flow
            is permitted to run, each with optional per child ceilings. Unset or an
            empty list means no delegation, which is the default for every flow. Nothing
            enforces the list yet.'
        git_clone_config:
          anyOf:
          - $ref: '#/components/schemas/GitCloneConfig-Output'
          - type: 'null'
        custom_commands:
          anyOf:
          - $ref: '#/components/schemas/CustomCommands'
          - type: 'null'
        is_preset:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Preset
          default: false
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
          default: true
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
        source_preset_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Preset Id
        source_prompt_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Prompt Hash
        source_tools_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Tools Hash
        prompt_customized:
          type: boolean
          title: Prompt Customized
          default: false
        tools_customized:
          type: boolean
          title: Tools Customized
          default: false
        preset_update_available:
          type: boolean
          title: Preset Update Available
          default: false
        runner_pool:
          anyOf:
          - type: string
          - type: 'null'
          title: Runner Pool
          description: Runner pool for executions of this flow. Accepts a runner id,
            name, or label; the literal 'auto' for any online private runner; or the
            literal 'server' for the hosted executor. When unset, the account default_runner_pool
            applies, then any online private runner, then the hosted executor. A trigger-time
            `--runner` / `_runner` override takes precedence. If no runner in a chosen
            private pool has a free slot the job queues for 15 minutes then FAILS.
        timeout_seconds:
          anyOf:
          - type: integer
            maximum: 86400.0
            minimum: 60.0
          - type: 'null'
          title: Timeout Seconds
          description: Wall-clock budget for one execution of this flow, in seconds.
            Leave unset to use the deployment default (3600). A run that exceeds the
            budget is stopped and fails with the timeout category, and the failure
            message names the budget that expired.
        approval_window_seconds:
          anyOf:
          - type: integer
            maximum: 2592000.0
            minimum: 60.0
          - type: 'null'
          title: Approval Window Seconds
          description: 'How long a human has to answer an approval or question raised
            by an execution of this flow, in seconds (60s to 30 days). Leave unset
            to use the approval workflow''s timeout, then the deployment default (300s).
            While the request is outstanding the execution is parked (WAITING_FOR_HUMAN):
            it holds no container and no runner, and the decision resumes it, so a
            window measured in days costs calendar time only.'
        notifications:
          anyOf:
          - $ref: '#/components/schemas/FlowNotifications'
          - type: 'null'
          description: When to comment on the triggering issue and raise a console
            attention item after a terminal execution. Leave unset for no notifications.
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        slug:
          anyOf:
          - type: string
          - type: 'null'
          title: Slug
        supports_persistent:
          type: boolean
          title: Supports Persistent
          default: false
        execution_stats:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Execution Stats
        schedule_state:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Schedule State
        ai_model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Name
        effective_publication_policy:
          $ref: '#/components/schemas/EffectivePublicationPolicy'
          description: Read-only description derived from this saved flow's configuration.
          readOnly: true
      type: object
      required:
      - id
      - created_at
      - updated_at
      - effective_publication_policy
      title: FlowResponse
    FlowSuccessNotifications:
      properties:
        comment_on_trigger_issue:
          type: boolean
          title: Comment On Trigger Issue
          description: 'Post a short ''PR opened: <url>'' comment on the triggering
            issue when the run recorded a pull request URL.'
          default: false
      type: object
      title: FlowSuccessNotifications
      description: What to do when an execution succeeds.
    FlowUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        icon:
          anyOf:
          - type: string
          - type: 'null'
          title: Icon
        trigger_event_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Trigger Event Source
        trigger_event_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Trigger Event Types
        trigger_organization_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Trigger Organization Id
        trigger_project_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Trigger Project Ids
        trigger_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Trigger Config
        webhook_config:
          anyOf:
          - $ref: '#/components/schemas/WebhookConfig'
          - type: 'null'
        schedule_config:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/CronSchedule'
            - $ref: '#/components/schemas/IntervalSchedule'
            - $ref: '#/components/schemas/DailySchedule'
            - $ref: '#/components/schemas/WeeklySchedule'
            discriminator:
              propertyName: type
              mapping:
                cron: '#/components/schemas/CronSchedule'
                daily: '#/components/schemas/DailySchedule'
                interval: '#/components/schemas/IntervalSchedule'
                weekly: '#/components/schemas/WeeklySchedule'
          - type: 'null'
          title: Schedule Config
        prompt_template:
          anyOf:
          - type: string
          - type: 'null'
          title: Prompt Template
        ai_model_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Ai Model Id
        agent_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Type
          default: openhands
        agent_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Agent Config
        allowed_mcp_servers:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Allowed Mcp Servers
        allowed_mcp_tools:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Allowed Mcp Tools
        callable_flows:
          anyOf:
          - items:
              $ref: '#/components/schemas/CallableFlowEntry'
            type: array
          - type: 'null'
          title: Callable Flows
          description: 'Delegation allowlist: the flows an execution of this flow
            is permitted to run, each with optional per child ceilings. Unset or an
            empty list means no delegation, which is the default for every flow. Nothing
            enforces the list yet.'
        git_clone_config:
          anyOf:
          - $ref: '#/components/schemas/GitCloneConfig-Input'
          - type: 'null'
        custom_commands:
          anyOf:
          - $ref: '#/components/schemas/CustomCommands'
          - type: 'null'
        is_preset:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Preset
          default: false
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
          default: true
        account_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Account Id
        source_preset_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Source Preset Id
        source_prompt_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Prompt Hash
        source_tools_hash:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Tools Hash
        prompt_customized:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Prompt Customized
          default: false
        tools_customized:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Tools Customized
          default: false
        preset_update_available:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Preset Update Available
          default: false
        runner_pool:
          anyOf:
          - type: string
          - type: 'null'
          title: Runner Pool
          description: Runner pool for executions of this flow. Accepts a runner id,
            name, or label; the literal 'auto' for any online private runner; or the
            literal 'server' for the hosted executor. When unset, the account default_runner_pool
            applies, then any online private runner, then the hosted executor. A trigger-time
            `--runner` / `_runner` override takes precedence. If no runner in a chosen
            private pool has a free slot the job queues for 15 minutes then FAILS.
        timeout_seconds:
          anyOf:
          - type: integer
            maximum: 86400.0
            minimum: 60.0
          - type: 'null'
          title: Timeout Seconds
          description: Wall-clock budget for one execution of this flow, in seconds.
            Leave unset to use the deployment default (3600). A run that exceeds the
            budget is stopped and fails with the timeout category, and the failure
            message names the budget that expired.
        approval_window_seconds:
          anyOf:
          - type: integer
            maximum: 2592000.0
            minimum: 60.0
          - type: 'null'
          title: Approval Window Seconds
          description: 'How long a human has to answer an approval or question raised
            by an execution of this flow, in seconds (60s to 30 days). Leave unset
            to use the approval workflow''s timeout, then the deployment default (300s).
            While the request is outstanding the execution is parked (WAITING_FOR_HUMAN):
            it holds no container and no runner, and the decision resumes it, so a
            window measured in days costs calendar time only.'
        notifications:
          anyOf:
          - $ref: '#/components/schemas/FlowNotifications'
          - type: 'null'
          description: When to comment on the triggering issue and raise a console
            attention item after a terminal execution. Leave unset for no notifications.
      type: object
      title: FlowUpdate
    FollowUpFiling-Input:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether approved follow ups in the run's result are filed as
            tracker issues after the agent has exited
          default: false
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Tracker project the issues are filed into. Defaults to the
            project of the repository this flow clones, then the project that triggered
            the run
        labels:
          items:
            type: string
          type: array
          maxItems: 10
          title: Labels
          description: Labels applied to every filed issue
        max_issues:
          type: integer
          maximum: 100.0
          minimum: 1.0
          title: Max Issues
          description: Ceiling on issues filed by one run, so a misconfigured portfolio
            cannot empty itself into a tracker
          default: 25
      additionalProperties: false
      type: object
      title: FollowUpFiling
      description: 'File the follow ups a human approved as issues in the tracker.


        Same argument as :class:`ReportPublication`, applied to the other output

        of a review: the agent holds no write tool, and the approved rows are

        turned into issues by the control plane after the agent has exited, using

        the account''s configured tracker credential. Issue #687.


        It lives beside ``report_publication`` because both describe what the

        platform does with a run''s outputs against the repository the flow is

        configured for, and because ``repositories[].project_id`` is where the

        tracker project is read from when this block does not name one.


        ``extra=''forbid''``: a misspelled key here is an issue filed somewhere

        else, or not at all.'
    FollowUpFiling-Output:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether approved follow ups in the run's result are filed as
            tracker issues after the agent has exited
          default: false
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
          description: Tracker project the issues are filed into. Defaults to the
            project of the repository this flow clones, then the project that triggered
            the run
        labels:
          items:
            type: string
          type: array
          maxItems: 10
          title: Labels
          description: Labels applied to every filed issue
        max_issues:
          type: integer
          maximum: 100.0
          minimum: 1.0
          title: Max Issues
          description: Ceiling on issues filed by one run, so a misconfigured portfolio
            cannot empty itself into a tracker
          default: 25
      additionalProperties: false
      type: object
      title: FollowUpFiling
      description: 'File the follow ups a human approved as issues in the tracker.


        Same argument as :class:`ReportPublication`, applied to the other output

        of a review: the agent holds no write tool, and the approved rows are

        turned into issues by the control plane after the agent has exited, using

        the account''s configured tracker credential. Issue #687.


        It lives beside ``report_publication`` because both describe what the

        platform does with a run''s outputs against the repository the flow is

        configured for, and because ``repositories[].project_id`` is where the

        tracker project is read from when this block does not name one.


        ``extra=''forbid''``: a misspelled key here is an issue filed somewhere

        else, or not at all.'
    GatewayBudgetSummary:
      properties:
        monthly_limit_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Monthly Limit Usd
        soft_limit_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Soft Limit Usd
        current_spend_usd:
          type: number
          title: Current Spend Usd
          default: 0.0
        soft_limit_exceeded:
          type: boolean
          title: Soft Limit Exceeded
          default: false
        hard_limit_exceeded:
          type: boolean
          title: Hard Limit Exceeded
          default: false
      type: object
      title: GatewayBudgetSummary
      description: Budget snapshot for gateway usage.
    GatewayTokenUsage:
      properties:
        prompt_tokens:
          type: integer
          title: Prompt Tokens
          default: 0
        completion_tokens:
          type: integer
          title: Completion Tokens
          default: 0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        input_tokens:
          type: integer
          title: Input Tokens
          default: 0
        output_tokens:
          type: integer
          title: Output Tokens
          default: 0
        cache_read_tokens:
          type: integer
          title: Cache Read Tokens
          default: 0
        cache_write_tokens:
          type: integer
          title: Cache Write Tokens
          default: 0
        uncached_input_tokens:
          type: integer
          title: Uncached Input Tokens
          default: 0
        cache_hit_ratio:
          anyOf:
          - type: number
          - type: 'null'
          title: Cache Hit Ratio
      type: object
      title: GatewayTokenUsage
      description: 'Token usage totals, split by direction and by cache participation.


        ``input_tokens`` / ``output_tokens`` are the product-facing names for the

        provider wire names ``prompt_tokens`` / ``completion_tokens``; both pairs

        are always populated and always agree, because the flow-execution endpoint

        already spoke the input/output names and clients should not have to know

        which endpoint they are reading. Callers construct with either pair. A

        payload that sets both names of a pair to different non-zero values is

        rejected rather than left disagreeing.


        The cache fields describe the input side only. ``cache_read_tokens`` is

        input served from a prompt cache, ``cache_write_tokens`` is input written

        into one, and ``uncached_input_tokens`` is the remainder the provider had

        to read afresh (see ``uncached_input_tokens`` in

        ``preloop.services.cache_accounting``), counted over the requests whose

        provider actually reported a cache split. ``cache_hit_ratio`` is ``None``

        when no request in the aggregate reported one: unknown, never "0% hit".'
    GatewayToolUsageByAgent:
      properties:
        runtime_principal_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Type
        runtime_principal_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Id
        invocation_count:
          type: integer
          title: Invocation Count
          default: 0
        estimated_schema_cost:
          type: number
          title: Estimated Schema Cost
          default: 0.0
      type: object
      title: GatewayToolUsageByAgent
      description: Tool activity attributed to one runtime principal (managed agent).
    GatewayUsageByDay:
      properties:
        date:
          type: string
          title: Date
        request_count:
          type: integer
          title: Request Count
          default: 0
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        input_tokens:
          type: integer
          title: Input Tokens
          default: 0
        output_tokens:
          type: integer
          title: Output Tokens
          default: 0
        cache_read_tokens:
          type: integer
          title: Cache Read Tokens
          default: 0
        cache_write_tokens:
          type: integer
          title: Cache Write Tokens
          default: 0
      type: object
      required:
      - date
      title: GatewayUsageByDay
      description: Daily usage aggregate.
    GatewayUsageByExecution:
      properties:
        flow_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Execution Id
        request_count:
          type: integer
          title: Request Count
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
      type: object
      required:
      - token_usage
      title: GatewayUsageByExecution
      description: Usage aggregate grouped by flow execution.
    GatewayUsageByFlow:
      properties:
        flow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        request_count:
          type: integer
          title: Request Count
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
      type: object
      required:
      - token_usage
      title: GatewayUsageByFlow
      description: Usage aggregate grouped by flow.
    GatewayUsageByModel:
      properties:
        ai_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Id
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        request_count:
          type: integer
          title: Request Count
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        unpriced_request_count:
          type: integer
          title: Unpriced Request Count
          description: Requests that consumed tokens and carried no price at all,
            so their cost is missing from every total.
          default: 0
        zero_priced_request_count:
          type: integer
          title: Zero Priced Request Count
          description: Requests a price was applied to and the price was exactly zero.
            Free tier, a promotion, or a price entered wrongly; unlike an unpriced
            request, nothing is missing from the total.
          default: 0
        failed_request_count:
          type: integer
          title: Failed Request Count
          description: Requests the gateway answered with a 4xx or 5xx status.
          default: 0
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
      type: object
      required:
      - token_usage
      title: GatewayUsageByModel
      description: Usage aggregate grouped by model.
    GatewayUsageBySession:
      properties:
        ai_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Id
        runtime_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
        session_source_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
        session_source_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Id
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
        session_summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Summary
        session_summary_updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Session Summary Updated At
        runtime_principal_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Type
        runtime_principal_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Id
        agent_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Name
        flow_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Execution Id
        flow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        request_count:
          type: integer
          title: Request Count
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
      type: object
      required:
      - token_usage
      title: GatewayUsageBySession
      description: Recent usage aggregate grouped by execution-backed session slices.
    GatewayUsageByTool:
      properties:
        tool_name:
          type: string
          title: Tool Name
        server_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Server Name
        invocation_count:
          type: integer
          title: Invocation Count
          default: 0
        successful_invocations:
          type: integer
          title: Successful Invocations
          default: 0
        failed_invocations:
          type: integer
          title: Failed Invocations
          default: 0
        schema_injections:
          type: integer
          title: Schema Injections
          default: 0
        schema_tokens_total:
          type: integer
          title: Schema Tokens Total
          default: 0
        estimated_schema_cost:
          type: number
          title: Estimated Schema Cost
          default: 0.0
        avg_cost_per_invocation:
          type: number
          title: Avg Cost Per Invocation
          default: 0.0
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        usage_by_agent:
          items:
            $ref: '#/components/schemas/GatewayToolUsageByAgent'
          type: array
          title: Usage By Agent
      type: object
      required:
      - tool_name
      title: GatewayUsageByTool
      description: Tool usage aggregate combining invocation counts and schema-injection
        cost.
    GatewayUsageSearchResultItem:
      properties:
        api_usage_id:
          type: string
          title: Api Usage Id
        ai_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Id
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        status_code:
          type: integer
          title: Status Code
        outcome:
          type: string
          title: Outcome
        endpoint:
          type: string
          title: Endpoint
        method:
          type: string
          title: Method
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        flow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        flow_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Execution Id
        runtime_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
        session_source_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
        session_source_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        runtime_principal_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Type
        runtime_principal_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        auth_subject_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Subject Type
        api_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key Id
        api_key_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key Name
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        excerpt:
          type: string
          title: Excerpt
        meta_data:
          additionalProperties: true
          type: object
          title: Meta Data
      type: object
      required:
      - api_usage_id
      - timestamp
      - status_code
      - outcome
      - endpoint
      - method
      - token_usage
      - excerpt
      title: GatewayUsageSearchResultItem
      description: One account-scoped gateway interaction search hit.
    GeneratePolicyFromAuditRequest:
      properties:
        start_date:
          anyOf:
          - type: string
          - type: 'null'
          title: Start Date
          description: Only consider logs after this ISO date (e.g. 2026-01-01)
        end_date:
          anyOf:
          - type: string
          - type: 'null'
          title: End Date
          description: Only consider logs before this ISO date
        audit_logs_json:
          anyOf:
          - type: string
          - type: 'null'
          title: Audit Logs Json
          description: Raw JSON dump of audit logs to analyse instead of querying
            the database. Must be a JSON array of log entries.
      type: object
      title: GeneratePolicyFromAuditRequest
      description: Request to generate a policy from audit-log patterns.
    GeneratePolicyRequest:
      properties:
        prompt:
          type: string
          title: Prompt
          description: Natural-language description of the desired policy
        include_current_config:
          type: boolean
          title: Include Current Config
          description: Include the account's current MCP servers and tools as context
            for the LLM (recommended for more accurate generation)
          default: true
        scope_mcp_server_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Scope Mcp Server Name
          description: When set, starter-policy generation scopes LLM context and
            raw model output to this MCP server. The merge still restores all current
            tools so the diff preview only shows this server's changes. Older clients
            that omit the field fall back to matching the prompt text.
      type: object
      required:
      - prompt
      title: GeneratePolicyRequest
      description: Request to generate a policy from a natural-language prompt.
    GeneratePolicyResponse:
      properties:
        yaml:
          type: string
          title: Yaml
          description: Generated policy YAML
        warnings:
          items:
            type: string
          type: array
          title: Warnings
          description: Non-fatal warnings from validation
      type: object
      required:
      - yaml
      title: GeneratePolicyResponse
      description: Response from a policy generation endpoint.
    GitCloneConfig-Input:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether git clone is enabled
          default: false
        repositories:
          items:
            $ref: '#/components/schemas/GitCloneRepository-Input'
          type: array
          title: Repositories
          description: List of repositories to clone
        git_user_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Git User Name
          description: Name to use for git commits
          default: Preloop
        git_user_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Git User Email
          description: Email to use for git commits
          default: git@preloop.ai
        source_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Branch
          description: Branch to checkout for base code
          default: main
        target_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Branch
          description: Branch to create for commits (auto-generated if empty)
        setup_commands:
          items:
            type: string
          type: array
          title: Setup Commands
          description: Shell commands run inside the container after clone/restore
            and before the agent starts (dependency install, service bring-up). Output
            is captured to /workspace/evidence/setup.log; a failure fails the execution
            with failure_category 'setup_failed'.
        create_pull_request:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Create Pull Request
          description: Whether to create a Pull Request / Merge Request
          default: false
        publication_mode:
          type: string
          enum:
          - legacy
          - isolated
          title: Publication Mode
          description: Legacy publishes inside the agent container. Isolated publishes
            from the trusted control plane after verification, using scoped App credentials.
          default: legacy
        publication_approval:
          anyOf:
          - type: boolean
          - type: string
            enum:
            - required
            - require
          - type: 'null'
          title: Publication Approval
          description: When true, required, or require, isolated publication mints
            a writer lease only after a human platform approval covers each frozen
            candidate repository, branch, base, and head SHA. Default flows omit this
            and keep existing publication behaviour.
        pull_request_template:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Pull Request Template
          description: Repository-relative PR template; otherwise conventional default
            then lexical first
        pull_request_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Pull Request Title
          description: Title for the Pull/Merge Request
        pull_request_description:
          anyOf:
          - type: string
          - type: 'null'
          title: Pull Request Description
          description: Description for the Pull/Merge Request
        verification:
          anyOf:
          - $ref: '#/components/schemas/VerificationPolicy'
          - type: 'null'
          description: 'Publication gate policy: required checks a commit must pass
            before the flow pushes it or opens a pull request'
        report_publication:
          anyOf:
          - $ref: '#/components/schemas/ReportPublication'
          - type: 'null'
          description: Publish the document this run generated as a pull request,
            after the agent has exited and without giving it write tools
        follow_up_filing:
          anyOf:
          - $ref: '#/components/schemas/FollowUpFiling-Input'
          - type: 'null'
          description: File the follow ups a human approved in this run as tracker
            issues, after the agent has exited and without giving it write tools
      type: object
      title: GitCloneConfig
      description: Configuration for git clone operations before agent execution.
    GitCloneConfig-Output:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether git clone is enabled
          default: false
        repositories:
          items:
            $ref: '#/components/schemas/GitCloneRepository-Output'
          type: array
          title: Repositories
          description: List of repositories to clone
        git_user_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Git User Name
          description: Name to use for git commits
          default: Preloop
        git_user_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Git User Email
          description: Email to use for git commits
          default: git@preloop.ai
        source_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Branch
          description: Branch to checkout for base code
          default: main
        target_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Branch
          description: Branch to create for commits (auto-generated if empty)
        setup_commands:
          items:
            type: string
          type: array
          title: Setup Commands
          description: Shell commands run inside the container after clone/restore
            and before the agent starts (dependency install, service bring-up). Output
            is captured to /workspace/evidence/setup.log; a failure fails the execution
            with failure_category 'setup_failed'.
        create_pull_request:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Create Pull Request
          description: Whether to create a Pull Request / Merge Request
          default: false
        publication_mode:
          type: string
          enum:
          - legacy
          - isolated
          title: Publication Mode
          description: Legacy publishes inside the agent container. Isolated publishes
            from the trusted control plane after verification, using scoped App credentials.
          default: legacy
        publication_approval:
          anyOf:
          - type: boolean
          - type: string
            enum:
            - required
            - require
          - type: 'null'
          title: Publication Approval
          description: When true, required, or require, isolated publication mints
            a writer lease only after a human platform approval covers each frozen
            candidate repository, branch, base, and head SHA. Default flows omit this
            and keep existing publication behaviour.
        pull_request_template:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Pull Request Template
          description: Repository-relative PR template; otherwise conventional default
            then lexical first
        pull_request_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Pull Request Title
          description: Title for the Pull/Merge Request
        pull_request_description:
          anyOf:
          - type: string
          - type: 'null'
          title: Pull Request Description
          description: Description for the Pull/Merge Request
        verification:
          anyOf:
          - $ref: '#/components/schemas/VerificationPolicy'
          - type: 'null'
          description: 'Publication gate policy: required checks a commit must pass
            before the flow pushes it or opens a pull request'
        report_publication:
          anyOf:
          - $ref: '#/components/schemas/ReportPublication'
          - type: 'null'
          description: Publish the document this run generated as a pull request,
            after the agent has exited and without giving it write tools
        follow_up_filing:
          anyOf:
          - $ref: '#/components/schemas/FollowUpFiling-Output'
          - type: 'null'
          description: File the follow ups a human approved in this run as tracker
            issues, after the agent has exited and without giving it write tools
      type: object
      title: GitCloneConfig
      description: Configuration for git clone operations before agent execution.
    GitCloneRepository-Input:
      properties:
        tracker_id:
          type: string
          format: uuid
          title: Tracker Id
          description: ID of the tracker (GitHub/GitLab) to use
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project ID to clone. If None, uses repository_url or trigger
            event
        repository_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Repository Url
          description: Repository URL to clone. If None, resolved from project or
            trigger
        clone_path:
          type: string
          title: Clone Path
          description: Relative path where repository should be cloned
          default: workspace
        branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Branch
          description: Branch to clone. If None, uses default branch
      type: object
      required:
      - tracker_id
      title: GitCloneRepository
      description: Configuration for a single repository to clone.
    GitCloneRepository-Output:
      properties:
        tracker_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Tracker Id
          description: ID of the tracker (GitHub/GitLab) to use
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
          description: Project ID to clone. If None, uses repository_url or trigger
            event
        repository_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Repository Url
          description: Repository URL to clone. If None, resolved from project or
            trigger
        clone_path:
          type: string
          title: Clone Path
          description: Relative path where repository should be cloned
          default: workspace
        branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Branch
          description: Branch to clone. If None, uses default branch
      type: object
      required:
      - tracker_id
      title: GitCloneRepository
      description: Configuration for a single repository to clone.
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HostExecProfileAdvertisement:
      properties:
        name:
          type: string
          maxLength: 64
          pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$
          title: Name
        capabilities:
          items:
            type: string
          type: array
          maxItems: 16
          title: Capabilities
        models:
          items:
            type: string
          type: array
          maxItems: 64
          title: Models
      type: object
      required:
      - name
      title: HostExecProfileAdvertisement
      description: Name and capability flags a runner advertises. No executable path.
    ImportedUsageByConversation:
      properties:
        conversation_id:
          type: string
          title: Conversation Id
        parent_conversation_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Parent Conversation Id
        source:
          anyOf:
          - type: string
          - type: 'null'
          title: Source
        event_count:
          type: integer
          title: Event Count
          default: 0
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
        reconciled_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Reconciled Cost
        last_event_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Event At
      type: object
      required:
      - conversation_id
      title: ImportedUsageByConversation
      description: 'Imported usage rolled up by source-side conversation.


        ``estimated_cost`` (hook/transcript-derived) and ``reconciled_cost``

        (billing export) are reported as SEPARATE fields and must never be

        summed into one number by any consumer. ``None`` means the source

        reported nothing for that quantity ("not reported") — it is not zero.

        Entries whose ``parent_conversation_id`` matches another entry''s

        ``conversation_id`` are subagent workers of that parent thread.'
    ImportedUsageByModel:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        source:
          anyOf:
          - type: string
          - type: 'null'
          title: Source
        request_count:
          type: integer
          title: Request Count
          default: 0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        imported_cost:
          type: number
          title: Imported Cost
          default: 0.0
        last_event_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Event At
      type: object
      title: ImportedUsageByModel
      description: Imported (observed) usage aggregate grouped by source model.
    ImportedUsageSummary:
      properties:
        event_count:
          type: integer
          title: Event Count
          default: 0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        imported_cost:
          type: number
          title: Imported Cost
          default: 0.0
        usage_by_model:
          items:
            $ref: '#/components/schemas/ImportedUsageByModel'
          type: array
          title: Usage By Model
        usage_by_conversation:
          items:
            $ref: '#/components/schemas/ImportedUsageByConversation'
          type: array
          title: Usage By Conversation
      type: object
      title: ImportedUsageSummary
      description: 'Spend ingested from outside the gateway (``usage_source=''imported''``).


        Kept as a separate block — never merged into ``estimated_cost`` or the

        budget figures — so gateway-metered and imported spend cannot be

        silently mixed.'
    IntervalSchedule:
      properties:
        timezone:
          type: string
          title: Timezone
          description: IANA timezone name the schedule is evaluated in
          default: UTC
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Static trigger payload merged into every scheduled run, for
            options a schedule has no other way to state (e.g. previous_result_execution_id).
            Bounded and inline only.
        type:
          type: string
          const: interval
          title: Type
          default: interval
        every:
          type: integer
          minimum: 1.0
          title: Every
          description: Run every N units
        unit:
          type: string
          enum:
          - minutes
          - hours
          - days
          title: Unit
          description: Unit of the interval
      type: object
      required:
      - every
      - unit
      title: IntervalSchedule
      description: 'Friendly schedule form: run every N minutes/hours/days.'
    IssueCreate:
      properties:
        title:
          type: string
          title: Title
          description: Issue title
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Issue description
        priority:
          anyOf:
          - type: string
          - type: 'null'
          title: Priority
          description: Issue priority (Low, Medium, High)
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: Issue status
        assignee:
          anyOf:
          - type: string
          - type: 'null'
          title: Assignee
          description: Issue assignee
        labels:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Labels
          description: Issue labels
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional issue metadata
        organization_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Id
          description: Organization ID (UUID)
        organization_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Name
          description: Organization name
        organization:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization
          description: Organization identifier (name or ID)
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
          description: Project ID (UUID)
        project_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Name
          description: Project name
        project:
          anyOf:
          - type: string
          - type: 'null'
          title: Project
          description: Project identifier (name or ID)
      type: object
      required:
      - title
      title: IssueCreate
      description: Model for creating a new issue.
    IssueListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/IssueResponse'
          type: array
          title: Items
          description: Issue list items
        total:
          type: integer
          title: Total
          description: Total number of matching issues
        skip:
          type: integer
          title: Skip
          description: Number of issues skipped
        limit:
          type: integer
          title: Limit
          description: Page size
      type: object
      required:
      - items
      - total
      - skip
      - limit
      title: IssueListResponse
      description: Paginated list of issues for a project or tracker.
    IssueResponse:
      properties:
        title:
          type: string
          title: Title
          description: Issue title
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Issue description
        priority:
          anyOf:
          - type: string
          - type: 'null'
          title: Priority
          description: Issue priority (Low, Medium, High)
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: Issue status
        assignee:
          anyOf:
          - type: string
          - type: 'null'
          title: Assignee
          description: Issue assignee
        labels:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Labels
          description: Issue labels
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional issue metadata
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
          description: Internal Preloop database ID (UUID)
        external_id:
          type: string
          title: External Id
          description: Issue ID in the original tracker
        key:
          type: string
          title: Key
          description: Human-readable issue key (e.g., PROJ-123)
        organization:
          type: string
          title: Organization
          description: Organization name
        project:
          type: string
          title: Project
          description: Project name
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
          description: Project ID
        project_identifier:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Identifier
          description: Project identifier/slug for API calls
        url:
          type: string
          title: Url
          description: URL to the issue in the original tracker
        created_at:
          type: string
          title: Created At
          description: Creation timestamp
        updated_at:
          type: string
          title: Updated At
          description: Last update timestamp
        comments:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Comments
          description: List of comments on the issue
          default: []
        score:
          anyOf:
          - type: number
          - type: 'null'
          title: Score
          description: Similarity score for search results (if applicable)
      type: object
      required:
      - title
      - id
      - external_id
      - key
      - organization
      - project
      - project_id
      - url
      - created_at
      - updated_at
      title: IssueResponse
      description: Response model for issue data.
    IssueUpdate:
      properties:
        organization:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization
          description: Organization name
        project:
          anyOf:
          - type: string
          - type: 'null'
          title: Project
          description: Project name
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
          description: New issue title
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: New issue description
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: New issue status
        priority:
          anyOf:
          - type: string
          - type: 'null'
          title: Priority
          description: New issue priority
        assignee:
          anyOf:
          - type: string
          - type: 'null'
          title: Assignee
          description: New issue assignee
        labels:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Labels
          description: New issue labels
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional issue metadata
      type: object
      title: IssueUpdate
      description: Model for updating an issue.
    KillSwitchActivateRequest:
      properties:
        scopes:
          items:
            type: string
            enum:
            - gateway
            - tools
            - flows
          type: array
          title: Scopes
          description: 'Which traffic classes to halt. Defaults to all of them (the
            emergency stop). Scope names: gateway, tools, flows.'
        reason:
          anyOf:
          - type: string
            maxLength: 500
          - type: 'null'
          title: Reason
          description: Why the halt was activated (recorded for audit)
      type: object
      title: KillSwitchActivateRequest
      description: Request body for activating the kill switch.
    KillSwitchDeactivateRequest:
      properties:
        reason:
          anyOf:
          - type: string
            maxLength: 500
          - type: 'null'
          title: Reason
          description: Reason for recovery, recorded for audit
        scopes:
          items:
            type: string
            enum:
            - gateway
            - tools
            - flows
          type: array
          title: Scopes
          description: Which traffic classes to re-enable. Pass a subset for staged
            recovery (e.g. only ['gateway'] first).
      type: object
      title: KillSwitchDeactivateRequest
      description: Request body for a staged or full re-enable.
    KillSwitchScopeState:
      properties:
        scope:
          type: string
          enum:
          - gateway
          - tools
          - flows
          title: Scope
        activated_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Activated By User Id
        activated_by_username:
          anyOf:
          - type: string
          - type: 'null'
          title: Activated By Username
        activated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Activated At
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
      type: object
      required:
      - scope
      title: KillSwitchScopeState
      description: Render-ready state for one halted scope.
    KillSwitchStatus:
      properties:
        active:
          type: boolean
          title: Active
          description: Whether any scope is currently halted
        scopes:
          items:
            $ref: '#/components/schemas/KillSwitchScopeState'
          type: array
          title: Scopes
          description: Currently-halted scopes with their activation audit data
      type: object
      required:
      - active
      title: KillSwitchStatus
      description: 'Aggregate kill-switch state used to drive the console banner.


        Any authenticated user can read this: knowing that agent activity is

        halted is not privileged information, and hiding it would defeat the

        point of the banner.'
    LedgerBackfillBucket:
      properties:
        day:
          type: string
          format: date
          title: Day
        family:
          type: string
          title: Family
        ledger_total:
          type: number
          title: Ledger Total
        row_count:
          type: integer
          title: Row Count
        allocated_total:
          type: number
          title: Allocated Total
        ledger_models:
          items:
            type: string
          type: array
          title: Ledger Models
      type: object
      required:
      - day
      - family
      - ledger_total
      - row_count
      - allocated_total
      title: LedgerBackfillBucket
      description: One (day x model family) allocation bucket of a ledger backfill.
    LedgerBackfillResidual:
      properties:
        day:
          type: string
          format: date
          title: Day
        family:
          type: string
          title: Family
        amount_usd:
          type: number
          title: Amount Usd
      type: object
      required:
      - day
      - family
      - amount_usd
      title: LedgerBackfillResidual
      description: Ledger spend with no eligible usage rows to receive it.
    LedgerBackfillResponse:
      properties:
        dry_run:
          type: boolean
          title: Dry Run
          default: true
        provider:
          type: string
          title: Provider
        start:
          type: string
          format: date
          title: Start
        end:
          type: string
          format: date
          title: End
        ledger_entries:
          type: integer
          title: Ledger Entries
          default: 0
        eligible_rows:
          type: integer
          title: Eligible Rows
          default: 0
        rows_to_reconcile:
          type: integer
          title: Rows To Reconcile
          default: 0
        total_allocated:
          type: number
          title: Total Allocated
          default: 0.0
        rows_updated:
          anyOf:
          - type: integer
          - type: 'null'
          title: Rows Updated
        other_residual_usd:
          type: number
          title: Other Residual Usd
          default: 0.0
        buckets:
          items:
            $ref: '#/components/schemas/LedgerBackfillBucket'
          type: array
          title: Buckets
        unallocated_ledger:
          items:
            $ref: '#/components/schemas/LedgerBackfillResidual'
          type: array
          title: Unallocated Ledger
        unmatched_rows:
          items:
            $ref: '#/components/schemas/LedgerBackfillUnmatched'
          type: array
          title: Unmatched Rows
        skipped_csv_rows:
          items:
            type: string
          type: array
          title: Skipped Csv Rows
      type: object
      required:
      - provider
      - start
      - end
      title: LedgerBackfillResponse
      description: 'Outcome (or dry-run preview) of a provider daily-ledger backfill.


        ``rows_updated`` is ``None`` on a dry run — nothing was written, which is

        different from "wrote 0 rows". ``other_residual_usd`` is the export''s

        "Other" bucket inside the window: the provider does not say which models

        it covers, so that spend is reported but never allocated and the matching

        rows stay unpriced.'
    LedgerBackfillUnmatched:
      properties:
        day:
          type: string
          format: date
          title: Day
        family:
          type: string
          title: Family
        row_count:
          type: integer
          title: Row Count
      type: object
      required:
      - day
      - family
      - row_count
      title: LedgerBackfillUnmatched
      description: Unpriced usage rows whose family had no ledger spend that day.
    LegalHoldCreate:
      properties:
        resource_type:
          type: string
          title: Resource Type
          description: One of execution, approval, evidence_pack, runtime_session
        resource_id:
          type: string
          maxLength: 255
          title: Resource Id
        reason:
          type: string
          maxLength: 2000
          minLength: 8
          title: Reason
          description: Why this record is frozen. Written to the audit log.
      type: object
      required:
      - resource_type
      - resource_id
      - reason
      title: LegalHoldCreate
      description: Place a hold on one execution, approval, evidence pack or session.
    LegalHoldRead:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        resource_type:
          type: string
          title: Resource Type
        resource_id:
          type: string
          title: Resource Id
        reason:
          type: string
          title: Reason
        placed_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Placed By User Id
        placed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Placed At
        released_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Released By User Id
        released_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Released At
        release_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Release Reason
        active:
          type: boolean
          title: Active
        flagged:
          anyOf:
          - additionalProperties:
              type: integer
            type: object
          - type: 'null'
          title: Flagged
      type: object
      required:
      - id
      - resource_type
      - resource_id
      - reason
      - active
      title: LegalHoldRead
      description: One hold, active or released.
    LegalHoldRelease:
      properties:
        reason:
          type: string
          maxLength: 2000
          minLength: 8
          title: Reason
          description: Why the hold is being lifted. Written to the audit log.
      type: object
      required:
      - reason
      title: LegalHoldRelease
      description: Lift a hold. The release reason is recorded too.
    LoginRequest:
      properties:
        username:
          type: string
          title: Username
        password:
          type: string
          title: Password
      type: object
      required:
      - username
      - password
      title: LoginRequest
      description: Model for login requests.
    MCPServerCreate:
      properties:
        name:
          type: string
          title: Name
        url:
          type: string
          title: Url
        transport:
          anyOf:
          - type: string
          - type: 'null'
          title: Transport
          description: 'Transport protocol (default: http-streaming)'
          default: http-streaming
        auth_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Type
          description: 'Authentication type: ''none'', ''bearer'', ''api_key'', ''oauth'''
          default: none
        auth_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Auth Config
          description: JSON configuration for authentication
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: 'Server status: ''active'', ''error'', ''disabled'''
          default: active
      type: object
      required:
      - name
      - url
      title: MCPServerCreate
      description: 'Schema for creating an MCP server.


        Note: account_id is not included here as it''s extracted from

        the authenticated user in the endpoint handler.'
    MCPServerResponse:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: User-defined name for this MCP server
        url:
          anyOf:
          - type: string
          - type: 'null'
          title: Url
          description: URL of the external MCP server
        transport:
          anyOf:
          - type: string
          - type: 'null'
          title: Transport
          description: 'Transport protocol (default: http-streaming)'
          default: http-streaming
        auth_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Type
          description: 'Authentication type: ''none'', ''bearer'', ''api_key'', ''oauth'''
          default: none
        auth_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Auth Config
          description: JSON configuration for authentication
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: 'Server status: ''active'', ''error'', ''disabled'''
          default: active
        id:
          type: string
          title: Id
        account_id:
          type: string
          title: Account Id
        last_scan_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Scan At
        last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - id
      - account_id
      - created_at
      - updated_at
      title: MCPServerResponse
      description: Schema for MCP server response.
    MCPServerUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: User-defined name for this MCP server
        url:
          anyOf:
          - type: string
          - type: 'null'
          title: Url
          description: URL of the external MCP server
        transport:
          anyOf:
          - type: string
          - type: 'null'
          title: Transport
          description: 'Transport protocol (default: http-streaming)'
          default: http-streaming
        auth_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Type
          description: 'Authentication type: ''none'', ''bearer'', ''api_key'', ''oauth'''
          default: none
        auth_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Auth Config
          description: JSON configuration for authentication
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
          description: 'Server status: ''active'', ''error'', ''disabled'''
          default: active
      type: object
      title: MCPServerUpdate
      description: Schema for updating an MCP server.
    MCPToolResponse:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Tool description
        input_schema:
          additionalProperties: true
          type: object
          title: Input Schema
        id:
          type: string
          title: Id
        mcp_server_id:
          type: string
          title: Mcp Server Id
        discovered_at:
          type: string
          title: Discovered At
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - name
      - input_schema
      - id
      - mcp_server_id
      - discovered_at
      - created_at
      - updated_at
      title: MCPToolResponse
      description: Schema for MCP tool response.
    ManagedAgentCredentialCreateRequest:
      properties:
        name:
          type: string
          maxLength: 100
          minLength: 1
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        expires_in_days:
          anyOf:
          - type: integer
            maximum: 3650.0
            minimum: 1.0
          - type: 'null'
          title: Expires In Days
          default: 365
        scopes:
          items:
            type: string
          type: array
          title: Scopes
      type: object
      required:
      - name
      title: ManagedAgentCredentialCreateRequest
      description: Request to create a durable credential for one managed agent.
    ManagedAgentCredentialCreateResponse:
      properties:
        credential:
          $ref: '#/components/schemas/ManagedAgentCredentialSummary'
        token:
          type: string
          title: Token
      type: object
      required:
      - credential
      - token
      title: ManagedAgentCredentialCreateResponse
      description: One-time response payload for a newly created agent credential.
    ManagedAgentCredentialSummary:
      properties:
        id:
          type: string
          title: Id
        api_key_id:
          type: string
          title: Api Key Id
        created_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By User Id
        name:
          type: string
          title: Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        credential_type:
          type: string
          title: Credential Type
        status:
          type: string
          title: Status
        scopes:
          items:
            type: string
          type: array
          title: Scopes
        key_prefix:
          anyOf:
          - type: string
          - type: 'null'
          title: Key Prefix
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        last_issued_at:
          type: string
          format: date-time
          title: Last Issued At
        last_used_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Used At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        revoked_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Revoked At
        revoked_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Revoked Reason
      type: object
      required:
      - id
      - api_key_id
      - name
      - credential_type
      - status
      - created_at
      - updated_at
      - last_issued_at
      title: ManagedAgentCredentialSummary
      description: Durable credential metadata for one managed agent.
    ManagedAgentDetailResponse:
      properties:
        agent:
          $ref: '#/components/schemas/ManagedAgentSummary'
        aggregate:
          $ref: '#/components/schemas/ManagedAgentUsageAggregate'
        usage_by_model:
          items:
            $ref: '#/components/schemas/GatewayUsageByModel'
          type: array
          title: Usage By Model
        activity_by_server:
          items:
            $ref: '#/components/schemas/ManagedAgentServerActivitySummary'
          type: array
          title: Activity By Server
        activity_by_tool:
          items:
            $ref: '#/components/schemas/ManagedAgentToolActivitySummary'
          type: array
          title: Activity By Tool
        sessions:
          items:
            $ref: '#/components/schemas/RuntimeSessionSummary'
          type: array
          title: Sessions
        credentials:
          items:
            $ref: '#/components/schemas/ManagedAgentCredentialSummary'
          type: array
          title: Credentials
        enrollments:
          items:
            $ref: '#/components/schemas/ManagedAgentEnrollmentSummary'
          type: array
          title: Enrollments
      type: object
      required:
      - agent
      - aggregate
      title: ManagedAgentDetailResponse
      description: One managed agent plus its recent runtime session history.
    ManagedAgentEnrollmentCreateRequest:
      properties:
        enrollment_type:
          type: string
          maxLength: 64
          minLength: 1
          title: Enrollment Type
        adapter_key:
          anyOf:
          - type: string
            maxLength: 64
          - type: 'null'
          title: Adapter Key
        status:
          type: string
          maxLength: 32
          minLength: 1
          title: Status
          default: pending
        target_config_path:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Target Config Path
        discovered_config:
          additionalProperties: true
          type: object
          title: Discovered Config
        managed_config:
          additionalProperties: true
          type: object
          title: Managed Config
        backup_metadata:
          additionalProperties: true
          type: object
          title: Backup Metadata
        validation_result:
          additionalProperties: true
          type: object
          title: Validation Result
        restore_available:
          type: boolean
          title: Restore Available
          default: false
        last_applied_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Applied At
        last_validated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Validated At
        last_restored_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Restored At
      type: object
      required:
      - enrollment_type
      title: ManagedAgentEnrollmentCreateRequest
      description: Request to persist an enrollment record for one managed agent.
    ManagedAgentEnrollmentRestoreRequest:
      properties:
        status:
          type: string
          maxLength: 32
          minLength: 1
          title: Status
          default: restored
        backup_metadata:
          additionalProperties: true
          type: object
          title: Backup Metadata
        validation_result:
          additionalProperties: true
          type: object
          title: Validation Result
      type: object
      title: ManagedAgentEnrollmentRestoreRequest
      description: Request to mark one enrollment as restored.
    ManagedAgentEnrollmentSummary:
      properties:
        id:
          type: string
          title: Id
        created_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By User Id
        enrollment_type:
          type: string
          title: Enrollment Type
        adapter_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Adapter Key
        status:
          type: string
          title: Status
        target_config_path:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Config Path
        discovered_config:
          additionalProperties: true
          type: object
          title: Discovered Config
        managed_config:
          additionalProperties: true
          type: object
          title: Managed Config
        backup_metadata:
          additionalProperties: true
          type: object
          title: Backup Metadata
        validation_result:
          additionalProperties: true
          type: object
          title: Validation Result
        restore_available:
          type: boolean
          title: Restore Available
          default: false
        last_applied_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Applied At
        last_validated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Validated At
        last_restored_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Restored At
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - id
      - enrollment_type
      - status
      - created_at
      - updated_at
      title: ManagedAgentEnrollmentSummary
      description: Durable enrollment state for one managed agent.
    ManagedAgentEnrollmentValidateRequest:
      properties:
        status:
          type: string
          maxLength: 32
          minLength: 1
          title: Status
          default: validated
        validation_result:
          additionalProperties: true
          type: object
          title: Validation Result
      type: object
      title: ManagedAgentEnrollmentValidateRequest
      description: Request to update validation state for one enrollment.
    ManagedAgentIdentityMutationCounts:
      properties:
        usage_moved:
          type: integer
          title: Usage Moved
          default: 0
        usage_deleted:
          type: integer
          title: Usage Deleted
          default: 0
        runtime_sessions_moved:
          type: integer
          title: Runtime Sessions Moved
          default: 0
        budget_spend_moved:
          type: integer
          title: Budget Spend Moved
          default: 0
        budget_spend_merged:
          type: integer
          title: Budget Spend Merged
          default: 0
        budget_policies_moved:
          type: integer
          title: Budget Policies Moved
          default: 0
        budget_policies_dropped:
          type: integer
          title: Budget Policies Dropped
          default: 0
        approvals_moved:
          type: integer
          title: Approvals Moved
          default: 0
        keys_deactivated:
          type: integer
          title: Keys Deactivated
          default: 0
        dropped_budget_policies:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Dropped Budget Policies
      type: object
      title: ManagedAgentIdentityMutationCounts
      description: Row counts returned by rekey/merge dry-run and execute responses.
    ManagedAgentMergeRequest:
      properties:
        duplicate_agent_id:
          type: string
          minLength: 1
          title: Duplicate Agent Id
        dry_run:
          type: boolean
          title: Dry Run
          default: false
      type: object
      required:
      - duplicate_agent_id
      title: ManagedAgentMergeRequest
      description: Request body for merging a duplicate managed agent into a survivor.
    ManagedAgentMergeResponse:
      properties:
        survivor:
          $ref: '#/components/schemas/ManagedAgentSummary'
        duplicate:
          $ref: '#/components/schemas/ManagedAgentSummary'
        dry_run:
          type: boolean
          title: Dry Run
        counts:
          $ref: '#/components/schemas/ManagedAgentIdentityMutationCounts'
      type: object
      required:
      - survivor
      - duplicate
      - dry_run
      - counts
      title: ManagedAgentMergeResponse
      description: Response for a merge dry-run or execute.
    ManagedAgentModelBindingSummary:
      properties:
        id:
          type: string
          title: Id
        ai_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Id
        binding_type:
          type: string
          title: Binding Type
        config_key:
          type: string
          title: Config Key
        gateway_alias:
          type: string
          title: Gateway Alias
        is_primary:
          type: boolean
          title: Is Primary
          default: false
        status:
          type: string
          title: Status
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        model_identifier:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Identifier
        ai_model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Ai Model Name
        first_seen_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: First Seen At
        last_seen_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Seen At
      type: object
      required:
      - id
      - binding_type
      - config_key
      - gateway_alias
      - status
      title: ManagedAgentModelBindingSummary
      description: One configured AI model binding for a managed agent.
    ManagedAgentModelBindingSyncItem:
      properties:
        ai_model_id:
          type: string
          title: Ai Model Id
        binding_type:
          type: string
          title: Binding Type
          default: configured
        config_key:
          type: string
          title: Config Key
        gateway_alias:
          type: string
          title: Gateway Alias
        is_primary:
          type: boolean
          title: Is Primary
          default: false
        status:
          type: string
          title: Status
          default: gateway_ready
      type: object
      required:
      - ai_model_id
      - config_key
      - gateway_alias
      title: ManagedAgentModelBindingSyncItem
      description: Upsert payload for one managed-agent model binding.
    ManagedAgentModelBindingSyncRequest:
      properties:
        bindings:
          items:
            $ref: '#/components/schemas/ManagedAgentModelBindingSyncItem'
          type: array
          title: Bindings
      type: object
      title: ManagedAgentModelBindingSyncRequest
      description: Replace request for one managed agent's configured model bindings.
    ManagedAgentRegisterRequest:
      properties:
        display_name:
          type: string
          maxLength: 255
          minLength: 1
          title: Display Name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        agent_kind:
          anyOf:
          - type: string
            maxLength: 64
            minLength: 1
          - type: 'null'
          title: Agent Kind
          description: Product kind for the agent, for example 'cursor' or 'windsurf'.
            Defaults to 'custom' when omitted. This records what the agent is; it
            does not change how it connects.
      type: object
      required:
      - display_name
      title: ManagedAgentRegisterRequest
      description: 'Request to register a custom managed agent the CLI cannot discover.


        Used when an operator wants to onboard an agent (for example a customer''s

        LangGraph agent) that has never connected through a runtime session, so a

        gateway credential can subsequently be minted for it.'
    ManagedAgentRekeyRequest:
      properties:
        new_session_source_id:
          type: string
          maxLength: 255
          minLength: 1
          title: New Session Source Id
        principal_identity:
          anyOf:
          - $ref: '#/components/schemas/PrincipalIdentityLite'
          - type: 'null'
      type: object
      required:
      - new_session_source_id
      title: ManagedAgentRekeyRequest
      description: Request body for rewriting a managed agent's durable principal
        id.
    ManagedAgentRekeyResponse:
      properties:
        agent:
          $ref: '#/components/schemas/ManagedAgentSummary'
        counts:
          $ref: '#/components/schemas/ManagedAgentIdentityMutationCounts'
      type: object
      required:
      - agent
      - counts
      title: ManagedAgentRekeyResponse
      description: Response for a successful rekey.
    ManagedAgentServerActivitySummary:
      properties:
        server_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Server Name
        call_count:
          type: integer
          title: Call Count
          default: 0
        successful_calls:
          type: integer
          title: Successful Calls
          default: 0
        failed_calls:
          type: integer
          title: Failed Calls
          default: 0
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
      type: object
      title: ManagedAgentServerActivitySummary
      description: Historical tool activity grouped by MCP server for one managed
        agent.
    ManagedAgentSummary:
      properties:
        id:
          type: string
          title: Id
        runtime_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
        owner_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner User Id
        owner_username:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner Username
        owner_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner Email
        agent_kind:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Kind
        display_name:
          type: string
          title: Display Name
        session_source_type:
          type: string
          title: Session Source Type
        session_source_id:
          type: string
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        enrollment_hostname:
          anyOf:
          - type: string
          - type: 'null'
          title: Enrollment Hostname
        identity_derivation:
          anyOf:
          - type: string
          - type: 'null'
          title: Identity Derivation
        enrolled_via:
          type: string
          title: Enrolled Via
        managed_mcp_servers:
          items:
            type: string
          type: array
          title: Managed Mcp Servers
        lifecycle_state:
          type: string
          title: Lifecycle State
          default: active
        lifecycle_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Lifecycle Reason
        lifecycle_updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Lifecycle Updated At
        is_active_now:
          type: boolean
          title: Is Active Now
          default: false
        activity_status:
          type: string
          title: Activity Status
          default: idle
        last_seen_at:
          type: string
          format: date-time
          title: Last Seen At
        started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Started At
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        ended_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Ended At
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        configured_model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Configured Model Alias
        configured_model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Configured Model Id
        configured_models:
          items:
            $ref: '#/components/schemas/ManagedAgentModelBindingSummary'
          type: array
          title: Configured Models
        latest_model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Model Alias
        latest_provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Provider Name
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
        mcp_proxy_configured:
          type: boolean
          title: Mcp Proxy Configured
          default: false
        model_gateway_configured:
          type: boolean
          title: Model Gateway Configured
          default: false
        onboarding_state:
          type: string
          title: Onboarding State
          default: incomplete
        live_validation_supported:
          type: boolean
          title: Live Validation Supported
          default: false
        live_validation_passed:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Live Validation Passed
        live_validation_status:
          type: string
          title: Live Validation Status
          default: unsupported
        last_validated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Validated At
        tags:
          additionalProperties:
            type: string
          type: object
          title: Tags
        control_feature_name:
          type: string
          title: Control Feature Name
          default: Agent Control
        control_capabilities:
          items:
            type: string
          type: array
          title: Control Capabilities
        control_state:
          type: string
          title: Control State
          default: unsupported
        control_enabled:
          type: boolean
          title: Control Enabled
          default: false
        control_online:
          type: boolean
          title: Control Online
          default: false
        supports_new_session:
          type: boolean
          title: Supports New Session
          default: false
        supports_existing_session:
          type: boolean
          title: Supports Existing Session
          default: false
        supports_voice:
          type: boolean
          title: Supports Voice
          default: false
        supports_interrupt:
          type: boolean
          title: Supports Interrupt
          default: false
        desktop:
          type: string
          enum:
          - vnc
          - rdp
          - none
          title: Desktop
          default: none
        desktop_display:
          anyOf:
          - type: string
          - type: 'null'
          title: Desktop Display
        control_session_mode:
          type: string
          title: Control Session Mode
          default: offline
        control_last_heartbeat_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Control Last Heartbeat At
        supported_input_modes:
          items:
            type: string
          type: array
          title: Supported Input Modes
        supported_output_modes:
          items:
            type: string
          type: array
          title: Supported Output Modes
      type: object
      required:
      - id
      - display_name
      - session_source_type
      - session_source_id
      - enrolled_via
      - last_seen_at
      title: ManagedAgentSummary
      description: Read-only summary for one enrolled external agent.
    ManagedAgentToolActivitySummary:
      properties:
        server_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Server Name
        tool_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Name
        call_count:
          type: integer
          title: Call Count
          default: 0
        successful_calls:
          type: integer
          title: Successful Calls
          default: 0
        failed_calls:
          type: integer
          title: Failed Calls
          default: 0
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
      type: object
      title: ManagedAgentToolActivitySummary
      description: Historical tool activity grouped by MCP server and tool name.
    ManagedAgentUpdateRequest:
      properties:
        owner_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner User Id
        display_name:
          anyOf:
          - type: string
            maxLength: 255
            minLength: 1
          - type: 'null'
          title: Display Name
        lifecycle_action:
          anyOf:
          - type: string
            pattern: ^(suspend|resume|decommission|reenroll)$
          - type: 'null'
          title: Lifecycle Action
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
        tags:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Tags
      type: object
      title: ManagedAgentUpdateRequest
      description: Operator-driven updates for one managed agent.
    ManagedAgentUsageAggregate:
      properties:
        session_count:
          type: integer
          title: Session Count
          default: 0
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        latest_model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Model Alias
        latest_provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Provider Name
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
      type: object
      required:
      - token_usage
      title: ManagedAgentUsageAggregate
      description: Historical usage aggregate across all sessions for one managed
        agent.
    MobileDeviceRegistration:
      properties:
        platform:
          type: string
          title: Platform
          description: 'Device platform: ''ios'' or ''android'''
        token:
          type: string
          minLength: 1
          title: Token
          description: Device push notification token
        device_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Device Name
          description: Optional device name for API key
      type: object
      required:
      - platform
      - token
      title: MobileDeviceRegistration
      description: Schema for registering a mobile device.
    MobileDeviceRegistrationResponse:
      properties:
        preferences:
          $ref: '#/components/schemas/NotificationPreferencesResponse'
        api_key:
          type: string
          title: Api Key
          description: API key for mobile app authentication
        api_key_id:
          type: string
          format: uuid
          title: Api Key Id
          description: API key ID
        api_key_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Api Key Expires At
          description: API key expiration
        push_registered:
          type: boolean
          title: Push Registered
          description: Whether the supplied push token was accepted and stored. False
            means pairing succeeded but push is not enabled for this device.
          default: true
        push_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Push Error
          description: Why the push token was rejected, if it was
      type: object
      required:
      - preferences
      - api_key
      - api_key_id
      title: MobileDeviceRegistrationResponse
      description: Schema for mobile device registration response with API key.
    ModelIODetectors:
      properties:
        pii:
          anyOf:
          - type: boolean
          - $ref: '#/components/schemas/PIIDetectorConfig'
          - type: 'null'
          title: Pii
          description: Enable PII scan, optionally with entity types
        injection:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Injection
          description: Enable prompt-injection heuristics
        moderation:
          anyOf:
          - type: boolean
          - $ref: '#/components/schemas/ModerationDetectorConfig'
          - type: 'null'
          title: Moderation
          description: Enable moderation check
      type: object
      title: ModelIODetectors
      description: 'Detectors enabled for one model I/O rule.


        A detector runs only when this block enables it or when a condition

        expression references its attributes (pii., injection., moderation.).'
    ModelIORule:
      properties:
        id:
          type: string
          minLength: 1
          title: Id
          description: Stable rule identifier
        target:
          $ref: '#/components/schemas/ModelIOTarget'
          description: model.request or model.response
        enabled:
          type: boolean
          title: Enabled
          description: Whether this rule is evaluated
          default: true
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Human-readable description
        approval_workflow:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow
          description: Approval workflow name for require_approval
        detectors:
          anyOf:
          - $ref: '#/components/schemas/ModelIODetectors'
          - type: 'null'
          description: Detectors to run for this rule
        detector_timeout_ms:
          type: integer
          maximum: 30000.0
          minimum: 50.0
          title: Detector Timeout Ms
          description: Hard timeout for detectors on this rule
          default: 500
        on_detector_timeout:
          $ref: '#/components/schemas/DetectorTimeoutFailMode'
          description: Fail mode when detectors time out (default deny)
          default: deny
        conditions:
          items:
            $ref: '#/components/schemas/ToolCondition'
          type: array
          minItems: 1
          title: Conditions
          description: First matching condition wins, same as tools
      type: object
      required:
      - id
      - target
      - conditions
      title: ModelIORule
      description: 'Content policy rule targeting model.request or model.response.


        Conditions use the same simple/CEL style as tool rules. Documented

        attributes include model.id, model.provider, model.name, session.id,

        request.text, response.text, pii.found, pii.types_found,

        injection.score, injection.matched_patterns, moderation.flagged,

        and moderation.categories.'
    ModelIORuleListResponse:
      properties:
        rules:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Rules
      type: object
      required:
      - rules
      title: ModelIORuleListResponse
      description: List of model I/O content policy rules.
    ModelIORulePatchRequest:
      properties:
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      type: object
      title: ModelIORulePatchRequest
      description: Partial update for enable/disable.
    ModelIOTarget:
      type: string
      enum:
      - model.request
      - model.response
      title: ModelIOTarget
      description: Stable rule targets for model request and response payloads.
    ModerationDetectorConfig:
      properties:
        backend:
          type: string
          title: Backend
          description: 'Registered moderation backend name (default: local)'
          default: local
      type: object
      title: ModerationDetectorConfig
      description: 'Moderation detector configuration.


        ``backend`` names a registered checker. The built-in ``local`` backend

        is a keyword ruleset and works without a live provider. Tests register

        a ``fake`` backend.'
    NotificationPreferencesResponse:
      properties:
        preferred_channel:
          type: string
          title: Preferred Channel
          description: 'Preferred notification channel: ''email'' or ''mobile_push'''
          default: email
        enable_email:
          type: boolean
          title: Enable Email
          description: Whether email notifications are enabled
          default: true
        enable_mobile_push:
          type: boolean
          title: Enable Mobile Push
          description: Whether mobile push notifications are enabled
          default: false
        stagger_email:
          type: boolean
          title: Stagger Email
          description: When True and push is enabled, delay approval email until the
            request is still pending after one minute
          default: true
        notify_when_needed:
          type: boolean
          title: Notify When Needed
          description: Push when the agent needs the operator (approvals, ask_user)
          default: true
        id:
          type: string
          format: uuid
          title: Id
        user_id:
          type: string
          format: uuid
          title: User Id
        mobile_device_tokens:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Mobile Device Tokens
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - id
      - user_id
      - created_at
      - updated_at
      title: NotificationPreferencesResponse
      description: Schema for notification preferences response.
    NotificationPreferencesUpdate:
      properties:
        preferred_channel:
          anyOf:
          - type: string
          - type: 'null'
          title: Preferred Channel
          description: Preferred notification channel
        enable_email:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enable Email
          description: Enable email notifications
        enable_mobile_push:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enable Mobile Push
          description: Enable mobile push notifications
        stagger_email:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Stagger Email
          description: Delay approval email when push is also enabled and the request
            is still pending
        notify_when_needed:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Notify When Needed
          description: Push when the agent needs the operator (approvals, ask_user)
      type: object
      title: NotificationPreferencesUpdate
      description: Schema for updating notification preferences.
    OnboardingRequest:
      properties:
        email:
          type: string
          title: Email
        username:
          type: string
          title: Username
        password:
          type: string
          title: Password
        full_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Full Name
        claim_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Claim Token
      type: object
      required:
      - email
      - username
      - password
      title: OnboardingRequest
    OperatorNoteAuthor:
      properties:
        user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: User Id
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
          description: Managed agent that wrote the note, when not a person.
        display:
          anyOf:
          - type: string
          - type: 'null'
          title: Display
        auth_method:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Method
      type: object
      title: OperatorNoteAuthor
      description: 'Who sent the note, as stamped into the delivered label.


        Exactly one of ``user_id`` and ``agent_id`` is set: a person wrote it, or

        a managed agent did through the ``send_note`` tool. ``auth_method`` is

        ``agent`` in the second case and is always derived server side.'
    OperatorNoteCreate:
      properties:
        text:
          type: string
          maxLength: 4096
          minLength: 1
          title: Text
          description: What the agent should be told, as the author typed it.
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
          description: Managed agent to steer, current or next session.
        runtime_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runtime Session Id
          description: One runtime session, and only that session.
        execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Execution Id
          description: Flow execution, resolved to its live runtime session.
        expires_in_seconds:
          anyOf:
          - type: integer
            maximum: 604800.0
            minimum: 60.0
          - type: 'null'
          title: Expires In Seconds
          description: 'How long the note stays deliverable. Default 24 hours: an
            undelivered note is stale advice, and expiring visibly beats rotting silently.'
      type: object
      required:
      - text
      title: OperatorNoteCreate
      description: 'Send one note to a running agent, session or flow execution.


        Exactly one target. A session id steers that conversation; an execution id

        resolves to the session that execution is running on; an agent id with no

        open session waits for the agent''s next one.'
    OperatorNoteList:
      properties:
        notes:
          items:
            $ref: '#/components/schemas/OperatorNoteResponse'
          type: array
          title: Notes
      type: object
      title: OperatorNoteList
      description: Notes for one agent, session or execution, newest first.
    OperatorNotePendingRequest:
      properties:
        channel:
          type: string
          enum:
          - hook
          - claude_channel
          - claude_message
          title: Channel
          description: Which harness transport is about to carry the note. Gateway
            delivery is recorded by the gateway itself, never by this pull.
          default: hook
        session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Id
          description: Harness session id, for logging only.
      type: object
      title: OperatorNotePendingRequest
      description: 'A harness-side pull for whatever this session has been told.


        Called by a hook, a Claude Code channel server or an inbox bridge, never

        by the model: an agent must not spend tokens deciding to check.'
    OperatorNotePendingResponse:
      properties:
        notes:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Notes
          description: 'Per note: id, the stored A2A envelope, the rendered element.'
        text:
          anyOf:
          - type: string
          - type: 'null'
          title: Text
          description: The framed block to hand the model verbatim as additional context.
            Null when nothing is pending.
        channel_event:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Channel Event
          description: The same block shaped as a Claude Code channel notification,
            so a channel server can forward it unchanged.
      type: object
      title: OperatorNotePendingResponse
      description: Pending notes, rendered once for the model and once for a machine.
    OperatorNoteResponse:
      properties:
        note_id:
          type: string
          title: Note Id
        state:
          type: string
          enum:
          - pending
          - delivered
          - acknowledged
          - cancelled
          - expired
          - failed
          title: State
        text:
          type: string
          title: Text
        managed_agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Managed Agent Id
        runtime_session_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runtime Session Id
        author:
          $ref: '#/components/schemas/OperatorNoteAuthor'
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        delivered_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Delivered At
        delivery_channel:
          anyOf:
          - type: string
            enum:
            - gateway
            - hook
            - claude_channel
            - claude_message
          - type: 'null'
          title: Delivery Channel
        delivered_turn_index:
          anyOf:
          - type: integer
          - type: 'null'
          title: Delivered Turn Index
        acknowledged_turn_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Acknowledged Turn Id
        cancelled_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Cancelled At
      type: object
      required:
      - note_id
      - state
      - text
      - author
      title: OperatorNoteResponse
      description: One note and everything the sender needs to know about its fate.
    OrganizationCreate:
      properties:
        name:
          type: string
          title: Name
          description: Organization name
        identifier:
          type: string
          title: Identifier
          description: Unique identifier for the organization
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Organization description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Organization-wide settings
      type: object
      required:
      - name
      - identifier
      title: OrganizationCreate
      description: Model for creating a new organization.
    OrganizationGroup:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        type:
          type: string
          title: Type
          default: organization
        children:
          items:
            $ref: '#/components/schemas/ProjectIdentifier'
          type: array
          title: Children
      type: object
      required:
      - id
      - name
      title: OrganizationGroup
    OrganizationResponse:
      properties:
        name:
          type: string
          title: Name
          description: Organization name
        identifier:
          type: string
          title: Identifier
          description: Unique identifier for the organization
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Organization description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Organization-wide settings
        id:
          type: string
          title: Id
          description: Organization ID
        tracker_id:
          type: string
          title: Tracker Id
          description: Tracker ID
        is_active:
          type: boolean
          title: Is Active
          description: Whether the organization is active
          default: true
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Creation timestamp
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
          description: Last update timestamp
        meta_data:
          additionalProperties: true
          type: object
          title: Meta Data
          description: Additional metadata
      type: object
      required:
      - name
      - identifier
      - id
      - tracker_id
      title: OrganizationResponse
      description: Response model for organization data.
    OrganizationUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: New organization name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: New organization description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Updated organization settings
      type: object
      title: OrganizationUpdate
      description: Model for updating an organization.
    PIIDetectorConfig:
      properties:
        types:
          items:
            type: string
          type: array
          title: Types
          description: PII entity types to scan (email, phone, credit_card)
      type: object
      title: PIIDetectorConfig
      description: 'Deterministic PII detector configuration.


        ``types`` selects which entity recognizers run. Unknown types are

        rejected at schema validation. Third-party recognizers are out of

        scope and must stay off by default.'
    PasskeySummary:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        created_at:
          type: string
          format: date-time
          title: Created At
        last_used_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Used At
      type: object
      required:
      - id
      - name
      - created_at
      title: PasskeySummary
      description: Non-sensitive passkey metadata for the settings UI.
    PasswordChangeRequest:
      properties:
        current_password:
          type: string
          title: Current Password
        new_password:
          type: string
          maxLength: 72
          minLength: 8
          title: New Password
      type: object
      required:
      - current_password
      - new_password
      title: PasswordChangeRequest
      description: Password change request schema.
    PasswordResetConfirmRequest:
      properties:
        token:
          type: string
          title: Token
        new_password:
          type: string
          maxLength: 72
          minLength: 8
          title: New Password
      type: object
      required:
      - token
      - new_password
      title: PasswordResetConfirmRequest
      description: Model for password reset confirmation.
    PasswordResetRequest:
      properties:
        email:
          type: string
          format: email
          title: Email
      type: object
      required:
      - email
      title: PasswordResetRequest
      description: Model for password reset requests.
    PickupReconciliationRequest:
      properties:
        issue_revision:
          type: string
          title: Issue Revision
        previous_execution_id:
          type: string
          format: uuid
          title: Previous Execution Id
        reason:
          type: string
          maxLength: 2000
          minLength: 1
          title: Reason
      type: object
      required:
      - issue_revision
      - previous_execution_id
      - reason
      title: PickupReconciliationRequest
      description: Approve a new revision while naming the exact implementation it
        replaces.
    PolicyDiffItem:
      properties:
        path:
          type: string
          title: Path
          description: JSON path to the changed item
        operation:
          type: string
          enum:
          - add
          - remove
          - modify
          title: Operation
          description: Type of change
        old_value:
          anyOf:
          - {}
          - type: 'null'
          title: Old Value
          description: Previous value
        new_value:
          anyOf:
          - {}
          - type: 'null'
          title: New Value
          description: New value
      type: object
      required:
      - path
      - operation
      title: PolicyDiffItem
      description: Single diff item between policies.
    PolicyDiffResult:
      properties:
        has_changes:
          type: boolean
          title: Has Changes
          description: Whether there are any changes
        changes:
          items:
            $ref: '#/components/schemas/PolicyDiffItem'
          type: array
          title: Changes
          description: List of changes
        summary:
          type: string
          title: Summary
          description: Human-readable summary of changes
      type: object
      required:
      - has_changes
      - summary
      title: PolicyDiffResult
      description: Result of comparing two policies.
    PolicyImportResult:
      properties:
        success:
          type: boolean
          title: Success
          description: Whether import was successful
        policy_name:
          type: string
          title: Policy Name
          description: Name of the imported policy
        mcp_servers_created:
          type: integer
          title: Mcp Servers Created
          description: Number of MCP servers created
          default: 0
        mcp_servers_updated:
          type: integer
          title: Mcp Servers Updated
          description: Number of MCP servers updated
          default: 0
        policies_created:
          type: integer
          title: Policies Created
          description: Number of approval workflows created
          default: 0
        policies_updated:
          type: integer
          title: Policies Updated
          description: Number of approval workflows updated
          default: 0
        tools_created:
          type: integer
          title: Tools Created
          description: Number of tool configs created
          default: 0
        tools_updated:
          type: integer
          title: Tools Updated
          description: Number of tool configs updated
          default: 0
        tools_skipped:
          type: integer
          title: Tools Skipped
          description: Number of tools skipped due to missing server references (when
            skip_missing_servers=true)
          default: 0
        model_io_rules_applied:
          type: integer
          title: Model Io Rules Applied
          description: Number of model I/O content rules applied
          default: 0
        warnings:
          items:
            type: string
          type: array
          title: Warnings
          description: Non-fatal warnings
        errors:
          items:
            type: string
          type: array
          title: Errors
          description: Errors that occurred
      type: object
      required:
      - success
      - policy_name
      title: PolicyImportResult
      description: Result of importing a policy.
    PolicyValidationError:
      properties:
        path:
          type: string
          title: Path
          description: JSON path to the error location
        message:
          type: string
          title: Message
          description: Error message
        value:
          anyOf:
          - {}
          - type: 'null'
          title: Value
          description: The invalid value
      type: object
      required:
      - path
      - message
      title: PolicyValidationError
      description: Validation error details.
    PolicyValidationResult:
      properties:
        is_valid:
          type: boolean
          title: Is Valid
          description: Whether the policy is valid
        errors:
          items:
            $ref: '#/components/schemas/PolicyValidationError'
          type: array
          title: Errors
          description: List of validation errors
        warnings:
          items:
            type: string
          type: array
          title: Warnings
          description: Non-fatal warnings
      type: object
      required:
      - is_valid
      title: PolicyValidationResult
      description: Result of policy validation.
    PolicyVersionFull:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        version_number:
          type: integer
          title: Version Number
        tag:
          anyOf:
          - type: string
          - type: 'null'
          title: Tag
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        is_active:
          type: boolean
          title: Is Active
        mcp_servers_count:
          type: integer
          title: Mcp Servers Count
        policies_count:
          type: integer
          title: Policies Count
        tools_count:
          type: integer
          title: Tools Count
        created_at:
          type: string
          title: Created At
        created_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Created By User Id
        snapshot_data:
          additionalProperties: true
          type: object
          title: Snapshot Data
      type: object
      required:
      - id
      - version_number
      - is_active
      - mcp_servers_count
      - policies_count
      - tools_count
      - created_at
      - snapshot_data
      title: PolicyVersionFull
      description: Full policy version including snapshot data.
    PolicyVersionListResponse:
      properties:
        versions:
          items:
            $ref: '#/components/schemas/PolicyVersionMetadata'
          type: array
          title: Versions
        total:
          type: integer
          title: Total
      type: object
      required:
      - versions
      - total
      title: PolicyVersionListResponse
      description: Response for listing policy versions.
    PolicyVersionMetadata:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        version_number:
          type: integer
          title: Version Number
        tag:
          anyOf:
          - type: string
          - type: 'null'
          title: Tag
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        is_active:
          type: boolean
          title: Is Active
        mcp_servers_count:
          type: integer
          title: Mcp Servers Count
        policies_count:
          type: integer
          title: Policies Count
        tools_count:
          type: integer
          title: Tools Count
        created_at:
          type: string
          title: Created At
        created_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Created By User Id
      type: object
      required:
      - id
      - version_number
      - is_active
      - mcp_servers_count
      - policies_count
      - tools_count
      - created_at
      title: PolicyVersionMetadata
      description: Metadata for a policy version (without full snapshot data).
    PriceCatalogInfo:
      properties:
        source_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Url
        fetched_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Fetched At
        model_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Model Count
      type: object
      title: PriceCatalogInfo
      description: Provenance of the vendored model-price catalog.
    PrincipalIdentity:
      properties:
        hostname:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Hostname
        config_path:
          anyOf:
          - type: string
            maxLength: 1024
          - type: 'null'
          title: Config Path
        source_type:
          anyOf:
          - type: string
            maxLength: 64
          - type: 'null'
          title: Source Type
        derivation:
          anyOf:
          - type: string
            maxLength: 16
          - type: 'null'
          title: Derivation
      type: object
      title: PrincipalIdentity
      description: Optional CLI-supplied identity metadata for a runtime principal.
    PrincipalIdentityLite:
      properties:
        hostname:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Hostname
        config_path:
          anyOf:
          - type: string
            maxLength: 1024
          - type: 'null'
          title: Config Path
        source_type:
          anyOf:
          - type: string
            maxLength: 64
          - type: 'null'
          title: Source Type
        derivation:
          anyOf:
          - type: string
            maxLength: 16
          - type: 'null'
          title: Derivation
      type: object
      title: PrincipalIdentityLite
      description: Identity metadata accepted on rekey/token flows.
    ProfileRule:
      properties:
        id:
          type: string
          title: Id
          description: Stable rule identifier recorded in evidence
        description:
          type: string
          title: Description
          description: What kind of change this rule covers
        path_globs:
          items:
            type: string
          type: array
          title: Path Globs
          description: fnmatch patterns matched against changed paths (e.g. 'backend/preloop/models/alembic/versions/*')
        commands:
          items:
            $ref: '#/components/schemas/VerificationCommand'
          type: array
          title: Commands
          description: Checks required when this rule matches
      type: object
      required:
      - id
      - description
      - path_globs
      title: ProfileRule
      description: Changed-file patterns that pull in a set of required checks.
    ProjectCreate:
      properties:
        name:
          type: string
          title: Name
          description: Project name
        identifier:
          type: string
          title: Identifier
          description: Project identifier
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Project description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Project-specific settings
        tracker_configurations:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tracker Configurations
          description: Issue tracker configurations
        organization_id:
          type: string
          title: Organization Id
          description: Organization ID
      type: object
      required:
      - name
      - identifier
      - organization_id
      title: ProjectCreate
      description: Model for creating a new project.
    ProjectIdentifier:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        identifier:
          type: string
          title: Identifier
        type:
          type: string
          title: Type
          default: project
      type: object
      required:
      - id
      - name
      - identifier
      title: ProjectIdentifier
    ProjectResponse:
      properties:
        name:
          type: string
          title: Name
          description: Project name
        identifier:
          type: string
          title: Identifier
          description: Project identifier
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Project description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Project-specific settings
        tracker_configurations:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tracker Configurations
          description: Issue tracker configurations
        id:
          type: string
          title: Id
          description: Project ID
        organization_id:
          type: string
          title: Organization Id
          description: Organization ID
        created_at:
          type: string
          title: Created At
          description: Creation timestamp
        updated_at:
          type: string
          title: Updated At
          description: Last update timestamp
      type: object
      required:
      - name
      - identifier
      - id
      - organization_id
      - created_at
      - updated_at
      title: ProjectResponse
      description: Response model for project data.
    ProjectUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: New project name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: New project description
        settings:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Settings
          description: Updated project settings
        tracker_configurations:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tracker Configurations
          description: Updated issue tracker configurations
      type: object
      title: ProjectUpdate
      description: Model for updating a project.
    PruneRequest:
      properties:
        older_than_days:
          type: integer
          title: Older Than Days
          description: Delete versions older than this many days
          default: 90
        keep_tagged:
          type: boolean
          title: Keep Tagged
          description: Keep tagged versions regardless of age
          default: true
        keep_count:
          type: integer
          title: Keep Count
          description: Always keep at least this many versions
          default: 10
      type: object
      title: PruneRequest
      description: Request to prune old versions.
    PruneResponse:
      properties:
        deleted_count:
          type: integer
          title: Deleted Count
      type: object
      required:
      - deleted_count
      title: PruneResponse
      description: Response from a prune operation.
    PullRequestItem:
      properties:
        number:
          type: integer
          title: Number
        iid:
          type: integer
          title: Iid
        title:
          type: string
          title: Title
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        url:
          type: string
          title: Url
        author:
          anyOf:
          - type: string
          - type: 'null'
          title: Author
        source_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Branch
        target_branch:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Branch
        state:
          type: string
          title: State
        draft:
          type: boolean
          title: Draft
          default: false
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
      type: object
      required:
      - number
      - iid
      - title
      - url
      - state
      title: PullRequestItem
      description: One open pull request or merge request from the tracker.
    PullRequestListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/PullRequestItem'
          type: array
          title: Items
        page:
          type: integer
          title: Page
        limit:
          type: integer
          title: Limit
        has_more:
          type: boolean
          title: Has More
        supported:
          type: boolean
          title: Supported
        fetched_at:
          type: string
          title: Fetched At
          description: ISO timestamp of the fetch
      type: object
      required:
      - items
      - page
      - limit
      - has_more
      - supported
      - fetched_at
      title: PullRequestListResponse
      description: Paginated live list of open pull/merge requests.
    QRCodeResponse:
      properties:
        token:
          type: string
          title: Token
          description: Registration token
        qr_data:
          type: string
          title: Qr Data
          description: QR code data (URL)
        expires_at:
          type: string
          title: Expires At
          description: Token expiry timestamp
        expires_in_seconds:
          type: integer
          title: Expires In Seconds
          description: Seconds until expiry
      type: object
      required:
      - token
      - qr_data
      - expires_at
      - expires_in_seconds
      title: QRCodeResponse
      description: Schema for QR code registration response.
    RateLimitByModel:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        rate_limited_requests:
          type: integer
          title: Rate Limited Requests
          default: 0
        blocked_ms:
          type: integer
          title: Blocked Ms
          default: 0
        last_rate_limited_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Rate Limited At
      type: object
      title: RateLimitByModel
      description: 429 telemetry grouped by model.
    RateLimitBySession:
      properties:
        runtime_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Session Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        rate_limited_requests:
          type: integer
          title: Rate Limited Requests
          default: 0
        blocked_ms:
          type: integer
          title: Blocked Ms
          default: 0
        last_rate_limited_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Rate Limited At
      type: object
      title: RateLimitBySession
      description: 429 telemetry grouped by runtime session (agent run).
    RateLimitSnapshotItem:
      properties:
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        observed_at:
          type: string
          format: date-time
          title: Observed At
        status_code:
          type: integer
          title: Status Code
        upstream_credential_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Upstream Credential Type
        rate_limit:
          additionalProperties: true
          type: object
          title: Rate Limit
      type: object
      required:
      - observed_at
      - status_code
      title: RateLimitSnapshotItem
      description: 'Latest observed rate-limit headers for one provider/model pair.


        ``rate_limit`` echoes the snapshot persisted from a real upstream

        response (normalized fields plus the verbatim ``headers`` map);

        ``observed_at`` timestamps that observation so consumers can label

        staleness. Nothing here is inferred.'
    RateLimitTotals:
      properties:
        rate_limited_requests:
          type: integer
          title: Rate Limited Requests
          default: 0
        blocked_ms:
          type: integer
          title: Blocked Ms
          default: 0
        last_rate_limited_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Rate Limited At
        quota_exhausted_count:
          type: integer
          title: Quota Exhausted Count
          default: 0
        transient_count:
          type: integer
          title: Transient Count
          default: 0
      type: object
      title: RateLimitTotals
      description: 'Aggregate 429 telemetry for the report window.


        ``blocked_ms`` sums the provider-advised ``Retry-After`` values observed

        on 429 responses: a lower bound on real wall-clock stall, taken directly

        from provider responses (never estimated). Subtype counts come from the

        per-row classification recorded at capture time; rows recorded before

        this feature carry no subtype and appear in neither count.'
    ReadinessContract:
      properties:
        issue_revision:
          type: string
          title: Issue Revision
        problem:
          type: string
          title: Problem
          default: ''
        user_outcome:
          type: string
          title: User Outcome
          default: ''
        constraints:
          items:
            type: string
          type: array
          title: Constraints
        non_goals:
          items:
            type: string
          type: array
          title: Non Goals
        assumptions:
          items:
            type: string
          type: array
          title: Assumptions
        blocking_decisions:
          items:
            type: string
          type: array
          title: Blocking Decisions
        conflicts:
          items:
            type: string
          type: array
          title: Conflicts
        criteria:
          items:
            $ref: '#/components/schemas/Criterion'
          type: array
          title: Criteria
        code_entry_points:
          items:
            type: string
          type: array
          title: Code Entry Points
        test_entry_points:
          items:
            type: string
          type: array
          title: Test Entry Points
        dependencies:
          items:
            type: integer
          type: array
          title: Dependencies
        environment_profile:
          type: string
          title: Environment Profile
          default: ''
        test_commands:
          items:
            type: string
          type: array
          title: Test Commands
      additionalProperties: false
      type: object
      required:
      - issue_revision
      title: ReadinessContract
      description: A proposal can be incomplete; readiness explains each missing decision.
    ReadyRequest:
      properties:
        issue_revision:
          type: string
          title: Issue Revision
      type: object
      required:
      - issue_revision
      title: ReadyRequest
      description: The reviewer approves one exact issue revision.
    RebuiltInputsRequest:
      properties:
        published_sha:
          type: string
          maxLength: 40
          minLength: 40
          title: Published Sha
        sbom_content_base64:
          type: string
          minLength: 1
          title: Sbom Content Base64
        pinned_build_ref:
          anyOf:
          - type: string
            maxLength: 512
            minLength: 1
          - type: 'null'
          title: Pinned Build Ref
      additionalProperties: false
      type: object
      required:
      - published_sha
      - sbom_content_base64
      title: RebuiltInputsRequest
      description: Authenticated rebuilt SBOM for the published commit. Not an automatic
        rebuild.
    RefreshRequest:
      properties:
        refresh_token:
          type: string
          title: Refresh Token
      type: object
      required:
      - refresh_token
      title: RefreshRequest
      description: Model for token refresh requests.
    RegistrationOptionsResponse:
      properties:
        options:
          additionalProperties: true
          type: object
          title: Options
        challenge_token:
          type: string
          title: Challenge Token
      type: object
      required:
      - options
      - challenge_token
      title: RegistrationOptionsResponse
      description: Options for navigator.credentials.create plus the challenge token.
    RegistrationVerifyRequest:
      properties:
        credential:
          additionalProperties: true
          type: object
          title: Credential
        challenge_token:
          type: string
          title: Challenge Token
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
      type: object
      required:
      - credential
      - challenge_token
      title: RegistrationVerifyRequest
      description: Authenticator response for the registration ceremony.
    ReportPublication:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether the run publishes its report as a pull request
          default: false
        source_path:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Source Path
          description: Workspace relative path of the generated document, for example
            evidence/portfolio-report.md
        destination_path:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Destination Path
          description: Repository relative path the document lands at, for example
            PORTFOLIO.md
        branch:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Branch
          description: Branch the document is maintained on. Defaults to preloop/report/<document
            slug>, which is stable across runs so a re-run updates the open pull request
            instead of opening a second
        commit_message:
          anyOf:
          - type: string
            maxLength: 512
          - type: 'null'
          title: Commit Message
          description: Commit subject. Defaults to 'Update <destination_path>'
      additionalProperties: false
      type: object
      title: ReportPublication
      description: 'Land a generated document in the repository as a pull request.


        For flows whose agent holds no write tools: the document the run produced

        is copied into a throwaway worktree of the checkout after the agent has

        exited, committed on a stable branch keyed on the document, and offered

        through the same pull request path every other change uses. Issue #648.


        ``extra=''forbid''``: a misspelled key here is a document that silently

        lands somewhere else, or not at all.'
    RepriceRequest:
      properties:
        start_date:
          type: string
          format: date-time
          title: Start Date
        end_date:
          type: string
          format: date-time
          title: End Date
        only_unpriced:
          type: boolean
          title: Only Unpriced
          default: true
        dry_run:
          type: boolean
          title: Dry Run
          default: false
      type: object
      required:
      - start_date
      - end_date
      title: RepriceRequest
      description: Request body for re-pricing historical gateway usage.
    RepriceResponse:
      properties:
        submitted_async:
          type: boolean
          title: Submitted Async
          default: false
        provider_lookup:
          anyOf:
          - additionalProperties:
              type: integer
            type: object
          - type: 'null'
          title: Provider Lookup
        job_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Job Id
        status_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Status Url
        rows_examined:
          anyOf:
          - type: integer
          - type: 'null'
          title: Rows Examined
        rows_updated:
          anyOf:
          - type: integer
          - type: 'null'
          title: Rows Updated
        rows_skipped:
          anyOf:
          - type: integer
          - type: 'null'
          title: Rows Skipped
        cost_before:
          anyOf:
          - type: number
          - type: 'null'
          title: Cost Before
        cost_after:
          anyOf:
          - type: number
          - type: 'null'
          title: Cost After
        dry_run:
          type: boolean
          title: Dry Run
          default: false
      type: object
      title: RepriceResponse
      description: 'Result of a repricing run (or async submission).


        The counters are ``None`` — not ``0`` — when the run was dispatched to a

        background worker (``submitted_async=True``): nothing has been scanned in

        this request, so reporting zeros would be indistinguishable from "the

        window really contained no rows" (the exact confusion behind "reprice

        examined 0 rows" on large windows).'
    ResolvedVerificationPolicy:
      properties:
        mode:
          type: string
          enum:
          - 'off'
          - gate
          title: Mode
          default: 'off'
        reason:
          type: string
          title: Reason
          default: no verification policy configured; publication is not gated
        profile:
          anyOf:
          - $ref: '#/components/schemas/VerificationProfile'
          - type: 'null'
        gate_budget_seconds:
          type: integer
          maximum: 14400.0
          minimum: 30.0
          title: Gate Budget Seconds
          default: 3600
      type: object
      title: ResolvedVerificationPolicy
      description: 'Effective policy as computed for one flow, for display and gating.


        Existing saved flows predate the gate: they resolve to ``mode="off"``

        with an explicit ``reason`` so consoles can *show* the effective policy

        instead of leaving behaviour implicit. Flows are never silently moved to

        the gate by a schema default; enabling it is a configuration act.'
    ResumeRequest:
      properties:
        reason:
          type: string
          maxLength: 2000
          minLength: 1
          title: Reason
      additionalProperties: false
      type: object
      required:
      - reason
      title: ResumeRequest
      description: Explicit human retry. Historical decisions stay append-only.
    RetentionClassRead:
      properties:
        record_class:
          type: string
          title: Record Class
        label:
          type: string
          title: Label
        days:
          type: integer
          title: Days
          description: Effective stored retention days; -1 means an unlimited subscription
            history promise prevents deletion
        source:
          type: string
          title: Source
          description: account, default, or subscription_history when a current/previous
            subscription requires longer storage
        floored:
          type: boolean
          title: Floored
          description: True when the stored value was raised to meet the floor
          default: false
      type: object
      required:
      - record_class
      - label
      - days
      - source
      title: RetentionClassRead
      description: Resolved retention for one record class.
    RetentionPurgePreview:
      properties:
        account_id:
          type: string
          format: uuid
          title: Account Id
        purge_enabled:
          type: boolean
          title: Purge Enabled
        classes:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Classes
        total:
          type: integer
          title: Total
      type: object
      required:
      - account_id
      - purge_enabled
      - classes
      - total
      title: RetentionPurgePreview
      description: What a purge would remove right now, without removing it.
    RetentionSettingsRead:
      properties:
        floor_days:
          type: integer
          title: Floor Days
          description: No record class can be set below this many days
        default_days:
          type: integer
          title: Default Days
          description: Applied to any class this account has not set
        max_days:
          type: integer
          title: Max Days
        classes:
          items:
            $ref: '#/components/schemas/RetentionClassRead'
          type: array
          title: Classes
        purge_enabled:
          type: boolean
          title: Purge Enabled
          description: False when this deployment never deletes. Retention is then
            a stated policy that nothing enforces.
        purge_dry_run:
          type: boolean
          title: Purge Dry Run
        purge_window_utc:
          anyOf:
          - type: string
          - type: 'null'
          title: Purge Window Utc
          description: Off-peak UTC hour window the purge runs in, e.g. '1-5'
        evidence_payload_hours:
          type: integer
          title: Evidence Payload Hours
          description: How long encrypted evidence payloads are kept. Separate from
            the 'evidence' record class, which governs the record row.
      type: object
      required:
      - floor_days
      - default_days
      - max_days
      - classes
      - purge_enabled
      - purge_dry_run
      - evidence_payload_hours
      title: RetentionSettingsRead
      description: Everything the console needs to render the retention panel.
    RetentionSettingsUpdate:
      properties:
        classes:
          additionalProperties:
            anyOf:
            - type: integer
            - type: 'null'
          type: object
          title: Classes
          description: 'Record class to days. null clears the account''s setting for
            that class. Known classes: audit, approvals, evidence, runtime_sessions,
            usage'
      type: object
      title: RetentionSettingsUpdate
      description: Full desired state. An omitted class falls back to the default.
    RollbackRequest:
      properties:
        preview_only:
          type: boolean
          title: Preview Only
          description: If true, return diff without applying changes
          default: false
      type: object
      title: RollbackRequest
      description: Request to rollback to a previous version.
    RollbackResponse:
      properties:
        success:
          type: boolean
          title: Success
        diff:
          anyOf:
          - $ref: '#/components/schemas/PolicyDiffResult'
          - type: 'null'
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
      type: object
      required:
      - success
      title: RollbackResponse
      description: Response from a rollback operation.
    RuleType:
      type: string
      enum:
      - INCLUDE
      - EXCLUDE
      title: RuleType
      description: Enum for rule types.
    RunPresetItemResult:
      properties:
        issue_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Issue Id
        issue_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Issue Key
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
        number:
          anyOf:
          - type: integer
          - type: 'null'
          title: Number
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
        execution_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Status
        execution_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Url
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        coalesced:
          type: boolean
          title: Coalesced
          default: false
      type: object
      title: RunPresetItemResult
      description: Target outcome for a preset run, including dispatch failure receipts.
    RunPresetRequest:
      properties:
        preset_slug:
          type: string
          title: Preset Slug
        target:
          anyOf:
          - $ref: '#/components/schemas/RunPresetTarget'
          - type: 'null'
        targets:
          anyOf:
          - items:
              $ref: '#/components/schemas/RunPresetTarget'
            type: array
          - type: 'null'
          title: Targets
        confirm_create:
          type: boolean
          title: Confirm Create
          default: false
      type: object
      required:
      - preset_slug
      title: RunPresetRequest
      description: Body for POST /flows/run-preset.
    RunPresetResponse:
      properties:
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
        flow_id:
          type: string
          title: Flow Id
        flow_name:
          type: string
          title: Flow Name
        flow_created:
          type: boolean
          title: Flow Created
        execution_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Url
        results:
          anyOf:
          - items:
              $ref: '#/components/schemas/RunPresetItemResult'
            type: array
          - type: 'null'
          title: Results
      type: object
      required:
      - flow_id
      - flow_name
      - flow_created
      title: RunPresetResponse
      description: Result of resolving (and optionally starting) a preset run.
    RunPresetTarget:
      properties:
        kind:
          type: string
          enum:
          - issue
          - pull_request
          title: Kind
        issue_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Issue Id
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
        number:
          anyOf:
          - type: integer
          - type: 'null'
          title: Number
      type: object
      required:
      - kind
      title: RunPresetTarget
      description: Issue or pull-request target for an ad hoc preset run.
    RunnerConcurrencyUpdate:
      properties:
        concurrency:
          type: integer
          maximum: 32.0
          minimum: 1.0
          title: Concurrency
      type: object
      required:
      - concurrency
      title: RunnerConcurrencyUpdate
      description: Edit one runner's slot ceiling from the console.
    RunnerFleetSummary:
      properties:
        runner_count:
          type: integer
          title: Runner Count
        online_runner_count:
          type: integer
          title: Online Runner Count
        last_runner_heartbeat:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Runner Heartbeat
      type: object
      required:
      - runner_count
      - online_runner_count
      title: RunnerFleetSummary
    RunnerRegisterRequest:
      properties:
        name:
          anyOf:
          - type: string
            maxLength: 200
          - type: 'null'
          title: Name
        hostname:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Hostname
        os:
          anyOf:
          - type: string
            maxLength: 30
          - type: 'null'
          title: Os
        arch:
          anyOf:
          - type: string
            maxLength: 30
          - type: 'null'
          title: Arch
        labels:
          items:
            type: string
          type: array
          title: Labels
        ephemeral:
          type: boolean
          title: Ephemeral
          default: false
        runner_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Runner Id
        instance_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Instance Id
        host_exec_profiles:
          anyOf:
          - items:
              $ref: '#/components/schemas/HostExecProfileAdvertisement'
            type: array
            maxItems: 64
          - type: 'null'
          title: Host Exec Profiles
        concurrency:
          anyOf:
          - type: integer
            maximum: 32.0
            minimum: 1.0
          - type: 'null'
          title: Concurrency
      type: object
      title: RunnerRegisterRequest
      description: Register or resume a runner for the logged-in account.
    RunnerRegisterResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        account_id:
          type: string
          format: uuid
          title: Account Id
        registered_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Registered By User Id
        instance_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Instance Id
        name:
          type: string
          title: Name
        hostname:
          anyOf:
          - type: string
          - type: 'null'
          title: Hostname
        os:
          anyOf:
          - type: string
          - type: 'null'
          title: Os
        arch:
          anyOf:
          - type: string
          - type: 'null'
          title: Arch
        labels:
          items:
            type: string
          type: array
          title: Labels
        ephemeral:
          type: boolean
          title: Ephemeral
          default: false
        status:
          type: string
          title: Status
        last_heartbeat:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Heartbeat
        current_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Current Execution Id
        concurrency:
          type: integer
          title: Concurrency
          default: 2
        reported_concurrency:
          anyOf:
          - type: integer
          - type: 'null'
          title: Reported Concurrency
        capacity:
          type: integer
          title: Capacity
          default: 2
        running_count:
          type: integer
          title: Running Count
          default: 0
        running_execution_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Running Execution Ids
        registered_by_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Registered By Email
        capabilities:
          additionalProperties: true
          type: object
          title: Capabilities
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        token:
          type: string
          title: Token
      type: object
      required:
      - id
      - account_id
      - name
      - status
      - created_at
      - updated_at
      - token
      title: RunnerRegisterResponse
    RunnerResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        account_id:
          type: string
          format: uuid
          title: Account Id
        registered_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Registered By User Id
        instance_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Instance Id
        name:
          type: string
          title: Name
        hostname:
          anyOf:
          - type: string
          - type: 'null'
          title: Hostname
        os:
          anyOf:
          - type: string
          - type: 'null'
          title: Os
        arch:
          anyOf:
          - type: string
          - type: 'null'
          title: Arch
        labels:
          items:
            type: string
          type: array
          title: Labels
        ephemeral:
          type: boolean
          title: Ephemeral
          default: false
        status:
          type: string
          title: Status
        last_heartbeat:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Heartbeat
        current_execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Current Execution Id
        concurrency:
          type: integer
          title: Concurrency
          default: 2
        reported_concurrency:
          anyOf:
          - type: integer
          - type: 'null'
          title: Reported Concurrency
        capacity:
          type: integer
          title: Capacity
          default: 2
        running_count:
          type: integer
          title: Running Count
          default: 0
        running_execution_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Running Execution Ids
        registered_by_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Registered By Email
        capabilities:
          additionalProperties: true
          type: object
          title: Capabilities
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - id
      - account_id
      - name
      - status
      - created_at
      - updated_at
      title: RunnerResponse
    RuntimeSessionActivityItem:
      properties:
        activity_type:
          type: string
          title: Activity Type
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        title:
          type: string
          title: Title
        summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Summary
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
        api_usage_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Usage Id
        tool_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Name
        server_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Server Name
        auth_subject_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Subject Type
        api_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key Id
        api_key_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key Name
        estimated_cost:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cost
        total_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Tokens
        request_fingerprint:
          anyOf:
          - type: string
          - type: 'null'
          title: Request Fingerprint
        gateway_attempt:
          anyOf:
          - type: integer
          - type: 'null'
          title: Gateway Attempt
        is_retry:
          type: boolean
          title: Is Retry
          default: false
        retry_of_api_usage_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Retry Of Api Usage Id
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      type: object
      required:
      - activity_type
      - timestamp
      - title
      title: RuntimeSessionActivityItem
      description: One activity item in a runtime session timeline.
    RuntimeSessionActivityListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/RuntimeSessionActivityItem'
          type: array
          title: Items
      type: object
      title: RuntimeSessionActivityListResponse
      description: List of activity items for a runtime session.
    RuntimeSessionCacheModelGroup:
      properties:
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        requests:
          type: integer
          title: Requests
          default: 0
        cache_read_tokens:
          type: integer
          title: Cache Read Tokens
          default: 0
        cache_creation_tokens:
          type: integer
          title: Cache Creation Tokens
          default: 0
        prompt_tokens:
          type: integer
          title: Prompt Tokens
          default: 0
        requests_with_unknown_prompt_tokens:
          type: integer
          title: Requests With Unknown Prompt Tokens
          default: 0
        write_reported:
          type: boolean
          title: Write Reported
          default: false
      type: object
      title: RuntimeSessionCacheModelGroup
      description: 'Per-(model, provider) breakdown inside the session cache rollup.


        ``requests_with_unknown_prompt_tokens`` counts this group''s rows whose

        provider reported no prompt total; their tokens are excluded from

        ``prompt_tokens`` rather than counted as zero.'
    RuntimeSessionCacheSummary:
      properties:
        requests_total:
          type: integer
          title: Requests Total
          default: 0
        requests_with_cache_data:
          type: integer
          title: Requests With Cache Data
          default: 0
        requests_without_cache_data:
          type: integer
          title: Requests Without Cache Data
          default: 0
        covered_prompt_tokens:
          type: integer
          title: Covered Prompt Tokens
          default: 0
        uncovered_prompt_tokens:
          type: integer
          title: Uncovered Prompt Tokens
          default: 0
        cached_prompt_tokens:
          type: integer
          title: Cached Prompt Tokens
          default: 0
        uncached_prompt_tokens:
          type: integer
          title: Uncached Prompt Tokens
          default: 0
        cache_write_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Cache Write Tokens
        cache_hit_ratio:
          anyOf:
          - type: number
          - type: 'null'
          title: Cache Hit Ratio
        estimated_cache_savings_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Estimated Cache Savings Usd
        savings_basis:
          anyOf:
          - type: string
          - type: 'null'
          title: Savings Basis
        savings_omitted_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Savings Omitted Reason
        requests_with_unknown_prompt_tokens:
          type: integer
          title: Requests With Unknown Prompt Tokens
          default: 0
        models:
          items:
            $ref: '#/components/schemas/RuntimeSessionCacheModelGroup'
          type: array
          title: Models
      type: object
      title: RuntimeSessionCacheSummary
      description: 'Whole-session prompt-cache rollup.


        The ratio''s denominator is the *covered* traffic only: requests whose

        provider actually reported a cache split. ``uncovered_prompt_tokens`` is

        surfaced next to it so the UI can state coverage instead of folding blind

        rows in as misses. ``requests_with_unknown_prompt_tokens`` counts rows

        whose provider reported no prompt total at all; their tokens are excluded

        from both sums rather than counted as zero.


        ``cache_write_tokens`` is ``None`` when no provider in this session has a

        billable cache-write concept at all (OpenAI, Gemini, DeepSeek), which is

        not the same statement as zero rewritten tokens.


        ``estimated_cache_savings_usd`` is computed only from explicit catalog

        input AND cache-read prices — never a multiplier-based approximation.

        ``savings_basis`` is ``catalog_exact`` when every model that contributed

        cache reads was priced, ``catalog_exact_partial`` when the figure is a

        lower bound covering only the priced models. When no contributing model is

        priced the figure is ``None`` and ``savings_omitted_reason`` says why.'
    RuntimeSessionInteractionSummary:
      properties:
        event_id:
          type: string
          title: Event Id
        title:
          type: string
          title: Title
        summary:
          type: string
          title: Summary
        key_points:
          items:
            type: string
          type: array
          title: Key Points
        risk_level:
          type: string
          title: Risk Level
          default: low
        next_action:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Action
        generated_by:
          type: string
          title: Generated By
          default: local
        model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Name
        estimated_summary_cost:
          type: number
          title: Estimated Summary Cost
          default: 0.0
      type: object
      required:
      - event_id
      - title
      - summary
      title: RuntimeSessionInteractionSummary
      description: Generated or locally derived summary for one gateway interaction.
    RuntimeSessionOptimizationActionListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/RuntimeSessionOptimizationAppliedAction'
          type: array
          title: Items
      type: object
      title: RuntimeSessionOptimizationActionListResponse
      description: Applied optimization actions for one runtime session.
    RuntimeSessionOptimizationActionSpec:
      properties:
        type:
          type: string
          title: Type
        params:
          additionalProperties: true
          type: object
          title: Params
      type: object
      required:
      - type
      title: RuntimeSessionOptimizationActionSpec
      description: 'Machine-applicable action attached to one optimization suggestion.


        Supported types: ``scope_tools`` (disable unused tools via subject-scoped

        governance), ``set_budget`` (create a scoped budget policy),

        ``enable_compression`` / ``cap_tool_results`` (subject-scoped context

        optimization transforms), ``manage_output_filter`` (open the output-filter

        UI to drop unused bulky tool-result fields; ``params``: ``server_name``,

        ``tool_name``, ``suggested_fields``, ``managed_agent_id``), and

        ``open_events`` (client-side replay deep-link to evidence events).'
    RuntimeSessionOptimizationAppliedAction:
      properties:
        id:
          type: string
          title: Id
        runtime_session_id:
          type: string
          title: Runtime Session Id
        suggestion_id:
          type: string
          title: Suggestion Id
        suggestion_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Suggestion Title
        action_type:
          type: string
          title: Action Type
        params:
          additionalProperties: true
          type: object
          title: Params
        status:
          type: string
          title: Status
          default: applied
        applied_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Applied By
        applied_at:
          type: string
          format: date-time
          title: Applied At
        result:
          additionalProperties: true
          type: object
          title: Result
        baseline:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Baseline
        outcome:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Outcome
      type: object
      required:
      - id
      - runtime_session_id
      - suggestion_id
      - action_type
      - applied_at
      title: RuntimeSessionOptimizationAppliedAction
      description: One applied optimization action, with measured outcome when available.
    RuntimeSessionOptimizationApplyRequest:
      properties:
        suggestion_id:
          type: string
          title: Suggestion Id
        suggestion_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Suggestion Title
        action:
          $ref: '#/components/schemas/RuntimeSessionOptimizationActionSpec'
      type: object
      required:
      - suggestion_id
      - action
      title: RuntimeSessionOptimizationApplyRequest
      description: Request payload to apply one suggestion's action server-side.
    RuntimeSessionOptimizationJobStatusResponse:
      properties:
        job_id:
          type: string
          title: Job Id
        status:
          type: string
          title: Status
        result:
          anyOf:
          - $ref: '#/components/schemas/RuntimeSessionOptimizationResponse'
          - type: 'null'
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
      type: object
      required:
      - job_id
      - status
      title: RuntimeSessionOptimizationJobStatusResponse
      description: Poll response for one async optimization job.
    RuntimeSessionOptimizationJobSubmitResponse:
      properties:
        job_id:
          type: string
          title: Job Id
        status:
          type: string
          title: Status
      type: object
      required:
      - job_id
      - status
      title: RuntimeSessionOptimizationJobSubmitResponse
      description: Acknowledgement for an accepted async optimization job.
    RuntimeSessionOptimizationRequest:
      properties:
        model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Id
        event_ids:
          items:
            type: string
          type: array
          title: Event Ids
        source_kinds:
          items:
            type: string
          type: array
          title: Source Kinds
        from_index:
          anyOf:
          - type: integer
          - type: 'null'
          title: From Index
        to_index:
          anyOf:
          - type: integer
          - type: 'null'
          title: To Index
        regenerate:
          type: boolean
          title: Regenerate
          default: false
        cache_only:
          type: boolean
          title: Cache Only
          default: false
      type: object
      title: RuntimeSessionOptimizationRequest
      description: Generation controls for runtime-session optimization suggestions.
    RuntimeSessionOptimizationResponse:
      properties:
        generated_by:
          type: string
          title: Generated By
          default: local
        cache_miss:
          type: boolean
          title: Cache Miss
          default: false
        fast_model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Fast Model Name
        model_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Id
        model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Name
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_optimization_cost:
          type: number
          title: Estimated Optimization Cost
          default: 0.0
        generated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Generated At
        from_cache:
          type: boolean
          title: From Cache
          default: false
        llm_skipped_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Llm Skipped Reason
        waste_score:
          anyOf:
          - type: integer
          - type: 'null'
          title: Waste Score
        potential_savings_tokens:
          type: integer
          title: Potential Savings Tokens
          default: 0
        potential_savings_usd:
          type: number
          title: Potential Savings Usd
          default: 0.0
        analyzed_scope_total_tokens:
          type: integer
          title: Analyzed Scope Total Tokens
          default: 0
        analyzed_scope_estimated_cost:
          type: number
          title: Analyzed Scope Estimated Cost
          default: 0.0
        context_profile:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Context Profile
        suggestions:
          items:
            $ref: '#/components/schemas/RuntimeSessionOptimizationSuggestion'
          type: array
          title: Suggestions
        is_example:
          type: boolean
          title: Is Example
          default: false
        example_notice:
          anyOf:
          - type: string
          - type: 'null'
          title: Example Notice
        example_provenance:
          anyOf:
          - type: string
          - type: 'null'
          title: Example Provenance
        example_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Example Title
        example_pricing_note:
          anyOf:
          - type: string
          - type: 'null'
          title: Example Pricing Note
      type: object
      title: RuntimeSessionOptimizationResponse
      description: Optimization suggestions for a runtime session.
    RuntimeSessionOptimizationSuggestion:
      properties:
        id:
          type: string
          title: Id
        title:
          type: string
          title: Title
        description:
          type: string
          title: Description
        expected_savings_tokens:
          type: integer
          title: Expected Savings Tokens
          default: 0
        expected_savings_usd:
          type: number
          title: Expected Savings Usd
          default: 0.0
        confidence:
          type: string
          title: Confidence
          default: medium
        action_label:
          type: string
          title: Action Label
        evidence:
          items:
            type: string
          type: array
          title: Evidence
        evidence_event_ids:
          items:
            type: string
          type: array
          title: Evidence Event Ids
        action:
          anyOf:
          - $ref: '#/components/schemas/RuntimeSessionOptimizationActionSpec'
          - type: 'null'
      type: object
      required:
      - id
      - title
      - description
      - action_label
      title: RuntimeSessionOptimizationSuggestion
      description: One actionable optimization suggestion for a runtime session.
    RuntimeSessionReplayCandidate:
      properties:
        removed_tool_names:
          items:
            type: string
          type: array
          title: Removed Tool Names
        filtered_output_fields:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
          title: Filtered Output Fields
      type: object
      title: RuntimeSessionReplayCandidate
      description: The candidate optimization to measure by re-execution replay.
    RuntimeSessionReplayRequest:
      properties:
        candidate:
          $ref: '#/components/schemas/RuntimeSessionReplayCandidate'
        suggestion_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Suggestion Id
        consented:
          type: boolean
          title: Consented
          default: false
        n_runs:
          type: integer
          maximum: 10.0
          minimum: 1.0
          title: N Runs
          default: 3
      type: object
      title: RuntimeSessionReplayRequest
      description: Request to verify a candidate's savings by replaying a stored request.
    RuntimeSessionReplayResponse:
      properties:
        id:
          type: string
          title: Id
        runtime_session_id:
          type: string
          title: Runtime Session Id
        status:
          type: string
          title: Status
        input_delta_tokens:
          type: integer
          title: Input Delta Tokens
        input_pct_saved:
          type: number
          title: Input Pct Saved
        end_to_end_delta_median:
          anyOf:
          - type: number
          - type: 'null'
          title: End To End Delta Median
        end_to_end_delta_low:
          anyOf:
          - type: number
          - type: 'null'
          title: End To End Delta Low
        end_to_end_delta_high:
          anyOf:
          - type: number
          - type: 'null'
          title: End To End Delta High
        inconclusive:
          type: boolean
          title: Inconclusive
        cost_spent:
          anyOf:
          - type: number
          - type: 'null'
          title: Cost Spent
        n_runs:
          type: integer
          title: N Runs
      type: object
      required:
      - id
      - runtime_session_id
      - status
      - input_delta_tokens
      - input_pct_saved
      - inconclusive
      - n_runs
      title: RuntimeSessionReplayResponse
      description: 'Outcome of a replay: the exact input delta plus the banded end-to-end.'
    RuntimeSessionRequestCache:
      properties:
        cache_read_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Cache Read Tokens
        cache_creation_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Cache Creation Tokens
        cache_miss_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Cache Miss Tokens
        cache_miss_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Cache Miss Source
        has_cache_data:
          type: boolean
          title: Has Cache Data
          default: false
        usage_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Usage Source
      type: object
      title: RuntimeSessionRequestCache
      description: 'Prompt-cache accounting for one gateway request.


        Every token field is ``Optional`` on purpose: ``None`` means *the provider

        did not report this*, and clients MUST render it as "not reported" rather

        than as zero. A ``0`` here is a real, provider-reported zero.


        ``cache_miss_source`` distinguishes a miss count the provider sent us

        (``reported`` — today only DeepSeek''s ``prompt_cache_miss_tokens``) from
        one

        we derived arithmetically as ``prompt - read - write`` (``derived``).'
    RuntimeSessionRequestItem:
      properties:
        id:
          type: string
          title: Id
        timestamp:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Timestamp
        model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Alias
        provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider Name
        status_code:
          type: integer
          title: Status Code
          default: 0
        is_error:
          type: boolean
          title: Is Error
          default: false
        error_class:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Class
        finish_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Finish Reason
        is_retry:
          type: boolean
          title: Is Retry
          default: false
        prompt_tokens:
          type: integer
          title: Prompt Tokens
          default: 0
        completion_tokens:
          type: integer
          title: Completion Tokens
          default: 0
        total_tokens:
          type: integer
          title: Total Tokens
          default: 0
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        endpoint:
          anyOf:
          - type: string
          - type: 'null'
          title: Endpoint
        auth_subject_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Auth Subject Type
        tools:
          items:
            $ref: '#/components/schemas/RuntimeSessionRequestTool'
          type: array
          title: Tools
        tools_total_schema_tokens:
          type: integer
          title: Tools Total Schema Tokens
          default: 0
        cache:
          $ref: '#/components/schemas/RuntimeSessionRequestCache'
      type: object
      required:
      - id
      title: RuntimeSessionRequestItem
      description: One per-request gateway usage row for the unified session timeline.
    RuntimeSessionRequestListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/RuntimeSessionRequestItem'
          type: array
          title: Items
        total:
          type: integer
          title: Total
          default: 0
        failed_count:
          type: integer
          title: Failed Count
          default: 0
        limit:
          type: integer
          title: Limit
          default: 100
        offset:
          type: integer
          title: Offset
          default: 0
        next_offset:
          anyOf:
          - type: integer
          - type: 'null'
          title: Next Offset
        has_more:
          type: boolean
          title: Has More
          default: false
        cache_summary:
          $ref: '#/components/schemas/RuntimeSessionCacheSummary'
      type: object
      required:
      - cache_summary
      title: RuntimeSessionRequestListResponse
      description: Paginated per-request rows for one runtime session.
    RuntimeSessionRequestTool:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        source:
          anyOf:
          - type: string
          - type: 'null'
          title: Source
        schema_tokens_estimate:
          type: integer
          title: Schema Tokens Estimate
          default: 0
        stripped:
          type: boolean
          title: Stripped
          default: false
      type: object
      title: RuntimeSessionRequestTool
      description: One tool carried by a single gateway request.
    RuntimeSessionSummary:
      properties:
        id:
          type: string
          title: Id
        session_source_type:
          type: string
          title: Session Source Type
        session_source_id:
          type: string
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        parent_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Parent Session Id
        runtime_principal_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Type
        runtime_principal_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Runtime Principal Name
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
        summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Summary
        summary_updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Summary Updated At
        started_at:
          type: string
          format: date-time
          title: Started At
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        ended_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Ended At
        flow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Id
        flow_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Name
        flow_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Flow Execution Id
        latest_model_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Model Alias
        latest_provider_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Provider Name
        is_active_now:
          type: boolean
          title: Is Active Now
          default: false
        activity_status:
          type: string
          title: Activity Status
          default: idle
        total_requests:
          type: integer
          title: Total Requests
          default: 0
        successful_requests:
          type: integer
          title: Successful Requests
          default: 0
        failed_requests:
          type: integer
          title: Failed Requests
          default: 0
        token_usage:
          $ref: '#/components/schemas/GatewayTokenUsage'
        estimated_cost:
          type: number
          title: Estimated Cost
          default: 0.0
        last_request_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Request At
        optimization_waste_score:
          anyOf:
          - type: integer
          - type: 'null'
          title: Optimization Waste Score
        optimization_potential_savings_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Optimization Potential Savings Tokens
        optimization_potential_savings_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Optimization Potential Savings Usd
        note_count:
          type: integer
          title: Note Count
          default: 0
        latest_note_author_display:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Note Author Display
        latest_note_author_auth_method:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Note Author Auth Method
        latest_note_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Latest Note At
        legal_hold:
          type: boolean
          title: Legal Hold
          description: 'True while a legal hold freezes this session: the retention
            purge leaves it and its activity alone until the hold is released'
          default: false
      type: object
      required:
      - id
      - session_source_type
      - session_source_id
      - started_at
      - token_usage
      title: RuntimeSessionSummary
      description: Aggregated runtime session summary for explorer views.
    RuntimeSessionSummaryInsight:
      properties:
        title:
          type: string
          title: Title
        description:
          type: string
          title: Description
        risk_level:
          type: string
          title: Risk Level
          default: low
        highlights:
          items:
            type: string
          type: array
          title: Highlights
        next_action:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Action
        generated_by:
          type: string
          title: Generated By
          default: local
        fast_model_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Fast Model Name
        estimated_summary_cost:
          type: number
          title: Estimated Summary Cost
          default: 0.0
      type: object
      required:
      - title
      - description
      title: RuntimeSessionSummaryInsight
      description: Generated or locally derived summary for one runtime session.
    RuntimeSessionTokenCreate:
      properties:
        session_source_type:
          type: string
          maxLength: 64
          minLength: 1
          title: Session Source Type
        session_source_id:
          type: string
          maxLength: 255
          minLength: 1
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Session Reference
        runtime_principal_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Runtime Principal Id
        runtime_principal_name:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Runtime Principal Name
        agent_kind:
          anyOf:
          - type: string
            maxLength: 64
          - type: 'null'
          title: Agent Kind
        expires_in_minutes:
          type: integer
          maximum: 1440.0
          minimum: 1.0
          title: Expires In Minutes
          default: 120
        scopes:
          items:
            type: string
          type: array
          title: Scopes
        allowed_mcp_tools:
          items: {}
          type: array
          title: Allowed Mcp Tools
        allowed_mcp_servers:
          items:
            type: string
          type: array
          title: Allowed Mcp Servers
        principal_identity:
          anyOf:
          - $ref: '#/components/schemas/PrincipalIdentity'
          - type: 'null'
      type: object
      required:
      - session_source_type
      - session_source_id
      title: RuntimeSessionTokenCreate
      description: Request model for minting a runtime-scoped session token.
    RuntimeSessionTokenResponse:
      properties:
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
        token:
          type: string
          title: Token
        expires_at:
          type: string
          format: date-time
          title: Expires At
        session_source_type:
          type: string
          title: Session Source Type
        session_source_id:
          type: string
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
      type: object
      required:
      - runtime_session_id
      - token
      - expires_at
      - session_source_type
      - session_source_id
      title: RuntimeSessionTokenResponse
      description: Response model for a minted runtime-scoped session token.
    RuntimeSessionUpdateRequest:
      properties:
        action:
          type: string
          pattern: ^(end)$
          title: Action
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
      type: object
      required:
      - action
      title: RuntimeSessionUpdateRequest
      description: Operator-driven updates for one runtime session.
    ScanFinding:
      properties:
        advisory_id:
          type: string
          maxLength: 128
          minLength: 1
          title: Advisory Id
        component_id:
          type: string
          maxLength: 512
          minLength: 1
          title: Component Id
        aliases:
          items:
            type: string
          type: array
          title: Aliases
        severity:
          type: string
          maxLength: 32
          title: Severity
          default: unknown
        present:
          type: boolean
          title: Present
          default: true
      additionalProperties: false
      type: object
      required:
      - advisory_id
      - component_id
      title: ScanFinding
      description: One advisory/component pair from a trusted scan ingest, not agent
        prose.
    ScanIngestRequest:
      properties:
        product_key:
          type: string
          maxLength: 128
          minLength: 1
          title: Product Key
        release_key:
          type: string
          maxLength: 128
          minLength: 1
          title: Release Key
        input_kind:
          type: string
          maxLength: 64
          minLength: 1
          title: Input Kind
          default: cyclonedx-json
        source:
          type: string
          maxLength: 64
          minLength: 1
          title: Source
          default: local
        execution_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Execution Id
        issue_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Issue Id
        sbom_content_base64:
          anyOf:
          - type: string
          - type: 'null'
          title: Sbom Content Base64
        workspace_files:
          items:
            $ref: '#/components/schemas/WorkspaceSeedInput'
          type: array
          title: Workspace Files
        evidence_ref:
          additionalProperties: true
          type: object
          title: Evidence Ref
        findings:
          items:
            $ref: '#/components/schemas/ScanFinding'
          type: array
          title: Findings
        available:
          type: boolean
          title: Available
          default: true
      additionalProperties: false
      type: object
      required:
      - product_key
      - release_key
      title: ScanIngestRequest
      description: Trusted controller ingest. Missing product/release is unsupported.
    SchedulePreviewRequest:
      properties:
        schedule_config:
          oneOf:
          - $ref: '#/components/schemas/CronSchedule'
          - $ref: '#/components/schemas/IntervalSchedule'
          - $ref: '#/components/schemas/DailySchedule'
          - $ref: '#/components/schemas/WeeklySchedule'
          title: Schedule Config
          discriminator:
            propertyName: type
            mapping:
              cron: '#/components/schemas/CronSchedule'
              daily: '#/components/schemas/DailySchedule'
              interval: '#/components/schemas/IntervalSchedule'
              weekly: '#/components/schemas/WeeklySchedule'
      type: object
      required:
      - schedule_config
      title: SchedulePreviewRequest
      description: Request body for previewing a schedule trigger configuration.
    SchedulePreviewResponse:
      properties:
        type:
          type: string
          title: Type
        description:
          type: string
          title: Description
        timezone:
          type: string
          title: Timezone
        next_run_times:
          items:
            type: string
            format: date-time
          type: array
          title: Next Run Times
          description: The next run times (UTC) the schedule would fire at
      type: object
      required:
      - type
      - description
      - timezone
      - next_run_times
      title: SchedulePreviewResponse
      description: Computed preview of a schedule trigger configuration.
    ScopeType:
      type: string
      enum:
      - ORGANIZATION
      - PROJECT
      title: ScopeType
      description: Enum for scope types.
    SearchResponse:
      properties:
        results:
          items:
            $ref: '#/components/schemas/SearchResultItem'
          type: array
          title: Results
      type: object
      required:
      - results
      title: SearchResponse
    SearchResultItem:
      properties:
        item_type:
          type: string
          title: Item Type
          description: 'Type of the search result item: ''issue'' or ''comment''.'
          examples:
          - issue
          - comment
        item:
          anyOf:
          - $ref: '#/components/schemas/IssueResponse'
          - $ref: '#/components/schemas/CommentResponse'
          title: Item
          description: The actual issue or comment object.
        similarity:
          type: number
          title: Similarity
          description: Similarity score of the item to the query.
      type: object
      required:
      - item_type
      - item
      - similarity
      title: SearchResultItem
    SecurityScreenRequest:
      properties:
        text:
          type: string
          maxLength: 64000
          title: Text
          description: Untrusted content to score
        hook:
          type: string
          maxLength: 100
          title: Hook
          description: 'Screening hook: user_input or tool_response'
          default: user_input
        metadata:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Metadata
          description: Opaque caller metadata (chunk coordinates, surface)
      type: object
      required:
      - text
      title: SecurityScreenRequest
      description: "One screened content chunk POSTed by a QM deployment.\n\nAttributes:\n\
        \    text: Untrusted content to score. QM sends overlapping chunks of at\n\
        \        most 1,600 characters; the cap here is a defensive bound only.\n\
        \    hook: Screening hook, ``user_input`` or ``tool_response``. Unknown\n\
        \        values are accepted for forward compatibility.\n    metadata: Optional\
        \ caller metadata (surface, origin, and chunk\n        coordinates under the\
        \ ``qm`` and provider-label namespaces).\n        Treated as opaque."
    SecurityScreenResponse:
      properties:
        score:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Score
          description: Score in [0, 1]
        threshold:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Threshold
          description: Strictness threshold in [0, 1]
        primary_outcome:
          anyOf:
          - type: string
          - type: 'null'
          title: Primary Outcome
          description: Lowercase label of the dominant rule category
      type: object
      required:
      - score
      - threshold
      title: SecurityScreenResponse
      description: "Screening verdict in the shape QM's proxy contract expects.\n\n\
        Attributes:\n    score: Finite score in [0, 1]; the chunk resolves to Strict\
        \ on the\n        caller side when the score is at or above the threshold.\n\
        \    threshold: Finite threshold in [0, 1] this deployment screens at.\n \
        \   primary_outcome: Optional lowercase label naming the dominant rule\n \
        \       category; omitted for benign content."
    SessionArtifactUsageByKind:
      properties:
        screenshot:
          type: integer
          title: Screenshot
        recording:
          type: integer
          title: Recording
      type: object
      required:
      - screenshot
      - recording
      title: SessionArtifactUsageByKind
      description: Available plaintext bytes by artifact kind.
    SessionArtifactUsageResponse:
      properties:
        used_bytes:
          type: integer
          title: Used Bytes
        budget_bytes:
          type: integer
          title: Budget Bytes
        by_kind:
          $ref: '#/components/schemas/SessionArtifactUsageByKind'
        evicted_count_30d:
          type: integer
          title: Evicted Count 30D
      type: object
      required:
      - used_bytes
      - budget_bytes
      - by_kind
      - evicted_count_30d
      title: SessionArtifactUsageResponse
      description: Account session-artifact usage against the storage budget.
    SessionEmbeddingSettingResponse:
      properties:
        enabled:
          type: boolean
          title: Enabled
          description: Whether this account has opted in to embedding.
        scope:
          type: string
          enum:
          - summaries_only
          - full
          title: Scope
          description: How much of a session is embedded.
        scope_help:
          type: string
          title: Scope Help
          description: One sentence on what the scopes cost.
          default: summaries_only embeds each session's generated title and summary,
            which is about one short chunk per session; full also embeds transcripts,
            which is roughly forty times the vectors and the provider spend for the
            same sessions. Keyword search covers the whole corpus either way.
        provider:
          type: string
          title: Provider
          description: Provider family the vectors come from.
        model_identifier:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Identifier
          description: Model name as the provider knows it.
        base_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Base Url
          description: Endpoint the text is posted to, when there is one.
        dimensions:
          type: integer
          title: Dimensions
          description: Width of the stored vectors.
        daily_cap_usd:
          anyOf:
          - type: number
          - type: 'null'
          title: Daily Cap Usd
          description: Account cap in USD; null falls back to the deployment.
        degraded_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Degraded Reason
          description: Why the last run did less than it wanted to.
        degraded_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Degraded At
          description: When that degraded state was recorded.
      type: object
      required:
      - enabled
      - scope
      - provider
      - dimensions
      title: SessionEmbeddingSettingResponse
      description: What one account's embedding setting says right now.
    SessionEmbeddingSettingUpdate:
      properties:
        scope:
          type: string
          enum:
          - summaries_only
          - full
          title: Scope
          description: summaries_only (default) or full.
      additionalProperties: false
      type: object
      required:
      - scope
      title: SessionEmbeddingSettingUpdate
      description: The one thing this endpoint lets an account change.
    SessionSavedSearchCreate:
      properties:
        name:
          type: string
          maxLength: 120
          minLength: 1
          title: Name
          description: Label shown in the saved list. Unique per author inside an
            account, compared with surrounding whitespace stripped.
        query:
          type: string
          maxLength: 512
          minLength: 1
          title: Query
          description: Search text, parsed exactly as the search endpoint parses it.
        mode:
          type: string
          enum:
          - keyword
          - semantic
          - hybrid
          title: Mode
          description: 'Mode to run in. Saved as asked: a semantic search saved on
            a day the provider was down is still a semantic search, and a run that
            cannot serve the mode answers with the degraded block rather than an error.'
          default: keyword
        filters:
          $ref: '#/components/schemas/SessionSearchFilters'
          description: Filters, validated here and stored as the validated object,
            so an unrunnable search cannot be saved.
        max_snippets_per_session:
          type: integer
          maximum: 10.0
          minimum: 0.0
          title: Max Snippets Per Session
          description: Snippets per session this saved search asks for.
          default: 3
        include_snippet_text:
          type: boolean
          title: Include Snippet Text
          description: Whether runs of this saved search return snippet text.
          default: true
        visibility:
          type: string
          enum:
          - private
          - account
          title: Visibility
          description: Defaults to private. Sharing with the account is a deliberate
            step, not something a save does on the author's behalf.
          default: private
      additionalProperties: false
      type: object
      required:
      - name
      - query
      title: SessionSavedSearchCreate
      description: Save one named search.
    SessionSavedSearchList:
      properties:
        items:
          items:
            $ref: '#/components/schemas/SessionSavedSearchRead'
          type: array
          title: Items
        total:
          type: integer
          title: Total
          description: Saved searches visible to this caller.
        limit:
          type: integer
          title: Limit
        offset:
          type: integer
          title: Offset
      type: object
      required:
      - total
      - limit
      - offset
      title: SessionSavedSearchList
      description: A page of saved searches visible to the caller.
    SessionSavedSearchRead:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        query:
          type: string
          title: Query
          description: Query as parsed, with whitespace collapsed.
        mode:
          type: string
          enum:
          - keyword
          - semantic
          - hybrid
          title: Mode
        filters:
          $ref: '#/components/schemas/SessionSearchFilters'
        max_snippets_per_session:
          type: integer
          title: Max Snippets Per Session
        include_snippet_text:
          type: boolean
          title: Include Snippet Text
        visibility:
          type: string
          enum:
          - private
          - account
          title: Visibility
        owner_user_id:
          type: string
          format: uuid
          title: Owner User Id
          description: The author, inside this account.
        owned_by_caller:
          type: boolean
          title: Owned By Caller
          description: Whether the caller is the author. Only the author may rename,
            edit, share or delete it; everyone else in the account may run a shared
            one.
        shared_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Shared At
          description: When it was last shared with the account; null while private.
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
        last_run_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Run At
          description: When it last ran, by anyone in the account.
        run_count:
          type: integer
          title: Run Count
          description: How many times it has been run through this endpoint.
          default: 0
        ranking_changed:
          type: boolean
          title: Ranking Changed
          description: Whether the ranking constants have changed since this search
            was saved or last edited. True means a run's ordering is not the one it
            was saved under; nothing about the query itself changed.
          default: false
        filter_schema_changed:
          type: boolean
          title: Filter Schema Changed
          description: Whether the stored filters were validated against an older
            filter schema than the one running now.
          default: false
      type: object
      required:
      - id
      - name
      - query
      - mode
      - filters
      - max_snippets_per_session
      - include_snippet_text
      - visibility
      - owner_user_id
      - owned_by_caller
      - created_at
      - updated_at
      title: SessionSavedSearchRead
      description: One saved search as its account sees it.
    SessionSavedSearchRunRequest:
      properties:
        limit:
          type: integer
          maximum: 50.0
          minimum: 1.0
          title: Limit
          description: Sessions per page, at most 50.
          default: 20
        offset:
          type: integer
          minimum: 0.0
          title: Offset
          description: Sessions to skip.
          default: 0
      additionalProperties: false
      type: object
      title: SessionSavedSearchRunRequest
      description: Paging for one run. The saved search owns everything else.
    SessionSavedSearchRunResponse:
      properties:
        saved_search:
          $ref: '#/components/schemas/SessionSavedSearchRead'
        unresolved_filters:
          items:
            $ref: '#/components/schemas/SessionSavedSearchUnresolvedFilter'
          type: array
          title: Unresolved Filters
          description: Saved filters that no longer name anything in this account.
            Empty when everything the search names still exists.
        ranking_changed:
          type: boolean
          title: Ranking Changed
          description: Same flag as on the saved search, repeated here so a client
            rendering only the run does not have to read it out of the saved search
            block.
          default: false
        search:
          $ref: '#/components/schemas/SessionSearchResponse'
          description: The ranked page, exactly as the search endpoint would have
            answered the same question, degraded block included.
      type: object
      required:
      - saved_search
      - search
      title: SessionSavedSearchRunResponse
      description: The saved question, what no longer resolves in it, and the answer.
    SessionSavedSearchUnresolvedFilter:
      properties:
        field:
          type: string
          title: Field
          description: Filter key, as named in the saved payload.
        value:
          anyOf:
          - type: string
          - type: 'null'
          title: Value
          description: Saved value, rendered as a string for display.
        reason:
          type: string
          title: Reason
          description: 'Why it does not resolve. One of: flow_not_found, api_key_not_found,
            source_kind_unknown, field_no_longer_defined.'
        applied:
          type: boolean
          title: Applied
          description: Whether the run still applied this filter. A filter naming
            a deleted flow is still applied, because corpus rows keep the flow id
            they were written with; only a key the schema no longer defines is left
            out, and it is reported here rather than dropped in silence.
      type: object
      required:
      - field
      - reason
      - applied
      title: SessionSavedSearchUnresolvedFilter
      description: One saved filter that no longer points at anything in the account.
    SessionSavedSearchUpdate:
      properties:
        name:
          anyOf:
          - type: string
            maxLength: 120
            minLength: 1
          - type: 'null'
          title: Name
        query:
          anyOf:
          - type: string
            maxLength: 512
            minLength: 1
          - type: 'null'
          title: Query
        mode:
          anyOf:
          - type: string
            enum:
            - keyword
            - semantic
            - hybrid
          - type: 'null'
          title: Mode
        filters:
          anyOf:
          - $ref: '#/components/schemas/SessionSearchFilters'
          - type: 'null'
        max_snippets_per_session:
          anyOf:
          - type: integer
            maximum: 10.0
            minimum: 0.0
          - type: 'null'
          title: Max Snippets Per Session
        include_snippet_text:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Include Snippet Text
        visibility:
          anyOf:
          - type: string
            enum:
            - private
            - account
          - type: 'null'
          title: Visibility
          description: Set to 'account' to share with the account, back to 'private'
            to unshare. Only the author may change it.
      additionalProperties: false
      type: object
      title: SessionSavedSearchUpdate
      description: 'Change a saved search. Every field is optional; at least one is
        required.


        Renaming, re-querying and sharing are the same operation on purpose: they

        are all "this saved question changed", and splitting them into three

        routes would say nothing extra.'
    SessionSearchDegraded:
      properties:
        keyword:
          type: boolean
          title: Keyword
          description: Whether keyword ranking contributed to this answer.
          default: true
        semantic:
          type: boolean
          title: Semantic
          description: Whether vector ranking contributed to this answer. False when
            the query could not be embedded and false when it could but the corpus
            holds no vector the query may be compared with.
          default: false
        reasons:
          items:
            type: string
          type: array
          title: Reasons
          description: 'Machine readable reason codes, empty when nothing degraded.
            One of: semantic_not_enabled, semantic_disabled_by_deployment, semantic_daily_cap_reached,
            semantic_provider_error, semantic_provider_misconfigured, semantic_model_mismatch,
            semantic_no_vectors, semantic_backfill_incomplete, fusion_candidates_truncated.'
        detail:
          anyOf:
          - type: string
          - type: 'null'
          title: Detail
          description: One sentence a console can show without decoding a code.
      type: object
      title: SessionSearchDegraded
      description: 'What the answer could not do, stated rather than implied.


        The two booleans are coverage, not health: they say which halves of the

        search are actually behind these results. ``reasons`` names every case

        that reduced coverage, and there can be more than one (a provider that

        failed while a backfill was also behind).'
    SessionSearchFilters:
      properties:
        start_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
          description: Only chunks at or after this instant. Must include a timezone
            offset; a naive value is rejected.
        end_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
          description: Only chunks strictly before this instant. Must include a timezone
            offset; a naive value is rejected.
        model_alias:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Model Alias
          description: Model alias recorded on the chunk, for example a gpt-5 alias.
        provider_name:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Provider Name
          description: Provider recorded on the chunk.
        runtime_principal_id:
          anyOf:
          - type: string
            minLength: 1
          - type: 'null'
          title: Runtime Principal Id
          description: Runtime principal, the agent or user the session ran as.
        api_key_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Api Key Id
          description: API key the traffic was attributed to.
        flow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Flow Id
          description: Flow the session belonged to.
        source_kind:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Kind
          description: 'One corpus source kind: gateway_interaction, transcript_message,
            tool_call, browser_step, operator_note, session_summary, flow_log.'
      additionalProperties: false
      type: object
      title: SessionSearchFilters
      description: 'Filters over the denormalised columns the corpus carries.


        Everything here is a column on the corpus row itself, snapshotted by the

        writer, so filtering never joins a source table and never reaches outside

        the account bound applied by the query.'
    SessionSearchRequest:
      properties:
        query:
          type: string
          maxLength: 512
          minLength: 1
          title: Query
          description: 'Search text, parsed the way a web search box is: a quoted
            phrase ("rolling restart") stays a phrase, `or` alternates and a leading
            `-` excludes.'
        mode:
          type: string
          enum:
          - keyword
          - semantic
          - hybrid
          title: Mode
          description: Requested ranking mode. A semantic or hybrid request whose
            semantic half cannot run (no opt in, cap reached, provider down) is answered
            with keyword results and a degraded marker naming the reason, never with
            an error.
          default: keyword
        filters:
          $ref: '#/components/schemas/SessionSearchFilters'
          description: Filters over the corpus columns. Unknown keys are rejected.
        limit:
          type: integer
          maximum: 50.0
          minimum: 1.0
          title: Limit
          description: Sessions per page, at most 50.
          default: 20
        offset:
          type: integer
          minimum: 0.0
          title: Offset
          description: Sessions to skip.
          default: 0
        max_snippets_per_session:
          type: integer
          maximum: 10.0
          minimum: 0.0
          title: Max Snippets Per Session
          description: Snippets to return per session, at most 10. Zero returns scored
            sessions with no snippet rows at all.
          default: 3
        include_snippet_text:
          type: boolean
          title: Include Snippet Text
          description: When false the snippet text is never generated, so no captured
            content leaves the database; the snippets still carry the identity needed
            to open the session at that turn.
          default: true
      additionalProperties: false
      type: object
      required:
      - query
      title: SessionSearchRequest
      description: One ranked search over the caller's own session corpus.
    SessionSearchResponse:
      properties:
        query:
          type: string
          title: Query
          description: Query as parsed, with whitespace collapsed.
        mode:
          type: string
          enum:
          - keyword
          - semantic
          - hybrid
          title: Mode
          description: Mode the caller asked for.
        effective_mode:
          type: string
          enum:
          - keyword
          - semantic
          - hybrid
          title: Effective Mode
          description: Mode that actually ran. A semantic or hybrid request falls
            back to keyword when the query could not be embedded at all; it stays
            semantic or hybrid when the vector half ran, even if the corpus had nothing
            for it.
        degraded:
          $ref: '#/components/schemas/SessionSearchDegraded'
        indexed_through:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Indexed Through
          description: Newest content this account has in the corpus. The corpus fills
            forward, so an empty answer older than this marker means no match, and
            one newer means not indexed yet.
        indexed_from:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Indexed From
          description: 'Oldest point in time this account''s corpus covers without
            gaps. Indexing on write only covers sessions written since search shipped,
            so on a deployment whose backfill has not run this is the deploy date
            and nothing before it is searchable. Read it with indexed_through: together
            they are the window these results actually come from. Null when the account
            has nothing indexed at all.'
        backfill_complete:
          type: boolean
          title: Backfill Complete
          description: Whether the backfill has walked this account's retained history
            to the end. False means the window above is still growing backwards, or
            that no backfill has been run.
          default: false
        backfill_state:
          type: string
          enum:
          - not_started
          - in_progress
          - complete
          title: Backfill State
          description: 'One of: not_started, in_progress, complete. ``not_started``
            on a deployment where the operator has not switched the backfill on, which
            is the shipped default.'
          default: not_started
        embedded_through:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Embedded Through
          description: Newest content this account has a vector for, from the model
            that embedded this query. Null in keyword mode and whenever the vector
            half did not run. Read against indexed_through it says how far behind
            the corpus the semantic half is.
        total:
          type: integer
          title: Total
          description: Sessions a caller can page through. In keyword mode it is the
            exact number of distinct sessions matching. In a fused mode it is the
            size of the fused candidate set, which the documented fusion depth bounds;
            the degraded block says so when that bound was reached.
        limit:
          type: integer
          title: Limit
        offset:
          type: integer
          title: Offset
        elapsed_ms:
          type: number
          title: Elapsed Ms
          description: Server side time spent on the search.
        results:
          items:
            $ref: '#/components/schemas/SessionSearchResult'
          type: array
          title: Results
      type: object
      required:
      - query
      - mode
      - effective_mode
      - degraded
      - total
      - limit
      - offset
      - elapsed_ms
      title: SessionSearchResponse
      description: Ranked sessions and everything needed to read them honestly.
    SessionSearchResult:
      properties:
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
        session_source_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
        session_source_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
        started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Started At
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        score:
          type: number
          title: Score
          description: The number this result was ordered by. In keyword mode it is
            the fused chunk relevance of the session; in a mode that ran the vector
            half it is the rank fusion score, which is on a different scale and is
            not comparable across modes.
        match_reason:
          type: string
          enum:
          - keyword
          - semantic
          - both
          title: Match Reason
          description: 'Which half of the search produced this session: the words
            (keyword), the vector (semantic) or both (both).'
          default: keyword
        similarity:
          anyOf:
          - type: number
          - type: 'null'
          title: Similarity
          description: Cosine similarity of this session's closest chunk to the query
            vector. Null for a session the vector half did not return.
        keyword_score:
          anyOf:
          - type: number
          - type: 'null'
          title: Keyword Score
          description: The session's keyword score, kept alongside the fused score
            so a fused ordering can still be read against the keyword one. Null for
            a session only the vector half returned.
        best_chunk_rank:
          type: number
          title: Best Chunk Rank
          description: Relevance of the single best keyword matched chunk. Zero for
            a session no keyword term matched.
        matched_chunk_count:
          type: integer
          title: Matched Chunk Count
          description: How many distinct chunks of this session matched the words.
        semantic_chunk_count:
          type: integer
          title: Semantic Chunk Count
          description: How many distinct chunks of this session the vector half returned
            above the similarity floor.
          default: 0
        first_match_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: First Match At
        last_match_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Match At
        snippets:
          items:
            $ref: '#/components/schemas/SessionSearchSnippet'
          type: array
          title: Snippets
      type: object
      required:
      - runtime_session_id
      - score
      - best_chunk_rank
      - matched_chunk_count
      title: SessionSearchResult
      description: One session that matched, with its score and why it is here.
    SessionSearchSnippet:
      properties:
        document_id:
          type: string
          format: uuid
          title: Document Id
          description: Corpus chunk row id.
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
          description: Session the chunk belongs to.
        source_kind:
          type: string
          title: Source Kind
          description: Which kind of turn this came from.
        source_id:
          type: string
          title: Source Id
          description: Identifier of the turn in its own table.
        chunk_index:
          type: integer
          title: Chunk Index
          description: Position of the chunk inside the turn.
        occurred_at:
          type: string
          format: date-time
          title: Occurred At
          description: When the turn happened.
        role:
          anyOf:
          - type: string
          - type: 'null'
          title: Role
          description: Role recorded for the turn.
        rank:
          type: number
          title: Rank
          description: Relevance of this chunk on its own.
        redaction_state:
          type: string
          title: Redaction State
          description: Whether the stored chunk was masked or metadata only.
        text:
          anyOf:
          - type: string
          - type: 'null'
          title: Text
          description: Database generated headline with the matching terms marked,
            or null when the request disabled snippet text. A chunk the vector half
            found and the keyword half did not carries no marked term, so the headline
            is that chunk's opening words.
        match_reason:
          type: string
          enum:
          - keyword
          - semantic
          - both
          title: Match Reason
          description: 'Which half of the search produced this chunk: the words, the
            vector, or both.'
          default: keyword
        similarity:
          anyOf:
          - type: number
          - type: 'null'
          title: Similarity
          description: Cosine similarity between the query vector and this chunk.
            Null for a chunk the vector half never scored, because a keyword match
            has no similarity to report.
      type: object
      required:
      - document_id
      - runtime_session_id
      - source_kind
      - source_id
      - chunk_index
      - occurred_at
      - rank
      - redaction_state
      title: SessionSearchSnippet
      description: 'One matching chunk of one session.


        The identity fields are the point: ``runtime_session_id`` with

        ``source_kind`` and ``source_id`` names the turn the match came from, and

        ``chunk_index`` names the piece of it, so a console or a CLI can open the

        session at that turn rather than at the top.'
    SigningKeyListRead:
      properties:
        active_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Active Key Id
        signature_schema:
          type: string
          title: Signature Schema
        signed_bytes_format:
          type: string
          title: Signed Bytes Format
          description: Exact layout of the bytes a signature covers
        keys:
          items:
            $ref: '#/components/schemas/SigningKeyRead'
          type: array
          title: Keys
      type: object
      required:
      - signature_schema
      - signed_bytes_format
      - keys
      title: SigningKeyListRead
      description: Every key this account has held, plus which one signs today.
    SigningKeyRead:
      properties:
        key_id:
          type: string
          title: Key Id
        algorithm:
          type: string
          title: Algorithm
        public_key:
          type: string
          title: Public Key
          description: Base64 raw Ed25519 public key
        active:
          type: boolean
          title: Active
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
        retired_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Retired At
      type: object
      required:
      - key_id
      - algorithm
      - public_key
      - active
      title: SigningKeyRead
      description: The public half of one signing key. Never the private half.
    SigningKeyRotateRead:
      properties:
        retired:
          anyOf:
          - $ref: '#/components/schemas/SigningKeyRead'
          - type: 'null'
        active:
          $ref: '#/components/schemas/SigningKeyRead'
      type: object
      required:
      - active
      title: SigningKeyRotateRead
      description: 'Result of a rotation: what was retired and what signs now.'
    SimilarSessionMatch:
      properties:
        document_id:
          type: string
          format: uuid
          title: Document Id
          description: Corpus chunk row id.
        source_kind:
          type: string
          title: Source Kind
          description: Which kind of turn this came from.
        source_id:
          type: string
          title: Source Id
          description: Identifier of the turn in its own table.
        chunk_index:
          type: integer
          title: Chunk Index
          description: Position of the chunk inside the turn.
        occurred_at:
          type: string
          format: date-time
          title: Occurred At
          description: When the turn happened.
        role:
          anyOf:
          - type: string
          - type: 'null'
          title: Role
          description: Role recorded for the turn.
        similarity:
          type: number
          title: Similarity
          description: Cosine similarity between this chunk and the probe chunk.
        band:
          type: string
          enum:
          - close
          - related
          - loose
          title: Band
          description: 'The similarity as a word: close, related or loose.'
        redaction_state:
          type: string
          title: Redaction State
          description: Whether the stored chunk was masked or metadata only.
        text:
          anyOf:
          - type: string
          - type: 'null'
          title: Text
          description: Opening characters of the matching chunk, or null when the
            request disabled match text or the chunk's redaction state forbids returning
            any.
        probe:
          $ref: '#/components/schemas/SimilarSessionProbe'
          description: The chunk of the session being viewed that this one is near,
            so a reader can see both ends of the match.
      type: object
      required:
      - document_id
      - source_kind
      - source_id
      - chunk_index
      - occurred_at
      - similarity
      - band
      - redaction_state
      - probe
      title: SimilarSessionMatch
      description: 'One matching chunk of the other session: the reason it is in the
        list.


        The identity fields name the turn, so a console can open the other session

        at the passage that matched rather than at its top.'
    SimilarSessionProbe:
      properties:
        document_id:
          type: string
          format: uuid
          title: Document Id
          description: Corpus chunk row id.
        source_kind:
          type: string
          title: Source Kind
          description: Which kind of turn this came from.
        source_id:
          type: string
          title: Source Id
          description: Identifier of the turn in its own table.
        chunk_index:
          type: integer
          title: Chunk Index
          description: Position of the chunk inside the turn.
        occurred_at:
          type: string
          format: date-time
          title: Occurred At
          description: When the turn happened.
        role:
          anyOf:
          - type: string
          - type: 'null'
          title: Role
          description: Role recorded for the turn.
      type: object
      required:
      - document_id
      - source_kind
      - source_id
      - chunk_index
      - occurred_at
      title: SimilarSessionProbe
      description: The passage of the viewed session that a match was found from.
    SimilarSessionResult:
      properties:
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
        session_source_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Type
        session_source_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Source Id
        session_reference:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Reference
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
        started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Started At
        last_activity_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activity At
        score:
          type: number
          title: Score
          description: 'The number this result was ordered by: the best matching pair
            of chunks plus a damped bonus for matching in several places. It is not
            a probability and is not comparable with a search score.'
        similarity:
          type: number
          title: Similarity
          description: Cosine similarity of the best matching pair of chunks.
        band:
          type: string
          enum:
          - close
          - related
          - loose
          title: Band
          description: 'The best similarity as a word: close, related or loose.'
        matched_chunk_count:
          type: integer
          title: Matched Chunk Count
          description: Distinct chunks of this session that were near a probe chunk,
            which is how broadly the two sessions overlap.
        matches:
          items:
            $ref: '#/components/schemas/SimilarSessionMatch'
          type: array
          title: Matches
          description: The closest matching chunks, strongest first.
      type: object
      required:
      - runtime_session_id
      - score
      - similarity
      - band
      - matched_chunk_count
      title: SimilarSessionResult
      description: One session similar to the one being viewed.
    SimilarSessionsDegraded:
      properties:
        semantic:
          type: boolean
          title: Semantic
          description: Whether a vector comparison actually ran. False when this session
            has no vectors to compare from.
          default: false
        reasons:
          items:
            type: string
          type: array
          title: Reasons
          description: 'Machine readable reason codes, empty when nothing narrowed
            this answer. One of: semantic_not_enabled, semantic_disabled_by_deployment,
            similar_session_not_embedded, semantic_model_mismatch, similar_no_comparable_sessions,
            semantic_backfill_incomplete, similar_session_sampled, similar_window_applied.'
        detail:
          anyOf:
          - type: string
          - type: 'null'
          title: Detail
          description: One sentence a console can show without decoding a code.
      type: object
      title: SimilarSessionsDegraded
      description: What this answer could not compare, stated rather than implied.
    SimilarSessionsResponse:
      properties:
        runtime_session_id:
          type: string
          format: uuid
          title: Runtime Session Id
          description: The session the list is about.
        model_identity:
          anyOf:
          - type: string
          - type: 'null'
          title: Model Identity
          description: Vector space the comparison ran in, as <provider>:<model>@<dimensions>.
            Null when nothing was compared. Sessions embedded with another model were
            not candidates.
        embedded_chunks:
          type: integer
          title: Embedded Chunks
          description: Chunks of this session that carry a vector of that model.
          default: 0
        probe_chunks:
          type: integer
          title: Probe Chunks
          description: Chunks of this session actually used as the query. Fewer than
            embedded_chunks means the session was represented by an evenly spread
            sample of itself, and the degraded block says so.
          default: 0
        pending_chunks:
          type: integer
          title: Pending Chunks
          description: Chunks of this session still waiting for a vector.
          default: 0
        window_days:
          anyOf:
          - type: integer
          - type: 'null'
          title: Window Days
          description: Time window the comparison was limited to, or null when the
            whole corpus was compared. No window applies by default.
        limit:
          type: integer
          title: Limit
          description: Sessions asked for, at most 20.
          default: 5
        max_matches_per_session:
          type: integer
          title: Max Matches Per Session
          description: Matching chunks asked for per session, at most 5.
          default: 2
        degraded:
          $ref: '#/components/schemas/SimilarSessionsDegraded'
        elapsed_ms:
          type: number
          title: Elapsed Ms
          description: Server side time spent building this list.
        results:
          items:
            $ref: '#/components/schemas/SimilarSessionResult'
          type: array
          title: Results
      type: object
      required:
      - runtime_session_id
      - degraded
      - elapsed_ms
      title: SimilarSessionsResponse
      description: Sessions like this one, and everything needed to read the list.
    SpeechToTextResponse:
      properties:
        text:
          type: string
          title: Text
        ai_model_id:
          type: string
          format: uuid
          title: Ai Model Id
        provider_name:
          type: string
          title: Provider Name
        model_identifier:
          type: string
          title: Model Identifier
      type: object
      required:
      - text
      - ai_model_id
      - provider_name
      - model_identifier
      title: SpeechToTextResponse
      description: Transcript returned from a resolved STT model.
    SubjectGovernanceConfig:
      properties:
        allowed_models:
          items:
            type: string
          type: array
          title: Allowed Models
        model_budgets:
          additionalProperties:
            additionalProperties: true
            type: object
          type: object
          title: Model Budgets
        tool_rules:
          additionalProperties:
            items:
              additionalProperties: true
              type: object
            type: array
          type: object
          title: Tool Rules
        tool_enabled_overrides:
          additionalProperties:
            type: boolean
          type: object
          title: Tool Enabled Overrides
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
          description: Approval workflow that governs this subject's native tool-call
            approvals (agents permission-check). Falls back to the account default
            workflow when unset.
        native_tool_approvals:
          anyOf:
          - type: string
            enum:
            - enforce
            - 'off'
          - type: 'null'
          title: Native Tool Approvals
          description: Whether native tool-call approvals (agents permission-check)
            are enforced for this subject. "off" makes the server auto-approve escalated
            (ask) calls instead of asking a human; the calls are still recorded as
            approval requests marked auto_approved_reason='native_tool_approvals_off'
            and audited with no approver, so the audit trail stays complete. Unlike
            a time-boxed approval bypass this setting does not expire; it stays off
            until changed. Absent/None means "enforce".
      type: object
      title: SubjectGovernanceConfig
    SubjectGovernanceResponse:
      properties:
        subject_type:
          type: string
          title: Subject Type
        subject_id:
          type: string
          title: Subject Id
        config:
          $ref: '#/components/schemas/SubjectGovernanceConfig'
      type: object
      required:
      - subject_type
      - subject_id
      - config
      title: SubjectGovernanceResponse
    SupportedReleaseCreate:
      properties:
        product_key:
          type: string
          maxLength: 128
          minLength: 1
          title: Product Key
        release_key:
          type: string
          maxLength: 128
          minLength: 1
          title: Release Key
        display_name:
          type: string
          maxLength: 255
          minLength: 1
          title: Display Name
        project_id:
          type: string
          format: uuid
          title: Project Id
        pinned_build_ref:
          type: string
          maxLength: 512
          minLength: 1
          title: Pinned Build Ref
        sbom_input_ref:
          type: string
          maxLength: 512
          minLength: 1
          title: Sbom Input Ref
        audit_flow_id:
          type: string
          format: uuid
          title: Audit Flow Id
        implementation_flow_id:
          type: string
          format: uuid
          title: Implementation Flow Id
        recheck_flow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Recheck Flow Id
        approval_workflow_id:
          type: string
          format: uuid
          title: Approval Workflow Id
        approval_owner_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Approval Owner User Id
        escalation_user_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Escalation User Ids
        escalation_after_seconds:
          type: integer
          maximum: 2592000.0
          minimum: 60.0
          title: Escalation After Seconds
          default: 604800
        max_retries:
          type: integer
          maximum: 20.0
          minimum: 0.0
          title: Max Retries
          default: 3
        allowed_model_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Allowed Model Ids
        allowed_input_kinds:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Allowed Input Kinds
        enabled:
          type: boolean
          title: Enabled
          default: true
      additionalProperties: false
      type: object
      required:
      - product_key
      - release_key
      - display_name
      - project_id
      - pinned_build_ref
      - sbom_input_ref
      - audit_flow_id
      - implementation_flow_id
      - approval_workflow_id
      title: SupportedReleaseCreate
      description: Opt-in inventory row. Creating a row is the only way to enroll
        a release.
    SupportedReleaseUpdate:
      properties:
        display_name:
          anyOf:
          - type: string
            maxLength: 255
            minLength: 1
          - type: 'null'
          title: Display Name
        pinned_build_ref:
          anyOf:
          - type: string
            maxLength: 512
            minLength: 1
          - type: 'null'
          title: Pinned Build Ref
        sbom_input_ref:
          anyOf:
          - type: string
            maxLength: 512
            minLength: 1
          - type: 'null'
          title: Sbom Input Ref
        audit_flow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Audit Flow Id
        implementation_flow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Implementation Flow Id
        recheck_flow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Recheck Flow Id
        approval_workflow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Approval Workflow Id
        approval_owner_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Approval Owner User Id
        escalation_user_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Escalation User Ids
        escalation_after_seconds:
          anyOf:
          - type: integer
            maximum: 2592000.0
            minimum: 60.0
          - type: 'null'
          title: Escalation After Seconds
        max_retries:
          anyOf:
          - type: integer
            maximum: 20.0
            minimum: 0.0
          - type: 'null'
          title: Max Retries
        allowed_model_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Allowed Model Ids
        allowed_input_kinds:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Allowed Input Kinds
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      additionalProperties: false
      type: object
      title: SupportedReleaseUpdate
      description: Partial inventory update. Identity keys are immutable.
    TestConnectionRequest:
      properties:
        organization:
          type: string
          title: Organization
          description: Organization identifier (name or ID)
        project:
          type: string
          title: Project
          description: Project identifier (name or ID)
      type: object
      required:
      - organization
      - project
      title: TestConnectionRequest
      description: Request model for testing a project connection.
    TestConnectionResponse:
      properties:
        success:
          type: boolean
          title: Success
          description: Whether the connection test was successful
        message:
          type: string
          title: Message
          description: Connection test result message
        details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Details
          description: Additional details about the connection
      type: object
      required:
      - success
      - message
      title: TestConnectionResponse
      description: Response model for testing a project connection.
    TestPushDeviceResult:
      properties:
        platform:
          type: string
          title: Platform
          description: Device platform
        token:
          type: string
          title: Token
          description: Masked device token
        transport:
          type: string
          title: Transport
          description: 'Delivery path used: apns/fcm/proxy/none'
        success:
          type: boolean
          title: Success
          description: Whether the provider accepted the send
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: Verbatim provider error
        error_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Reason
          description: Machine-readable reason code
        remediation:
          anyOf:
          - type: string
          - type: 'null'
          title: Remediation
          description: Operator-facing fix hint
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
          description: Firebase project of the server credentials (FCM only)
        status_code:
          anyOf:
          - type: integer
          - type: 'null'
          title: Status Code
          description: Provider HTTP status (APNs)
        pruned:
          type: boolean
          title: Pruned
          description: Whether the dead token was removed
          default: false
      type: object
      required:
      - platform
      - token
      - transport
      - success
      title: TestPushDeviceResult
      description: Per-device outcome of a test push send.
    TestPushRequest:
      properties:
        kind:
          type: string
          enum:
          - approval
          - question
          title: Kind
          description: '''approval'' for a test approval request, ''question'' for
            a test ask_user question'
          default: approval
        platform:
          anyOf:
          - type: string
            enum:
            - ios
            - android
          - type: 'null'
          title: Platform
          description: Restrict the test to one platform; default is all devices
      type: object
      title: TestPushRequest
      description: Schema for triggering an admin diagnostic test push.
    TestPushResponse:
      properties:
        kind:
          type: string
          title: Kind
          description: Kind of test sent
        request_id:
          type: string
          title: Request Id
          description: Synthetic request id; intentionally not persisted
        sent:
          type: integer
          title: Sent
          description: Number of devices that accepted the send
        failed:
          type: integer
          title: Failed
          description: Number of devices that failed
        results:
          items:
            $ref: '#/components/schemas/TestPushDeviceResult'
          type: array
          title: Results
          description: Per-device results
      type: object
      required:
      - kind
      - request_id
      - sent
      - failed
      title: TestPushResponse
      description: Schema for the result of an admin diagnostic test push.
    TextToSpeechRequest:
      properties:
        input:
          type: string
          maxLength: 16000
          minLength: 1
          title: Input
        voice:
          type: string
          maxLength: 100
          minLength: 1
          title: Voice
          default: alloy
        response_format:
          type: string
          enum:
          - mp3
          - opus
          - aac
          - flac
          - wav
          - pcm
          title: Response Format
          default: mp3
        ai_model_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Ai Model Id
      type: object
      required:
      - input
      title: TextToSpeechRequest
      description: Text payload for server-side TTS fallback.
    Token:
      properties:
        access_token:
          type: string
          title: Access Token
        refresh_token:
          type: string
          title: Refresh Token
        token_type:
          type: string
          title: Token Type
        expires_in:
          type: integer
          title: Expires In
      type: object
      required:
      - access_token
      - refresh_token
      - token_type
      - expires_in
      title: Token
      description: Token response model.
    ToolApprovalConditionCreate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Optional human-readable name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description
        is_enabled:
          type: boolean
          title: Is Enabled
          description: Whether the condition is enabled
          default: true
        condition_type:
          type: string
          title: Condition Type
          description: 'Type of condition evaluator: ''argument'', ''state'', ''risk'''
          default: argument
        condition_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Expression
          description: CEL expression for conditional approval
        condition_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Condition Config
          description: Additional configuration
        tool_configuration_id:
          type: string
          format: uuid
          title: Tool Configuration Id
          description: Tool configuration ID
      type: object
      required:
      - tool_configuration_id
      title: ToolApprovalConditionCreate
      description: Schema for creating a tool approval condition.
    ToolApprovalConditionResponse:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Optional human-readable name
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description
        is_enabled:
          type: boolean
          title: Is Enabled
          description: Whether the condition is enabled
          default: true
        condition_type:
          type: string
          title: Condition Type
          description: 'Type of condition evaluator: ''argument'', ''state'', ''risk'''
          default: argument
        condition_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Condition Expression
          description: CEL expression for conditional approval
        condition_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Condition Config
          description: Additional configuration
        id:
          type: string
          format: uuid
          title: Id
        account_id:
          type: string
          format: uuid
          title: Account Id
        tool_configuration_id:
          type: string
          format: uuid
          title: Tool Configuration Id
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - id
      - account_id
      - tool_configuration_id
      - created_at
      - updated_at
      title: ToolApprovalConditionResponse
      description: Schema for tool approval condition response.
    ToolCondition:
      properties:
        expression:
          type: string
          title: Expression
          description: Expression to evaluate against tool args
        action:
          $ref: '#/components/schemas/ConditionAction'
          description: Action when condition matches
          default: require_approval
        condition_type:
          $ref: '#/components/schemas/ConditionType'
          description: 'Expression type: ''simple'' (open source) or ''cel'' (enterprise)'
          default: simple
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Human-readable description of this condition
      type: object
      required:
      - expression
      title: ToolCondition
      description: "Condition for when to apply actions to tool invocations.\n\nSupports\
        \ two types of conditions for the open core model:\n\nSimple conditions (open\
        \ source):\n    Basic comparisons using Python-like syntax. These are evaluated\n\
        \    using a lightweight parser that supports basic operators.\n\n    Supported\
        \ operators: ==, !=, >, <, >=, <=\n\n    Examples:\n        - \"args.amount\
        \ > 500\"\n        - \"args.recipient == 'bob'\"\n        - \"args.count <=\
        \ 10\"\n\nCEL conditions (enterprise):\n    Full CEL (Common Expression Language)\
        \ expressions with advanced\n    functions like contains(), startsWith(),\
        \ endsWith(), exists(), etc.\n\n    Examples:\n        - \"args.command.contains('rm\
        \ -rf')\"\n        - \"args.path.startsWith('/etc/')\"\n        - \"args.environment\
        \ == 'production' && args.force == true\"\n        - \"args.tags.exists(t,\
        \ t == 'sensitive')\"\n\nAttributes:\n    expression: Expression to evaluate\
        \ against tool arguments.\n    action: Action to take when condition matches.\n\
        \    condition_type: Type of expression - 'simple' (open source) or 'cel'\
        \ (enterprise).\n    description: Optional human-readable description."
    ToolConfigurationCreate:
      properties:
        tool_name:
          type: string
          title: Tool Name
        tool_source:
          type: string
          title: Tool Source
          default: builtin
        mcp_server_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Mcp Server Id
          description: Reference to MCP server (if tool_source='mcp')
        http_endpoint_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Http Endpoint Id
          description: 'Reference to HTTP endpoint (future: if tool_source=''http'')'
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
          description: Whether the tool is enabled
          default: true
        approval_workflow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Approval Workflow Id
          description: Reference to approval workflow (approval required if set and
            condition matches)
        tool_description:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Description
          description: Description of what the tool does
        tool_schema:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tool Schema
          description: JSON schema for tool parameters
        custom_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Custom Config
          description: Additional configuration options
        justification_mode:
          anyOf:
          - type: string
            enum:
            - optional
            - required
          - type: 'null'
          title: Justification Mode
          description: 'Justification parameter mode: null (disabled), ''optional'',
            or ''required'''
        account_id:
          type: string
          title: Account Id
        managed_agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Managed Agent Id
          description: Optional managed-agent scope. Null = account-wide (default);
            set = the configuration applies only to that agent
      type: object
      required:
      - tool_name
      - account_id
      title: ToolConfigurationCreate
      description: Schema for creating tool configuration.
    ToolConfigurationResponse:
      properties:
        tool_name:
          type: string
          title: Tool Name
        tool_source:
          type: string
          title: Tool Source
        mcp_server_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Mcp Server Id
        http_endpoint_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Http Endpoint Id
        is_enabled:
          type: boolean
          title: Is Enabled
        approval_workflow_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Approval Workflow Id
        tool_description:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Description
          description: Description of what the tool does
        tool_schema:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tool Schema
          description: JSON schema for tool parameters
        custom_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Custom Config
          description: Additional configuration options
        justification_mode:
          anyOf:
          - type: string
          - type: 'null'
          title: Justification Mode
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
        account_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Account Id
        managed_agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Managed Agent Id
        created_at:
          type: string
          format: date-time
          title: Created At
        updated_at:
          type: string
          format: date-time
          title: Updated At
      type: object
      required:
      - tool_name
      - tool_source
      - is_enabled
      - id
      - account_id
      - created_at
      - updated_at
      title: ToolConfigurationResponse
      description: Schema for tool configuration response.
    ToolConfigurationUpdate:
      properties:
        tool_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Name
          description: Name of the tool
        tool_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Source
          description: 'Source type: ''builtin'', ''mcp'', ''http'', ''agent'''
          default: builtin
        mcp_server_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Mcp Server Id
          description: Reference to MCP server (if tool_source='mcp')
        http_endpoint_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Http Endpoint Id
          description: 'Reference to HTTP endpoint (future: if tool_source=''http'')'
        is_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Enabled
          description: Whether the tool is enabled
          default: true
        approval_workflow_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Approval Workflow Id
          description: Reference to approval workflow (approval required if set and
            condition matches)
        tool_description:
          anyOf:
          - type: string
          - type: 'null'
          title: Tool Description
          description: Description of what the tool does
        tool_schema:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Tool Schema
          description: JSON schema for tool parameters
        custom_config:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Custom Config
          description: Additional configuration options
        justification_mode:
          anyOf:
          - type: string
            enum:
            - optional
            - required
          - type: 'null'
          title: Justification Mode
          description: 'Justification parameter mode: null (disabled), ''optional'',
            or ''required'''
      type: object
      title: ToolConfigurationUpdate
      description: Schema for updating tool configuration.
    ToolUsageStatsResponse:
      properties:
        period_start:
          type: string
          format: date-time
          title: Period Start
        period_end:
          type: string
          format: date-time
          title: Period End
        tools:
          items:
            $ref: '#/components/schemas/GatewayUsageByTool'
          type: array
          title: Tools
      type: object
      required:
      - period_start
      - period_end
      title: ToolUsageStatsResponse
      description: Account-wide tool invocation and schema-cost stats.
    TrackerCreateResponse:
      properties:
        id:
          type: string
          title: Id
        warnings:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Warnings
        unlocked_tool_names:
          items:
            type: string
          type: array
          title: Unlocked Tool Names
      type: object
      required:
      - id
      title: TrackerCreateResponse
      description: Response model for tracker creation.
    TrackerResponse:
      properties:
        name:
          type: string
          title: Name
          description: User-friendly name for the tracker
        tracker_type:
          $ref: '#/components/schemas/TrackerType'
          description: Type of the issue tracker
        url:
          anyOf:
          - type: string
            maxLength: 2083
            minLength: 1
            format: uri
          - type: 'null'
          title: Url
          description: URL of the tracker instance (required for Jira)
        is_active:
          type: boolean
          title: Is Active
          description: Whether the tracker is active
          default: true
        connection_details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Connection Details
          description: Tracker-specific connection details
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Additional metadata
        subscribed_events:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Subscribed Events
          description: List of specific webhook event names to subscribe to. Empty
            list implies default/all events based on client logic.
        jira_webhook_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Jira Webhook Id
          description: Stored Jira Webhook ID
        id:
          type: string
          format: uuid
          title: Id
          description: Tracker unique identifier (UUID)
        account_id:
          type: string
          format: uuid
          title: Account Id
          description: Account ID owning this tracker
        is_valid:
          type: boolean
          title: Is Valid
          description: Whether the connection is validated
          default: false
        last_validation:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Validation
          description: Timestamp of the last validation attempt
        validation_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Validation Message
          description: Result message from the last validation
        created:
          type: string
          format: date-time
          title: Created
          description: Creation timestamp
        last_updated:
          type: string
          format: date-time
          title: Last Updated
          description: Last update timestamp
        scope_rules:
          items:
            $ref: '#/components/schemas/TrackerScopeRuleResponse'
          type: array
          title: Scope Rules
          description: List of scope rules for the tracker
        auth_type:
          type: string
          title: Auth Type
          description: 'How the tracker authenticates: ''api_token'', ''github_app''
            or ''oauth_app'''
          default: api_token
        oauth_installation_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Oauth Installation Id
          description: OAuth App installation this tracker is bound to (OAuth auth
            types only)
        github_installation_target_login:
          anyOf:
          - type: string
          - type: 'null'
          title: Github Installation Target Login
          description: Login of the account the bound installation targets (OAuth
            auth types only)
        degraded_projects:
          items:
            type: string
          type: array
          title: Degraded Projects
          description: 'Project identifiers seen on webhooks that never resolved.


            These are repositories/projects the tracker receives webhooks for but

            which repeated syncs could not import -- almost always because the

            integration''s scope excludes them (GitHub App installed on selected

            repositories only, or an EXCLUDE scope rule). Surfaced so the user can

            fix the scope instead of the sync retrying forever.'
          readOnly: true
      type: object
      required:
      - name
      - tracker_type
      - id
      - account_id
      - created
      - last_updated
      - degraded_projects
      title: TrackerResponse
      description: Response model for tracker data (excluding sensitive info like
        api_key).
    TrackerScopeRuleCreate:
      properties:
        scope_type:
          $ref: '#/components/schemas/ScopeType'
          description: The type of scope (ORGANIZATION or PROJECT).
        rule_type:
          $ref: '#/components/schemas/RuleType'
          description: The type of rule (INCLUDE or EXCLUDE).
        identifier:
          type: string
          title: Identifier
          description: The identifier for the scope (e.g., 'my-org' or 'my-org/my-repo').
      type: object
      required:
      - scope_type
      - rule_type
      - identifier
      title: TrackerScopeRuleCreate
      description: Schema for creating a new tracker scope rule.
    TrackerScopeRuleResponse:
      properties:
        scope_type:
          $ref: '#/components/schemas/ScopeType'
          description: The type of scope (ORGANIZATION or PROJECT).
        rule_type:
          $ref: '#/components/schemas/RuleType'
          description: The type of rule (INCLUDE or EXCLUDE).
        identifier:
          type: string
          title: Identifier
          description: The identifier for the scope (e.g., 'my-org' or 'my-org/my-repo').
        id:
          type: string
          title: Id
          description: The unique ID of the scope rule.
      type: object
      required:
      - scope_type
      - rule_type
      - identifier
      - id
      title: TrackerScopeRuleResponse
      description: Schema for responding with tracker scope rule details.
    TrackerTestRequest:
      properties:
        tracker_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Tracker Id
          description: Tracker unique identifier (UUID)
        tracker_type:
          $ref: '#/components/schemas/TrackerType'
          description: Type of the issue tracker
        url:
          anyOf:
          - type: string
          - type: 'null'
          title: Url
          description: URL of the tracker instance
        api_key:
          type: string
          title: Api Key
          description: API key or token for the tracker
        connection_details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Connection Details
          description: Tracker-specific connection details
        organization_identifier:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Identifier
          description: Identifier for the organization to fetch projects from
      type: object
      required:
      - tracker_type
      - api_key
      title: TrackerTestRequest
      description: Model for testing tracker connection and listing projects.
    TrackerTestResponse:
      properties:
        success:
          type: boolean
          title: Success
          description: Whether the connection test was successful
        message:
          type: string
          title: Message
          description: Connection test result message
        orgs:
          anyOf:
          - items:
              $ref: '#/components/schemas/OrganizationGroup'
            type: array
          - type: 'null'
          title: Orgs
          description: List of organizations if connection succeeded
      type: object
      required:
      - success
      - message
      title: TrackerTestResponse
      description: Response model for testing tracker connection.
    TrackerType:
      type: string
      enum:
      - github
      - gitlab
      - jira
      title: TrackerType
      description: Enum for tracker types.
    TrackerUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: New name for the tracker
        url:
          anyOf:
          - type: string
          - type: 'null'
          title: Url
          description: New URL for the tracker instance
        api_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Key
          description: New API key or token for the tracker
        is_active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Active
          description: New active status
        connection_details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Connection Details
          description: Updated connection details
        meta_data:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta Data
          description: Updated metadata
        scope_rules:
          anyOf:
          - items:
              $ref: '#/components/schemas/TrackerScopeRuleCreate'
            type: array
          - type: 'null'
          title: Scope Rules
          description: Updated list of scope rules for the tracker
        subscribed_events:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Subscribed Events
          description: Updated list of specific webhook event names to subscribe to.
        jira_webhook_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Jira Webhook Id
          description: Updated Jira Webhook ID
        jira_webhook_secret:
          anyOf:
          - type: string
          - type: 'null'
          title: Jira Webhook Secret
          description: Updated Secret for Jira webhook validation (handle with care)
      type: object
      title: TrackerUpdate
      description: Model for updating an existing tracker.
    UnpricedModelUsage:
      properties:
        model:
          type: string
          title: Model
        requests:
          type: integer
          title: Requests
          default: 0
        tokens:
          type: integer
          title: Tokens
          default: 0
      type: object
      required:
      - model
      title: UnpricedModelUsage
      description: 'One model''s share of the window''s unpriced gateway usage.


        Names the models behind ``unpriced_requests``/``unpriced_tokens`` so the

        console can say WHICH models are missing from the price catalog instead

        of only how many requests are affected.'
    UpdateTagRequest:
      properties:
        tag:
          type: string
          title: Tag
          description: New tag value
      type: object
      required:
      - tag
      title: UpdateTagRequest
      description: Request to update a version's tag.
    UsageImportCsvResponse:
      properties:
        imported:
          type: integer
          title: Imported
          default: 0
        skipped_duplicates:
          type: integer
          title: Skipped Duplicates
          default: 0
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
        agent_display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Display Name
        source:
          type: string
          title: Source
          default: cursor
        parsed_rows:
          type: integer
          title: Parsed Rows
          default: 0
        skipped_rows:
          type: integer
          title: Skipped Rows
          default: 0
        skipped_row_reasons:
          items:
            type: string
          type: array
          title: Skipped Row Reasons
      type: object
      title: UsageImportCsvResponse
      description: Result of a CSV import, including rows the parser could not use.
    UsageImportEvent:
      properties:
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        model:
          type: string
          maxLength: 255
          minLength: 1
          title: Model
        prompt_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Prompt Tokens
        completion_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Completion Tokens
        total_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Total Tokens
        cache_read_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Cache Read Tokens
        cache_creation_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Cache Creation Tokens
        charged_cents:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Charged Cents
          description: Amount charged by the source vendor, in cents (USD).
        cost_usd:
          anyOf:
          - type: number
            minimum: 0.0
          - type: 'null'
          title: Cost Usd
          description: Amount charged in USD. Mutually exclusive with charged_cents.
        session_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Session Id
        kind:
          anyOf:
          - type: string
            maxLength: 100
          - type: 'null'
          title: Kind
          description: Source billing category (e.g. 'Usage-based', 'Included').
        max_mode:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Max Mode
        meta:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Meta
      type: object
      required:
      - timestamp
      - model
      title: UsageImportEvent
      description: One normalized usage event observed outside the model gateway.
    UsageImportRequest:
      properties:
        events:
          items:
            $ref: '#/components/schemas/UsageImportEvent'
          type: array
          maxItems: 5000
          minItems: 1
          title: Events
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
          description: Managed agent to attribute the events to. When omitted, the
            account's managed Cursor agent (from `preloop agents onboard cursor`)
            is used if exactly resolvable.
        source:
          type: string
          maxLength: 64
          minLength: 1
          title: Source
          description: Origin label stored on each row (e.g. 'cursor').
          default: cursor
      type: object
      required:
      - events
      title: UsageImportRequest
      description: Batch of normalized usage events to ingest.
    UsageImportResponse:
      properties:
        imported:
          type: integer
          title: Imported
          default: 0
        skipped_duplicates:
          type: integer
          title: Skipped Duplicates
          default: 0
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
        agent_display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Display Name
        source:
          type: string
          title: Source
          default: cursor
      type: object
      title: UsageImportResponse
      description: Result of an ingest request.
    UsageIngestRecord:
      properties:
        external_id:
          type: string
          maxLength: 255
          minLength: 1
          title: External Id
          description: Source-side record/turn id; dedupe key with `source`.
        conversation_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Conversation Id
        parent_conversation_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Parent Conversation Id
          description: Conversation this record's conversation was spawned from (e.g.
            a subagent worker's parent thread), for cost rollup.
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        event_type:
          type: string
          enum:
          - session_start
          - session_end
          - subagent_start
          - subagent_stop
          - response
          - compaction
          - usage
          title: Event Type
          description: Hook-shaped lifecycle marker. 'usage' (default) requires a
            model and at least one measurement; lifecycle events (session/subagent
            start/stop, response, compaction) may carry zero token/cost data and no
            model.
          default: usage
        model:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Model
          description: Source-reported model name. Required for event_type='usage'.
        charged_cost:
          anyOf:
          - type: number
            minimum: 0.0
          - type: string
            pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          - type: 'null'
          title: Charged Cost
          description: Amount the source vendor charged, in USD. The billing truth;
            never derived from tokens. Stored as a float on the ledger (estimated_cost
            is a Float column), matching the CSV import path.
        cost_basis:
          type: string
          enum:
          - estimated
          - reconciled
          title: Cost Basis
          description: '''estimated'' (hook/transcript-derived) or ''reconciled''
            (billing export). Reconciled records supersede estimated records with
            the same (source, conversation_id) in cost summaries; the two are never
            summed together.'
          default: estimated
        input_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Input Tokens
        output_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Output Tokens
        cache_read_tokens:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Cache Read Tokens
          description: Cache-read tokens, reported distinctly; never summed into charged
            spend or total tokens.
        message_count:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Message Count
          description: Messages in the conversation so far (growth tripwire).
        tool_call_count:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Tool Call Count
          description: Tool calls in the conversation so far (growth tripwire).
        metadata:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Metadata
          description: 'Small JSON object. Recognized session keys: `session_title`
            (sets the runtime session title), `session_title_default` (fills the title
            only while it is empty) and `session_summary` (replaces the runtime session
            summary).'
        transcript:
          anyOf:
          - items:
              $ref: '#/components/schemas/UsageIngestTranscriptMessage'
            type: array
            maxItems: 50
          - type: 'null'
          title: Transcript
          description: Opt-in transcript text for this record's conversation, stored
            as runtime session activities. Shippers send it only when the operator
            enabled transcript storage; by default only counts, titles and short summaries
            leave the machine.
      type: object
      required:
      - external_id
      - timestamp
      title: UsageIngestRecord
      description: 'One usage record pushed continuously by an external harness.


        Unlike ``UsageImportEvent`` (batch CSV/JSON import, content-hash

        dedupe), a pushed record carries a caller-supplied ``external_id`` so

        replays after network timeouts are idempotent, plus conversation ids so

        subagent workers billed on separate conversations can be rolled up

        under their parent thread in Cost analytics.'
    UsageIngestRecordResult:
      properties:
        external_id:
          type: string
          title: External Id
        deduplicated:
          type: boolean
          title: Deduplicated
          default: false
        conflict:
          type: boolean
          title: Conflict
          description: True when a deduplicated replay carried a DIFFERENT payload
            than the stored record (first write wins; the batch still returns 200
            so shippers can retry blindly).
          default: false
      type: object
      required:
      - external_id
      title: UsageIngestRecordResult
      description: Per-record outcome of a push-ingest request.
    UsageIngestRequest:
      properties:
        records:
          items:
            $ref: '#/components/schemas/UsageIngestRecord'
          type: array
          maxItems: 1000
          minItems: 1
          title: Records
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
          description: Managed agent to attribute the records to. When omitted, the
            account's single managed agent whose kind matches `source` is used if
            exactly resolvable.
        source:
          type: string
          maxLength: 64
          minLength: 1
          title: Source
          description: Origin label stored on each row (e.g. 'cursor').
      type: object
      required:
      - records
      - source
      title: UsageIngestRequest
      description: Batch of pushed usage records to ingest.
    UsageIngestResponse:
      properties:
        source:
          type: string
          title: Source
        agent_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Agent Id
        agent_display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Display Name
        accepted:
          type: integer
          title: Accepted
          default: 0
        deduplicated:
          type: integer
          title: Deduplicated
          default: 0
        conflicts:
          type: integer
          title: Conflicts
          default: 0
        results:
          items:
            $ref: '#/components/schemas/UsageIngestRecordResult'
          type: array
          title: Results
      type: object
      required:
      - source
      title: UsageIngestResponse
      description: 'Result of a push-ingest request.


        Replays return 200 with the replayed records flagged

        ``deduplicated`` — spend is never double-counted. Replays whose

        payload differs from the stored record are additionally flagged

        ``conflict`` (first write wins; never a 409).'
    UsageIngestTranscriptMessage:
      properties:
        role:
          type: string
          enum:
          - user
          - assistant
          - tool
          - tool_use
          title: Role
        text:
          type: string
          maxLength: 4000
          minLength: 1
          title: Text
        timestamp:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Timestamp
      type: object
      required:
      - role
      - text
      title: UsageIngestTranscriptMessage
      description: One role-tagged text chunk of a conversation transcript.
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    VerificationCommand:
      properties:
        id:
          type: string
          title: Id
          description: Stable identifier used in evidence records
        command:
          type: string
          title: Command
          description: Shell command executed in the repository working tree
        reason:
          type: string
          title: Reason
          description: Why this check exists; recorded with the evidence
        scope:
          type: string
          title: Scope
          description: Coarse impact area (docs, migration, frontend, backend, api,
            shared, unknown) used for reporting and narrow-diff assertions
          default: unknown
        timeout_seconds:
          type: integer
          maximum: 3600.0
          minimum: 1.0
          title: Timeout Seconds
          description: Wall-clock budget for one execution of this command
          default: 900
      type: object
      required:
      - id
      - command
      - reason
      title: VerificationCommand
      description: 'One required check: a shell command plus why it exists.'
    VerificationPolicy:
      properties:
        mode:
          type: string
          enum:
          - 'off'
          - gate
          title: Mode
          default: gate
        profile:
          $ref: '#/components/schemas/VerificationProfile'
          description: Versioned trusted test profile driving required checks
        image:
          anyOf:
          - type: string
          - type: 'null'
          title: Image
          description: Digest-pinned generic toolchain image for credential-isolated
            checks. Must contain dependencies required by the trusted profile.
        gate_budget_seconds:
          type: integer
          maximum: 14400.0
          minimum: 30.0
          title: Gate Budget Seconds
          description: Wall-clock budget for the whole verification run; a check that
            cannot start inside it is blocked, not failed
          default: 3600
      type: object
      required:
      - profile
      title: VerificationPolicy
      description: 'Publication gate policy inside ``git_clone_config`` (issue #428).


        ``mode: "gate"`` requires the runner-controlled verifier to allow

        publication (push / pull-request creation) before the agent''s commits

        leave the workspace. There is no draft-publication exception: unverified

        work stays unpushed.'
    VerificationProfile:
      properties:
        version:
          type: string
          const: v1
          title: Version
          default: v1
        profile_id:
          type: string
          title: Profile Id
          description: Stable profile name, e.g. 'default'
        description:
          type: string
          title: Description
          default: ''
        always:
          items:
            $ref: '#/components/schemas/VerificationCommand'
          type: array
          title: Always
        rules:
          items:
            $ref: '#/components/schemas/ProfileRule'
          type: array
          title: Rules
        unknown_default:
          items:
            $ref: '#/components/schemas/VerificationCommand'
          type: array
          title: Unknown Default
      type: object
      required:
      - profile_id
      title: VerificationProfile
      description: 'Versioned, trusted set of required checks for a repository.


        Ships with the flow configuration, not inside the repository under test.

        Inexpensive ``always`` hooks are required on every change; rules match

        the changed-file diff; ``unknown_default`` covers unknown impact so a

        diff never resolves to an empty required list.'
    WebhookCatalogue:
      properties:
        version:
          type: string
          title: Version
        event_types:
          items:
            $ref: '#/components/schemas/WebhookEventTypeInfo'
          type: array
          title: Event Types
        signature_header:
          type: string
          title: Signature Header
        tolerance_seconds:
          type: integer
          title: Tolerance Seconds
        max_attempts:
          type: integer
          title: Max Attempts
        retry_delays_seconds:
          items:
            type: integer
          type: array
          title: Retry Delays Seconds
      type: object
      required:
      - version
      - event_types
      - signature_header
      - tolerance_seconds
      - max_attempts
      - retry_delays_seconds
      title: WebhookCatalogue
      description: What a console needs to render the create form.
    WebhookConfig:
      properties:
        webhook_secret:
          type: string
          title: Webhook Secret
          description: Secure token for authenticating webhook requests (auto-generated)
        dedupe_path:
          anyOf:
          - type: string
          - type: 'null'
          title: Dedupe Path
          description: Dotted JSON path into the webhook body used to build a deduplication
            key (e.g. 'data.issue.id'). When unset, defaults to 'attachments.0.title_link'
            then 'data.issue.id'.
      type: object
      required:
      - webhook_secret
      title: WebhookConfig
      description: Configuration for webhook triggers.
    WebhookDeliveryRead:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        endpoint_id:
          type: string
          format: uuid
          title: Endpoint Id
        event_id:
          type: string
          format: uuid
          title: Event Id
        event_type:
          type: string
          title: Event Type
        status:
          type: string
          title: Status
        attempt_count:
          type: integer
          title: Attempt Count
        generation:
          type: integer
          title: Generation
        occurred_at:
          type: string
          format: date-time
          title: Occurred At
        next_attempt_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Next Attempt At
        delivered_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Delivered At
        response_status:
          anyOf:
          - type: integer
          - type: 'null'
          title: Response Status
        last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
      type: object
      required:
      - id
      - endpoint_id
      - event_id
      - event_type
      - status
      - attempt_count
      - generation
      - occurred_at
      title: WebhookDeliveryRead
      description: One delivery attempt row.
    WebhookEndpointCreate:
      properties:
        url:
          type: string
          maxLength: 2048
          title: Url
          description: HTTPS URL to POST events to
        description:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Description
        event_types:
          items:
            type: string
          type: array
          title: Event Types
          description: Event types to receive. Empty means every v1 event.
        active:
          type: boolean
          title: Active
          default: true
      type: object
      required:
      - url
      title: WebhookEndpointCreate
      description: Create request. The secret is generated server side.
    WebhookEndpointCreated:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        url:
          type: string
          title: Url
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        event_types:
          items:
            type: string
          type: array
          title: Event Types
        active:
          type: boolean
          title: Active
        source:
          type: string
          title: Source
        secret_hint:
          anyOf:
          - type: string
          - type: 'null'
          title: Secret Hint
        created_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Created By User Id
        consecutive_failures:
          type: integer
          title: Consecutive Failures
          default: 0
        circuit_open:
          type: boolean
          title: Circuit Open
          default: false
        last_delivery_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Delivery Status
        last_delivery_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Delivery At
        last_response_code:
          anyOf:
          - type: integer
          - type: 'null'
          title: Last Response Code
        last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        secret:
          type: string
          title: Secret
        secret_note:
          type: string
          title: Secret Note
          default: Shown once. Store it now; it cannot be read back.
      type: object
      required:
      - id
      - url
      - active
      - source
      - secret
      title: WebhookEndpointCreated
      description: Create response, the one and only time the secret is returned.
    WebhookEndpointRead:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        url:
          type: string
          title: Url
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        event_types:
          items:
            type: string
          type: array
          title: Event Types
        active:
          type: boolean
          title: Active
        source:
          type: string
          title: Source
        secret_hint:
          anyOf:
          - type: string
          - type: 'null'
          title: Secret Hint
        created_by_user_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Created By User Id
        consecutive_failures:
          type: integer
          title: Consecutive Failures
          default: 0
        circuit_open:
          type: boolean
          title: Circuit Open
          default: false
        last_delivery_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Delivery Status
        last_delivery_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Delivery At
        last_response_code:
          anyOf:
          - type: integer
          - type: 'null'
          title: Last Response Code
        last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
      type: object
      required:
      - id
      - url
      - active
      - source
      title: WebhookEndpointRead
      description: Endpoint as returned by the API. Never carries the secret.
    WebhookEndpointUpdate:
      properties:
        url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Url
        description:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: Description
        event_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Event Types
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
      type: object
      title: WebhookEndpointUpdate
      description: Partial update. Omitted fields are left alone.
    WebhookEventTypeInfo:
      properties:
        name:
          type: string
          title: Name
        description:
          type: string
          title: Description
      type: object
      required:
      - name
      - description
      title: WebhookEventTypeInfo
      description: One entry in the subscribable event catalogue.
    WebhookReplayResult:
      properties:
        event_id:
          type: string
          format: uuid
          title: Event Id
        delivery_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Delivery Ids
        queued:
          type: integer
          title: Queued
          default: 0
      type: object
      required:
      - event_id
      title: WebhookReplayResult
      description: Result of replaying one event id.
    WebhookTestSendResult:
      properties:
        event_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Event Id
        delivery_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Delivery Ids
        queued:
          type: integer
          title: Queued
          default: 0
      type: object
      title: WebhookTestSendResult
      description: Result of queueing a test event.
    WeeklySchedule:
      properties:
        timezone:
          type: string
          title: Timezone
          description: IANA timezone name the schedule is evaluated in
          default: UTC
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Static trigger payload merged into every scheduled run, for
            options a schedule has no other way to state (e.g. previous_result_execution_id).
            Bounded and inline only.
        type:
          type: string
          const: weekly
          title: Type
          default: weekly
        days:
          items:
            type: string
            enum:
            - mon
            - tue
            - wed
            - thu
            - fri
            - sat
            - sun
          type: array
          minItems: 1
          title: Days
          description: Weekdays the schedule fires on (mon..sun)
        at:
          type: string
          title: At
          description: Time of day in 24h 'HH:MM' format
      type: object
      required:
      - days
      - at
      title: WeeklySchedule
      description: 'Friendly schedule form: run on selected weekdays at a fixed time.'
    WorkspaceSeedInput:
      properties:
        path:
          type: string
          maxLength: 512
          minLength: 1
          title: Path
        content_base64:
          type: string
          minLength: 1
          title: Content Base64
      additionalProperties: false
      type: object
      required:
      - path
      - content_base64
      title: WorkspaceSeedInput
      description: Inline workspace seed carried on the existing trigger contract.
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token for authentication

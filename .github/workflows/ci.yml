# Preloop Open Source - CI Pipeline
# Runs unit tests on pull requests and pushes to main
# Builds and pushes Docker images on main and version tags (not on PRs —
# release.yml already publishes images, and a multi-arch QEMU build is the
# long tail after tests go green).
#
# PR jobs are path-filtered so a docs-only or frontend-only change does not
# pay for eight Postgres shards. Filters are job-level, not workflow-level:
# a workflow `on.pull_request.paths` skip made stacked PRs look clean because
# no checks ran at all. `main` and tags always run every suite.
#
# Make the `CI` aggregator the required check. Individual shards may be
# skipped; skipped-because-paths-did-not-change is success, a red shard is not.

name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  # No base-branch filter: a PR should get CI regardless of what it targets.
  # Restricting this to `main` meant stacked PRs — ones opened against another
  # in-flight branch rather than main — silently ran no checks at all, so they
  # looked clean because nothing had examined them.
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "22"
  REGISTRY_GHCR: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read

jobs:
  # ==========================================================================
  # Changed paths (PRs only; push/tag forces every output true)
  # ==========================================================================

  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.out.outputs.backend }}
      frontend: ${{ steps.out.outputs.frontend }}
      cli: ${{ steps.out.outputs.cli }}
      egress: ${{ steps.out.outputs.egress }}
      browser: ${{ steps.out.outputs.browser }}
      plugins: ${{ steps.out.outputs.plugins }}
      helm: ${{ steps.out.outputs.helm }}
      workflow: ${{ steps.out.outputs.workflow }}
      requirements: ${{ steps.out.outputs.requirements }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: github.event_name == 'pull_request'

      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        if: github.event_name == 'pull_request'
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'pyproject.toml'
              - 'requirements/**'
              - '.github/requirements/app-dev.txt'
              - '.github/requirements/coverage.txt'
              - '.github/requirements/test.txt'
              - '.github/requirements/test.in'
              - 'scripts/init_db.py'
            frontend:
              - 'frontend/**'
            cli:
              - 'cli/**'
            egress:
              - 'environments/egress-proxy/**'
            browser:
              - 'environments/preloop/**'
              - 'environments/egress-proxy/**'
              - 'backend/tests/services/test_flow_environment.py'
              - 'backend/preloop/services/flow_environment.py'
            plugins:
              - 'runtime-plugins/**'
              - 'pyproject.toml'
              - '.github/requirements/runtime-plugin-test.txt'
              - '.github/requirements/runtime-test.txt'
              - '.github/requirements/runtime-test.in'
            helm:
              - 'helm/**'
              - 'scripts/helm-render-check.sh'
            workflow:
              - '.github/workflows/ci.yml'
            # Anything that can change what a hash-pinned lock resolves to.
            requirements:
              - 'requirements/**'
              - '.github/requirements/**'
              - 'pyproject.toml'
              - 'Dockerfile'

      - name: Set job outputs
        id: out
        env:
          EVENT: ${{ github.event_name }}
          BACKEND: ${{ steps.filter.outputs.backend }}
          FRONTEND: ${{ steps.filter.outputs.frontend }}
          CLI: ${{ steps.filter.outputs.cli }}
          EGRESS: ${{ steps.filter.outputs.egress }}
          BROWSER: ${{ steps.filter.outputs.browser }}
          PLUGINS: ${{ steps.filter.outputs.plugins }}
          HELM: ${{ steps.filter.outputs.helm }}
          WORKFLOW: ${{ steps.filter.outputs.workflow }}
          REQUIREMENTS: ${{ steps.filter.outputs.requirements }}
        run: |
          if [ "$EVENT" != "pull_request" ] || [ "$WORKFLOW" = "true" ]; then
            echo "backend=true" >> "$GITHUB_OUTPUT"
            echo "frontend=true" >> "$GITHUB_OUTPUT"
            echo "cli=true" >> "$GITHUB_OUTPUT"
            echo "egress=true" >> "$GITHUB_OUTPUT"
            echo "browser=true" >> "$GITHUB_OUTPUT"
            echo "plugins=true" >> "$GITHUB_OUTPUT"
            echo "helm=true" >> "$GITHUB_OUTPUT"
            echo "workflow=true" >> "$GITHUB_OUTPUT"
            echo "requirements=true" >> "$GITHUB_OUTPUT"
          else
            echo "backend=${BACKEND:-false}" >> "$GITHUB_OUTPUT"
            echo "frontend=${FRONTEND:-false}" >> "$GITHUB_OUTPUT"
            echo "cli=${CLI:-false}" >> "$GITHUB_OUTPUT"
            echo "egress=${EGRESS:-false}" >> "$GITHUB_OUTPUT"
            echo "browser=${BROWSER:-false}" >> "$GITHUB_OUTPUT"
            echo "plugins=${PLUGINS:-false}" >> "$GITHUB_OUTPUT"
            echo "helm=${HELM:-false}" >> "$GITHUB_OUTPUT"
            echo "workflow=false" >> "$GITHUB_OUTPUT"
            echo "requirements=${REQUIREMENTS:-false}" >> "$GITHUB_OUTPUT"
          fi

  # ==========================================================================
  # Where do the test jobs run?
  # ==========================================================================
  #
  # ubuntu-latest is the default. Idle self-hosted Linux/X64 runners are
  # extra capacity, not a replacement pool: GitHub will wait forever on a
  # self-hosted `runs-on` rather than overflow onto hosted, so sending every
  # test job to three VMs serializes the suite and is slower than before.
  #
  # Frontend, plugins, and coverage stay on ubuntu-latest and do not wait
  # for this job. Backend is eight shards; this job emits `backend_plan`,
  # an array of eight {runner, container, db_host, postgres_ports} entries.
  # The first 8-N shards stay hosted (group 1 is the long pole). The last
  # N shards — the excess — go to idle self-hosted runners, each in a
  # python:<PYTHON_VERSION>-bookworm container so two listeners on one VM
  # do not collide on host 5432. N is min(idle matching runners, 8).
  #
  # Anything else, including every failure mode below, is N=0 (all hosted).
  # This job NEVER fails the workflow: a PR must not go red because the
  # runner inventory could not be read.
  #
  # Two knobs the repo admin owns:
  #
  #   secret CI_RUNNER_READ_TOKEN  a fine-grained PAT scoped to this repo with
  #                                Repository permissions > Administration:
  #                                Read-only. Listing runners needs
  #                                `administration: read`, which the workflow
  #                                GITHUB_TOKEN cannot be granted. Absent or
  #                                empty (forks, Dependabot, not created yet)
  #                                means all hosted.
  #   variable CI_SELF_HOSTED_TESTS  set to "false" to force every shard
  #                                onto ubuntu-latest without a commit.
  #                                Unset or anything else means the routing
  #                                above is live.
  #
  # Python: setup-python has no Debian 12 builds. Jobs use
  # .github/actions/setup-ci-python, which venvs a matching system
  # interpreter (needs python3.11-venv on Debian) and only then falls
  # back to setup-python. Hosted ubuntu-latest has no system 3.11, so
  # those jobs still download from the Ubuntu manifest. Self-hosted
  # backend shards additionally run in python:<PYTHON_VERSION>-bookworm,
  # so they see that image's interpreter rather than the VM's.
  pick-runner:
    name: Pick test runner
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      backend_plan: ${{ steps.pick.outputs.backend_plan }}
    steps:
      - name: Assign overflow backend shards to idle self-hosted runners
        id: pick
        env:
          # Empty on forks and Dependabot: secrets are not exposed there.
          GH_TOKEN: ${{ secrets.CI_RUNNER_READ_TOKEN }}
          SELF_HOSTED_TESTS: ${{ vars.CI_SELF_HOSTED_TESTS }}
          REPO: ${{ github.repository }}
          PYTHON_VERSION: ${{ env.PYTHON_VERSION }}
        run: |
          set -uo pipefail

          emit() {
            idle="$1"
            reason="$2"
            case "$idle" in
              ''|*[!0-9]*) idle=0 ;;
            esac
            if [ "$idle" -gt 8 ]; then idle=8; fi

            plan=$(IDLE="$idle" PYVER="${PYTHON_VERSION:-3.11}" jq -cn '
              def public:
                {runner:"ubuntu-latest",container:null,db_host:"localhost",
                 postgres_ports:["5432:5432"]};
              def private:
                {runner:["self-hosted","Linux","X64"],
                 container:{image:("python:"+$ENV.PYVER+"-bookworm")},
                 db_host:"postgres",postgres_ports:[]};
              ($ENV.IDLE|tonumber) as $idle
              | [null] + [range(0;8) | if . >= (8 - $idle) then private else public end]
            ') || plan=""

            if [ -z "$plan" ]; then
              idle=0
              reason="$reason (plan fallback: all public)"
              plan='[null,{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]},{"runner":"ubuntu-latest","container":null,"db_host":"localhost","postgres_ports":["5432:5432"]}]'
            fi

            {
              echo "backend_plan<<EOF"
              echo "$plan"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            echo "reason: $reason"
            echo "overflow shards on self-hosted: $idle / 8"
            echo "- overflow self-hosted backend shards: \`$idle / 8\`" >> "$GITHUB_STEP_SUMMARY"
            echo "- reason: $reason" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          }

          if [ "${SELF_HOSTED_TESTS:-true}" = "false" ]; then
            emit 0 "repository variable CI_SELF_HOSTED_TESTS is false"
          fi

          if [ -z "${GH_TOKEN:-}" ]; then
            emit 0 "secret CI_RUNNER_READ_TOKEN is absent or empty"
          fi

          if ! runners=$(gh api "repos/${REPO}/actions/runners" --paginate 2>&1); then
            echo "runner list unavailable: $runners"
            emit 0 "could not list repository runners (token scope or API error)"
          fi

          # Online, not already busy, and carrying all three labels. Busy
          # matters: a working VM must not steal shards it cannot start.
          available=$(printf '%s' "$runners" | jq -s '
            [ .[].runners[]?
              | select(.status == "online" and .busy == false)
              | [.labels[].name] as $labels
              | select(($labels | index("self-hosted")) != null
                   and ($labels | index("Linux")) != null
                   and ($labels | index("X64")) != null)
            ] | length' 2>/dev/null) || available=""

          case "$available" in
            ''|*[!0-9]*)
              emit 0 "could not parse the runner list"
              ;;
          esac

          if [ "$available" -ge 1 ]; then
            emit "$available" "$available idle self-hosted Linux/X64 runner(s) take overflow shards"
          fi

          emit 0 "no idle self-hosted Linux/X64 runner is online"

  # ==========================================================================
  # Requirements resolve (guard)
  # ==========================================================================
  # Every hash-pinned lock in the repo, asked one question: can pip resolve
  # you? Nothing is installed and nothing is built.
  #
  # This exists because requirements/runtime.txt is installed by no job that
  # runs on a pull request. "Build & Push Docker Images" is the only consumer
  # in CI and it is `if: github.event_name != 'pull_request'` (a multi-arch
  # QEMU build, too slow to gate a PR on). The backend shards install
  # .github/requirements/app-dev.txt, a different lock. So dependabot PR #537
  # merged with an all-green CI and a runtime.txt that pip cannot resolve
  # (pydantic 2.13.5 next to pydantic-core 2.48.0; pydantic pins
  # pydantic-core==2.46.5), and main's Docker and Release builds went red.
  #
  # `--report /dev/null` lets pip use PEP 658 index metadata for resolution
  # instead of downloading every wheel body, and `--require-hashes` is what
  # Dockerfile:28-29 and scripts/generate_sbom.sh use, so this checks the same
  # thing those do. Measured cold-cache locally: 47s for all ten lock files.
  requirements-resolve:
    name: Requirements Resolve
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.requirements == 'true'
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-resolve-${{ hashFiles('requirements/*.txt', '.github/requirements/*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-resolve-

      - name: Resolve every hash-pinned lock
        run: |
          set -u
          failed=0
          for f in requirements/*.txt .github/requirements/*.txt; do
            echo "::group::$f"
            if python -m pip install \
                 --require-hashes \
                 --dry-run \
                 --ignore-installed \
                 --report /dev/null \
                 -r "$f"; then
              echo "ok: $f"
            else
              echo "::error file=$f::$f does not resolve with --require-hashes"
              failed=1
            fi
            echo "::endgroup::"
          done
          exit "$failed"

  # ==========================================================================
  # Backend Tests
  # ==========================================================================

  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      # App + [dev] extra hash-pinned via app-dev.txt. `--no-deps` then
      # installs only the local package (Scorecard requires that for `-e`).
      # Regenerate: uv pip compile --universal --generate-hashes
      #   --python-version 3.11 --extra dev pyproject.toml
      #   -o .github/requirements/app-dev.txt
      - name: Install Python dependencies
        run: |
          pip install --require-hashes -r .github/requirements/app-dev.txt
          pip install --no-deps -e .

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Run pre-commit
        run: pre-commit run --all-files

      - name: Run ruff
        run: ruff check .

  helm-lint:
    name: Helm Chart Checks
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.helm == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # helm is preinstalled on ubuntu-latest runners
      - name: Helm render checks (chart lint + CNPG backup wiring)
        run: sh scripts/helm-render-check.sh

  # Eight shards, each with its own Postgres. GitLab splits this suite into
  # dozens of per-file jobs; that is a poor fit here because each GitHub
  # runner spends ~1.5m on checkout/pip/migrations before pytest starts.
  # Four shards left group 1 (the first duration_based_chunks slice) as the
  # wall-clock pole: ~7-9m vs ~3-4.5m for the others. Eight shards cut that
  # first slice in half without going back to per-file jobs.
  # pytest-split keeps new tests in the partition automatically. Use
  # duration_based_chunks (not least_duration): without a timings file,
  # least_duration round-robins individual tests across shards and breaks
  # unittest setUp isolation plus tests that share a process-wide DB
  # session. Chunks keep collection order, so files stay together. The 60%
  # coverage floor is applied after shards are combined -- a single shard
  # only sees part of the tree.
  #
  # Runner: ubuntu-latest is the default. pick-runner may send the last
  # N shards (idle self-hosted count) to private VMs as overflow. Public
  # shards stay on the VM with Postgres at localhost:5432. Overflow
  # shards run in python:<version>-bookworm and reach Postgres by the
  # service hostname, with no host port, so two listeners on one machine
  # do not collide on 5432. The VM still needs Docker and the runner
  # user in the docker group.
  test-backend:
    name: Backend Tests (${{ matrix.group }}/8)
    runs-on: ${{ fromJSON(needs.pick-runner.outputs.backend_plan)[matrix.group].runner }}
    container: ${{ fromJSON(needs.pick-runner.outputs.backend_plan)[matrix.group].container }}
    needs: [changes, pick-runner]
    if: needs.changes.outputs.backend == 'true'
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        group: [1, 2, 3, 4, 5, 6, 7, 8]
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports: ${{ fromJSON(needs.pick-runner.outputs.backend_plan)[matrix.group].postgres_ports }}
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://test_user:test_password@${{ fromJSON(needs.pick-runner.outputs.backend_plan)[matrix.group].db_host }}:5432/test_db
      # Explicit opt-in for isolated schema lease/crash/lifecycle fixtures.
      FLOW_FEEDBACK_TEST_DATABASE_URL: postgresql://test_user:test_password@${{ fromJSON(needs.pick-runner.outputs.backend_plan)[matrix.group].db_host }}:5432/test_db
      OPENAI_API_KEY: mock_key
      ANTHROPIC_API_KEY: mock_key
      PRELOOP_DISABLE_TELEMETRY: "true"
      COVERAGE_FILE: coverage-data.${{ matrix.group }}

    steps:
      # clean: true is the default, but a self-hosted workspace is reused
      # between runs, so say it out loud.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          clean: true

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('.github/requirements/app-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # App + [dev] extra hash-pinned via app-dev.txt; `--no-deps` installs
      # only the local package.
      - name: Install dependencies
        run: |
          pip install --require-hashes -r .github/requirements/app-dev.txt
          pip install --no-deps -e .

      - name: Initialize database
        run: python scripts/init_db.py --force

      - name: Run tests
        run: |
          pytest -v \
            -m "not integration" \
            --splits 8 \
            --group ${{ matrix.group }} \
            --splitting-algorithm=duration_based_chunks \
            --cov=preloop \
            --cov-report= \
            --junitxml=test-results.xml

      - name: Upload coverage data
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-coverage-${{ matrix.group }}
          path: coverage-data.${{ matrix.group }}
          if-no-files-found: error

      - name: Upload test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: backend-test-results-${{ matrix.group }}
          path: test-results.xml

  test-backend-coverage:
    name: Backend Coverage
    runs-on: ubuntu-latest
    needs: [changes, test-backend]
    if: needs.changes.outputs.backend == 'true'
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          clean: true

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-coverage-${{ hashFiles('.github/requirements/coverage.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # This job only combines/reports shard data, so it installs a tiny
      # dedicated lock (coverage.txt) instead of the full app-dev lock. The
      # two locks must pin the same coverage.py version or `coverage combine`
      # may reject/misread the data files written by the shards (which do use
      # app-dev.txt).
      - name: Install coverage
        run: pip install --require-hashes -r .github/requirements/coverage.txt

      - name: Download shard coverage data
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: backend-coverage-*
          path: coverage-shards
          merge-multiple: true

      - name: Combine coverage and enforce floor
        run: |
          mapfile -t coverage_files < <(find coverage-shards -name 'coverage-data.*' -type f | sort)
          if [ ${#coverage_files[@]} -ne 8 ]; then
            echo "Expected 8 shard coverage files, found ${#coverage_files[@]}"
            find coverage-shards -type f -print
            exit 1
          fi
          coverage combine "${coverage_files[@]}"
          coverage xml -o coverage.xml
          coverage report --fail-under=60

      - name: Upload coverage
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7.1.1
        if: github.event_name != 'pull_request'
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  # ==========================================================================
  # Frontend Tests
  # ==========================================================================

  test-frontend:
    name: Frontend Tests
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.frontend == 'true'
    timeout-minutes: 45
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          clean: true

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Typecheck
        run: npm run typecheck

      - name: Production build
        run: |
          npm run build
          if find dist -name '*.css' -print | grep -q .; then
            echo "standalone CSS files must not be emitted; CSS is injected into JS" >&2
            find dist -name '*.css' -print >&2
            exit 1
          fi
          if ! grep -q 'vite-plugin-css-injected-by-js' dist/assets/main-*.js; then
            echo "CSS injection marker missing from the main JS bundle" >&2
            exit 1
          fi

      - name: Read the Playwright version
        id: playwright
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"

      # runner.environment is in the key because Playwright ships per-distro
      # browser builds: the GitHub image gets an ubuntu-xx build, a Debian VM
      # gets a debian-xx one, and both report runner.os == Linux. Sharing one
      # key would have each restore the other's flavour and re-download.
      # The pip caches below need no such split: those are manylinux wheels.
      - name: Cache Playwright browsers
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ runner.environment }}-${{ steps.playwright.outputs.version }}

      # GitHub-hosted only: the runner image preconfigures Google's
      # chrome-stable apt source, which this job never uses. Its index
      # intermittently fails with "Hash Sum mismatch" and takes
      # "playwright install --with-deps" down with it (main and five PRs on
      # 2026-09-09). Needs root, and a self-hosted runner user may have no
      # sudo and no such apt source, so it is skipped there.
      - name: Drop the runner image's chrome apt source
        if: runner.environment == 'github-hosted'
        run: sudo rm -f /etc/apt/sources.list.d/google-chrome*.list /etc/apt/sources.list.d/google.list

      # --with-deps shells out to apt-get as root and only knows Ubuntu
      # package names, so it is used on the GitHub image only. A self-hosted
      # runner installs the browser into ~/.cache/ms-playwright without
      # touching the system; its OS libraries are a one-time setup on that
      # machine ("npx playwright install-deps" as root, once).
      - name: Install Playwright browsers
        env:
          WITH_DEPS: ${{ runner.environment == 'github-hosted' && '--with-deps' || '' }}
        run: |
          # Retry: a no-op for cached browsers.
          for attempt in 1 2 3; do
            # shellcheck disable=SC2086 # WITH_DEPS is one flag or empty
            npx playwright install $WITH_DEPS chromium && exit 0
            echo "playwright install failed (attempt $attempt), retrying in 15s"
            sleep 15
          done
          echo "playwright install failed after 3 attempts" >&2
          exit 1

      - name: Run tests
        run: npm run test

  # ==========================================================================
  # Runtime Plugin Tests (Hermes + OpenClaw)
  # ==========================================================================

  test-runtime-plugins:
    name: Runtime Plugin Tests
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.plugins == 'true'
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          clean: true

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: runtime-plugins/openclaw-preloop/package-lock.json

      - name: Cache pip
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-runtime-plugins-${{ hashFiles('.github/requirements/runtime-plugin-test.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-runtime-plugins-
            ${{ runner.os }}-pip-

      # Runtime + pytest hash-pinned together (a merge of runtime.txt and
      # runtime-test.txt conflicts on pygments). `--no-deps` installs only
      # the local package. Regenerate:
      #   uv pip compile --universal --generate-hashes --python-version 3.11 \
      #     pyproject.toml .github/requirements/runtime-test.in \
      #     -o .github/requirements/runtime-plugin-test.txt
      - name: Install Python dependencies
        run: |
          pip install --require-hashes -r .github/requirements/runtime-plugin-test.txt
          pip install --no-deps -e .

      - name: Run runtime-plugin pytest
        run: |
          pytest -v \
            runtime-plugins/tests \
            runtime-plugins/hermes-preloop/tests

      - name: Test Pi and DeepSeek Harness plugins
        working-directory: runtime-plugins/harness-preloop
        env:
          PRELOOP_DISABLE_TELEMETRY: "true"
        run: |
          npm ci --ignore-scripts
          npm test

      - name: Install OpenClaw plugin dependencies
        working-directory: runtime-plugins/openclaw-preloop
        run: npm ci

      - name: Run OpenClaw plugin tests
        working-directory: runtime-plugins/openclaw-preloop
        run: npm test

      - name: Install Claude plugin dependencies
        working-directory: runtime-plugins/claude-preloop
        run: npm ci

      - name: Run Claude plugin tests
        working-directory: runtime-plugins/claude-preloop
        run: npm test

      - name: Install Codex plugin dependencies
        working-directory: runtime-plugins/codex-preloop
        run: npm ci

      - name: Run Codex plugin tests
        working-directory: runtime-plugins/codex-preloop
        run: npm test

  # ==========================================================================
  # CLI (Windows)
  # ==========================================================================
  #
  # Windows CLI binaries already ship on every release (see release.yml), but
  # nothing had ever exercised them. This job is the first Windows runner in the
  # repo. It is still INFORMATIONAL ONLY:
  #   - continue-on-error: true, so a failure never turns the check red, and
  #   - it is absent from build-and-push's `needs:`, so it cannot gate a release.
  #
  # The three original failure causes have been addressed, so this job is
  # EXPECTED TO PASS. It has not yet been observed green on a real Windows
  # runner, which is the only reason continue-on-error is still set — flip it
  # (and add this job to build-and-push's `needs:`) once a few runs confirm it.
  # A failure here is now a signal, not background noise: triage it.
  #
  #   1. FIXED. Tests redirected the home dir with t.Setenv("HOME", ...), but
  #      os.UserHomeDir() reads %USERPROFILE% on Windows, so they operated on
  #      the real profile instead of their temp dir. All 94 call sites now go
  #      through internal/testenv.SetHome, which sets HOME, USERPROFILE, and
  #      the APPDATA/LOCALAPPDATA roots behind os.UserConfigDir/UserCacheDir.
  #      internal/testenv's own tests assert the redirection through the stdlib
  #      accessors, so they fail here if the helper ever stops working.
  #   2. SKIPPED. Ten tests write a #!/bin/sh stub and exec it; Windows has no
  #      shebang. They call skipNoShebangOnWindows with the untested behavior
  #      named in the skip message. Fixture limitation, not a product problem.
  #   3. SKIPPED — REAL PRODUCT GAP. The managed-launcher feature is POSIX-only:
  #      it emits a bash script to ~/.local/bin and gates on the 0111 executable
  #      bit, which Windows files never carry. Five tests call
  #      skipManagedLauncherOnWindows. These skips hide missing functionality,
  #      NOT a test defect — `preloop agents onboard` cannot install a managed
  #      launcher on Windows today. See the managedLauncherWindowsGap doc
  #      comment in cli/internal/cmd/testsupport_test.go.

  test-cli-windows:
    name: CLI Tests (Windows)
    runs-on: windows-latest
    continue-on-error: true
    needs: changes
    if: needs.changes.outputs.cli == 'true'

    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      # Kept as a separate step so a build failure and a test failure are
      # distinguishable at a glance in the checks UI.
      - name: Test
        env:
          PRELOOP_DISABLE_TELEMETRY: "true"
        run: go test ./...

  # ==========================================================================
  # Egress proxy (allowlist forward proxy for sandboxed browser traffic)
  # ==========================================================================
  #
  # Path-filtered like the CLI job. A change under environments/egress-proxy
  # runs vet and the race tests; other PRs skip this job. The CI aggregator
  # treats that skip as success and a failure here as a failed suite.

  test-egress-proxy:
    name: Egress Proxy
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.egress == 'true'
    defaults:
      run:
        working-directory: environments/egress-proxy
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: environments/egress-proxy/go.mod

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race ./...

  # ==========================================================================
  # Browser profile (Playwright MCP forced through the egress proxy)
  # ==========================================================================
  #
  # Path-filtered. Installs the pinned @playwright/mcp tree, the Chromium
  # build that tree expects, and runs the self-check against the proxy.
  # PRELOOP_BROWSER_CI=1 makes a missing browser a failure, not a skip.

  test-browser-profile:
    name: Browser profile
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.browser == 'true'
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: environments/egress-proxy/go.mod

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: environments/preloop/tools/package-lock.json

      - name: Install pinned Playwright MCP and Chromium
        working-directory: environments/preloop/tools
        run: |
          npm ci
          npx playwright install --with-deps chromium

      - name: Set up Python
        uses: ./.github/actions/setup-ci-python
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install test dependencies
        run: |
          pip install --require-hashes -r .github/requirements/app-dev.txt
          pip install --no-deps -e .

      - name: Prove browser egress is enforced
        env:
          PRELOOP_DISABLE_TELEMETRY: "true"
          PRELOOP_BROWSER_CI: "1"
        run: |
          pytest -q --tb=short \
            backend/tests/services/test_flow_environment.py::test_enable_blocks_metadata_and_non_allowlisted_origin \
            backend/tests/services/test_flow_environment.py::test_rendered_harness_config_is_isolated_and_selfcheck_fails_closed \
            backend/tests/services/test_flow_environment.py::test_removed_proxy_env_aborts_profile_setup \
            backend/tests/services/test_flow_environment.py::test_example_browser_profile_is_a_valid_environment_profile \
            backend/tests/services/test_flow_environment.py::test_browser_scripts_pass_bash_n

  # ==========================================================================
  # CLI Vulnerability Scan
  # ==========================================================================
  #
  # Deliberately NOT path-filtered, unlike every other suite here. The failure
  # this job exists to catch is a dependency going stale while nobody touches
  # cli/: golang.org/x/crypto sat at v0.54.0 with four published advisories
  # and no check in this repo would have said so. A filter on cli/** would
  # reproduce exactly that blind spot, and the job costs well under a minute.
  #
  # govulncheck is a reachability scanner, not a manifest scanner. It reports
  # at three levels and only the first one fails the build:
  #   Symbol   your code calls the vulnerable function. Fix it.
  #   Package  you import the package but do not call the symbol.
  #   Module   you require the module but do not import the package.
  # It is also the tool that settles VEX claims: security/vex/ asserts
  # vulnerable_code_not_present for the x/crypto advisories, and this job is
  # the evidence behind that assertion.

  cli-vuln-scan:
    name: CLI Vulnerability Scan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.7.0

      # -show verbose prints the package- and module-level findings too. They
      # do not fail the build, but they are what a release audit will ask
      # about, so they belong in the log rather than behind a rerun.
      - name: Run govulncheck
        run: govulncheck -show verbose ./...

  # ==========================================================================
  # Build and Push Docker Images
  # ==========================================================================

  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    # PRs do not build images: this job is a multi-arch QEMU build that does
    # not push, and release.yml already publishes from main/tags.
    if: github.event_name != 'pull_request'
    needs: [lint, test-backend-coverage, test-frontend, test-runtime-plugins]
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@99012661954931238ded8c8b007157a8430204e1 # v4.4.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY_GHCR }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request' && vars.PUSH_TO_DOCKERHUB == 'true'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata for backend image
        id: meta-backend
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: |
            ${{ env.REGISTRY_GHCR }}/preloop/preloop
            ${{ vars.PUSH_TO_DOCKERHUB == 'true' && format('{0}/preloop', vars.DOCKERHUB_ORG) || '' }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push backend image
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract metadata for frontend image
        id: meta-frontend
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: |
            ${{ env.REGISTRY_GHCR }}/preloop/console
            ${{ vars.PUSH_TO_DOCKERHUB == 'true' && format('{0}/console', vars.DOCKERHUB_ORG) || '' }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push frontend image
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: ./frontend
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          build-args: |
            BRAND=preloop
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Required check. Per-suite jobs may skip when their tree did not change;
  # a failed suite must still fail this job (skipped-because-needs-failed
  # would otherwise look like a path-filter skip). `changes` is in `needs`
  # so a checkout/filter crash cannot leave this job green with no tests.
  ci:
    name: CI
    runs-on: ubuntu-latest
    if: always() && !cancelled()
    needs:
      - changes
      - pick-runner
      - lint
      - helm-lint
      - requirements-resolve
      - test-backend
      - test-backend-coverage
      - test-frontend
      - test-runtime-plugins
      - test-egress-proxy
      - test-browser-profile
      - cli-vuln-scan
    steps:
      - name: Require every suite to have passed or been skipped
        env:
          CHANGES: ${{ needs.changes.result }}
          PICKRUNNER: ${{ needs.pick-runner.result }}
          LINT: ${{ needs.lint.result }}
          HELM: ${{ needs.helm-lint.result }}
          REQRESOLVE: ${{ needs.requirements-resolve.result }}
          BACKEND: ${{ needs.test-backend.result }}
          COVERAGE: ${{ needs.test-backend-coverage.result }}
          FRONTEND: ${{ needs.test-frontend.result }}
          PLUGINS: ${{ needs.test-runtime-plugins.result }}
          EGRESS: ${{ needs.test-egress-proxy.result }}
          BROWSER: ${{ needs.test-browser-profile.result }}
          CLIVULN: ${{ needs.cli-vuln-scan.result }}
        run: |
          failed=0
          check() {
            name="$1"
            result="$2"
            echo "$name=$result"
            case "$result" in
              success|skipped) ;;
              *)
                echo "::error::$name ended $result"
                failed=1
                ;;
            esac
          }
          check changes "$CHANGES"
          check pick-runner "$PICKRUNNER"
          check lint "$LINT"
          check helm-lint "$HELM"
          check requirements-resolve "$REQRESOLVE"
          check test-backend "$BACKEND"
          check test-backend-coverage "$COVERAGE"
          check test-frontend "$FRONTEND"
          check test-runtime-plugins "$PLUGINS"
          check test-egress-proxy "$EGRESS"
          check test-browser-profile "$BROWSER"
          check cli-vuln-scan "$CLIVULN"
          exit "$failed"
